# -*- coding: utf-8 -*-
"""FORECAST question bank - Estimation (25 items), covering point estimation,
MLE/MoM, sufficiency/completeness, Rao-Blackwell/Cramer-Rao, confidence
intervals and resampling. AI-GENERATED forecast content, NOT authentic PYQs.
"""

FORECAST = [
dict(topic="Point Estimation", subtopic="Characteristics of a Good Estimator (Unbiasedness, Consistency, Efficiency, MSE)",
 qtype="Definition", diff="Easy",
 question=r"An estimator $T_n$ of $\theta$ is said to be (weakly) consistent if",
 options=[r"$P(|T_n-\theta|>\varepsilon)\to0$ as $n\to\infty$, for every $\varepsilon>0$",
          r"$E(T_n)=\theta$ for every $n$",
          r"$\operatorname{Var}(T_n)=0$ for every $n$",
          r"$T_n=\theta$ exactly for all sufficiently large $n$"],
 ans=0,
 sc=r"Consistency is convergence in probability of $T_n$ to $\theta$ - a large-sample property, distinct from (and not implied by) unbiasedness.",
 tips=[r"An estimator can be consistent yet biased for every finite $n$ (bias $\to0$ is enough), and unbiased yet inconsistent (variance not $\to0$).",
       r"Do not confuse this with almost-sure convergence (strong consistency) - the exam usually intends the weak/probability version."],
 sol=[r"By definition, $T_n\xrightarrow{P}\theta$ means for every $\varepsilon>0$, $P(|T_n-\theta|>\varepsilon)\to0$ as $n\to\infty$.",
      r"Option (a)."]),

dict(topic="Point Estimation", subtopic="Characteristics of a Good Estimator (Unbiasedness, Consistency, Efficiency, MSE)",
 qtype="Definition", diff="Easy",
 question=r"For an estimator $T$ of $\theta$, the mean squared error $\text{MSE}(T)=E[(T-\theta)^2]$ decomposes as",
 options=[r"$\operatorname{Var}(T)+[\text{Bias}(T)]^2$", r"$\operatorname{Var}(T)-[\text{Bias}(T)]^2$",
          r"$\operatorname{Var}(T)/\text{Bias}(T)$", r"$[\operatorname{Var}(T)]^2+\text{Bias}(T)$"],
 ans=0,
 sc=r"The bias-variance decomposition: $\text{MSE}=\operatorname{Var}(T)+[\text{Bias}(T)]^2$.",
 tips=[r"For an unbiased estimator, MSE reduces to just the variance - this is why unbiased comparisons focus purely on variance.",
       r"A biased estimator can still have LOWER MSE than an unbiased one if it has substantially smaller variance."],
 sol=[r"$E[(T-\theta)^2]=E[(T-E(T)+E(T)-\theta)^2]=\operatorname{Var}(T)+[\text{Bias}(T)]^2$ (cross term vanishes).",
      r"Option (a)."]),

dict(topic="Point Estimation", subtopic="Characteristics of a Good Estimator (Unbiasedness, Consistency, Efficiency, MSE)",
 qtype="Conceptual", diff="Medium",
 question=r"Regarding minimization of MSE over ALL estimators (not just unbiased ones), which statement is correct?",
 options=[r"No single estimator achieves uniformly minimum MSE over all $\theta$ among all estimators - restricting to unbiased estimators is necessary for a meaningful 'best estimator' theory",
          r"The MLE always has the smallest MSE for every $\theta$",
          r"The sample mean always has the smallest MSE among all estimators of the population mean",
          r"Minimizing MSE and requiring unbiasedness are equivalent conditions"],
 ans=0,
 sc=r"The trivial constant estimator $T\equiv\theta_0$ has $\text{MSE}=(\theta_0-\theta)^2=0$ exactly at $\theta=\theta_0$, so no estimator can beat it everywhere - this is why UMVUE theory restricts attention to unbiased estimators.",
 tips=[r"This is precisely why 'uniformly minimum variance UNBIASED estimator' (UMVUE), not 'uniformly minimum MSE estimator', is the object of interest in estimation theory.",
       r"A silly constant guess always wins the MSE race at exactly one point - hence no universal winner exists without the unbiasedness restriction."],
 sol=[r"Consider $T\equiv\theta_0$ (ignore the data, always guess $\theta_0$): its MSE is $0$ when $\theta=\theta_0$, beating every other estimator there.",
      r"Since this holds for every choice of $\theta_0$, no single estimator can have uniformly (for all $\theta$) minimum MSE among ALL estimators - the restriction to unbiased estimators removes this pathology.",
      r"Option (a)."]),

dict(topic="Methods of Estimation", subtopic="Maximum Likelihood Estimation",
 qtype="Conceptual", diff="Medium",
 question=r"For $X_1,\dots,X_n$ iid $U(0,\theta)$, the maximum likelihood estimator of $\theta$ is",
 options=[r"$X_{(n)}$, the largest order statistic", r"$\bar X$", r"$2\bar X$", r"$X_{(1)}$, the smallest order statistic"],
 ans=0,
 sc=r"Likelihood $=\theta^{-n}$ for $\theta\ge X_{(n)}$, strictly decreasing in $\theta$ - so the MLE is the smallest admissible $\theta$, namely $X_{(n)}$.",
 tips=[r"$2\bar X$ is the method-of-moments estimator for this family, NOT the MLE - a very common mix-up.",
       r"Whenever the support depends on $\theta$, differentiating the log-likelihood is invalid; reason directly from the likelihood's shape instead."],
 sol=[r"$f(x,\theta)=1/\theta$ for $0\le x\le\theta$, so $L(\theta)=\theta^{-n}$ provided $\theta\ge\max(x_i)=X_{(n)}$, and $L=0$ otherwise.",
      r"$L(\theta)$ is decreasing in $\theta$ over its valid range, so it is maximized at the smallest permissible $\theta$, i.e. $\hat\theta_{MLE}=X_{(n)}$.",
      r"Option (a)."]),

dict(topic="Methods of Estimation", subtopic="Maximum Likelihood Estimation",
 qtype="Conceptual", diff="Hard",
 question=r"For $X_1,\dots,X_n$ iid $U(\theta-\tfrac12,\theta+\tfrac12)$, the maximum likelihood estimator of $\theta$",
 options=[r"is not unique - any value in $[X_{(n)}-\tfrac12,\ X_{(1)}+\tfrac12]$ maximizes the likelihood equally",
          r"is uniquely $\bar X$",
          r"is uniquely the midrange $(X_{(1)}+X_{(n)})/2$",
          r"does not exist"],
 ans=0,
 sc=r"The likelihood is constant ($=1$) for every $\theta$ satisfying $X_{(n)}-\tfrac12\le\theta\le X_{(1)}+\tfrac12$, so the MLE is non-unique - the midrange is merely one conventional choice among infinitely many.",
 tips=[r"This is a classic 'the MLE need not be unique' example, distinct from the earlier $U(0,\theta)$ case where it IS unique.",
       r"The midrange is popular because it is also the midpoint of the valid interval, but any point of that interval is equally valid as 'an' MLE."],
 sol=[r"The likelihood equals $1$ (constant) as long as every $x_i$ lies in $[\theta-\tfrac12,\theta+\tfrac12]$, i.e. $\theta\ge X_{(n)}-\tfrac12$ and $\theta\le X_{(1)}+\tfrac12$.",
      r"Any $\theta$ in this interval gives the same (maximal) likelihood value, so the MLE set is the whole interval, not a single point.",
      r"Option (a)."]),

dict(topic="Methods of Estimation", subtopic="Method of Moments",
 qtype="Numerical", diff="Medium",
 question=r"An iid sample from $U(a,b)$ has $\bar X=10$ and $\frac1n\sum(X_i-\bar X)^2=12$. The method-of-moments estimates of $a$ and $b$ are",
 options=[r"$a=4,\ b=16$", r"$a=-2,\ b=22$", r"$a=8,\ b=12$", r"$a=0,\ b=20$"],
 ans=0,
 sc=r"$a+b=2\bar X=20$; $b-a=2\sqrt{3\times12}=2\sqrt{36}=12$; solving gives $a=4,b=16$.",
 tips=[r"MoM for $U(a,b)$ always produces the tell-tale factor $2\sqrt3$ multiplying the sample standard deviation for $b-a$.",
       r"Always solve the pair of equations $a+b=2\bar X$, $b-a=2\sqrt{3m_2}$ simultaneously - don't average or guess."],
 sol=[r"Mean: $(a+b)/2=\bar X=10\Rightarrow a+b=20$.",
      r"Variance: $(b-a)^2/12=m_2=12\Rightarrow (b-a)^2=144\Rightarrow b-a=12$.",
      r"Solving: $b=16,\ a=4$.",
      r"Option (a)."]),

dict(topic="Methods of Estimation", subtopic="Method of Moments",
 qtype="Conceptual", diff="Easy",
 question=r"For $X_1,\dots,X_n$ iid Exponential with rate $\theta$ (mean $1/\theta$), do the method-of-moments and maximum-likelihood estimators of $\theta$ coincide?",
 options=[r"Yes, both equal $1/\bar X$", r"No: MLE $=1/\bar X$ but MoM $=\bar X$",
          r"No, they differ by a factor of $n/(n-1)$", r"Only asymptotically, not for finite $n$"],
 ans=0,
 sc=r"Both MLE and MoM for the exponential rate reduce to $\hat\theta=1/\bar X$ - a rare case of exact coincidence for finite $n$.",
 tips=[r"This coincidence is special to the exponential (and a few other one-parameter families) - it does NOT hold in general.",
       r"Contrast with $U(0,\theta)$, where MLE $=X_{(n)}$ but MoM $=2\bar X$ - clearly different there."],
 sol=[r"MLE: maximizing $L(\theta)=\theta^ne^{-\theta\sum x_i}$ gives $\hat\theta_{MLE}=1/\bar X$.",
      r"MoM: setting $E(X)=1/\theta=\bar X$ gives $\hat\theta_{MoM}=1/\bar X$ as well.",
      r"Option (a)."]),

dict(topic="Sufficiency and Completeness", subtopic="Sufficient Estimator and Factorization Theorem",
 qtype="Conceptual", diff="Medium",
 question=r"For $X_1,\dots,X_n$ iid with pdf $f(x,\theta)=e^{-(x-\theta)}$ for $x>\theta$ (shifted exponential), a sufficient statistic for $\theta$ is",
 options=[r"$X_{(1)}$, the minimum order statistic", r"$\bar X$", r"$X_{(n)}$, the maximum order statistic", r"$\sum X_i^2$"],
 ans=0,
 sc=r"Since the support $x>\theta$ depends on $\theta$ through the SMALLEST value needed, the binding constraint - and hence the sufficient statistic - is $X_{(1)}$.",
 tips=[r"This mirrors $U(0,\theta)$ needing $X_{(n)}$: whichever order statistic sits at the boundary that $\theta$ controls is the sufficient one.",
       r"The joint density factors as $e^{n\theta}e^{-\sum x_i}\cdot\mathbb 1(X_{(1)}>\theta)$, isolating $X_{(1)}$ as the only $\theta$-relevant function of the data via the indicator."],
 sol=[r"$f(x,\theta)=e^{-(x-\theta)}\mathbb 1(x>\theta)$; the joint density is $e^{n\theta-\sum x_i}\mathbb 1(x_{(1)}>\theta)$.",
      r"By Fisher-Neyman factorization, this depends on the data (through the indicator) only via $X_{(1)}$, which is therefore sufficient.",
      r"Option (a)."]),

dict(topic="Sufficiency and Completeness", subtopic="Sufficient Estimator and Factorization Theorem",
 qtype="Conceptual", diff="Hard",
 question=r"For $X_1,X_2$ iid Poisson$(\theta)$, why does $T=X_1+2X_2$ fail to be sufficient for $\theta$, while $T'=X_1+X_2$ succeeds?",
 options=[r"The conditional distribution of $(X_1,X_2)$ given $X_1+X_2=t$ is Binomial$(t,\tfrac12)$, free of $\theta$; the conditional distribution given $X_1+2X_2$ still involves $\theta$",
          r"$X_1+2X_2$ need not be an integer",
          r"$X_1+2X_2$ has larger variance than $X_1+X_2$",
          r"Sufficiency requires equal coefficients only for continuous distributions, not discrete ones"],
 ans=0,
 sc=r"The joint pmf depends on the data only through $x_1+x_2$ (the exponent of $\theta$); a differently-weighted combination like $x_1+2x_2$ does not capture this and leaves $\theta$ in the conditional distribution.",
 tips=[r"Fisher-Neyman factorization is the fast way to see this: $f(x_1,x_2;\theta)\propto\theta^{x_1+x_2}e^{-2\theta}$ depends on data only via $x_1+x_2$.",
       r"A sufficient statistic must match how $\theta$ actually enters the likelihood - an arbitrary linear combination generally will not."],
 sol=[r"Joint pmf $\propto\theta^{x_1+x_2}e^{-2\theta}/(x_1!x_2!)$ - the $\theta$-dependent part involves the data only through $x_1+x_2$.",
      r"Given $T'=x_1+x_2=t$, $X_1\sim\text{Binomial}(t,\tfrac12)$ regardless of $\theta$ - the conditional law is free of $\theta$, confirming sufficiency of $T'$.",
      r"No such reduction exists for $T=x_1+2x_2$; its conditional distribution given a fixed value of $T$ still depends on $\theta$, so $T$ is not sufficient.",
      r"Option (a)."]),

dict(topic="Optimal Estimation", subtopic="Rao-Blackwell Theorem and UMVUE Construction",
 qtype="Definition", diff="Medium",
 question=r"If $T$ is sufficient for $\theta$ and $U$ is any unbiased estimator of $\theta$, then $\varphi(T)=E[U\mid T]$ satisfies",
 options=[r"$\varphi(T)$ is free of $\theta$, unbiased for $\theta$, with $\operatorname{Var}(\varphi(T))\le\operatorname{Var}(U)$ for every $\theta$",
          r"$\varphi(T)$ may still depend on $\theta$",
          r"$\varphi(T)$ is unbiased but always has strictly larger variance than $U$",
          r"$\varphi(T)$ always equals $U$"],
 ans=0,
 sc=r"This is exactly the Rao-Blackwell theorem: conditioning on a sufficient statistic never increases variance, and sufficiency guarantees the conditional expectation is a genuine (data-only) statistic.",
 tips=[r"Sufficiency of $T$ is what makes $E[U|T]$ computable without knowledge of $\theta$ - this is the crucial role sufficiency plays here.",
       r"Improvement can be strict or the two can coincide (if $U$ was already a function of $T$) - '$\le$', not '$<$', is the precise claim."],
 sol=[r"Because $T$ is sufficient, the conditional distribution of the sample given $T$ does not involve $\theta$, so $\varphi(T)=E[U|T]$ is a genuine statistic (free of $\theta$).",
      r"By the tower property $E[\varphi(T)]=E[U]=\theta$ (still unbiased), and by the Rao-Blackwell variance inequality $\operatorname{Var}(\varphi(T))\le\operatorname{Var}(U)$ for every $\theta$.",
      r"Option (a)."]),

dict(topic="Optimal Estimation", subtopic="Rao-Blackwell Theorem and UMVUE Construction",
 qtype="Definition", diff="Medium",
 question=r"Rao-Blackwellizing an unbiased estimator using a statistic $T$ produces the UMVUE if and only if $T$ is",
 options=[r"complete and sufficient", r"sufficient only", r"complete only", r"ancillary"],
 ans=0,
 sc=r"Lehmann-Scheffe: completeness (in addition to sufficiency) is exactly what makes the Rao-Blackwellized estimator unique, and hence the UMVUE.",
 tips=[r"Without completeness, Rao-Blackwellization can still improve variance but need not yield a UNIQUE (and hence optimal) result.",
       r"This is the standard two-step recipe examined repeatedly: find a complete sufficient statistic, then Rao-Blackwellize any unbiased estimator using it."],
 sol=[r"Rao-Blackwell alone (sufficiency) guarantees variance improvement but not uniqueness across different unbiased starting points.",
      r"Lehmann-Scheffe adds completeness of $T$, which forces any two unbiased functions of $T$ to be identical almost surely - giving a unique, hence UMVUE, result.",
      r"Option (a)."]),

dict(topic="Optimal Estimation", subtopic="Cramer-Rao Inequality, MVB and Fisher Information",
 qtype="Numerical", diff="Medium",
 question=r"For $X\sim\text{Poisson}(\theta)$, based on a sample of size $n$, the Cramer-Rao lower bound for the variance of any unbiased estimator of $\theta$ is",
 options=[r"$\theta/n$", r"$\theta^2/n$", r"$n/\theta$", r"$1/(n\theta^2)$"],
 ans=0,
 sc=r"Single-observation Fisher information for Poisson is $I(\theta)=1/\theta$; for $n$ iid observations, $I_n(\theta)=n/\theta$, so CRLB $=\theta/n$.",
 tips=[r"$\bar X$ itself attains this bound exactly (its variance is $\theta/n$), so $\bar X$ is the UMVUE for Poisson $\theta$.",
       r"Always scale single-observation Fisher information by $n$ before inverting to get the CRLB for a sample."],
 sol=[r"$\log f(x,\theta)=x\log\theta-\theta-\log x!$; $\partial^2/\partial\theta^2=-x/\theta^2$; $E[-\partial^2/\partial\theta^2]=E(X)/\theta^2=1/\theta$.",
      r"So $I(\theta)=1/\theta$ per observation, $I_n(\theta)=n/\theta$, and CRLB $=1/I_n(\theta)=\theta/n$.",
      r"Option (a)."]),

dict(topic="Optimal Estimation", subtopic="Cramer-Rao Inequality, MVB and Fisher Information",
 qtype="Numerical", diff="Hard",
 question=r"For a single observation from $N(\mu,\theta)$ with $\mu$ known and $\theta=\sigma^2$, the Fisher information $I(\theta)$ equals",
 options=[r"$1/(2\theta^2)$", r"$1/\theta^2$", r"$n/(2\theta^2)$", r"$2\theta^2$"],
 ans=0,
 sc=r"$\log f=-\tfrac12\log(2\pi\theta)-(x-\mu)^2/(2\theta)$; differentiating twice and taking $E[-\cdot]$ gives $I(\theta)=1/(2\theta^2)$ for one observation.",
 tips=[r"For $n$ iid observations, information adds: $I_n(\theta)=n/(2\theta^2)$ - watch which version (single-observation or full-sample) a question actually asks for.",
       r"This is the same computation underlying the CRLB for estimating a normal variance with known mean."],
 sol=[r"$\partial\log f/\partial\theta=-\tfrac1{2\theta}+\tfrac{(x-\mu)^2}{2\theta^2}$; $\partial^2\log f/\partial\theta^2=\tfrac1{2\theta^2}-\tfrac{(x-\mu)^2}{\theta^3}$.",
      r"$E[(x-\mu)^2]=\theta$, so $E[\partial^2\log f/\partial\theta^2]=\tfrac1{2\theta^2}-\tfrac1{\theta^2}=-\tfrac1{2\theta^2}$; hence $I(\theta)=1/(2\theta^2)$.",
      r"Option (a)."]),

dict(topic="Optimal Estimation", subtopic="Bhattacharya Bounds",
 qtype="Conceptual", diff="Medium",
 question=r"The Bhattacharya bound generalizes the Cramer-Rao bound by",
 options=[r"incorporating higher-order derivatives of the log-likelihood to give a bound at least as sharp (tight) as the CRLB",
          r"ignoring the likelihood function entirely",
          r"applying only to biased estimators",
          r"requiring the estimator to be sufficient"],
 ans=0,
 sc=r"Bhattacharya bounds use second- and higher-order score derivatives, giving a sequence of bounds that are never looser than, and often strictly sharper than, the first-order Cramer-Rao bound.",
 tips=[r"When the CRLB is not attained by any estimator, the Bhattacharya bound can still be tight, or closer to the true minimum variance.",
       r"The first Bhattacharya bound (using only first derivatives) reduces exactly to the ordinary CRLB - it is a strict generalization."],
 sol=[r"CRLB uses only the first derivative of the log-likelihood (the score); Bhattacharya bounds incorporate a vector of higher-order derivatives, producing a bound that dominates (is $\ge$) the CRLB.",
      r"Option (a)."]),

dict(topic="Interval Estimation", subtopic="Confidence Intervals, Pivots and Optimum Bounds",
 qtype="Conceptual", diff="Medium",
 question=r"For $X_1,\dots,X_n$ iid Exponential with mean $\theta$, a pivotal quantity for constructing a confidence interval for $\theta$ is",
 options=[r"$2n\bar X/\theta\sim\chi^2_{2n}$", r"$\bar X/\theta\sim N(0,1)$", r"$n\bar X^2/\theta\sim F$", r"$\bar X-\theta\sim\chi^2_1$"],
 ans=0,
 sc=r"Sums of exponential variables relate to the gamma/chi-square family: $2n\bar X/\theta\sim\chi^2_{2n}$ exactly, for every $n$.",
 tips=[r"This pivot appears repeatedly across confidence-interval and hypothesis-testing questions on the exponential distribution - memorize it.",
       r"Being a PIVOT means its distribution is completely free of $\theta$, which is exactly what makes it usable to build a CI."],
 sol=[r"If $X_i$ are iid Exponential(mean $\theta$), then $2X_i/\theta\sim\chi^2_2$, and by additivity of independent chi-squares, $2\sum X_i/\theta=2n\bar X/\theta\sim\chi^2_{2n}$.",
      r"This quantity's distribution does not depend on $\theta$, so it is a valid pivot for a CI.",
      r"Option (a)."]),

dict(topic="Interval Estimation", subtopic="Confidence Intervals, Pivots and Optimum Bounds",
 qtype="Numerical", diff="Medium",
 question=r"A 95% confidence interval for $\mu$ (with $\sigma=4$ known), based on a sample of size $n$, is reported as $(18.04,\ 21.96)$. The sample mean and sample size are",
 options=[r"$\bar X=20,\ n=16$", r"$\bar X=20,\ n=4$", r"$\bar X=20,\ n=64$", r"$\bar X=19,\ n=16$"],
 ans=0,
 sc=r"Midpoint $=20=\bar X$; half-width $=1.96=1.96(4)/\sqrt n\Rightarrow\sqrt n=4\Rightarrow n=16$.",
 tips=[r"The midpoint of any symmetric CI is always the point estimate; only the half-width carries information about $n$.",
       r"Solve $1.96\sigma/\sqrt n=\text{half-width}$ for $n$ - a frequent 'reverse-engineer the CI' question type."],
 sol=[r"Midpoint: $(18.04+21.96)/2=20=\bar X$.",
      r"Half-width: $(21.96-18.04)/2=1.96$; setting $1.96=1.96\times4/\sqrt n$ gives $\sqrt n=4\Rightarrow n=16$.",
      r"Option (a)."]),

dict(topic="Interval Estimation", subtopic="Resampling: Bootstrap and Jackknife",
 qtype="Definition", diff="Easy",
 question=r"The bootstrap method estimates the sampling distribution of a statistic by",
 options=[r"resampling with replacement from the observed data",
          r"resampling without replacement, taking exactly half the data each time",
          r"assuming normality and computing exact moments",
          r"requiring a known parametric family for the population"],
 ans=0,
 sc=r"Bootstrap = resample WITH replacement from the empirical distribution (the observed sample) to approximate the true sampling distribution non-parametrically.",
 tips=[r"'With replacement' is essential - it is what allows bootstrap samples to differ from each other and from the original sample.",
       r"The bootstrap requires no assumption about the population's parametric family, unlike classical CI theory."],
 sol=[r"Bootstrap draws many resamples of size $n$, WITH replacement, from the original data, computes the statistic on each, and uses the resulting empirical distribution to approximate the true sampling distribution.",
      r"Option (a)."]),

dict(topic="Interval Estimation", subtopic="Resampling: Bootstrap and Jackknife",
 qtype="Definition", diff="Medium",
 question=r"The jackknife estimator of the bias of a statistic $T_n$ is based on",
 options=[r"leave-one-out recomputation of the statistic",
          r"bootstrap resampling with replacement",
          r"a single random split of the data into two halves",
          r"repeating the full-sample estimate $n$ times"],
 ans=0,
 sc=r"Jackknife systematically deletes one observation at a time, recomputes $T_{(i)}$ each time, and uses the spread of these leave-one-out values to estimate bias (and variance).",
 tips=[r"Jackknife bias estimate: $(n-1)(\bar T_{(\cdot)}-T_n)$, where $\bar T_{(\cdot)}$ is the average of the $n$ leave-one-out estimates.",
       r"Unlike bootstrap, jackknife is deterministic (exactly $n$ leave-one-out samples, no randomness)."],
 sol=[r"For $i=1,\dots,n$, compute $T_{(i)}$ from the sample with observation $i$ removed; these $n$ 'leave-one-out' values are the basis of the jackknife bias and variance estimators.",
      r"Option (a)."]),

dict(topic="Sufficiency and Completeness", subtopic="Completeness, Minimal Sufficiency, Ancillarity, Basu's Theorem",
 qtype="Conceptual", diff="Hard",
 question=r"For $X\sim N(0,\theta)$ (mean known $=0$, $\theta$ the unknown variance), which statement is correct regarding completeness?",
 options=[r"$X$ alone is not complete for $\theta$; but $X^2$ is complete for $\theta$",
          r"Both $X$ and $X^2$ are complete for $\theta$",
          r"Neither $X$ nor $X^2$ is complete for $\theta$",
          r"$X$ is complete for $\theta$, but $X^2$ is not"],
 ans=0,
 sc=r"$g(X)=X$ has $E_\theta[X]=0$ for every $\theta$ without $X\equiv0$ a.s., so $X$ is not complete; but $X^2/\theta\sim\chi^2_1$ scaling makes $X^2$ complete.",
 tips=[r"A single counterexample function with zero expectation for all $\theta$ (here, $g(X)=X$ itself) is enough to break completeness.",
       r"This exact pair ($X$ vs $X^2$ for $N(0,\theta)$) is a favourite conceptual contrast in this topic."],
 sol=[r"$E_\theta(X)=0$ for every $\theta$, yet $X$ is not a.s. $0$ - this single example already shows $X$ is NOT complete for $\theta$.",
      r"By contrast, $X^2$ is a one-to-one function of a scaled chi-square variable and can be shown to be complete sufficient for $\theta$ in this family.",
      r"Option (a)."]),

dict(topic="Sufficiency and Completeness", subtopic="Completeness, Minimal Sufficiency, Ancillarity, Basu's Theorem",
 qtype="Conceptual", diff="Hard",
 question=r"For iid observations from $U(\theta-\tfrac12,\theta+\tfrac12)$, the range $R=X_{(n)}-X_{(1)}$ is",
 options=[r"ancillary - its distribution does not depend on $\theta$ at all",
          r"a complete sufficient statistic for $\theta$",
          r"an unbiased estimator of $\theta$",
          r"distributed as Poisson$(\theta)$"],
 ans=0,
 sc=r"Shifting $\theta$ shifts the whole sample by a constant, leaving the range unchanged in distribution - the textbook example of an ancillary statistic.",
 tips=[r"$(X_{(1)},X_{(n)})$ jointly is minimal sufficient but NOT complete, precisely because a function of it (the range) is ancillary - a nonzero function with $\theta$-free expectation.",
       r"This links directly to why the MLE is non-unique for this family: the location is only pinned down up to this ancillary range."],
 sol=[r"Because the family is a location family (shifted by $\theta$), the DIFFERENCE between any two order statistics has a distribution free of $\theta$ - hence $R$ is ancillary.",
      r"This also shows $(X_{(1)},X_{(n)})$, while minimal sufficient, cannot be complete: $R$ (a nonzero function of it) has $\theta$-free expectation.",
      r"Option (a)."]),

dict(topic="Point Estimation", subtopic="Characteristics of a Good Estimator (Unbiasedness, Consistency, Efficiency, MSE)",
 qtype="Conceptual", diff="Medium",
 question=r"For iid observations from a standard Cauchy distribution, the sample mean $\bar X$ as an estimator of the location parameter",
 options=[r"is inconsistent - $\bar X$ has exactly the same Cauchy distribution regardless of $n$",
          r"is consistent and efficient",
          r"is unbiased with variance decreasing as $1/n$",
          r"equals the maximum likelihood estimator"],
 ans=0,
 sc=r"The Cauchy distribution has no finite mean or variance; remarkably, $\bar X$ for $n$ iid standard Cauchy variables has EXACTLY the same standard Cauchy distribution as a single observation - it never concentrates.",
 tips=[r"The sample MEDIAN, by contrast, IS a consistent estimator of the Cauchy location parameter - medians are far more robust than means for heavy-tailed distributions.",
       r"This is the standard counterexample showing 'more data always helps' is false without finite-variance assumptions."],
 sol=[r"For iid standard Cauchy $X_1,\dots,X_n$, the characteristic function of $\bar X$ equals that of a single $X_i$ - so $\bar X\stackrel{d}{=}X_1$ for every $n$, meaning $\bar X$ does not concentrate around the true location as $n\to\infty$.",
      r"Hence $\bar X$ is inconsistent for the Cauchy location; the sample median is the standard robust, consistent alternative here.",
      r"Option (a)."]),

dict(topic="Sufficiency and Completeness", subtopic="Sufficient Estimator and Factorization Theorem",
 qtype="Conceptual", diff="Medium",
 question=r"For $X_1,\dots,X_n$ iid $N(\mu,\sigma^2)$ with both parameters unknown, a complete sufficient statistic for $(\mu,\sigma^2)$ is",
 options=[r"$(\bar X,\ S^2)$ jointly", r"$\bar X$ alone", r"$S^2$ alone", r"(sample median, sample range) jointly"],
 ans=0,
 sc=r"The two-parameter normal family has $(\bar X,S^2)$ (equivalently $(\sum X_i,\sum X_i^2)$) as its complete sufficient statistic - neither component alone suffices for both parameters.",
 tips=[r"Neither $\bar X$ alone nor $S^2$ alone is sufficient for the FULL pair $(\mu,\sigma^2)$ - sufficiency here is inherently two-dimensional.",
       r"By Basu's theorem, $\bar X$ and $S^2$ are also INDEPENDENT for the normal family - a separate, equally famous fact."],
 sol=[r"The joint density's exponential-family form shows the natural sufficient statistic is $(\sum x_i,\sum x_i^2)$, a one-to-one function of $(\bar X,S^2)$; completeness of the two-parameter exponential family carries over to this pair.",
      r"Option (a)."]),

dict(topic="Optimal Estimation", subtopic="Rao-Blackwell Theorem and UMVUE Construction",
 qtype="Numerical", diff="Hard",
 question=r"For $X_1,\dots,X_n$ iid Poisson$(\theta)$, with $T=\sum X_i$ complete sufficient, the UMVUE of $\theta^2$ is",
 options=[r"$T(T-1)/n^2$", r"$T^2/n^2$", r"$T(T-1)/n$", r"$T(T+1)/n^2$"],
 ans=0,
 sc=r"Using the Poisson factorial-moment identity $E[T(T-1)]=(n\theta)^2$, dividing by $n^2$ gives an exactly unbiased estimator of $\theta^2$ built from the complete sufficient $T$.",
 tips=[r"$T^2/n^2$ is NOT unbiased for $\theta^2$ (it has an extra $+\theta/n$ bias term) - the falling-factorial correction $T(T-1)$ is what removes this bias exactly.",
       r"This falling-factorial trick generalizes: the UMVUE of $\theta^k$ is $T(T-1)\cdots(T-k+1)/n^k$."],
 sol=[r"Since $T\sim\text{Poisson}(n\theta)$, its factorial moments satisfy $E[T(T-1)]=(n\theta)^2$, so $E[T(T-1)/n^2]=\theta^2$ exactly.",
      r"By Lehmann-Scheffe (T is complete sufficient), this unbiased function of $T$ is the UMVUE of $\theta^2$.",
      r"Option (a)."]),

dict(topic="Interval Estimation", subtopic="Confidence Intervals, Pivots and Optimum Bounds",
 qtype="Conceptual", diff="Easy",
 question=r"To halve the width of a 95% confidence interval for $\mu$ (with $\sigma$ known), the sample size must be",
 options=[r"quadrupled", r"doubled", r"halved", r"increased by 50%"],
 ans=0,
 sc=r"CI width $\propto1/\sqrt n$; halving the width requires $\sqrt n$ to double, i.e. $n$ to quadruple.",
 tips=[r"This inverse-square-root relationship between precision and sample size is worth memorizing as a quick mental-math check.",
       r"The same logic applies to standard errors generally: to halve any SE that scales as $1/\sqrt n$, you must quadruple $n$."],
 sol=[r"Width $\propto\sigma/\sqrt n$. Setting new width $=\tfrac12\times$ old width requires $\sqrt{n_{\text{new}}}=2\sqrt{n_{\text{old}}}$, i.e. $n_{\text{new}}=4n_{\text{old}}$.",
      r"Option (a)."]),

dict(topic="Methods of Estimation", subtopic="Maximum Likelihood Estimation",
 qtype="Definition", diff="Medium",
 question=r"If $\hat\theta$ is the maximum likelihood estimator of $\theta$, and $g$ is any function, the MLE of $g(\theta)$ is",
 options=[r"$g(\hat\theta)$ - the invariance property of MLEs",
          r"undefined unless $g$ is linear",
          r"$g^{-1}(\hat\theta)$",
          r"equal to $\hat\theta$ only if $g$ is the identity function"],
 ans=0,
 sc=r"The MLE invariance property: for ANY function $g$ (not just one-to-one ones, via the induced-likelihood extension), the MLE of $g(\theta)$ is simply $g(\hat\theta)$.",
 tips=[r"This is why, e.g., the MLE of $\theta^2$ or $e^{-\theta}$ can be written down immediately once the MLE of $\theta$ is known - no fresh derivation needed.",
       r"Invariance is a property MLEs enjoy that many other estimators (like unbiased or MoM estimators) do NOT - e.g. $g(\hat\theta_{MoM})$ need not be the MoM estimator of $g(\theta)$."],
 sol=[r"By the invariance principle of maximum likelihood, if $\hat\theta$ maximizes the likelihood for $\theta$, then $g(\hat\theta)$ maximizes the induced likelihood for $g(\theta)$, for any function $g$.",
      r"Option (a)."]),
]
