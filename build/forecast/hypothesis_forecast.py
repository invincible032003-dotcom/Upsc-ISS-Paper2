# -*- coding: utf-8 -*-
"""FORECAST question bank - Hypothesis Testing (25 items), covering testing
basics, Neyman-Pearson/UMP tests, randomized tests, LRT, SPRT and decision
theory. AI-GENERATED forecast content, NOT authentic PYQs.
"""

FORECAST = [
dict(topic="Hypothesis Testing", subtopic="Simple and Composite Hypotheses, Two Kinds of Error, Size and Power",
 qtype="Definition", diff="Easy",
 question=r"The power of a test at an alternative $\theta\in H_1$ is defined as",
 options=[r"$1-\beta(\theta)=P_\theta(\text{reject }H_0)$", r"$\alpha=P(\text{reject }H_0\mid H_0\text{ true})$",
          r"$\beta(\theta)=P_\theta(\text{accept }H_0)$", r"$1-\alpha$"],
 ans=0,
 sc=r"Power is the probability of correctly rejecting $H_0$ when the alternative is true - i.e. $1$ minus the Type II error probability at that alternative.",
 tips=[r"Power is a FUNCTION of $\theta\in H_1$, not a single number, unless the alternative is simple.",
       r"Do not confuse power ($1-\beta$) with $1-\alpha$, which is the probability of correctly ACCEPTING $H_0$ when it is true."],
 sol=[r"By definition, power at $\theta$ is $P_\theta(\text{reject }H_0)=1-P_\theta(\text{accept }H_0)=1-\beta(\theta)$.",
      r"Option (a)."]),

dict(topic="Simple and Composite Hypotheses, Two Kinds of Error, Size and Power", subtopic="Simple and Composite Hypotheses, Two Kinds of Error, Size and Power",
 qtype="Definition", diff="Medium",
 question=r"For a composite null hypothesis $H_0$, the size of a test with critical region $C$ is defined as",
 options=[r"$\sup_{\theta\in H_0}P_\theta(C)$", r"$\inf_{\theta\in H_0}P_\theta(C)$",
          r"the average of $P_\theta(C)$ over $H_0$", r"the Type II error probability"],
 ans=0,
 sc=r"For a composite null, size is the SUPREMUM (worst case) of the rejection probability over all $\theta\in H_0$ - guaranteeing the Type I error never exceeds this bound anywhere in $H_0$.",
 tips=[r"For a SIMPLE null ($H_0:\theta=\theta_0$), size just reduces to $P_{\theta_0}(C)$ - the supremum definition is only needed for composite nulls.",
       r"Using $\inf$ instead of $\sup$ would understate the true worst-case Type I error rate - a common conceptual trap."],
 sol=[r"To control the Type I error rate uniformly across a composite null, size must be defined as the WORST (largest) rejection probability attainable within $H_0$, i.e. $\sup_{\theta\in H_0}P_\theta(C)$.",
      r"Option (a)."]),

dict(topic="Hypothesis Testing", subtopic="Neyman-Pearson Lemma and Best Critical Region",
 qtype="Numerical", diff="Medium",
 question=r"$X$ is a single observation from Exponential$(\theta)$ with pdf $f(x,\theta)=\theta e^{-\theta x}$, $x>0$. To test $H_0:\theta=1$ against $H_1:\theta=2$, the test rejects $H_0$ if $X<0.5$. The size and power of this test are respectively",
 options=[r"$0.39,\ 0.63$", r"$0.63,\ 0.39$", r"$0.50,\ 0.50$", r"$0.39,\ 0.39$"],
 ans=0,
 sc=r"Size $=P(X<0.5\mid\theta=1)=1-e^{-0.5}\approx0.39$; power $=P(X<0.5\mid\theta=2)=1-e^{-1}\approx0.63$.",
 tips=[r"For Exponential$(\theta)$, $P(X<c)=1-e^{-\theta c}$ - substitute $\theta$ and $c$ directly, no calculus needed.",
       r"Since $H_1$ has a larger rate (smaller mean), small $X$ correctly favours $H_1$ - the direction of the critical region matters as much as its size."],
 sol=[r"Size: $P(X<0.5\mid\theta=1)=1-e^{-1(0.5)}=1-e^{-0.5}=1-0.6065=0.3935\approx0.39$.",
      r"Power: $P(X<0.5\mid\theta=2)=1-e^{-2(0.5)}=1-e^{-1}=1-0.3679=0.6321\approx0.63$.",
      r"Option (a)."]),

dict(topic="Hypothesis Testing", subtopic="Neyman-Pearson Lemma and Best Critical Region",
 qtype="Conceptual", diff="Medium",
 question=r"For $X\sim\text{Poisson}(\theta)$, testing $H_0:\theta=\theta_0$ against $H_1:\theta=\theta_1$ with $\theta_1>\theta_0$, the Neyman-Pearson best critical region is of the form",
 options=[r"$X\ge c$", r"$X\le c$", r"$X=c$ exactly", r"$|X-\theta_0|\ge c$"],
 ans=0,
 sc=r"Likelihood ratio $f(x;\theta_0)/f(x;\theta_1)=e^{\theta_1-\theta_0}(\theta_0/\theta_1)^x$ is decreasing in $x$ since $\theta_0/\theta_1<1$; small ratio (reject $H_0$) means large $x$.",
 tips=[r"$\theta_1>\theta_0$ means $H_1$ predicts a LARGER mean, so it makes sense that large observed values favour $H_1$ - a useful sanity check on the direction.",
       r"Always derive the direction from the monotonicity of the likelihood ratio - never guess it from intuition alone, especially for asymmetric families."],
 sol=[r"The NP lemma rejects $H_0$ where the likelihood ratio $f(x;\theta_0)/f(x;\theta_1)$ is small; since this ratio is a decreasing function of $x$ here, 'small ratio' corresponds to 'large $x$'.",
      r"So the best critical region is $X\ge c$ for a suitable threshold $c$.",
      r"Option (a)."]),

dict(topic="Hypothesis Testing", subtopic="UMP, MLR, Unbiased and Similar Tests",
 qtype="Conceptual", diff="Medium",
 question=r"A family with Monotone Likelihood Ratio (MLR) in a statistic $T$ guarantees the existence of a UMP test for",
 options=[r"one-sided hypotheses, e.g. $H_0:\theta\le\theta_0$ vs $H_1:\theta>\theta_0$",
          r"two-sided hypotheses, $H_0:\theta=\theta_0$ vs $H_1:\theta\ne\theta_0$",
          r"any hypothesis, one- or two-sided, without exception",
          r"only point-null versus point-alternative tests"],
 ans=0,
 sc=r"MLR guarantees a UMP test exists for ONE-SIDED composite alternatives; for two-sided alternatives no UMP test generally exists, and one instead seeks a UMP-unbiased (UMPU) test.",
 tips=[r"This is why textbooks pair 'MLR family' with 'one-sided UMP test' as a fixed combination - watch for two-sided alternatives disguised as a trick option.",
       r"Common MLR families: one-parameter exponential families (Binomial, Poisson, Normal with known variance, Exponential) all have MLR in their natural sufficient statistic."],
 sol=[r"The Karlin-Rubin theorem states that MLR in $T$ ensures a UMP level-$\alpha$ test exists for one-sided hypotheses, of the form 'reject if $T$ is large' (or small, depending on direction).",
      r"For two-sided alternatives, no test can be uniformly most powerful against alternatives on both sides simultaneously.",
      r"Option (a)."]),

dict(topic="UMP, MLR, Unbiased and Similar Tests", subtopic="UMP, MLR, Unbiased and Similar Tests",
 qtype="Definition", diff="Medium",
 question=r"For testing $H_0:\theta=\theta_0$ vs $H_1:\theta\ne\theta_0$ in a one-parameter exponential family, since no UMP test exists in general, one instead seeks a",
 options=[r"UMP Unbiased (UMPU) test", r"randomized test only",
          r"test with power exactly $0.5$ everywhere", r"test most powerful against a single chosen alternative only"],
 ans=0,
 sc=r"The standard resolution to 'no UMP test exists for two-sided alternatives' is to restrict attention to UNBIASED tests and find the best one within that class - the UMPU test.",
 tips=[r"UMPU tests for two-sided normal-mean and normal-variance problems are the classical, most commonly examined examples.",
       r"Unbiasedness here means power $\ge\alpha$ at every alternative and $\le\alpha$ throughout $H_0$ - it rules out perverse tests that are powerful in only one direction."],
 sol=[r"Restricting the search to unbiased tests (power never below the size, everywhere in $H_1$) makes the two-sided testing problem tractable, yielding a UMP-unbiased (UMPU) test as the standard solution.",
      r"Option (a)."]),

dict(topic="Hypothesis Testing", subtopic="Randomized Tests",
 qtype="Numerical", diff="Medium",
 question=r"$X\sim\text{Binomial}(10,\,p)$. To test $H_0:p=0.5$ vs $H_1:p>0.5$ at nominal level $\alpha=0.05$ using the NON-randomized test 'reject $H_0$ if $X\ge9$', the EXACT size of this test is",
 options=[r"$0.0107$", r"$0.0547$", r"$0.05$ exactly", r"$0.1074$"],
 ans=0,
 sc=r"$P(X\ge9\mid p=0.5)=\dfrac{\binom{10}{9}+\binom{10}{10}}{2^{10}}=\dfrac{11}{1024}\approx0.0107$ - discreteness prevents hitting $\alpha=0.05$ exactly without randomizing.",
 tips=[r"Discreteness of the Binomial means a non-randomized test can rarely achieve a nominal $\alpha$ exactly - only a randomized test at the boundary point ($X=8$ here) can hit $0.05$ precisely.",
       r"$P(X\ge8\mid p=0.5)=56/1024\approx0.0547$ overshoots $0.05$, while $P(X\ge9)\approx0.0107$ undershoots it - the true size always lies strictly below the nominal level for the conservative non-randomized choice."],
 sol=[r"$P(X\ge9)=P(X=9)+P(X=10)=\dfrac{\binom{10}{9}+\binom{10}{10}}{2^{10}}=\dfrac{10+1}{1024}=\dfrac{11}{1024}=0.01074$.",
      r"Option (a)."]),

dict(topic="Hypothesis Testing", subtopic="Randomized Tests",
 qtype="Numerical", diff="Hard",
 question=r"$X\sim\text{Poisson}(\lambda)$. To test $H_0:\lambda=2$ vs $H_1:\lambda=3$, the randomized test $\varphi(x)=1$ if $x\ge4$; $\varphi(x)=0.4$ if $x=3$; $\varphi(x)=0$ if $x\le2$ has size and power respectively approximately",
 options=[r"$0.215,\ 0.442$", r"$0.442,\ 0.215$", r"$0.323,\ 0.577$", r"$0.180,\ 0.224$"],
 ans=0,
 sc=r"Size $=P(X\ge4\mid\lambda=2)+0.4P(X=3\mid\lambda=2)$; power is the same formula evaluated at $\lambda=3$.",
 tips=[r"Compute $P(X\ge4)=1-P(X\le3)$ from the Poisson pmf, then add the randomization term $0.4\times P(X=3)$ separately - do not fold the randomized point into the 'always reject' region.",
       r"Verify power exceeds size (as it must for any sensible test with $\lambda_1>\lambda_0$) as a quick sanity check."],
 sol=[r"At $\lambda=2$: $P(X\le3)=e^{-2}(1+2+2+\tfrac43)=0.8571$, so $P(X\ge4)=0.1429$; $P(X=3)=\tfrac{8}{6}e^{-2}=0.1804$.",
      r"Size $=0.1429+0.4(0.1804)=0.1429+0.0722=0.2151\approx0.215$.",
      r"At $\lambda=3$: $P(X\le3)=e^{-3}(1+3+4.5+4.5)=0.6472$, so $P(X\ge4)=0.3528$; $P(X=3)=4.5e^{-3}=0.2240$.",
      r"Power $=0.3528+0.4(0.2240)=0.3528+0.0896=0.4424\approx0.442$.",
      r"Option (a)."]),

dict(topic="Hypothesis Testing", subtopic="Randomized Tests",
 qtype="Conceptual", diff="Medium",
 question=r"Randomized tests are needed in hypothesis testing primarily because",
 options=[r"for discrete distributions, no non-randomized critical region can generally achieve an arbitrary prescribed size $\alpha$ exactly",
          r"they always have strictly higher power than non-randomized tests",
          r"continuous distributions require randomization to define a critical region",
          r"randomization removes the need to specify $H_1$"],
 ans=0,
 sc=r"Discreteness of the sample space (Binomial, Poisson, etc.) means the achievable exact sizes form a finite set of values; randomizing at one boundary point lets us hit any $\alpha$ in between exactly.",
 tips=[r"For CONTINUOUS distributions, a non-randomized test can typically achieve any $\alpha$ exactly - randomization is a discreteness-specific device.",
       r"Randomized tests do not improve power in any absolute sense; they trade off precisely hitting a target size against a small amount of extra 'coin-flip' variability."],
 sol=[r"With a discrete test statistic, only finitely many rejection-region sizes are achievable exactly; to hit an arbitrary $\alpha$ strictly between two such achievable sizes, one randomizes (e.g. via a coin toss) at the boundary observation.",
      r"Option (a)."]),

dict(topic="Sequential and Decision Theory", subtopic="Wald's SPRT, OC and ASN Functions",
 qtype="Conceptual", diff="Medium",
 question=r"In Wald's SPRT for testing $H_0$ vs $H_1$ with Type I error $\alpha$ and Type II error $\beta$, the approximate stopping bounds $A$ (lower) and $B$ (upper) for the likelihood ratio satisfy",
 options=[r"$A\approx\dfrac{\beta}{1-\alpha}$, $B\approx\dfrac{1-\beta}{\alpha}$",
          r"$A\approx\dfrac{1-\beta}{\alpha}$, $B\approx\dfrac{\beta}{1-\alpha}$",
          r"$A\approx\dfrac{\alpha}{1-\beta}$, $B\approx\dfrac{1-\alpha}{\beta}$",
          r"$A=\alpha$, $B=\beta$"],
 ans=0,
 sc=r"Wald's bounds: continue sampling while $A<\text{LR}<B$, with $A\approx\beta/(1-\alpha)$ (lower) and $B\approx(1-\beta)/\alpha$ (upper) - getting these two the wrong way round is the classic slip.",
 tips=[r"A quick sanity check: for small $\alpha,\beta$, $B$ is a large number ($\approx1/\alpha$) and $A$ is a small number ($\approx\beta$) - if your formula gives it backwards, you have swapped them.",
       r"These approximate bounds are derived by ignoring the 'overshoot' at the boundary when the SPRT actually stops - an approximation, not exact."],
 sol=[r"Wald's approximation sets $A\approx\dfrac{P(\text{reject }H_0\mid H_1)}{P(\text{reject }H_0\mid H_0)}=\dfrac{1-\beta}{\alpha}$... "
      r"more precisely the SPRT continues while $\dfrac{\beta}{1-\alpha}<\text{LR}_{10}<\dfrac{1-\beta}{\alpha}$, i.e. $A=\dfrac{\beta}{1-\alpha}$ and $B=\dfrac{1-\beta}{\alpha}$.",
      r"Option (a)."]),

dict(topic="Sequential and Decision Theory", subtopic="Wald's SPRT, OC and ASN Functions",
 qtype="Conceptual", diff="Medium",
 question=r"In SPRT, the Operating Characteristic function $L(\theta)$ gives the probability of accepting $H_0$. At $\theta=\theta_0$ (i.e. under $H_0$), $L(\theta_0)$ approximately equals",
 options=[r"$1-\alpha$", r"$\alpha$", r"$1-\beta$", r"$\beta$"],
 ans=0,
 sc=r"$L(\theta_0)$ is the probability of (correctly) accepting $H_0$ when $H_0$ is true, which is $1$ minus the Type I error rate, i.e. $1-\alpha$.",
 tips=[r"Similarly, $L(\theta_1)\approx\beta$ - the probability of (incorrectly) accepting $H_0$ when $H_1$ is true.",
       r"The OC curve is the SPRT analogue of the power function, but expressed as the probability of ACCEPTING (not rejecting) $H_0$."],
 sol=[r"$L(\theta)=P_\theta(\text{accept }H_0)$; at $\theta=\theta_0$, accepting $H_0$ is the correct decision, so $L(\theta_0)=1-P_{\theta_0}(\text{reject }H_0)=1-\alpha$.",
      r"Option (a)."]),

dict(topic="Sequential and Decision Theory", subtopic="Wald's SPRT, OC and ASN Functions",
 qtype="Conceptual", diff="Medium",
 question=r"The Average Sample Number (ASN) of Wald's SPRT, compared to a fixed-sample-size Neyman-Pearson test achieving the SAME $\alpha,\beta$, is generally",
 options=[r"smaller - SPRT is more efficient on average, often needing roughly half the expected sample size",
          r"always larger than the fixed-sample test",
          r"exactly equal to the fixed sample size",
          r"infinite"],
 ans=0,
 sc=r"SPRT's key advantage (Wald-Wolfowitz optimality) is achieving the same $(\alpha,\beta)$ error rates with a SMALLER expected sample size than any fixed-sample test.",
 tips=[r"This efficiency gain is largest when the true parameter is close to $\theta_0$ or $\theta_1$ (the hypothesized values); near the 'indifference zone' midpoint, ASN can be larger.",
       r"SPRT trades a fixed sample size for a RANDOM (but typically smaller on average) one - this is its central practical appeal in sequential analysis."],
 sol=[r"The Wald-Wolfowitz theorem proves that among all tests (sequential or fixed) achieving given $\alpha,\beta$, Wald's SPRT minimizes the expected sample size at $\theta=\theta_0$ and $\theta=\theta_1$ simultaneously.",
      r"Option (a)."]),

dict(topic="Hypothesis Testing", subtopic="Likelihood Ratio Test",
 qtype="Definition", diff="Medium",
 question=r"The likelihood ratio test statistic for testing $H_0:\theta\in\Theta_0$ against the general alternative $\theta\in\Theta$ is defined as",
 options=[r"$\lambda=\dfrac{\sup_{\theta\in\Theta_0}L(\theta)}{\sup_{\theta\in\Theta}L(\theta)}$, and $H_0$ is rejected for small $\lambda$",
          r"$\lambda=\dfrac{\sup_{\theta\in\Theta}L(\theta)}{\sup_{\theta\in\Theta_0}L(\theta)}$, and $H_0$ is rejected for small $\lambda$",
          r"$\lambda$ is always $\ge1$",
          r"$\lambda=L(\theta_0)$ alone, ignoring the alternative"],
 ans=0,
 sc=r"LRT statistic $\lambda\in(0,1]$ compares the best-fit likelihood restricted to $H_0$ against the best-fit likelihood over the whole parameter space; small $\lambda$ signals the data fit $H_0$ poorly relative to the unrestricted fit.",
 tips=[r"$\lambda$ always lies in $(0,1]$ since $\Theta_0\subseteq\Theta$ makes the numerator's supremum never exceed the denominator's.",
       r"Rejecting for SMALL $\lambda$ (not large) is the correct direction - a frequent sign error."],
 sol=[r"By construction $\lambda=\sup_{\Theta_0}L/\sup_\Theta L\le1$; values close to $1$ mean $H_0$ fits nearly as well as the unrestricted model, while small $\lambda$ indicates a poor fit under $H_0$ - hence 'reject for small $\lambda$'.",
      r"Option (a)."]),

dict(topic="Hypothesis Testing", subtopic="Likelihood Ratio Test",
 qtype="Conceptual", diff="Medium",
 question=r"Under $H_0$, as $n\to\infty$, $-2\log\lambda$ (where $\lambda$ is the LRT statistic) asymptotically follows",
 options=[r"$\chi^2_r$, where $r$ is the number of restrictions imposed by $H_0$",
          r"$N(0,1)$", r"an $F$ distribution", r"$t_{n-1}$"],
 ans=0,
 sc=r"Wilks' theorem: $-2\log\lambda\xrightarrow{d}\chi^2_r$ under $H_0$, with $r=\dim(\Theta)-\dim(\Theta_0)$, the number of independent restrictions $H_0$ imposes.",
 tips=[r"This asymptotic result underlies most 'large-sample LRT' procedures used when an exact finite-sample distribution of $\lambda$ is unavailable.",
       r"$r$ counts PARAMETER RESTRICTIONS, not sample size or number of observations - a common point of confusion."],
 sol=[r"Wilks' theorem states that, under standard regularity conditions and $H_0$ true, $-2\log\lambda$ converges in distribution to $\chi^2_r$, where $r$ is the difference in dimension between $\Theta$ and $\Theta_0$.",
      r"Option (a)."]),

dict(topic="Sequential and Decision Theory", subtopic="Decision Theory, Loss, Risk and Bayes Estimators",
 qtype="Definition", diff="Easy",
 question=r"In decision-theoretic estimation, the risk function $R(\theta,\delta)$ of a decision rule $\delta$ is defined as",
 options=[r"$E_\theta[L(\theta,\delta(X))]$", r"$L(\theta,\delta(X))$ for a single realization of $X$",
          r"$\operatorname{Var}_\theta[\delta(X)]$", r"the maximum possible loss over all $\theta$"],
 ans=0,
 sc=r"Risk is the EXPECTED loss, averaged over the sampling distribution of $X$ at each fixed $\theta$ - not the loss for one particular realization.",
 tips=[r"Under squared error loss $L(\theta,\delta)=(\theta-\delta)^2$, risk reduces exactly to MSE: $R(\theta,\delta)=E_\theta[(\delta(X)-\theta)^2]$.",
       r"Risk is a FUNCTION of $\theta$ (a curve), which is why comparing decision rules typically means comparing their entire risk functions, not a single number."],
 sol=[r"By definition, $R(\theta,\delta)=E_\theta[L(\theta,\delta(X))]$, the expectation of the loss taken over the randomness in $X$ for each fixed $\theta$.",
      r"Option (a)."]),

dict(topic="Sequential and Decision Theory", subtopic="Decision Theory, Loss, Risk and Bayes Estimators",
 qtype="Definition", diff="Easy",
 question=r"A decision rule that minimizes the MAXIMUM risk over all $\theta$ is called",
 options=[r"minimax", r"Bayes", r"UMVUE", r"admissible"],
 ans=0,
 sc=r"'Minimax' literally means minimizing the maximum (worst-case) risk across the parameter space - a conservative, distribution-free criterion.",
 tips=[r"Contrast with a BAYES rule, which minimizes the AVERAGE risk with respect to a prior distribution on $\theta$, not the worst case.",
       r"Minimax rules are often (though not always) also Bayes rules with respect to a specific 'least favourable' prior."],
 sol=[r"By definition, a minimax rule $\delta^*$ satisfies $\sup_\theta R(\theta,\delta^*)=\inf_\delta\sup_\theta R(\theta,\delta)$ - it minimizes the worst-case risk.",
      r"Option (a)."]),

dict(topic="Sequential and Decision Theory", subtopic="Decision Theory, Loss, Risk and Bayes Estimators",
 qtype="Definition", diff="Medium",
 question=r"A decision rule $\delta$ is INADMISSIBLE if",
 options=[r"there exists $\delta'$ with $R(\theta,\delta')\le R(\theta,\delta)$ for every $\theta$, and strictly less for at least one $\theta$",
          r"it has the single highest risk at exactly one value of $\theta$",
          r"it is unbiased",
          r"it is a randomized rule"],
 ans=0,
 sc=r"Inadmissibility means another rule DOMINATES it: never worse anywhere, and strictly better somewhere - so there is no reason to ever use the dominated rule.",
 tips=[r"Admissibility is a MINIMAL requirement (not being dominated), not a claim of overall optimality - an admissible rule can still have poor risk everywhere.",
       r"Both minimax and Bayes rules are typically (though with some technical exceptions) admissible."],
 sol=[r"$\delta$ is inadmissible if some other rule $\delta'$ is at least as good at every $\theta$ and strictly better at some $\theta$ - i.e. $\delta'$ dominates $\delta$, making $\delta$ pointless to use.",
      r"Option (a)."]),

dict(topic="Sequential and Decision Theory", subtopic="Decision Theory, Loss, Risk and Bayes Estimators",
 qtype="Definition", diff="Medium",
 question=r"Under squared error loss, the Bayes estimator of $\theta$ given data $x$ is",
 options=[r"the posterior mean $E[\theta\mid x]$", r"the posterior mode", r"the maximum likelihood estimate", r"the prior mean, ignoring the data"],
 ans=0,
 sc=r"For squared error loss specifically, the Bayes estimator (minimizer of posterior expected loss) is exactly the posterior mean - a standard, frequently-tested result.",
 tips=[r"Under ABSOLUTE error loss, the Bayes estimator would instead be the posterior MEDIAN - the optimal point estimate depends on the loss function chosen.",
       r"Under 0-1 loss (for discrete $\theta$), the Bayes estimator is the posterior MODE (MAP estimate) - three different loss functions, three different Bayes rules."],
 sol=[r"Minimizing $E[(\theta-a)^2\mid x]$ over $a$ gives $a=E[\theta\mid x]$, the posterior mean - a standard calculus result (complete the square, or differentiate and set to zero).",
      r"Option (a)."]),

dict(topic="Hypothesis Testing", subtopic="UMP, MLR, Unbiased and Similar Tests",
 qtype="Definition", diff="Hard",
 question=r"A critical region $C$ is called 'similar' with respect to a nuisance parameter if",
 options=[r"its size is the same constant for every value of the nuisance parameter, throughout $H_0$",
          r"it contains only sample points that resemble each other",
          r"it has size exactly $0.5$",
          r"it is necessarily randomized"],
 ans=0,
 sc=r"A similar region has rejection probability that does NOT vary with the nuisance parameter anywhere in $H_0$ - essential when $H_0$ is composite due to an unknown nuisance parameter.",
 tips=[r"Similar regions are the standard tool for handling composite nulls with nuisance parameters, often constructed via conditioning on a sufficient statistic for the nuisance parameter.",
       r"Every similar test is, by construction, also of constant size across the nuisance parameter's range - the two ideas are directly linked."],
 sol=[r"When $H_0$ involves a nuisance parameter $\eta$ (e.g. $H_0:\mu=\mu_0$, $\sigma$ unknown), a region $C$ is 'similar on the boundary' if $P_{\theta_0,\eta}(C)=\alpha$ for every value of $\eta$ - i.e. its size is unaffected by the nuisance parameter.",
      r"Option (a)."]),

dict(topic="Hypothesis Testing", subtopic="UMP, MLR, Unbiased and Similar Tests",
 qtype="Definition", diff="Medium",
 question=r"A test $\varphi$ is said to be UNBIASED for testing $H_0$ against $H_1$ if",
 options=[r"its power at every alternative in $H_1$ is at least as large as its size",
          r"its power equals exactly $\alpha$ everywhere",
          r"it has power $1$ at every alternative",
          r"it never commits a Type II error"],
 ans=0,
 sc=r"Unbiasedness of a test requires the power function to be $\ge\alpha$ throughout $H_1$ and $\le\alpha$ throughout $H_0$ - the test should never be 'more likely' to reject under a false alternative than under some point of $H_0$ itself.",
 tips=[r"An unbiased test rules out perverse behaviour where power at some alternative dips BELOW the size - which would mean the test is less likely to detect that alternative than it is to make a Type I error.",
       r"UMPU tests are found by first restricting to the (typically large) class of unbiased tests, then finding the most powerful one within that class."],
 sol=[r"Formally, $\varphi$ is unbiased if $\beta_\varphi(\theta)\ge\alpha$ for all $\theta\in H_1$ and $\beta_\varphi(\theta)\le\alpha$ for all $\theta\in H_0$, where $\beta_\varphi$ is the power function.",
      r"Option (a)."]),

dict(topic="Hypothesis Testing", subtopic="Simple and Composite Hypotheses, Two Kinds of Error, Size and Power",
 qtype="Definition", diff="Easy",
 question=r"The power function of a test $\varphi$, $\beta(\theta)=E_\theta[\varphi(X)]$, is defined",
 options=[r"for every $\theta$ in the full parameter space, both under $H_0$ and $H_1$",
          r"only for $\theta\in H_1$",
          r"as the constant $1-\alpha$ everywhere",
          r"only at a single point, $\theta=\theta_0$"],
 ans=0,
 sc=r"The power function $\beta(\theta)=P_\theta(\text{reject }H_0)$ is defined over the ENTIRE parameter space: under $H_0$ it equals the (nominal) size, and under $H_1$ it equals the power.",
 tips=[r"Plotting $\beta(\theta)$ across the WHOLE parameter space (not just $H_1$) is exactly how size, power, and unbiasedness are all read off one curve.",
       r"A common oversight is to think of 'power' as only meaningful under $H_1$ - but the SAME function, evaluated at $H_0$ points, gives the size."],
 sol=[r"$\beta(\theta)=E_\theta[\varphi(X)]=P_\theta(\text{reject }H_0)$ is well-defined for every $\theta$ in the parameter space; restricted to $H_0$ it is the size, restricted to $H_1$ it is the power.",
      r"Option (a)."]),

dict(topic="Hypothesis Testing", subtopic="Neyman-Pearson Lemma and Best Critical Region",
 qtype="Numerical", diff="Hard",
 question=r"$X\sim N(\mu,25)$. A sample of size $n=25$ is used to test $H_0:\mu=50$ against $H_1:\mu=53$ at $\alpha=0.05$ (one-sided, using $z_{0.05}=1.645$). The power of the test is approximately",
 options=[r"$0.91$", r"$0.05$", r"$0.50$", r"$0.99$"],
 ans=0,
 sc=r"Critical value $\bar x=50+1.645(5/5)=51.645$; power $=P(\bar X>51.645\mid\mu=53)=P(Z>-1.355)\approx0.91$.",
 tips=[r"$SE=\sigma/\sqrt n=5/5=1$ here - always recompute the standard error before converting to a $z$-score.",
       r"A NEGATIVE $z$ in the power calculation (as here) means the critical value lies BELOW the true alternative mean, correctly signalling high power."],
 sol=[r"$SE=5/\sqrt{25}=1$; critical value $=50+1.645(1)=51.645$.",
      r"Power $=P(\bar X>51.645\mid\mu=53)=P\!\left(Z>\dfrac{51.645-53}{1}\right)=P(Z>-1.355)=\Phi(1.355)\approx0.912$.",
      r"Option (a)."]),

dict(topic="Hypothesis Testing", subtopic="UMP, MLR, Unbiased and Similar Tests",
 qtype="Conceptual", diff="Medium",
 question=r"For $X\sim N(\mu,\sigma^2)$ with $\sigma^2$ known, testing $H_0:\mu\le\mu_0$ against $H_1:\mu>\mu_0$, the UMP test rejects $H_0$ when",
 options=[r"$\bar X>\mu_0+z_\alpha\sigma/\sqrt n$", r"$\bar X<\mu_0-z_\alpha\sigma/\sqrt n$",
          r"$|\bar X-\mu_0|>z_{\alpha/2}\sigma/\sqrt n$", r"$\bar X=\mu_0$"],
 ans=0,
 sc=r"The standard one-sided UMP test for a normal mean (variance known) rejects for large $\bar X$, with the threshold set by the UPPER-tail critical value $z_\alpha$.",
 tips=[r"Using $z_{\alpha/2}$ (the two-sided critical value) here would be a mismatch - one-sided alternatives always pair with one-sided ($z_\alpha$) critical values.",
       r"This UMP result follows directly from MLR of the normal family in $\bar X$ (or equivalently in $\sum x_i$), via the Karlin-Rubin theorem."],
 sol=[r"By the Karlin-Rubin theorem, the normal family (known $\sigma^2$) has MLR in $\bar X$, so the UMP test for this one-sided problem rejects for large $\bar X$: specifically $\bar X>\mu_0+z_\alpha\sigma/\sqrt n$.",
      r"Option (a)."]),

dict(topic="Hypothesis Testing", subtopic="Likelihood Ratio Test",
 qtype="Conceptual", diff="Medium",
 question=r"For testing equality of two normal population variances, $H_0:\sigma_1^2=\sigma_2^2$, using independent samples of sizes $n_1,n_2$, the statistic $S_1^2/S_2^2$ under $H_0$ follows",
 options=[r"$F_{n_1-1,\,n_2-1}$", r"$t_{n_1+n_2-2}$", r"$\chi^2_{n_1+n_2-2}$", r"$N(0,1)$"],
 ans=0,
 sc=r"The ratio of two independent sample variances (each scaled chi-square) from normal populations with equal true variance follows the $F$ distribution with numerator and denominator df equal to each sample's error df.",
 tips=[r"Always match numerator df to the FIRST sample's variance ($n_1-1$) and denominator df to the SECOND's ($n_2-1$) - swapping them inverts the test statistic's distribution.",
       r"This is the standard variance-ratio test underlying, among other things, the preliminary check before choosing pooled vs. Welch two-sample $t$-tests."],
 sol=[r"$(n_1-1)S_1^2/\sigma_1^2\sim\chi^2_{n_1-1}$ and $(n_2-1)S_2^2/\sigma_2^2\sim\chi^2_{n_2-1}$, independent; under $H_0:\sigma_1^2=\sigma_2^2$, their ratio (each divided by its own df) gives $S_1^2/S_2^2\sim F_{n_1-1,n_2-1}$.",
      r"Option (a)."]),

dict(topic="Sequential and Decision Theory", subtopic="Wald's SPRT, OC and ASN Functions",
 qtype="Conceptual", diff="Medium",
 question=r"Under Wald's SPRT, if $\theta$ is the true parameter value (with $\theta_0\ne\theta_1$ and finite, nonzero boundaries $A,B$), the probability that the procedure eventually terminates with a decision is",
 options=[r"$1$ - the SPRT terminates with probability $1$", r"$0$", r"$\alpha$", r"$\beta$"],
 ans=0,
 sc=r"A fundamental SPRT result: as long as $\theta_0\ne\theta_1$, the procedure terminates (reaches a decision) with probability $1$, regardless of the true $\theta$.",
 tips=[r"This is what makes SPRT a well-defined, always-terminating procedure in practice - sampling is guaranteed to stop eventually, even though the sample size itself is random.",
       r"The proof relies on the log-likelihood-ratio random walk having a nonzero drift whenever $\theta\ne(\theta_0+\theta_1)/2$-type boundary cases are handled appropriately, ensuring it eventually crosses one of the two boundaries."],
 sol=[r"Wald's theory establishes that the sequential probability ratio random walk crosses one of the two finite boundaries $A,B$ with probability $1$ in finite (expected) time, for any true $\theta$, whenever $\theta_0\ne\theta_1$.",
      r"Option (a)."]),
]
