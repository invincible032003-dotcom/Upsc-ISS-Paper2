# -*- coding: utf-8 -*-
"""FORECAST question bank - Linear Models (25 items).

These are AI-GENERATED forecast questions, NOT authentic UPSC ISS PYQs.
They are built from the archetype/topic-weightage analysis in
04_Forecast_Model_2027.md and the theory ledger in
01_Linear_Models_PYQ_Mined.md, and are independently verified below.
Every entry must carry provenance="forecast" once assembled - never to
be mixed into an authentic-PYQ mock.
"""

FORECAST = [
dict(topic="Matrix Theory for Linear Models", subtopic="Generalized Inverse of a Matrix",
 qtype="Conceptual", diff="Medium",
 question=r"If $A$ is an $m\times n$ matrix of rank $r$, and $A^-$ is a generalized inverse satisfying $AA^-A=A$, what are the order of $A^-$ and the minimum possible rank of $A^-$ over all valid choices?",
 options=[r"Order $n\times m$; minimum rank $r$",
          r"Order $m\times n$; minimum rank $r$",
          r"Order $n\times m$; minimum rank $m$",
          r"Order $n\times m$; minimum rank $\min(m,n)$"],
 ans=0,
 sc=r"Conformability forces $A^-$ to be $n\times m$; a reflexive g-inverse achieves the lower bound $\operatorname{rank}(A^-)=r$.",
 tips=[r"$\operatorname{rank}(A^-)\ge r$ always; equality holds for reflexive g-inverses ($A^-AA^-=A^-$ too).",
       r"Conformability of $AA^-A$ pins the order of $A^-$ to $n\times m$ whenever $A$ is $m\times n$."],
 sol=[r"For $AA^-A$ to be defined and equal $A$ ($m\times n$), $A^-$ must be $n\times m$.",
      r"Any g-inverse satisfies $\operatorname{rank}(A^-)\ge\operatorname{rank}(A)=r$; a reflexive choice attains rank exactly $r$, the minimum possible.",
      r"Option (a)."]),

dict(topic="Matrix Theory for Linear Models", subtopic="Generalized Inverse of a Matrix",
 qtype="Conceptual", diff="Medium",
 question=r"For a symmetric matrix $A$, which statement about its generalized inverse is correct?",
 options=[r"A generalized inverse of a symmetric matrix is always itself symmetric",
          r"A symmetric generalized inverse can always be found, but not every g-inverse of a symmetric matrix is symmetric",
          r"Symmetric matrices never possess a generalized inverse",
          r"A g-inverse of a symmetric matrix is symmetric only if $A$ is also idempotent"],
 ans=1,
 sc=r"Spectral decomposition $A=PDP'$ gives a ready-made symmetric g-inverse $PD^-P'$, but the g-inverse family is much larger and includes non-symmetric members too.",
 tips=[r"Existence of a symmetric g-inverse follows from the spectral theorem for symmetric $A$.",
       r"Uniqueness fails: the class of all g-inverses of a symmetric $A$ contains both symmetric and non-symmetric matrices."],
 sol=[r"By the spectral theorem, $A=PDP'$ with $P$ orthogonal, $D$ diagonal; $PD^-P'$ is a symmetric g-inverse of $A$.",
      r"However, the defining condition $AGA=A$ does not force $G$ to be symmetric in general - other, non-symmetric $G$ also satisfy it.",
      r"Option (b)."]),

dict(topic="Matrix Theory for Linear Models", subtopic="Generalized Inverse of a Matrix",
 qtype="Numerical", diff="Medium",
 question=r"Let $A=\begin{pmatrix}1&2\\2&4\end{pmatrix}$. Which of the following matrices $G$ satisfies $AGA=A$ (i.e. is a generalized inverse of $A$)?",
 options=[r"$\begin{pmatrix}1&0\\0&0\end{pmatrix}$",
          r"$\begin{pmatrix}0.5&0\\0&0.5\end{pmatrix}$",
          r"$\begin{pmatrix}0&0\\0&1\end{pmatrix}$",
          r"$\begin{pmatrix}1&1\\1&1\end{pmatrix}$"],
 ans=0,
 sc=r"$A$ has rank 1; test each $G$ by computing $AGA$ and checking it reproduces $A$ exactly - only option (a) survives.",
 tips=[r"For rank-deficient $A$, most 'plausible-looking' matrices fail $AGA=A$ - always verify by direct substitution.",
       r"Since $A=uu'$ with $u=(1,2)'$, any g-inverse must satisfy $u'Gu=1$; check this shortcut against each option."],
 sol=[r"With $G=\begin{pmatrix}1&0\\0&0\end{pmatrix}$: $AG=\begin{pmatrix}1&0\\2&0\end{pmatrix}$, then $(AG)A=\begin{pmatrix}1&2\\2&4\end{pmatrix}=A$. Valid.",
      r"With $G=0.5I$: $AGA=0.5\,A^2$; since $A^2=5A$ (as $A=uu'$, $u'u=5$), $AGA=2.5A\ne A$. Invalid.",
      r"With $G=\begin{pmatrix}0&0\\0&1\end{pmatrix}$: $AGA=4A\ne A$. Invalid.",
      r"With $G=\begin{pmatrix}1&1\\1&1\end{pmatrix}$: $AGA=9A\ne A$. Invalid.",
      r"Option (a)."]),

dict(topic="Theory of Linear Estimation", subtopic="Gauss-Markov Theorem, Normal Equations and BLUE",
 qtype="Numerical", diff="Hard",
 question=r"Let $X_i,Y_i,Z_i$ ($i=1,2,3$) be nine independent observations with common variance $\sigma^2$, and $E(X_i)=\theta_1+\theta_2$, $E(Y_i)=\theta_1-\theta_2$, $E(Z_i)=2\theta_1$. Writing $X,Y,Z$ for $\sum_{i=1}^3X_i,\sum_{i=1}^3Y_i,\sum_{i=1}^3Z_i$, the BLUE of $\theta_1$ is",
 options=[r"$\dfrac{1}{18}(X+Y+2Z)$",
          r"$\dfrac{1}{9}(X+Y+Z)$",
          r"$\dfrac{1}{18}(X-Y+2Z)$",
          r"$\dfrac{1}{18}(2X+2Y+Z)$"],
 ans=0,
 sc=r"$X\sim3(\theta_1+\theta_2)$, $Y\sim3(\theta_1-\theta_2)$, $Z\sim6\theta_1$, each with variance $3\sigma^2$ and mutually independent; minimize $a^2+b^2+c^2$ subject to unbiasedness for $\theta_1$.",
 tips=[r"Unbiasedness forces $a=b$ (to cancel the $\theta_2$ terms) and $6a+6c=1$.",
       r"Minimizing $2a^2+c^2$ subject to $a+c=1/6$ gives $a=1/18,\ c=1/9$ by calculus."],
 sol=[r"For $aX+bY+cZ$ to be unbiased for $\theta_1$: $3(a+b+2c)\theta_1+3(a-b)\theta_2=\theta_1$, so $a=b$ and $6a+6c=1$.",
      r"Variance $\propto a^2+b^2+c^2=2a^2+c^2$ with $c=1/6-a$; minimizing gives $a=1/18$, hence $b=1/18$, $c=1/9$.",
      r"BLUE $=\frac{1}{18}X+\frac{1}{18}Y+\frac{1}{9}Z=\frac{1}{18}(X+Y+2Z)$; direct check: $E=[3(\theta_1+\theta_2)+3(\theta_1-\theta_2)+12\theta_1]/18=\theta_1$.",
      r"Option (a)."]),

dict(topic="Theory of Linear Estimation", subtopic="Regression Specifics, Design Matrix, Multicollinearity",
 qtype="Conceptual", diff="Medium",
 question=r"For the model $y_i=\beta_1+\beta_2(x_i-\alpha)$, $i=1,\dots,n$, the least-squares estimators $\hat\beta_1$ and $\hat\beta_2$ are uncorrelated when $\alpha$ equals",
 options=[r"$\bar x$ (the sample mean of $x$)",
          r"$0$",
          r"the sample median of $x$",
          r"$\sum x_i^2/n$"],
 ans=0,
 sc=r"$X'X$ is diagonal exactly when $\sum(x_i-\alpha)=0$, i.e. $\alpha=\bar x$ - the standard centering trick.",
 tips=[r"Centering the regressor at its own mean is the general device for decorrelating intercept and slope estimates.",
       r"With $D(\hat\beta)=\sigma^2(X'X)^{-1}$, a diagonal $X'X$ immediately gives $\operatorname{Cov}(\hat\beta_1,\hat\beta_2)=0$."],
 sol=[r"Design matrix columns are $1$ and $(x_i-\alpha)$; $X'X=\begin{pmatrix}n&\sum(x_i-\alpha)\\\sum(x_i-\alpha)&\sum(x_i-\alpha)^2\end{pmatrix}$.",
      r"The off-diagonal entry is $n(\bar x-\alpha)$, which vanishes iff $\alpha=\bar x$, making $X'X$ diagonal and hence $(X'X)^{-1}$ diagonal too.",
      r"Option (a)."]),

dict(topic="Analysis of Variance", subtopic="Two-way Classification and ANOVA",
 qtype="Numerical", diff="Easy",
 question=r"In a two-way classification with interaction, factor $A$ at 3 levels, factor $B$ at 4 levels, and 5 observations per cell, the error degrees of freedom equal",
 options=[r"48", r"59", r"12", r"44"],
 ans=0,
 sc=r"Error df $=ab(m-1)=3\times4\times4=48$.",
 tips=[r"Total df $=abm-1=59$ splits as $A$:2, $B$:3, $AB$:6, Error:48 - these sum to 59, a useful check.",
       r"Error df in a replicated two-way design is always $ab(m-1)$, independent of how $A,B$ df are computed."],
 sol=[r"$N=abm=3\times4\times5=60$ total observations.",
      r"Error df $=N-ab=60-12=48$, equivalently $ab(m-1)=12\times4=48$.",
      r"Check: $2+3+6+48=59=N-1$. Consistent.",
      r"Option (a)."]),

dict(topic="Analysis of Variance", subtopic="One-way Classification and ANOVA",
 qtype="Numerical", diff="Easy",
 question=r"In a one-way ANOVA with 4 treatments and 5 observations per treatment, Treatment SS $=45$ and Total SS $=105$. The value of the $F$ statistic is",
 options=[r"4.00", r"3.00", r"5.00", r"6.00"],
 ans=0,
 sc=r"Error SS $=105-45=60$; $F=(45/3)/(60/16)=15/3.75=4$.",
 tips=[r"Treatment df $=k-1=3$; Error df $=N-k=16$ (since $N=20$).",
       r"Always subtract Treatment SS from Total SS to get Error SS before forming the $F$ ratio."],
 sol=[r"$N=20$; Treatment df $=3$, Error df $=16$; Error SS $=105-45=60$.",
      r"MS(Treatment)$=45/3=15$; MS(Error)$=60/16=3.75$.",
      r"$F=15/3.75=4.00$.",
      r"Option (a)."]),

dict(topic="Theory of Linear Estimation", subtopic="Estimable Functions, Estimation and Error Space",
 qtype="Conceptual", diff="Medium",
 question=r"In the Gauss-Markov model $Y=X\beta+e$, a linear parametric function $\lambda'\beta$ is estimable if and only if",
 options=[r"$\lambda'=a'X$ for some vector $a$ (i.e. $\lambda$ lies in the row space of $X$)",
          r"$\lambda$ lies in the null space of $X$",
          r"$X$ has full column rank",
          r"$\lambda'\lambda=1$"],
 ans=0,
 sc=r"Estimability is exactly membership of $\lambda'$ in the row space of $X$ (equivalently, of $X'X$).",
 tips=[r"When $X$ is full column rank, EVERY $\lambda'\beta$ is estimable, since the row space of $X$ is then all of $\mathbb R^p$.",
       r"This condition does not require normality or independence of errors - it is purely a linear-algebra fact about $X$."],
 sol=[r"$\lambda'\beta$ is estimable iff there is a linear unbiased estimator $a'Y$ for it, i.e. $E(a'Y)=a'X\beta=\lambda'\beta$ for all $\beta$, i.e. $a'X=\lambda'$.",
      r"This says exactly that $\lambda'$ is a linear combination of the rows of $X$ - the row space condition.",
      r"Option (a)."]),

dict(topic="Theory of Linear Estimation", subtopic="Gauss-Markov Theorem, Normal Equations and BLUE",
 qtype="Conceptual", diff="Medium",
 question=r"For the model $Y=X\beta+e$ with possibly singular $X'X$, if $G$ is any generalized inverse of $X'X$, then $XGX'X$ equals",
 options=[r"$X$, for every choice of generalized inverse $G$", r"$X'X$", r"the identity matrix", r"the zero matrix"],
 ans=0,
 sc=r"$XGX'X=X$ for any g-inverse $G$ of $X'X$ - the key lemma behind invariance of fitted values $\hat Y=X\hat\beta$ to the choice of $G$.",
 tips=[r"This is why $\hat\beta=G X'Y$ can differ across choices of $G$, yet $X\hat\beta$ (and every estimable function) stays the same.",
       r"The lemma follows from $\operatorname{rank}(X'X)=\operatorname{rank}(X)$ and a standard g-inverse identity."],
 sol=[r"This is a standard lemma of generalized-inverse theory for normal equations: whenever $G$ satisfies $(X'X)G(X'X)=X'X$, it follows that $XGX'X=X$.",
      r"Consequently fitted values and all estimable functions of $\beta$ are invariant to the choice of $G$ used to solve $X'X\beta=X'Y$.",
      r"Option (a)."]),

dict(topic="Theory of Linear Estimation", subtopic="Variances and Covariances of LSEs; Error Variance",
 qtype="Numerical", diff="Medium",
 question=r"In simple linear regression with $n=5$, $\bar x=3$, $\sum(x_i-\bar x)^2=20$ and error variance $\sigma^2=4$, $\operatorname{Cov}(\hat\beta_0,\hat\beta_1)$ equals",
 options=[r"$-0.6$", r"$0.6$", r"$-1.2$", r"$0$"],
 ans=0,
 sc=r"$\operatorname{Cov}(\hat\beta_0,\hat\beta_1)=-\sigma^2\bar x/\sum(x_i-\bar x)^2=-4(3)/20=-0.6$.",
 tips=[r"The covariance is always $-\sigma^2\bar x/S_{xx}$ - note the negative sign, a frequent trap.",
       r"It vanishes only when $\bar x=0$, matching the centering result used elsewhere in this section."],
 sol=[r"$\operatorname{Cov}(\hat\beta_0,\hat\beta_1)=-\sigma^2\bar x/\sum(x_i-\bar x)^2=-4\times3/20=-12/20=-0.6$.",
      r"Option (a)."]),

dict(topic="Matrix Theory for Linear Models", subtopic="Hat Matrix, Residuals and Quadratic Forms",
 qtype="Conceptual", diff="Medium",
 question=r"For the hat matrix $H=X(X'X)^{-1}X'$ in a full-rank regression model, which statement is FALSE?",
 options=[r"$\operatorname{Cov}(e_i,e_j)=+\sigma^2h_{ij}$ for $i\ne j$",
          r"$0\le h_{ii}\le1$ for every $i$",
          r"$\sum_ih_{ii}=p$, the number of parameters",
          r"$\operatorname{Cov}(e_i,e_j)=-\sigma^2h_{ij}$ for $i\ne j$"],
 ans=0,
 sc=r"Residual covariance carries a MINUS sign: $\operatorname{Cov}(e_i,e_j)=-\sigma^2h_{ij}$; the '+' version is the planted false statement.",
 tips=[r"$D(e)=\sigma^2(I-H)$, so off-diagonal entries are $-\sigma^2h_{ij}$, never positive $h_{ij}$.",
       r"$h_{ii}\in[0,1]$ and $\sum h_{ii}=\operatorname{tr}(H)=\operatorname{rank}(H)=p$ are both standard, true facts."],
 sol=[r"Since $e=(I-H)Y$ has $D(e)=\sigma^2(I-H)$, the $(i,j)$ entry for $i\ne j$ is $-\sigma^2h_{ij}$, not $+\sigma^2h_{ij}$.",
      r"The other three statements ($h_{ii}\in[0,1]$, $\sum h_{ii}=p$) are standard true properties of the idempotent projection $H$.",
      r"Option (a) is false."]),

dict(topic="Analysis of Variance", subtopic="Fixed, Random and Mixed Effects Models",
 qtype="Conceptual", diff="Hard",
 question=r"In a two-way ANOVA with both factors random and replication present, testing $H_0:$ no main effect of factor $A$ requires the $F$-test denominator to be",
 options=[r"MS(AB), the interaction mean square", r"MSE, the error mean square",
          r"MS(B)", r"the total mean square"],
 ans=0,
 sc=r"Under the random-effects model, $E[\text{MS}(A)]=\sigma_e^2+m\sigma_{AB}^2+bm\sigma_A^2$ and $E[\text{MS}(AB)]=\sigma_e^2+m\sigma_{AB}^2$, so $F=\text{MS}(A)/\text{MS}(AB)$ isolates $\sigma_A^2$.",
 tips=[r"Testing the interaction itself DOES use MSE as the denominator, since $E[\text{MSE}]=\sigma_e^2$ alone.",
       r"Using MSE (instead of MS(AB)) to test a random main effect is a classic error - it overstates significance."],
 sol=[r"Expected mean squares under the random-effects model show MS(A) and MS(AB) share the term $m\sigma_{AB}^2$, differing only in the $\sigma_A^2$ contribution.",
      r"Hence $F=\text{MS}(A)/\text{MS}(AB)$ is the correct test for $H_0:\sigma_A^2=0$; using MSE instead would inflate the Type I error rate.",
      r"Option (a)."]),

dict(topic="Theory of Linear Estimation", subtopic="Regression Specifics, Design Matrix, Multicollinearity",
 qtype="Conceptual", diff="Medium",
 question=r"For nested linear models where Model 1 (with $p_1$ parameters) is a special case of Model 2 (with $p_2>p_1$ parameters), both fitted to the same data by least squares, which is necessarily true of the residual sums of squares?",
 options=[r"$\text{RSS}_1\ge\text{RSS}_2$ always, and $\text{RSS}_1-\text{RSS}_2=0$ is possible even when $p_2>p_1$",
          r"$\text{RSS}_1<\text{RSS}_2$ always",
          r"$\text{RSS}_1=\text{RSS}_2$ always",
          r"$\text{RSS}_1-\text{RSS}_2$ can be negative"],
 ans=0,
 sc=r"Model 2's larger parameter space contains Model 1's fit as a special case, so its minimized RSS can only be $\le$ Model 1's.",
 tips=[r"Equality $\text{RSS}_1=\text{RSS}_2$ occurs precisely when the extra parameters' least-squares estimates are exactly zero (or otherwise redundant for this data) - a measure-zero but real event.",
       r"This monotonicity is the basis of the partial-$F$ test for nested models."],
 sol=[r"Since Model 1 is a restriction of Model 2, any fit achievable under Model 1 is also achievable under Model 2; therefore Model 2's minimum RSS cannot exceed Model 1's.",
      r"So $\text{RSS}_1\ge\text{RSS}_2$ always, and the gap $Q=\text{RSS}_1-\text{RSS}_2\ge0$ can still be exactly $0$ for particular data even when $p_2>p_1$.",
      r"Option (a)."]),

dict(topic="Theory of Linear Estimation", subtopic="Correlated Observations, Weighted LS and GLS",
 qtype="Conceptual", diff="Hard",
 question=r"In the model $Y=X\beta+e$ with $D(e)=\sigma^2G$ ($G$ known, positive definite), transforming $Z=G^{-1/2}Y$ reduces the model to Gauss-Markov form. The resulting GLS estimator of $\beta$ is",
 options=[r"$(X'G^{-1}X)^{-1}X'G^{-1}Y$", r"$(X'X)^{-1}X'Y$",
          r"$(X'GX)^{-1}X'GY$", r"$(X'G^2X)^{-1}X'G^2Y$"],
 ans=0,
 sc=r"OLS on the transformed model $Z=WY,\ W=G^{-1/2}X$ gives $(W'W)^{-1}W'Z=(X'G^{-1}X)^{-1}X'G^{-1}Y$ since $G^{-1/2}{}'G^{-1/2}=G^{-1}$.",
 tips=[r"GLS is just OLS after the whitening transform $G^{-1/2}$; it is unbiased and BLUE under the transformed Gauss-Markov assumptions.",
       r"Never use $G$ or $G^2$ in place of $G^{-1}$ in the GLS formula - a very common transcription error."],
 sol=[r"Under $Z=G^{-1/2}Y$, $W=G^{-1/2}X$, the transformed errors $G^{-1/2}e$ have covariance $\sigma^2 G^{-1/2}GG^{-1/2}{}'=\sigma^2I$.",
      r"OLS on $(W,Z)$ gives $\hat\beta=(W'W)^{-1}W'Z=(X'G^{-1}X)^{-1}X'G^{-1}Y$.",
      r"Option (a)."]),

dict(topic="Analysis of Variance", subtopic="Multiple Comparison Tests (Tukey, Scheffe, SNK-Duncan)",
 qtype="Factual", diff="Medium",
 question=r"Which multiple comparison procedure uses the studentized range distribution and a SINGLE fixed critical value for all pairwise comparisons among treatment means (with equal sample sizes)?",
 options=[r"Tukey's HSD test", r"Scheffe's test", r"Student-Newman-Keuls (SNK) test", r"Bonferroni's method"],
 ans=0,
 sc=r"Tukey's HSD: one studentized-range critical value applied uniformly to all pairwise differences - the most standard equal-$n$ pairwise procedure.",
 tips=[r"SNK also uses the studentized range but with STEP-WISE critical values that change with the number of means spanned - not a single fixed value.",
       r"Scheffe's test is more conservative and valid for ANY contrast, not just pairwise comparisons, using the $F$ distribution instead."],
 sol=[r"Tukey's HSD applies one critical value, $q_{\alpha,k,\nu}\cdot\sqrt{\text{MSE}/n}$, to every pairwise comparison - simple, uniform, and standard for equal-$n$ designs.",
      r"SNK varies the critical value with the 'span' of means being compared; Scheffe covers arbitrary contrasts at the cost of power for simple pairwise tests.",
      r"Option (a)."]),

dict(topic="Theory of Linear Estimation", subtopic="Correlated Observations, Weighted LS and GLS",
 qtype="Conceptual", diff="Medium",
 question=r"In the heteroscedastic model $y_i=\beta x_i+e_i$ (no intercept) with $\operatorname{Var}(e_i)=\sigma^2/w_i$ for known weights $w_i$, the weighted least squares estimator of $\beta$ is",
 options=[r"$\dfrac{\sum w_ix_iy_i}{\sum w_ix_i^2}$", r"$\dfrac{\sum x_iy_i}{\sum x_i^2}$",
          r"$\dfrac{\sum w_iy_i}{\sum w_ix_i}$", r"$\dfrac{\sum y_i}{\sum x_i}$"],
 ans=0,
 sc=r"Minimizing $\sum w_i(y_i-\beta x_i)^2$ and setting the derivative to zero gives $\hat\beta=\sum w_ix_iy_i/\sum w_ix_i^2$.",
 tips=[r"WLS weights each squared residual by $w_i$ (inversely proportional to its variance), down-weighting noisier observations.",
       r"Setting all $w_i=1$ recovers the ordinary (unweighted) least-squares slope, a useful sanity check."],
 sol=[r"Minimize $S(\beta)=\sum w_i(y_i-\beta x_i)^2$; $dS/d\beta=-2\sum w_ix_i(y_i-\beta x_i)=0$.",
      r"$\Rightarrow \sum w_ix_iy_i=\beta\sum w_ix_i^2 \Rightarrow \hat\beta=\sum w_ix_iy_i/\sum w_ix_i^2$.",
      r"Option (a)."]),

dict(topic="Matrix Theory for Linear Models", subtopic="Hat Matrix, Residuals and Quadratic Forms",
 qtype="Conceptual", diff="Medium",
 question=r"If $P$ is an idempotent matrix ($P^2=P$) of order $n\times n$ with rank $r$, its eigenvalues are",
 options=[r"All $0$ or $1$, with exactly $r$ eigenvalues equal to $1$",
          r"All equal to $1$",
          r"All equal to $r/n$",
          r"All equal to $\pm1$"],
 ans=0,
 sc=r"Idempotency forces eigenvalues to satisfy $\lambda^2=\lambda$, so $\lambda\in\{0,1\}$; the count of $1$'s equals $\operatorname{tr}(P)=\operatorname{rank}(P)=r$.",
 tips=[r"This underlies why $\sum h_{ii}=p$ for the hat matrix - it is exactly this eigenvalue-counting fact applied to $H$.",
       r"Any eigenvalue $\lambda$ of $P$ satisfies $Pv=\lambda v\Rightarrow P^2v=\lambda^2v=Pv=\lambda v\Rightarrow\lambda^2=\lambda$."],
 sol=[r"If $Pv=\lambda v$, then $P^2v=\lambda^2v$; but $P^2=P$ gives $P^2v=Pv=\lambda v$, so $\lambda^2=\lambda$, forcing $\lambda\in\{0,1\}$.",
      r"Since $\operatorname{tr}(P)=\sum\lambda_i=\operatorname{rank}(P)=r$ for an idempotent matrix, exactly $r$ of the eigenvalues equal $1$ and the rest are $0$.",
      r"Option (a)."]),

dict(topic="Theory of Linear Estimation", subtopic="Regression Specifics, Design Matrix, Multicollinearity",
 qtype="Conceptual", diff="Easy",
 question=r"For the design matrix $X$ ($n\times p$) in a linear model, $\operatorname{rank}(X'X)$ equals",
 options=[r"$\operatorname{rank}(X)$", r"$p$, always", r"$n$, always", r"$\min(n,p)$, regardless of $X$"],
 ans=0,
 sc=r"$\operatorname{rank}(X'X)=\operatorname{rank}(X)$ is a standard linear-algebra identity, true whether or not $X$ has full column rank.",
 tips=[r"This identity is exactly why solving the normal equations via a generalized inverse works even when $X$ is rank-deficient.",
       r"It follows because $X$ and $X'X$ have the same null space: $Xv=0\Leftrightarrow X'Xv=0$."],
 sol=[r"$Xv=0\Rightarrow X'Xv=0$ trivially; conversely $X'Xv=0\Rightarrow v'X'Xv=0\Rightarrow\|Xv\|^2=0\Rightarrow Xv=0$.",
      r"So $X$ and $X'X$ share the same null space, hence the same rank.",
      r"Option (a)."]),

dict(topic="Theory of Linear Estimation", subtopic="Gauss-Markov Theorem, Normal Equations and BLUE",
 qtype="Conceptual", diff="Easy",
 question=r"When $X$ has full column rank $p$, the Gauss-Markov (BLUE) estimator of $\beta$ itself is",
 options=[r"unique, equal to $(X'X)^{-1}X'Y$",
          r"non-unique even though $X'X$ is invertible",
          r"unique only for estimable functions, not for $\beta$ itself",
          r"undefined"],
 ans=0,
 sc=r"Full column rank makes $X'X$ invertible, so every $\lambda'\beta$ (including $\beta$ itself) is estimable with a unique BLUE $(X'X)^{-1}X'Y$.",
 tips=[r"Only in the RANK-DEFICIENT case does one need to fall back on 'only estimable functions have unique BLUEs' - full rank sidesteps that entirely.",
       r"This is the standard, simplest form of the Gauss-Markov theorem taught first, before the generalized-inverse extension."],
 sol=[r"With $X$ full column rank, $X'X$ is nonsingular, so the normal equations $X'X\beta=X'Y$ have the unique solution $\hat\beta=(X'X)^{-1}X'Y$, which is BLUE by Gauss-Markov.",
      r"Option (a)."]),

dict(topic="Theory of Linear Estimation", subtopic="Variances and Covariances of LSEs; Error Variance",
 qtype="Numerical", diff="Medium",
 question=r"In a simple linear regression with error df $=10$, $\hat\beta_1=2.5$, $SE(\hat\beta_1)=0.5$, and $t_{0.025,10}=2.228$, a 95% confidence interval for $\beta_1$ is",
 options=[r"$(1.39,\ 3.61)$", r"$(1.50,\ 3.50)$", r"$(2.00,\ 3.00)$", r"$(0.50,\ 4.50)$"],
 ans=0,
 sc=r"$2.5\pm2.228(0.5)=2.5\pm1.114=(1.386,\ 3.614)$.",
 tips=[r"Always use the $t$ critical value at the ERROR degrees of freedom, not $n-1$, for regression coefficient CIs.",
       r"Half-width $=t\times SE$; add/subtract from the point estimate."],
 sol=[r"Half-width $=2.228\times0.5=1.114$.",
      r"CI $=2.5\pm1.114=(1.386,\ 3.614)\approx(1.39,\ 3.61)$.",
      r"Option (a)."]),

dict(topic="Theory of Linear Estimation", subtopic="Regression Specifics, Design Matrix, Multicollinearity",
 qtype="Conceptual", diff="Medium",
 question=r"Under severe multicollinearity among the regressors (with $X$ still of full column rank), the OLS estimator $\hat\beta=(X'X)^{-1}X'Y$ remains",
 options=[r"unbiased, but with inflated variance and unstable estimates",
          r"biased and inconsistent",
          r"no longer a linear estimator",
          r"completely unaffected"],
 ans=0,
 sc=r"Gauss-Markov only needs full rank, not a well-conditioned $X'X$; multicollinearity inflates $(X'X)^{-1}$'s diagonal (variance) without breaking unbiasedness.",
 tips=[r"Large standard errors and sign instability across resamples are the practical symptoms of this variance inflation.",
       r"Ridge regression deliberately trades a little bias for a large reduction in this variance."],
 sol=[r"Unbiasedness and the Gauss-Markov BLUE property depend only on $X$ having full column rank, which multicollinearity (short of exact linear dependence) does not violate.",
      r"However, near-singular $X'X$ makes $(X'X)^{-1}$ have very large entries, inflating $\operatorname{Var}(\hat\beta_j)=\sigma^2[(X'X)^{-1}]_{jj}$.",
      r"Option (a)."]),

dict(topic="Theory of Linear Estimation", subtopic="Gauss-Markov Theorem, Normal Equations and BLUE",
 qtype="Definition", diff="Medium",
 question=r"The Gauss-Markov theorem states that, under $E(e)=0$, $D(e)=\sigma^2I$, the least-squares estimator of any estimable $\lambda'\beta$ is",
 options=[r"BLUE among all linear unbiased estimators, without requiring normality",
          r"the MLE, which requires a normality assumption",
          r"the UMVUE among ALL unbiased estimators, not just linear ones",
          r"unbiased only, with no minimum-variance guarantee"],
 ans=0,
 sc=r"Gauss-Markov's guarantee is BLUE - best among LINEAR unbiased estimators - and needs no distributional assumption at all.",
 tips=[r"Upgrading BLUE to full UMVUE (among ALL unbiased estimators) needs normality plus a completeness argument via Lehmann-Scheffe - a strictly stronger claim.",
       r"This distinction (BLUE vs UMVUE) is a favourite conceptual trap in this topic."],
 sol=[r"Gauss-Markov compares the LSE only against other LINEAR unbiased estimators and shows it has minimum variance in that restricted class - hence 'BLUE'.",
      r"No normality is assumed or needed; normality is what additionally makes the LSE the UMVUE among all (not just linear) unbiased estimators.",
      r"Option (a)."]),

dict(topic="Matrix Theory for Linear Models", subtopic="Hat Matrix, Residuals and Quadratic Forms",
 qtype="Conceptual", diff="Hard",
 question=r"If $Y\sim N_n(X\beta,\sigma^2I)$ and $H=X(X'X)^{-1}X'$ has rank $p$, then $(Y-X\hat\beta)'(Y-X\hat\beta)/\sigma^2$ follows",
 options=[r"$\chi^2$ with $(n-p)$ degrees of freedom",
          r"$\chi^2$ with $n$ degrees of freedom",
          r"$\chi^2$ with $p$ degrees of freedom",
          r"an $F$ distribution with $(p,n-p)$ degrees of freedom"],
 ans=0,
 sc=r"Residual SS $=e'e=Y'(I-H)Y$ with $(I-H)$ idempotent of rank $n-p$, giving $\chi^2_{n-p}$ under normality.",
 tips=[r"$(I-H)X=0$, so $Y-X\hat\beta=(I-H)Y=(I-H)e$, and $(I-H)$'s rank ($=n-p$) is exactly the chi-square degrees of freedom.",
       r"This result underlies every $t$- and $F$-test built from regression residuals."],
 sol=[r"$Y-X\hat\beta=(I-H)Y$; since $(I-H)$ is symmetric idempotent with rank $n-p$, the standard quadratic-form theorem gives $(Y-X\hat\beta)'(Y-X\hat\beta)/\sigma^2\sim\chi^2_{n-p}$.",
      r"Option (a)."]),

dict(topic="Analysis of Variance", subtopic="One-way Classification and ANOVA",
 qtype="Conceptual", diff="Easy",
 question=r"In one-way ANOVA testing equality of $k$ treatment means from $N$ total observations, the statistic $F=\text{MST}/\text{MSE}$ follows, under $H_0$, the distribution",
 options=[r"$F_{k-1,\,N-k}$", r"$F_{N-k,\,k-1}$", r"$F_{k,\,N-1}$", r"$F_{N-1,\,N-k}$"],
 ans=0,
 sc=r"Numerator df = treatment df = $k-1$; denominator df = error df = $N-k$.",
 tips=[r"The numerator is always the 'smaller', more restrictive source (Treatment); mixing up numerator/denominator df is the classic slip.",
       r"This holds regardless of whether group sizes are equal, as long as $N=\sum n_i$."],
 sol=[r"MST has $k-1$ df; MSE has $N-k$ df; their ratio under $H_0$ follows $F_{k-1,N-k}$ by Cochran's theorem.",
      r"Option (a)."]),

dict(topic="Analysis of Variance", subtopic="Multiple Comparison Tests (Tukey, Scheffe, SNK-Duncan)",
 qtype="Factual", diff="Medium",
 question=r"Compared to Tukey's HSD, Scheffe's method for multiple comparisons is",
 options=[r"more conservative for simple pairwise comparisons, but valid for testing ANY linear contrast, not just pairwise ones",
          r"always identical to Tukey's HSD",
          r"less conservative than Tukey's HSD for pairwise comparisons",
          r"valid only when comparing exactly two treatments"],
 ans=0,
 sc=r"Scheffe controls the family-wise error rate over the INFINITE family of all contrasts, so it must be wider (more conservative) than Tukey for the narrower pairwise-only case.",
 tips=[r"Use Tukey when only pairwise comparisons are of interest (it is more powerful there); use Scheffe when arbitrary contrasts (not just pairs) are to be tested.",
       r"Scheffe's critical value is based on the $F$ distribution, not the studentized range."],
 sol=[r"Because Scheffe's method must simultaneously protect against every possible linear contrast (an uncountable family), its critical value is necessarily larger than Tukey's for the pairwise-only case, making it more conservative there - but uniquely valid for post-hoc contrasts Tukey cannot cover.",
      r"Option (a)."]),
]
