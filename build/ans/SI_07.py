# -*- coding: utf-8 -*-
# Statistical Inference answer pack 7 (2021 Q27-Q54)
A = {
"2021-Q27": dict(ans=2, qtype="Numerical", diff="Hard",
 sc=r"$T=\sum X_i\sim B(2n,\theta)$ is complete sufficient, and $E[T(T-1)]=2n(2n-1)\theta^2$, so $\frac{T^2-T}{2n(2n-1)}$ is the UMVUE of $\theta^2$.",
 tips=[r"Sum of $n$ independent $B(2,\theta)$ variables is $B(2n,\theta)$ - the effective number of trials is $2n$.",
       r"Second factorial moment of $B(N,\theta)$: $E[T(T-1)]=N(N-1)\theta^2$; here $N=2n$.",
       r"$T^2-T=T(T-1)$ - recognising the factorial form is the whole trick."],
 sol=[r"$X_i\sim B(2,\theta)$ independently, so $T=\sum_{i=1}^nX_i\sim B(2n,\theta)$.",
      r"The likelihood is $\theta^T(1-\theta)^{2n-T}$ times a constant, so $T$ is sufficient; being the natural statistic of a full-rank exponential family, it is complete. Statement 1 true.",
      r"$E\left[T(T-1)\right]=2n(2n-1)\theta^2$, so $\dfrac{T^2-T}{2n(2n-1)}$ is unbiased for $\theta^2$.",
      r"It is a function of the complete sufficient statistic, hence the UMVUE by Lehmann-Scheffe. Statement 2 true.",
      r"Option (c)."]),

"2021-Q28": dict(ans=0, qtype="Numerical", diff="Easy",
 sc=r"$\bar x=256/64=4$, $\text{SE}=\sqrt{\bar x/n}=\sqrt{4/64}=0.25$, so the interval is $4\pm1.96(0.25)=(3.51,4.49)$.",
 tips=[r"Poisson: $V(\bar X)=\lambda/n$, estimated by $\bar x/n$ - so the SE is $\sqrt{\bar x/n}$.",
       r"$\sqrt{4/64}=\sqrt{0.0625}=0.25$ exactly - the numbers are designed to be clean.",
       r"Margin $=1.96\times0.25=0.49$, so the interval is $4\pm0.49$."],
 sol=[r"$\bar x=\dfrac{256}{64}=4$, the MLE of $\lambda$.",
      r"$V(\bar X)=\dfrac\lambda n$, estimated by $\dfrac{\bar x}{n}=\dfrac{4}{64}=0.0625$, so $\text{SE}=0.25$.",
      r"$95\%$ CI $=\bar x\pm1.96\,\text{SE}=4\pm1.96(0.25)=4\pm0.49$.",
      r"$=(3.51,\ 4.49)$ - option (a)."]),

"2021-Q29": dict(ans=3, qtype="Conceptual", diff="Easy",
 sc=r"All three hold: $\lambda$ is a ratio of a constrained to an unconstrained supremum (hence in $[0,1]$), $-2\log\lambda$ is asymptotically $\chi^2$ (Wilks), and the LRT is consistent under regularity.",
 tips=[r"$\lambda\le1$ because the numerator maximises over a SUBSET of the denominator's parameter space.",
       r"Wilks' df $=$ number of independent restrictions imposed by $H_0$.",
       r"Consistency here means the power tends to 1 at every fixed alternative as $n\to\infty$."],
 sol=[r"Statement 1: $\lambda=\dfrac{\sup_{\Theta_0}L}{\sup_{\Theta}L}$ with $\Theta_0\subseteq\Theta$, so $0\le\lambda\le1$. True.",
      r"Statement 2: by Wilks' theorem, $-2\log\lambda\xrightarrow{d}\chi^2_r$ under $H_0$ and regularity. True.",
      r"Statement 3: under regularity conditions the likelihood ratio test is consistent. True.",
      r"Option (d)."]),

"2021-Q30": dict(ans=0, qtype="Numerical", diff="Hard",
 sc=r"The density is $N(\mu,2)$, so $Z_i=x_i-5$ and $\sum Z_i\ge\log A$ with $A=\frac{0.95}{0.05}=19$ gives $\sum x_i\ge5n+\log19$.",
 tips=[r"Identify the variance from the constant: $\frac1{2\sqrt\pi}=\frac1{\sqrt{2\pi\sigma^2}}\Rightarrow\sigma^2=2$.",
       r"$Z_i=\frac{(\mu_1-\mu_0)x_i}{\sigma^2}-\frac{\mu_1^2-\mu_0^2}{2\sigma^2}=\frac{2x_i}{2}-\frac{36-16}{4}=x_i-5$.",
       r"$A=\frac{1-\beta}{\alpha}=\frac{0.95}{0.05}=19$ - the $\log19$ in the answer is the give-away."],
 sol=[r"Matching $\dfrac1{2\sqrt\pi}e^{-(x-\mu)^2/4}$ with $\dfrac{1}{\sqrt{2\pi\sigma^2}}e^{-(x-\mu)^2/(2\sigma^2)}$ gives $\sigma^2=2$.",
      r"$Z_i=\log\dfrac{f(x_i;6)}{f(x_i;4)}=-\dfrac{(x_i-6)^2}{4}+\dfrac{(x_i-4)^2}{4}=\dfrac{4x_i-20}{4}=x_i-5$.",
      r"SPRT rejects $H_0$ when $\sum Z_i\ge\log A$ with $A=\dfrac{1-\beta}{\alpha}=\dfrac{0.95}{0.05}=19$.",
      r"So $\sum x_i-5n\ge\log19$, i.e. $\sum x_i\ge5n+\log19$ - option (a)."]),

"2021-Q31": dict(ans=2, qtype="Numerical", diff="Easy",
 sc=r"$E|X|=2\int_0^\infty x\phi(x)dx=\sqrt{2/\pi}=\dfrac{\sqrt2}{\sqrt\pi}$.",
 tips=[r"$E|X|=\sqrt{2/\pi}\approx0.7979$ for the standard normal - memorise the value.",
       r"It must be less than $\sigma=1$ (since $E|X|\le\sqrt{E X^2}=1$), which eliminates $\sqrt{2\pi}$ and $\sqrt{\pi/2}$.",
       r"This constant appears in the mean absolute deviation and in robust scale estimation."],
 sol=[r"By symmetry, $E|X|=2\displaystyle\int_0^\infty x\cdot\frac{1}{\sqrt{2\pi}}e^{-x^2/2}dx$.",
      r"Substituting $u=x^2/2$: $=\dfrac{2}{\sqrt{2\pi}}\displaystyle\int_0^\infty e^{-u}du=\dfrac{2}{\sqrt{2\pi}}$.",
      r"$\dfrac{2}{\sqrt{2\pi}}=\sqrt{\dfrac{2}{\pi}}=\dfrac{\sqrt2}{\sqrt\pi}\approx0.7979$.",
      r"Option (c)."]),

"2021-Q32": dict(ans=2, qtype="Numerical", diff="Medium",
 sc=r"The unrestricted MLE is $X$; projecting onto $\left[\frac{a}{a+b},\frac{b}{a+b}\right]$ gives $\frac{a}{a+b}$ at $X=0$ and $\frac{b}{a+b}$ at $X=1$ - i.e. $\frac{(b-a)X+a}{a+b}$.",
 tips=[r"Test each candidate formula at $X=0$ and $X=1$; only one returns the two endpoints.",
       r"$X=0$: $L(p)=1-p$ decreasing $\Rightarrow$ take the smallest admissible $p$. $X=1$: $L(p)=p$ increasing $\Rightarrow$ take the largest.",
       r"Restricted parameter space $\Rightarrow$ the MLE is the projection of the unrestricted maximiser."],
 sol=[r"For one Bernoulli observation, $L(p)=p^X(1-p)^{1-X}$.",
      r"If $X=0$, $L(p)=1-p$ is decreasing, so $\hat p=\dfrac{a}{a+b}$ (the left endpoint).",
      r"If $X=1$, $L(p)=p$ is increasing, so $\hat p=\dfrac{b}{a+b}$ (the right endpoint).",
      r"Both are produced by $\hat p=\dfrac{(b-a)X+a}{a+b}$: at $X=0$ it gives $\frac{a}{a+b}$, at $X=1$ it gives $\frac{b-a+a}{a+b}=\frac{b}{a+b}$.",
      r"Option (c)."]),

"2021-Q33": dict(ans=1, qtype="Conceptual", diff="Hard",
 sc=r"Unbiased estimators are not unique (1 false); the MLE is a function of the sufficient statistic, not the reverse (2 false); consistent estimators are not unique (3 true); in discrete problems the randomisation of a UMP test can be re-allocated, so it is not unique (4 true).",
 tips=[r"The correct implication is 'MLE is a function of the sufficient statistic' - statement 2 reverses it.",
       r"If $T_n$ is consistent so is $T_n+1/n$, giving infinitely many consistent estimators.",
       r"In discrete problems the boundary randomisation probability can be spread across several points with the same likelihood ratio, all giving the same size and power - hence non-uniqueness."],
 sol=[r"Statement 1: any convex combination of two unbiased estimators is unbiased, so uniqueness fails. False.",
      r"Statement 2: the standard result runs the other way - when a sufficient statistic exists, the MLE is a function of it. False as stated.",
      r"Statement 3: if $T_n$ is consistent, so is $T_n+\frac1n$; consistency is shared by infinitely many estimators. True.",
      r"Statement 4: in discrete problems the boundary randomisation can be distributed over several equally-ranked outcomes, producing distinct UMP tests of the same size and power. True.",
      r"Hence 3 and 4 only - option (b)."]),

"2021-Q34": dict(ans=3, qtype="Conceptual", diff="Hard",
 sc=r"Every similar test has Neyman structure with respect to $T$ when $T$ is a BOUNDEDLY COMPLETE SUFFICIENT statistic for the nuisance parameter on the boundary.",
 tips=[r"Neyman structure: $E\left[\phi(X)\mid T=t\right]=\alpha$ for (almost) every $t$ - a much stronger condition than mere similarity.",
       r"Bounded completeness is exactly what forces the conditional size to be constant; sufficiency alone is not enough.",
       r"This result is the engine behind UMPU tests in the presence of nuisance parameters (e.g. the $t$-test)."],
 sol=[r"A test $\phi$ is similar of size $\alpha$ if $E_\theta\phi(X)=\alpha$ for all $\theta$ on the boundary of $H_0$.",
      r"$\phi$ has Neyman structure with respect to $T$ if $E\left[\phi(X)\mid T=t\right]=\alpha$ for almost all $t$.",
      r"The theorem states: if $T$ is a boundedly complete sufficient statistic for the boundary family, then every similar test has Neyman structure.",
      r"Sufficiency alone gives the conditional expectation a $\theta$-free meaning; bounded completeness forces it to equal the constant $\alpha$.",
      r"Option (d)."]),

"2021-Q35": dict(ans=0, qtype="Conceptual", diff="Easy",
 sc=r"Unbiased estimators may fail to exist (e.g. $1/p$ in Bernoulli), and when they exist they are generally NOT unique.",
 tips=[r"Non-existence example: no unbiased estimator of $1/p$ exists in the Bernoulli model.",
       r"Non-uniqueness: any convex combination of two unbiased estimators is unbiased.",
       r"Uniqueness belongs to the UMVUE, not to unbiased estimators in general."],
 sol=[r"Statement 1: for $Y=\sum X_i\sim B(n,p)$, every $E[g(Y)]$ is a polynomial in $p$, so no unbiased estimator of $1/p$ exists. True.",
      r"Statement 2: if $T_1,T_2$ are unbiased then so is $aT_1+(1-a)T_2$ for every $a$, giving infinitely many. False.",
      r"Hence 1 only - option (a)."]),

"2021-Q36": dict(ans=0, qtype="Conceptual", diff="Easy",
 sc=r"Unbiasedness does not imply consistency ($X_1$ is unbiased but not consistent), and consistency does not imply unbiasedness.",
 tips=[r"$X_1$ alone is unbiased for $\mu$ but its variance never shrinks - unbiased, not consistent.",
       r"$\hat\sigma^2$ with divisor $n$ is consistent but biased - the converse counterexample.",
       r"The two properties are logically independent; neither implies the other."],
 sol=[r"Statement 1: take $t=X_1$. It is unbiased for $\mu$ but $V(X_1)=\sigma^2$ for every $n$, so it is not consistent. True.",
      r"Statement 2: $\hat\sigma^2=\frac1n\sum(X_i-\bar X)^2$ is consistent for $\sigma^2$ yet biased. False.",
      r"Hence 1 only - option (a)."]),

"2021-Q37": dict(ans=3, qtype="Numerical", diff="Hard",
 sc=r"For this Rayleigh form $E(X)=\sqrt{\pi\theta/2}$; equating to $\bar x$ gives $\hat\theta=\dfrac{2\bar x^2}{\pi}$.",
 tips=[r"Rayleigh with $f(x)=\frac{x}{\theta}e^{-x^2/(2\theta)}$ has $E(X)=\sqrt{\frac{\pi\theta}{2}}$ and $E(X^2)=2\theta$.",
       r"Since $E(X)\propto\sqrt\theta$, the moment estimator must involve $\bar x^2$ - options linear in $\bar x$ are impossible.",
       r"Using the SECOND moment instead gives the simpler $\hat\theta=\frac1{2n}\sum x_i^2$, which is also the MLE."],
 sol=[r"$E(X)=\displaystyle\int_0^\infty x\cdot\frac{x}{\theta}e^{-x^2/(2\theta)}dx$. Substituting $x=\sqrt{2\theta}\,u$ gives $E(X)=\sqrt{2\theta}\displaystyle\int_0^\infty 2u^2e^{-u^2}du=\sqrt{2\theta}\cdot\frac{\sqrt\pi}{2}=\sqrt{\frac{\pi\theta}{2}}$.",
      r"Method of moments: $\sqrt{\dfrac{\pi\hat\theta}{2}}=\bar x$.",
      r"Squaring: $\dfrac{\pi\hat\theta}{2}=\bar x^2$, so $\hat\theta=\dfrac{2\bar x^2}{\pi}$.",
      r"Option (d)."]),

"2021-Q38": dict(ans=1, qtype="Conceptual", diff="Medium",
 sc=r"MLEs need not be unique (1 false), may be inconsistent in irregular models (2 true), and are frequently biased (3 true).",
 tips=[r"Non-uniqueness: $U(\theta-\tfrac12,\theta+\tfrac12)$ has a whole interval of MLEs.",
       r"Inconsistency: the Neyman-Scott problem (many incidental parameters) gives an inconsistent MLE of $\sigma^2$.",
       r"Bias: $\hat\sigma^2$ with divisor $n$ in the normal model."],
 sol=[r"Statement 1: for $U(\theta-\tfrac12,\theta+\tfrac12)$ every $\theta$ in an interval maximises $L$, so the MLE is not unique. False.",
      r"Statement 2: when the regularity conditions fail (e.g. the number of parameters grows with $n$, as in the Neyman-Scott problem), the MLE can be inconsistent. True.",
      r"Statement 3: $\hat\sigma^2=\frac1n\sum(X_i-\bar X)^2$ is the MLE and is biased. True.",
      r"Hence 2 and 3 only - option (b)."]),

"2021-Q39": dict(ans=2, qtype="Numerical", diff="Hard",
 sc=r"$E\left[(1-\tfrac1n)^T\right]=e^{-\theta}$ with $T=\sum x_i$, but its variance $e^{-2\theta}\left(e^{\theta/n}-1\right)$ strictly exceeds the CR bound $\theta e^{-2\theta}/n$.",
 tips=[r"Use the Poisson pgf with $T\sim\text{Poisson}(n\theta)$: $E(a^T)=e^{n\theta(a-1)}$; set $a=1-\frac1n$.",
       r"$e^{u}-1>u$ for $u>0$, so $e^{\theta/n}-1>\theta/n$: the variance always exceeds the bound.",
       r"Options (a) and (d) are not even unbiased for a probability (they are unbounded), so they fail immediately."],
 sol=[r"$T=\sum X_i\sim\text{Poisson}(n\theta)$, and $E\left(a^T\right)=e^{n\theta(a-1)}$.",
      r"Taking $a=1-\dfrac1n$: $E\left[\left(1-\tfrac1n\right)^T\right]=e^{n\theta(-1/n)}=e^{-\theta}$ - unbiased.",
      r"$E\left[\left(1-\tfrac1n\right)^{2T}\right]=e^{n\theta\left[(1-1/n)^2-1\right]}=e^{-2\theta+\theta/n}$, so the variance is $e^{-2\theta}\left(e^{\theta/n}-1\right)$.",
      r"The Cramer-Rao bound for $e^{-\theta}$ is $\dfrac{\left(e^{-\theta}\right)^2}{n/\theta}=\dfrac{\theta e^{-2\theta}}{n}$, and $e^{\theta/n}-1>\dfrac\theta n$, so the bound is not attained.",
      r"Option (c)."]),

"2021-Q40": dict(ans=2, qtype="Numerical", diff="Hard",
 sc=r"$n=\left[\frac{(z_\alpha+z_\beta)\sigma}{\theta_1-\theta_0}\right]^2=\left[\frac{(1.645+2.326)(10)}{10}\right]^2=15.77$, so $n=16$.",
 tips=[r"One-sided sample size formula: $n=\left[\frac{(z_\alpha+z_\beta)\sigma}{\delta}\right]^2$ with $\delta=|\theta_1-\theta_0|$.",
       r"$\alpha=0.05\Rightarrow z_\alpha=1.645$; $\beta=0.01\Rightarrow z_\beta=2.326$.",
       r"Always round UP: $15.77$ becomes $16$, since a smaller $n$ would not meet both error requirements."],
 sol=[r"$\sigma^2=100$ so $\sigma=10$; $\delta=\theta_1-\theta_0=100-90=10$.",
      r"$\alpha=0.05$ gives $z_\alpha=1.645$; $\beta=P(\text{accept }H_0\mid H_1)=0.01$ gives $z_\beta=2.326$.",
      r"$n=\left[\dfrac{(z_\alpha+z_\beta)\sigma}{\delta}\right]^2=\left[\dfrac{(1.645+2.326)\times10}{10}\right]^2=(3.971)^2=15.77$.",
      r"Rounding up, $n=16$ - option (c)."]),

"2021-Q41": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"The likelihood is $\theta^{-n}I\left(\max|x_i|\le\theta/2\right)$, a function of $\left(X_{(1)},X_{(n)}\right)$; and the full order statistic is always sufficient.",
 tips=[r"The whole order statistic is sufficient for ANY iid model - statement 1 is automatically true.",
       r"Here the binding constraints are $X_{(1)}\ge-\theta/2$ and $X_{(n)}\le\theta/2$, so the extremes suffice.",
       r"Minimal sufficiency reduces further to $\max\left(|X_{(1)}|,X_{(n)}\right)$, but that is not asked."],
 sol=[r"$L(\theta)=\theta^{-n}$ provided $-\theta/2\le x_i\le\theta/2$ for all $i$, i.e. $X_{(1)}\ge-\theta/2$ and $X_{(n)}\le\theta/2$.",
      r"So $L=\theta^{-n}I\left(X_{(1)}\ge-\tfrac\theta2\right)I\left(X_{(n)}\le\tfrac\theta2\right)$, depending on the data only through $\left(X_{(1)},X_{(n)}\right)$: statement 2 is true.",
      r"The complete order statistic is sufficient for any iid sample (it determines the likelihood entirely): statement 1 is true.",
      r"Option (c)."]),

"2021-Q42": dict(ans=3, qtype="Numerical", diff="Hard",
 sc=r"$X_{(1)}\sim\theta+\text{Exp}(n)$, so bias $=\frac1n+k$ and $\text{MSE}=\frac1{n^2}+\left(\frac1n+k\right)^2$, minimised at $k=-\frac1n$.",
 tips=[r"$n\left(X_{(1)}-\theta\right)\sim\text{Exp}(1)$, so $E\left(X_{(1)}\right)=\theta+\frac1n$ and $V\left(X_{(1)}\right)=\frac1{n^2}$.",
       r"Adding a constant changes only the bias, never the variance - so minimise the squared bias alone.",
       r"The minimiser removes the bias entirely here, so the MSE-optimal choice happens to coincide with the unbiased one."],
 sol=[r"$P\left(X_{(1)}>t\right)=e^{-n(t-\theta)}$ for $t>\theta$, so $X_{(1)}-\theta\sim\text{Exp}(n)$ with mean $\frac1n$ and variance $\frac1{n^2}$.",
      r"$T=X_{(1)}+k$ has bias $b(k)=E(T)-\theta=\dfrac1n+k$ and variance $\dfrac1{n^2}$ (independent of $k$).",
      r"$\text{MSE}(k)=\dfrac1{n^2}+\left(\dfrac1n+k\right)^2$.",
      r"Differentiating: $2\left(\frac1n+k\right)=0\Rightarrow k=-\dfrac1n$.",
      r"So the MSE-optimal estimator is $X_{(1)}-\dfrac1n$ - option (d)."]),

"2021-Q43": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"Support-dependent endpoints: $\hat\alpha=X_{(1)}=\min x_i$ and $\hat\beta=X_{(n)}=\max x_i$.",
 tips=[r"$L=(\beta-\alpha)^{-n}$ is maximised by making $\beta-\alpha$ as SMALL as the data permit.",
       r"The smallest admissible interval containing all observations is $\left[\min x_i,\ \max x_i\right]$.",
       r"Option (d) gives the half-range and midrange, which are the MLEs in the $U(\theta-\tfrac12,\theta+\tfrac12)$ parametrisation, not here."],
 sol=[r"$L(\alpha,\beta)=\dfrac{1}{(\beta-\alpha)^n}$ subject to $\alpha\le X_{(1)}$ and $\beta\ge X_{(n)}$.",
      r"The likelihood increases as $\beta-\alpha$ shrinks, so it is maximised by taking $\alpha$ as large and $\beta$ as small as the constraints allow.",
      r"Hence $\hat\alpha=X_{(1)}=\min(x_1,\dots,x_n)$ and $\hat\beta=X_{(n)}=\max(x_1,\dots,x_n)$.",
      r"Option (c)."]),

"2021-Q44": dict(ans=0, qtype="Conceptual", diff="Medium",
 sc=r"MVBUE $\Rightarrow$ UMVUE (1 true) and MVBUE must be a function of a sufficient statistic (2 true); but UMVUE need NOT attain the bound (3 false).",
 tips=[r"MVB attainment forces the score to be proportional to $T-\gamma(\theta)$, which makes $T$ sufficient.",
       r"UMVUE is the weaker notion: minimum variance among unbiased estimators, whether or not the CR bound is reached.",
       r"Standard counterexample to 3: for $U(0,\theta)$ the UMVUE exists but no CR bound applies at all."],
 sol=[r"Statement 1: if $T$ attains the Cramer-Rao bound, no unbiased estimator can have smaller variance, so $T$ is UMVUE. True.",
      r"Statement 2: attainment requires $\dfrac{\partial\log L}{\partial\theta}=A(\theta)\left[T-\theta\right]$, so $L$ factorises through $T$ - hence $T$ is sufficient. True.",
      r"Statement 3: a UMVUE may have variance strictly above the CR bound (or the bound may not exist at all, as for $U(0,\theta)$). False.",
      r"Hence 1 and 2 only - option (a)."]),

"2021-Q45": dict(ans=3, qtype="Conceptual", diff="Medium",
 sc=r"For the Cauchy the sample mean is neither sufficient (no non-trivial sufficient statistic exists) nor an MVB estimator (no MVB estimator exists).",
 tips=[r"Cauchy location family admits no non-trivial sufficient statistic - the order statistic is minimal sufficient.",
       r"$\bar X$ has the same Cauchy distribution as $X_1$, so its variance does not even exist.",
       r"An estimator with infinite variance cannot attain any finite variance bound."],
 sol=[r"Statement 1: the Cauchy likelihood $\prod\left[1+(x_i-\theta)^2\right]^{-1}$ does not factorise through any statistic of dimension less than $n$; the order statistic is minimal sufficient. So $\bar X$ is not sufficient. False.",
      r"Statement 2: $\bar X$ for a Cauchy sample is again Cauchy, so $V(\bar X)$ does not exist and no CR bound can be attained. Indeed no MVB estimator of $\theta$ exists. False.",
      r"Neither statement is correct - option (d)."]),

"2021-Q46": dict(ans=3, qtype="Numerical", diff="Easy",
 sc=r"$V(\bar X)=\lambda/n$, estimated by $\bar x/n$, so the interval is $\bar x\pm1.96\sqrt{\bar x/n}$.",
 tips=[r"The standard error of a Poisson mean is $\sqrt{\lambda/n}$ - the square root is essential.",
       r"Options (a) and (c) forget the square root; option (b) divides by $n$ instead of $\sqrt n$ inside.",
       r"Dimensional check: the margin must be of the same order as $\sqrt{\bar x}/\sqrt n$."],
 sol=[r"For Poisson($\lambda$), $E(X)=V(X)=\lambda$, so $V(\bar X)=\dfrac\lambda n$.",
      r"Estimating $\lambda$ by $\bar x$ gives $\text{SE}(\bar X)=\sqrt{\dfrac{\bar x}{n}}$.",
      r"For large $n$ the CLT gives $\bar X\approx N\left(\lambda,\dfrac\lambda n\right)$, so the $95\%$ interval is $\bar x\pm1.96\sqrt{\dfrac{\bar x}{n}}$.",
      r"Option (d)."]),

"2021-Q47": dict(ans=1, qtype="Conceptual", diff="Easy",
 sc=r"Unbiased means power $\ge$ size: $1-\beta\ge\alpha$, i.e. $\alpha+\beta\le1$.",
 tips=[r"Write out the definition and rearrange - there is no need to memorise the inequality separately.",
       r"A test with $\alpha+\beta>1$ would be improved by simply reversing its decisions.",
       r"Comparisons of $\alpha$ with $\beta$ individually (options (a), (d)) have nothing to do with unbiasedness."],
 sol=[r"For simple $H_0$ against simple $H_1$, size $=\alpha$ and power $=1-\beta$.",
      r"Unbiasedness requires power $\ge$ size: $1-\beta\ge\alpha$.",
      r"Rearranging, $\alpha+\beta\le1$.",
      r"Option (b)."]),

"2021-Q48": dict(ans=0, qtype="Numerical", diff="Hard",
 sc=r"The ratio $X/Y$ has a distribution FREE of $\theta$, so size $=$ power $=P(X/Y>0.65)=1-\frac{0.65}{2}=0.675$.",
 tips=[r"Scale cancels in a ratio: $X/Y=(X/\theta)/(Y/\theta)$ with both standardised uniforms.",
       r"Since the statistic is ancillary, the test has no power at all - size equals power for every alternative.",
       r"$P(X/Y>c)=1-c/2$ for $0<c\le1$ when $X,Y$ are iid $U(0,1)$."],
 sol=[r"Write $U=X/\theta$ and $V=Y/\theta$, both $U(0,1)$ and independent. Then $X/Y=U/V$, whose distribution does not involve $\theta$: the statistic is ancillary.",
      r"$P\left(\dfrac UV>c\right)=1-P(U\le cV)=1-\displaystyle\int_0^1cv\,dv=1-\frac c2$ for $0<c\le1$.",
      r"With $c=0.65$: $1-0.325=0.675$.",
      r"Since the distribution is free of $\theta$, both the Type-I error and the power equal $0.675$ - option (a)."]),

"2021-Q49": dict(ans=3, qtype="Conceptual", diff="Hard",
 sc=r"$\frac{f_1}{f_0}\propto\exp\left(\frac{x^2}{2}-|x|\right)$, and $u\mapsto\frac{u^2}2-u$ is minimised at $u=1$: the ratio is large for both very large and very small $|x|$, giving $|x|>k_1$ or $|x|<k_2$.",
 tips=[r"Form the ratio, take logs, and study the resulting function of $|x|$ - here a parabola in $u=|x|$ with minimum at $u=1$.",
       r"A U-shaped log-ratio produces a TWO-PIECE critical region (both tails in $|x|$), not an interval.",
       r"The Laplace has heavier tails AND a sharper peak than the normal - so both extremes favour $H_1$."],
 sol=[r"$f_0(x)=\dfrac1{\sqrt{2\pi}}e^{-x^2/2}$ and $f_1(x)=\tfrac12e^{-|x|}$.",
      r"$\dfrac{f_1}{f_0}=\dfrac{\sqrt{2\pi}}{2}\exp\left(\dfrac{x^2}{2}-|x|\right)$.",
      r"Let $u=|x|\ge0$ and $g(u)=\dfrac{u^2}{2}-u$. Then $g'(u)=u-1$, so $g$ decreases on $[0,1)$ and increases on $(1,\infty)$ with minimum $g(1)=-\tfrac12$.",
      r"Hence $\dfrac{f_1}{f_0}>k$ holds for $u$ large or $u$ small, i.e. $|x|>k_1$ or $|x|<k_2$.",
      r"Option (d)."]),

"2021-Q50": dict(ans=2, qtype="Numerical", diff="Medium",
 sc=r"$\dfrac{L_1}{L_0}=\dfrac{e^{-36}3^{\sum x}}{e^{-24}2^{\sum x}}=(1.5)^{\sum x_i}e^{-12}$ with $n=12$ months.",
 tips=[r"$e^{-12(3)}/e^{-12(2)}=e^{-12}$ - the exponent is $n(\lambda_1-\lambda_0)=12$.",
       r"$(3/2)^{\sum x_i}=(1.5)^{\sum x_i}$ - the data enter only through the total, as sufficiency requires.",
       r"$n=12$ throughout; options with $24$ double the sample size and are wrong."],
 sol=[r"For a Poisson sample of size $n=12$, $L(\lambda)=\dfrac{e^{-12\lambda}\lambda^{\sum x_i}}{\prod x_i!}$.",
      r"$\dfrac{L(\lambda=3)}{L(\lambda=2)}=\dfrac{e^{-36}\,3^{\sum x_i}}{e^{-24}\,2^{\sum x_i}}$.",
      r"$=e^{-12}\left(\dfrac32\right)^{\sum x_i}=(1.5)^{\sum_{i=1}^{12}x_i}e^{-12}$.",
      r"Option (c)."]),

"2021-Q51": dict(ans=0, qtype="Conceptual", diff="Easy",
 sc=r"Wald's boundaries: $A=\dfrac{1-\beta}{\alpha}$ (upper) and $B=\dfrac{\beta}{1-\alpha}$ (lower).",
 tips=[r"$A>1>B$: the upper boundary must exceed 1 and the lower must be below 1 - a sign check that eliminates (c) and (d).",
       r"Mnemonic: $A$ carries $(1-\beta)$ = power over $\alpha$ = size.",
       r"$L(\theta_0)=1-\alpha$ and $L(\theta_1)=\beta$ are the two anchor values of the OC function."],
 sol=[r"In Wald's SPRT one continues sampling while $B<\lambda_m<A$, rejects $H_0$ when $\lambda_m\ge A$ and accepts when $\lambda_m\le B$.",
      r"The strength-$(\alpha,\beta)$ approximations are $A\approx\dfrac{1-\beta}{\alpha}$ and $B\approx\dfrac{\beta}{1-\alpha}$.",
      r"Option (a)."]),

"2021-Q52": dict(ans=1, qtype="Numerical", diff="Hard",
 sc=r"$h(\theta)$ solves $E_\theta\left[e^{hZ}\right]=1$; for $N(\theta,1)$ this gives $h(\theta)=\dfrac{\theta_0+\theta_1-2\theta}{\theta_1-\theta_0}$.",
 tips=[r"$h(\theta_0)=1$ and $h(\theta_1)=-1$ - use these two checks to identify the right option instantly.",
       r"Option (a) gives $h(\theta_0)=\frac{\theta_1-2\theta_0+\ldots}{\ldots}$, which fails the check; only (b) passes both.",
       r"$L(\theta_0)=\frac{A-1}{A-B}=1-\alpha$ and $L(\theta_1)=\frac{A^{-1}-1}{A^{-1}-B^{-1}}=\beta$ confirm the construction."],
 sol=[r"For $N(\theta,1)$, $Z=\log\dfrac{f(x;\theta_1)}{f(x;\theta_0)}=(\theta_1-\theta_0)x-\dfrac{\theta_1^2-\theta_0^2}{2}$.",
      r"$h=h(\theta)$ is the non-zero solution of $E_\theta\left[e^{hZ}\right]=1$. Using the normal mgf, this reduces to $h(\theta_1-\theta_0)\theta-\frac{h(\theta_1^2-\theta_0^2)}{2}+\frac{h^2(\theta_1-\theta_0)^2}{2}=0$.",
      r"Dividing by $h(\theta_1-\theta_0)/2\ne0$: $2\theta-(\theta_1+\theta_0)+h(\theta_1-\theta_0)=0$.",
      r"Hence $h(\theta)=\dfrac{\theta_1+\theta_0-2\theta}{\theta_1-\theta_0}$, and indeed $h(\theta_0)=1$, $h(\theta_1)=-1$ - option (b)."]),

"2021-Q53": dict(ans=1, qtype="Conceptual", diff="Easy",
 sc=r"Beta-binomial conjugacy: posterior is Beta$(x+\alpha,\ n-x+\beta)$, so $p_1=x+\alpha$.",
 tips=[r"Posterior $\propto\theta^{x+\alpha-1}(1-\theta)^{n-x+\beta-1}$ - read the parameters off the exponents plus one.",
       r"Mnemonic: add the successes to $\alpha$ and the failures to $\beta$.",
       r"Posterior mean $=\dfrac{x+\alpha}{n+\alpha+\beta}$, a weighted average of $x/n$ and the prior mean."],
 sol=[r"Likelihood $\propto\theta^x(1-\theta)^{n-x}$; prior $\propto\theta^{\alpha-1}(1-\theta)^{\beta-1}$.",
      r"Posterior $\propto\theta^{x+\alpha-1}(1-\theta)^{n-x+\beta-1}$, which is Beta$(x+\alpha,\ n-x+\beta)$.",
      r"Hence $p_1=x+\alpha$ - option (b)."]),

"2021-Q54": dict(ans=2, qtype="Conceptual", diff="Medium",
 flag=r"Source anomaly: the beta-binomial posterior is Beta$(x+\alpha,\ n-x+\beta)$, so the exact value is $p_2=n-x+\beta$. The printed option (c) reads $n+x-\beta$, which appears to carry two sign slips; it is nevertheless the only option built from $n$, $x$ and $\beta$ and is taken as the intended key. Options and wording are preserved as printed.",
 sc=r"Posterior is Beta$(x+\alpha,\ n-x+\beta)$, so the second parameter counts the FAILURES $n-x$ added to $\beta$.",
 tips=[r"$p_1$ collects successes ($x+\alpha$); $p_2$ collects failures ($n-x+\beta$).",
       r"Check: $p_1+p_2=n+\alpha+\beta$, which is why option (a) is planted - it is the SUM, not $p_2$.",
       r"Posterior mean $\frac{p_1}{p_1+p_2}=\frac{x+\alpha}{n+\alpha+\beta}$ shrinks the sample proportion toward the prior mean."],
 sol=[r"Likelihood $\propto\theta^x(1-\theta)^{n-x}$; prior $\propto\theta^{\alpha-1}(1-\theta)^{\beta-1}$.",
      r"Posterior $\propto\theta^{x+\alpha-1}(1-\theta)^{(n-x+\beta)-1}$.",
      r"So the posterior is Beta$(x+\alpha,\ n-x+\beta)$ and the second parameter is $p_2=n-x+\beta$.",
      r"Among the printed options only (c) is formed from $n$, $x$ and $\beta$; it is taken as the intended key (see the source flag)."]),
}
