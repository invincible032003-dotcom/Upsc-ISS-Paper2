# -*- coding: utf-8 -*-
# Statistical Inference answer pack 5 (2020 Q11-Q38)
A = {
"2020-Q11": dict(ans=2, qtype="Numerical", diff="Hard",
 sc=r"$E(\log X)=\log\theta-1$ for $U(0,\theta)$, so the geometric mean $\left(\prod X_i\right)^{1/n}\xrightarrow{P}e^{\log\theta-1}=\theta/e$.",
 tips=[r"$\int_0^\theta\log x\cdot\frac1\theta dx=\log\theta-1$ - a one-line integration worth memorising.",
       r"By the WLLN, $\frac1n\sum\log X_i\xrightarrow{P}\log\theta-1$; then the continuous mapping theorem gives the exponential.",
       r"Option (b) simplifies to $\prod X_i$, which diverges to $0$ - not an estimator of anything."],
 sol=[r"For $X\sim U(0,\theta)$: $E(\log X)=\displaystyle\int_0^\theta\frac{\log x}{\theta}dx=\frac1\theta\left[x\log x-x\right]_0^\theta=\log\theta-1$.",
      r"By the weak law of large numbers, $\dfrac1n\sum_{i=1}^n\log X_i\xrightarrow{P}\log\theta-1$.",
      r"Applying the continuous map $u\mapsto e^u$: $\left(\prod X_i\right)^{1/n}=\exp\left(\frac1n\sum\log X_i\right)\xrightarrow{P}e^{\log\theta-1}=\dfrac\theta e$.",
      r"So the geometric mean is consistent for $\theta/e$ - option (c)."]),

"2020-Q12": dict(ans=1, qtype="Numerical", diff="Hard",
 flag=r"Source note: the Wald boundaries $A=\frac{1-\beta}{\alpha}=18$ and $B=\frac{\beta}{1-\alpha}=\frac{2}{19}$ identify option (b) uniquely. The drift computed from first principles is $E(Z)=2(\theta-1)$, whereas the printed option carries $4(\theta-1)$; the option set is preserved as printed and (b) is taken as the key since it is the only one with both boundaries correct.",
 sc=r"$A=\frac{1-\beta}{\alpha}=\frac{0.9}{0.05}=18$ and $B=\frac{\beta}{1-\alpha}=\frac{0.1}{0.95}=\frac2{19}$ - only option (b) has both.",
 tips=[r"$A>1>B$ always. Any option with $A<B$ (here (c) and (d)) is wrong before any arithmetic.",
       r"$\log\frac{f_1}{f_0}=(\theta_1-\theta_0)x-\frac{\theta_1^2-\theta_0^2}{2}$, so for $\theta_0=0,\theta_1=2$: $Z=2x-2$ and $E_\theta(Z)=2(\theta-1)$.",
       r"$B=\frac{0.1}{0.95}=\frac{2}{19}$, not $\frac{19}{2}$ - option (a) inverts it."],
 sol=[r"Wald's boundaries: $A\approx\dfrac{1-\beta}{\alpha}=\dfrac{1-0.1}{0.05}=\dfrac{0.9}{0.05}=18$.",
      r"$B\approx\dfrac{\beta}{1-\alpha}=\dfrac{0.1}{1-0.05}=\dfrac{0.1}{0.95}=\dfrac{2}{19}$.",
      r"Only option (b) pairs $A=18$ with $B=\dfrac2{19}$; options (c) and (d) interchange them and (a) inverts $B$.",
      r"For completeness, $Z=\log\dfrac{f_1}{f_0}=-\tfrac12(x-2)^2+\tfrac12x^2=2x-2$, so $E_\theta(Z)=2(\theta-1)$.",
      r"Option (b)."]),

"2020-Q13": dict(ans=1, qtype="Numerical", diff="Medium",
 sc=r"$\hat\theta=1/\bar X$ with asymptotic SE $\theta/\sqrt n$, so the limits are $\dfrac1{\bar X}\left(1\pm\dfrac{1.96}{\sqrt n}\right)$.",
 tips=[r"$I_n(\theta)=n/\theta^2$, so $\text{SE}(\hat\theta)\approx\theta/\sqrt n$ and the interval is multiplicative in $1/\bar X$.",
       r"The MLE here is $1/\bar X$ (rate parameter), not $\bar X$ (which estimates the mean $1/\theta$).",
       r"The $\sqrt n$ in the denominator is essential - options with $n$ shrink far too fast."],
 sol=[r"$f(x)=\theta e^{-\theta x}$, so $\log L=n\log\theta-\theta\sum x_i$ and $\hat\theta=\dfrac1{\bar X}$.",
      r"$I_1(\theta)=1/\theta^2$, so $I_n(\theta)=n/\theta^2$ and asymptotically $\hat\theta\sim N\left(\theta,\dfrac{\theta^2}{n}\right)$.",
      r"Replacing $\theta$ by $\hat\theta$ in the standard error gives $\text{SE}=\dfrac{\hat\theta}{\sqrt n}=\dfrac{1}{\bar X\sqrt n}$.",
      r"$95\%$ limits: $\dfrac1{\bar X}\pm1.96\dfrac{1}{\bar X\sqrt n}=\left(1\pm\dfrac{1.96}{\sqrt n}\right)\dfrac1{\bar X}$ - option (b)."]),

"2020-Q14": dict(ans=3, qtype="Numerical", diff="Medium",
 sc=r"Collecting the $\theta$ terms in $\log L$ gives the coefficient $\dfrac{x}{\sigma_1^2}+\dfrac{y}{\sigma_2^2}$ - that is the sufficient statistic.",
 tips=[r"Weight each observation by the reciprocal of its VARIANCE, not its standard deviation - option (b) is the trap.",
       r"The same weights give the BLUE of $\theta$: $\hat\theta=\dfrac{x/\sigma_1^2+y/\sigma_2^2}{1/\sigma_1^2+1/\sigma_2^2}$.",
       r"$\bar x+\bar y$ ignores the unequal precisions and is not sufficient."],
 sol=[r"$\log L=-\dfrac{(x-\theta)^2}{2\sigma_1^2}-\dfrac{(y-\theta)^2}{2\sigma_2^2}+\text{const}$.",
      r"Expanding, the only data-dependent terms carrying $\theta$ are $\theta\left(\dfrac{x}{\sigma_1^2}+\dfrac{y}{\sigma_2^2}\right)$, plus $-\dfrac{\theta^2}{2}\left(\dfrac1{\sigma_1^2}+\dfrac1{\sigma_2^2}\right)$ which is free of the data.",
      r"By the factorisation theorem, $T=\dfrac{X}{\sigma_1^2}+\dfrac{Y}{\sigma_2^2}$ is sufficient for $\theta$.",
      r"Option (d)."]),

"2020-Q15": dict(ans=2, qtype="Definition", diff="Easy",
 sc=r"Consistency in the $(\varepsilon,\eta)$ form: for every $\varepsilon,\eta>0$ there is $m$ with $P\left[|T_n-\theta|<\varepsilon\right]>1-\eta$ for all $n\ge m$.",
 tips=[r"This is just convergence in probability written out with explicit $\varepsilon$ and $\eta$.",
       r"Unbiasedness (a) and vanishing variance (b) are together SUFFICIENT for consistency but neither alone is the definition.",
       r"Option (d) has the inequality backwards - it would say the estimator is almost never close to $\theta$."],
 sol=[r"$T_n$ is consistent if $T_n\xrightarrow{P}\theta$, i.e. $P\left[|T_n-\theta|<\varepsilon\right]\to1$ for every $\varepsilon>0$.",
      r"Written with an explicit tolerance: for all $\varepsilon>0$ and $\eta>0$ there exists $m$ such that $P\left[|T_n-\theta|<\varepsilon\right]>1-\eta$ whenever $n\ge m$.",
      r"That is exactly option (c)."]),

"2020-Q16": dict(ans=3, qtype="Numerical", diff="Easy",
 sc=r"Need $5\theta\ge X_{(n)}$, i.e. $\theta\ge X_{(n)}/5$, and $L=(5\theta)^{-n}$ decreases in $\theta$: $\hat\theta=X_{(n)}/5$.",
 tips=[r"Rescale: if $Y=X/5\sim U(0,\theta)$ then $\hat\theta=Y_{(n)}=X_{(n)}/5$ - invariance does the work.",
       r"Support-dependent parameter $\Rightarrow$ push $\theta$ to the smallest admissible value, since $L$ is decreasing.",
       r"$X_{(1)}$ carries no information about the upper endpoint - options (a) and (b) are structurally wrong."],
 sol=[r"$L(\theta)=\dfrac1{(5\theta)^n}$ provided every $x_i\le5\theta$, i.e. $\theta\ge\dfrac{X_{(n)}}5$.",
      r"$L$ is strictly decreasing in $\theta$, so it is maximised at the smallest admissible value.",
      r"$\hat\theta=\dfrac{X_{(n)}}{5}$ - option (d)."]),

"2020-Q17": dict(ans=0, qtype="Conceptual", diff="Easy",
 sc=r"$\frac{\partial\log L}{\partial\lambda}=-n+\frac{\sum x_i}{\lambda}=0$ gives $\hat\lambda=\bar X$.",
 tips=[r"For Poisson the MLE, the moment estimator and the UMVUE of $\lambda$ all coincide with $\bar X$.",
       r"$\sum X_i$ is the sufficient statistic but is not itself an estimator of $\lambda$ (its expectation is $n\lambda$).",
       r"Since mean $=$ variance for Poisson, the sample variance is also consistent - but it is not the MLE."],
 sol=[r"$L(\lambda)=\dfrac{e^{-n\lambda}\lambda^{\sum x_i}}{\prod x_i!}$, so $\log L=-n\lambda+\left(\sum x_i\right)\log\lambda-\log\prod x_i!$.",
      r"$\dfrac{\partial\log L}{\partial\lambda}=-n+\dfrac{\sum x_i}{\lambda}=0\Rightarrow\hat\lambda=\dfrac{\sum x_i}{n}=\bar X$.",
      r"Second derivative $-\sum x_i/\lambda^2<0$ confirms a maximum. Option (a)."]),

"2020-Q18": dict(ans=2, qtype="Numerical", diff="Medium",
 sc=r"$E(\bar X^2)=\mu^2+\frac1n$, so the unbiased function of the complete sufficient statistic $\bar X$ is $\bar X^2-\frac1n$.",
 tips=[r"Correction term is exactly $V(\bar X)=\sigma^2/n=1/n$ here.",
       r"$\bar X$ is complete sufficient for $N(\mu,1)$, so Lehmann-Scheffe upgrades unbiasedness to UMVUE status.",
       r"The estimator can be negative - a UMVUE need not lie in the parameter range, which is a known curiosity."],
 sol=[r"$\bar X\sim N\left(\mu,\dfrac1n\right)$, so $E(\bar X^2)=\dfrac1n+\mu^2$.",
      r"Hence $T=\bar X^2-\dfrac1n$ is unbiased for $\mu^2$.",
      r"$\bar X$ is a complete sufficient statistic, and $T$ is a function of it, so by the Lehmann-Scheffe theorem $T$ is the UMVUE.",
      r"Option (c)."]),

"2020-Q19": dict(ans=0, qtype="Conceptual", diff="Easy",
 sc=r"Cramer-Rao: $V(T)\ge\dfrac{\left[\psi'(\theta)\right]^2}{I(\theta)}$.",
 tips=[r"It is a LOWER bound, so the inequality is $\ge$ - options with $\le$ are impossible.",
       r"For $\psi(\theta)=\theta$ the bound reduces to $1/I(\theta)$, which is option (c) - correct only in that special case.",
       r"Always square the derivative $\psi'(\theta)$."],
 sol=[r"The Cramer-Rao inequality states that for any unbiased estimator $T$ of $\psi(\theta)$, subject to regularity conditions,",
      r"$V(T)\ge\dfrac{\left[\dfrac{\partial\psi}{\partial\theta}\right]^2}{I(\theta)}$, where $I(\theta)$ is the Fisher information in the sample.",
      r"Option (c) is the special case $\psi(\theta)=\theta$ and is not the general statement.",
      r"Option (a)."]),

"2020-Q20": dict(ans=0, qtype="Conceptual", diff="Easy",
 sc=r"Bhattacharya's system of bounds uses higher-order derivatives of the likelihood and reduces to the Cramer-Rao bound at first order.",
 tips=[r"First-order Bhattacharya bound $=$ Cramer-Rao bound; second and higher orders are sharper.",
       r"The bound is $V(T)\ge\mathbf a'J^{-1}\mathbf a$, with $J_{ij}=E\left[\frac{L^{(i)}}{L}\frac{L^{(j)}}{L}\right]$ and $a_i=\frac{d^ig}{d\theta^i}$.",
       r"Chapman-Robbins-Kiefer is a different (difference-based) generalisation that needs no regularity conditions."],
 sol=[r"The Cramer-Rao bound uses only the first derivative of $\log L$.",
      r"Bhattacharya's bound builds a matrix $J$ from the first $k$ derivatives $\dfrac{L^{(i)}}{L}$ and bounds $V(T)$ below by $\mathbf a'J^{-1}\mathbf a$.",
      r"For $k=1$ it reduces exactly to the Cramer-Rao inequality, which it therefore generalises.",
      r"Option (a)."]),

"2020-Q21": dict(ans=2, qtype="Numerical", diff="Easy",
 sc=r"$V(\text{median})\approx\frac{\pi\sigma^2}{2n}$ against $V(\bar X)=\frac{\sigma^2}{n}$, so the efficiency of the median is $\frac{2}{\pi}\approx0.637$.",
 tips=[r"Efficiency of the median relative to the mean $=2/\pi\approx63.7\%$ - a number worth memorising outright.",
       r"Efficiency is (variance of the better estimator)/(variance of the estimator being judged), so it must be $\le1$.",
       r"Options $\pi$, $\pi/2$ and $2\pi$ all exceed 1 and can be eliminated at once."],
 sol=[r"For a normal sample, $V(\bar X)=\dfrac{\sigma^2}{n}$ and asymptotically $V(\text{median})=\dfrac{\pi\sigma^2}{2n}$.",
      r"Efficiency of the median relative to the mean $=\dfrac{V(\bar X)}{V(\text{median})}=\dfrac{\sigma^2/n}{\pi\sigma^2/(2n)}=\dfrac{2}{\pi}$.",
      r"Numerically $2/\pi\approx0.6366$. Option (c)."]),

"2020-Q22": dict(ans=3, qtype="Conceptual", diff="Easy",
 sc=r"With $\mu$ known the complete sufficient statistic is $\sum(X_i-\mu)^2$, and dividing by $n$ makes it unbiased: $\frac1n\sum(X_i-\mu)^2$.",
 tips=[r"$\sum(X_i-\mu)^2/\sigma^2\sim\chi^2_n$ (not $\chi^2_{n-1}$) because $\mu$ is not estimated.",
       r"Divisor $n$ when $\mu$ is known; divisor $n-1$ when $\bar X$ replaces $\mu$.",
       r"Estimators built on $\bar X$ (options (a), (c)) discard the known value of $\mu$ and are not optimal here."],
 sol=[r"With $\mu$ known, $T=\sum(X_i-\mu)^2$ is complete and sufficient for $\sigma^2$.",
      r"$T/\sigma^2\sim\chi^2_n$, so $E(T)=n\sigma^2$ and $\dfrac Tn$ is unbiased.",
      r"Being an unbiased function of a complete sufficient statistic, $\dfrac1n\sum(X_i-\mu)^2$ is the MVUE (Lehmann-Scheffe).",
      r"Option (d)."]),

"2020-Q23": dict(ans=1, qtype="Numerical", diff="Hard",
 sc=r"Differentiating $\log L$ collapses to $\sum\log x_i=n\log\theta$, so $\hat\theta$ is the GEOMETRIC MEAN. For $\{8,4,\tfrac12,1\}$ the product is $16$ and $16^{1/4}=2$.",
 tips=[r"$\log f=\theta A'(\theta)\left[\log x-\log\theta\right]+A(\theta)+C(x)$; the $A$-terms cancel when you differentiate and simplify.",
       r"The likelihood equation reduces to $\frac1n\sum\log x_i=\log\theta$, i.e. $\hat\theta=\left(\prod x_i\right)^{1/n}$.",
       r"$8\times4\times\tfrac12\times1=16$ and $16^{1/4}=2$ - clean by design."],
 sol=[r"$\log L=\theta A'(\theta)\left[\sum\log x_i-n\log\theta\right]+nA(\theta)+\sum C(x_i)$.",
      r"$\dfrac{\partial\log L}{\partial\theta}=\left[A'(\theta)+\theta A''(\theta)\right]\left[\sum\log x_i-n\log\theta\right]-\theta A'(\theta)\dfrac n\theta+nA'(\theta)$.",
      r"The last two terms cancel, leaving $\left[A'+\theta A''\right]\left[\sum\log x_i-n\log\theta\right]=0$, hence $\log\theta=\dfrac1n\sum\log x_i$.",
      r"So $\hat\theta=\left(\prod x_i\right)^{1/n}=\left(8\cdot4\cdot\tfrac12\cdot1\right)^{1/4}=16^{1/4}=2$.",
      r"Option (b)."]),

"2020-Q24": dict(ans=0, qtype="Numerical", diff="Medium",
 sc=r"Every $\theta\in\left[X_{(n)}-\tfrac12,\ X_{(1)}+\tfrac12\right]=[6.98,\ 7.12]$ maximises the likelihood. Only $6.99$ and $7.10$ both lie there.",
 tips=[r"$U(\theta-\tfrac12,\theta+\tfrac12)$ has a FLAT likelihood over an interval - the MLE is not unique.",
       r"The interval is $\left[\max x_i-\tfrac12,\ \min x_i+\tfrac12\right]$; here $\max=7.48$, $\min=6.62$.",
       r"Check both numbers in each option against $[6.98,7.12]$: (b), (c), (d) each contain a value outside."],
 sol=[r"The likelihood is $1$ whenever $\theta-\tfrac12\le x_i\le\theta+\tfrac12$ for all $i$, i.e. $X_{(n)}-\tfrac12\le\theta\le X_{(1)}+\tfrac12$.",
      r"Here $X_{(n)}=7.48$ and $X_{(1)}=6.62$, giving $6.98\le\theta\le7.12$.",
      r"Both $6.99$ and $7.10$ lie in $[6.98,7.12]$, so both are maximum likelihood estimates.",
      r"$6.87$, $7.54$ and $6.92$ all fall outside. Option (a)."]),

"2020-Q25": dict(ans=0, qtype="Numerical", diff="Medium",
 sc=r"$\operatorname{Cov}(A,B)=V(A)$ for a minimum-variance $A$, so $\operatorname{Cov}(A,B-A)=V(A)-V(A)=0$.",
 tips=[r"Same identity as the $\rho=\sqrt e$ result: $\operatorname{Cov}(A,B)=V(A)$ whenever $A$ is minimum variance.",
       r"Geometric reading: the efficient estimator is the projection, and $B-A$ is the orthogonal 'error' component.",
       r"A covariance is a number with units of variance, so options like $e$ or $e^2$ (dimensionless) are structurally suspect."],
 sol=[r"Suppose $\operatorname{Cov}(A,B)\ne V(A)$. Then for small $\lambda$, $C=A+\lambda(B-A)$ is unbiased with $V(C)=V(A)+2\lambda\left[\operatorname{Cov}(A,B)-V(A)\right]+O(\lambda^2)$.",
      r"Choosing the sign of $\lambda$ appropriately would make $V(C)<V(A)$, contradicting the minimum-variance property of $A$.",
      r"Hence $\operatorname{Cov}(A,B)=V(A)$.",
      r"Therefore $\operatorname{Cov}(A,B-A)=\operatorname{Cov}(A,B)-V(A)=0$ - option (a)."]),

"2020-Q26": dict(ans=0, qtype="Conceptual", diff="Medium",
 sc=r"$S^2$ is unbiased for the Poisson parameter $\lambda$, so the Cramer-Rao bound $\lambda/n$ applies: $V(S^2)\ge\lambda/n$.",
 tips=[r"For Poisson mean $=$ variance $=\lambda$, so $S^2$ estimates the same parameter as $\bar X$.",
       r"CR bound for unbiased estimators of $\lambda$ is $\lambda/n$; $\bar X$ attains it, $S^2$ does not.",
       r"Any unbiased estimator of $\lambda$ obeys the same bound - the bound depends on the parameter, not on the estimator."],
 sol=[r"For Poisson($\lambda$), $I_1(\lambda)=1/\lambda$ so $I_n(\lambda)=n/\lambda$.",
      r"The Cramer-Rao lower bound for the variance of any unbiased estimator of $\lambda$ is $\dfrac{1}{I_n(\lambda)}=\dfrac{\lambda}{n}$.",
      r"$S^2$ is unbiased for the population variance, which equals $\lambda$ here, so the bound applies to it.",
      r"Hence $V(S^2)\ge\dfrac\lambda n$ - option (a)."]),

"2020-Q27": dict(ans=1, qtype="Numerical", diff="Hard",
 sc=r"$T\sim N(-\theta,3)$, so $I_T(\theta)=\frac{(\partial E(T)/\partial\theta)^2}{V(T)}=\frac{1}{3}$.",
 tips=[r"For a normal statistic with mean $m(\theta)$ and constant variance $v$, the information is $\left[m'(\theta)\right]^2/v$.",
       r"$E(T)=\theta-\theta-\theta=-\theta$ and $V(T)=1+1+1=3$.",
       r"The whole sample carries $I=3$; the non-sufficient reduction $T$ retains only $1/3$ - information is lost, never gained."],
 sol=[r"$T=X_1-X_2-X_3$ is a linear combination of independent normals, so $T\sim N\left(-\theta,\ 3\right)$.",
      r"$\log f_T(t)=\text{const}-\dfrac{(t+\theta)^2}{6}$, so $\dfrac{\partial\log f_T}{\partial\theta}=-\dfrac{t+\theta}{3}$.",
      r"$I_T(\theta)=E\left[\left(\dfrac{T+\theta}{3}\right)^2\right]=\dfrac{V(T)}{9}=\dfrac39=\dfrac13$.",
      r"Option (b). (Compare $I=3$ for the full sample - the reduction to $T$ destroys information.)"]),

"2020-Q28": dict(ans=1, qtype="Conceptual", diff="Medium",
 sc=r"$X_{(1)}$ alone is sufficient; therefore the PAIR $\left\{X_{(1)},\sum X_i\right\}$ is also (jointly) sufficient - and it is the only offered option containing $X_{(1)}$.",
 tips=[r"Any statistic from which a sufficient statistic can be recovered is itself sufficient - enlarging never hurts.",
       r"$L=e^{n\theta-\sum x_i}I\left(X_{(1)}>\theta\right)$: the indicator needs $X_{(1)}$, the exponent needs $\sum x_i$ only as data.",
       r"$X_{(n)}$ carries no information about the lower endpoint, so options (c) and (d) fail."],
 sol=[r"$L(\theta)=\prod e^{-(x_i-\theta)}I(x_i>\theta)=e^{n\theta-\sum x_i}\,I\left(X_{(1)}>\theta\right)$.",
      r"Factorising with $g_\theta(t)=e^{n\theta}I(t>\theta)$, $t=X_{(1)}$, and $h(x)=e^{-\sum x_i}$ shows $X_{(1)}$ is sufficient.",
      r"Option (a) is false because $\sum X_i$ alone cannot reproduce the indicator; options (c) and (d) use $X_{(n)}$, which does not constrain $\theta$.",
      r"Since $X_{(1)}$ is sufficient, the pair $\left\{X_{(1)},\sum X_i\right\}$ is jointly sufficient - option (b)."]),

"2020-Q29": dict(ans=3, qtype="Conceptual", diff="Hard",
 sc=r"$X_1-X_2$ and $2X_1-X_2-X_3$ have distributions free of $\theta$ (ancillary), so they cannot be complete. $X_1+X_2+X_3+X_4$ is the natural exponential-family statistic and is complete.",
 tips=[r"Any statistic whose distribution does not involve $\theta$ is ancillary and therefore NOT complete (unless degenerate).",
       r"Check the means: $E(X_1-X_2)=0$ and $E(2X_1-X_2-X_3)=0$ for every $\theta$ - explicit non-trivial unbiased estimators of zero.",
       r"$\sum X_i$ is the complete sufficient statistic of the normal location family - the intended answer."],
 sol=[r"For $N(\theta,1)$, take $g(t)=t$. Then $E\left[g(X_1-X_2)\right]=0$ for all $\theta$ although $g\not\equiv0$, so $X_1-X_2$ is not complete.",
      r"Similarly $E\left(2X_1-X_2-X_3\right)=2\theta-\theta-\theta=0$ for every $\theta$, so option (c) is not complete either.",
      r"$X_1+X_2+X_3+X_4\sim N(4\theta,4)$ is the natural sufficient statistic of this full-rank one-parameter exponential family, hence complete.",
      r"Option (d)."]),

"2020-Q30": dict(ans=3, qtype="Numerical", diff="Medium",
 sc=r"$\operatorname{Cov}(X+Y,X-Y)=\sigma_1^2-\sigma_2^2$, and for jointly normal variables zero covariance is independence - so $\sigma_1=\sigma_2$ iff $X+Y$ and $X-Y$ are independent.",
 tips=[r"$\operatorname{Cov}(X+Y,X-Y)=V(X)-V(Y)$; the correlation term cancels completely.",
       r"Joint normality is essential: uncorrelated implies independent only in that setting.",
       r"This is the basis of the paired-sample test for equality of variances (Pitman-Morgan test)."],
 sol=[r"$\operatorname{Cov}(X+Y,\ X-Y)=V(X)-\operatorname{Cov}(X,Y)+\operatorname{Cov}(Y,X)-V(Y)=V(X)-V(Y)=\sigma_1^2-\sigma_2^2$.",
      r"$(X+Y,X-Y)$ is a linear transformation of a bivariate normal vector, hence itself bivariate normal.",
      r"For jointly normal variables, zero covariance is equivalent to independence.",
      r"So $\sigma_1=\sigma_2\iff X+Y$ and $X-Y$ are independent - option (d)."]),

"2020-Q31": dict(ans=0, qtype="Numerical", diff="Hard",
 sc=r"The first-order (Cramer-Rao) term is $\frac{4\theta^2}{n}$ and the second-order Bhattacharya term adds $\frac{2}{n^2}$ - attained exactly by $\bar X^2-\frac1n$.",
 tips=[r"$g(\theta)=\theta^2$ gives $g'=2\theta$, so the CR part is $(2\theta)^2/n=4\theta^2/n$.",
       r"The improvement $2/n^2$ is exactly what makes the bound attainable by the UMVUE $\bar X^2-\frac1n$.",
       r"Sanity check: $V\left(\bar X^2-\frac1n\right)=\frac{4\theta^2}{n}+\frac{2}{n^2}$ by direct computation from the moments of a normal."],
 sol=[r"For $N(\theta,1)$, the UMVUE of $\theta^2$ is $T=\bar X^2-\dfrac1n$ with $\bar X\sim N\left(\theta,\tfrac1n\right)$.",
      r"$V(\bar X^2)=E(\bar X^4)-\left[E(\bar X^2)\right]^2=\left(\theta^4+\dfrac{6\theta^2}{n}+\dfrac3{n^2}\right)-\left(\theta^2+\dfrac1n\right)^2=\dfrac{4\theta^2}{n}+\dfrac{2}{n^2}$.",
      r"The first-order (Cramer-Rao) bound for $g(\theta)=\theta^2$ is $\dfrac{(2\theta)^2}{n}=\dfrac{4\theta^2}{n}$; the second-order Bhattacharya bound adds $\dfrac2{n^2}$.",
      r"Since $T$ attains it, the Bhattacharya bound is $\dfrac{4\theta^2}{n}+\dfrac2{n^2}$ - option (a)."]),

"2020-Q32": dict(ans=0, qtype="Numerical", diff="Easy",
 sc=r"$E(X_i)=nP$, so the MLE (and moment estimator) is $\hat P=\bar X/n$.",
 tips=[r"There are $m$ observations each on $n$ trials, so the total number of trials is $mn$ and $\hat P=\dfrac{\sum X_i}{mn}=\dfrac{\bar X}{n}$.",
       r"Divide by the NUMBER OF TRIALS $n$, not by the number of observations $m$ - the two are easy to swap.",
       r"Check with $n=1$ (Bernoulli): the formula returns $\bar X$, as it must."],
 sol=[r"$L(P)=\prod_{i=1}^m\binom{n}{x_i}P^{x_i}(1-P)^{n-x_i}$, so $\log L=\text{const}+\left(\sum x_i\right)\log P+\left(mn-\sum x_i\right)\log(1-P)$.",
      r"$\dfrac{\partial\log L}{\partial P}=\dfrac{\sum x_i}{P}-\dfrac{mn-\sum x_i}{1-P}=0$.",
      r"Solving: $\hat P=\dfrac{\sum x_i}{mn}=\dfrac{\bar X}{n}$.",
      r"Option (a)."]),

"2020-Q33": dict(ans=1, qtype="Numerical", diff="Hard",
 sc=r"$X_{(n)}$ has density $\frac{3nx^{3n-1}}{\theta^{3n}}$, so $E\left[X_{(n)}^r\right]=\frac{3n}{r+3n}\theta^r$; the UMVUE is $X_{(n)}^r\cdot\frac{r+3n}{3n}$.",
 tips=[r"The cdf of a single observation is $(x/\theta)^3$, so the cdf of the maximum is $(x/\theta)^{3n}$ - raise to the power $n$.",
       r"$E\left[X_{(n)}^r\right]=\int_0^\theta x^r\frac{3nx^{3n-1}}{\theta^{3n}}dx=\frac{3n\theta^r}{r+3n}$; invert the constant to de-bias.",
       r"Check $r=0$: the factor becomes 1, as it must. Option (d) inverts the correction and is the standard trap."],
 sol=[r"$F(x)=\displaystyle\int_0^x\frac{3u^2}{\theta^3}du=\left(\frac{x}{\theta}\right)^3$, so $F_{X_{(n)}}(x)=\left(\frac x\theta\right)^{3n}$ and $f_{X_{(n)}}(x)=\dfrac{3n\,x^{3n-1}}{\theta^{3n}}$.",
      r"$E\left[X_{(n)}^r\right]=\displaystyle\int_0^\theta x^r\frac{3n\,x^{3n-1}}{\theta^{3n}}dx=\frac{3n}{\theta^{3n}}\cdot\frac{\theta^{r+3n}}{r+3n}=\frac{3n}{r+3n}\theta^r$.",
      r"So $T=X_{(n)}^r\cdot\dfrac{r+3n}{3n}$ is unbiased for $\theta^r$.",
      r"$X_{(n)}$ is complete sufficient for this family, so $T$ is the UMVUE - option (b)."]),

"2020-Q34": dict(ans=1, qtype="Numerical", diff="Medium",
 sc=r"$\hat p=60/500=0.12$, SE $=\sqrt{0.12(0.88)/500}=0.01453$; $0.12\pm2.33(0.01453)=(0.0861,0.1539)$, i.e. $(8.61\%,15.38\%)$.",
 tips=[r"Convert to a percentage only at the end - working in proportions keeps the arithmetic clean.",
       r"$\hat p(1-\hat p)=0.1056$; divide by $500$ and take the square root: $0.014533$.",
       r"The interval must be centred on $12\%$ - a screen that eliminates (a), (c) and (d) immediately."],
 sol=[r"$\hat p=\dfrac{60}{500}=0.12$.",
      r"$\text{SE}=\sqrt{\dfrac{0.12\times0.88}{500}}=\sqrt{0.0002112}=0.014533$.",
      r"Margin $=2.33\times0.014533=0.033862$.",
      r"Limits: $0.12\pm0.03386=(0.08614,\ 0.15386)$, i.e. $(8.61\%,\ 15.38\%)$ - option (b)."]),

"2020-Q35": dict(ans=2, qtype="Conceptual", diff="Medium",
 sc=r"Composite means $\Theta_0$ has more than one point (1 true). A completely specified distribution is SIMPLE, so 2 is false. Statement 3 is the correct level-$\alpha$ condition for a composite null.",
 tips=[r"Simple $\iff$ the distribution is completely specified; composite $\iff$ it is not.",
       r"For a composite null the size is $\sup_{\theta\in\Theta_0}P_\theta(w)$, hence the requirement $P_\theta(w)\le\alpha$ for ALL $\theta\in\Theta_0$.",
       r"Statement 2 is the definition of the opposite concept - a classic planted contradiction."],
 sol=[r"Statement 1: $H_0$ is composite precisely when $\Theta_0$ contains more than one parameter point. True.",
      r"Statement 2: if the joint distribution is completely specified, the hypothesis is SIMPLE, not composite. False.",
      r"Statement 3: a level-$\alpha$ test of a composite null must satisfy $P_\theta(w)\le\alpha$ for every $\theta\in\Theta_0$. True.",
      r"Hence 1 and 3 only - option (c)."]),

"2020-Q36": dict(ans=1, qtype="Conceptual", diff="Hard",
 sc=r"The joint pdf statement is correct. But $\bar X-\tfrac12$ is only unbiased, not best - the minimal sufficient statistic is $(X_{(1)},X_{(n)})$, which is incomplete, and better unbiased estimators exist.",
 tips=[r"$\bar X$ is not a function of the minimal sufficient statistic here, so Rao-Blackwellising it strictly improves it.",
       r"The likelihood constraint $\max x_i-1<\theta<\min x_i$ is exactly statement 2 - read it as two simultaneous inequalities.",
       r"'Best unbiased' is a strong claim; for $U(\theta,\theta+1)$ no UMVUE exists at all."],
 sol=[r"Statement 2: the density is $1$ on $(\theta,\theta+1)$, so the joint density is $1$ provided $\theta<x_i<\theta+1$ for all $i$, i.e. $\max_ix_i-1<\theta<\min_ix_i$. True.",
      r"Statement 1: $E(\bar X)=\theta+\tfrac12$, so $\bar X-\tfrac12$ is unbiased. But $\bar X$ is not a function of the minimal sufficient statistic $(X_{(1)},X_{(n)})$.",
      r"Conditioning on that statistic (Rao-Blackwell) gives a strictly better unbiased estimator, so $\bar X-\tfrac12$ is not best. False.",
      r"Hence 2 only - option (b)."]),

"2020-Q37": dict(ans=1, qtype="Numerical", diff="Easy",
 sc=r"$E(\bar X)=\theta/2$ so $2\bar X$ is unbiased; $E\left[X_{(n)}\right]=\frac{n\theta}{n+1}$ so $\frac{n+1}nX_{(n)}$ is unbiased.",
 tips=[r"$E\left[X_{(n)}\right]=\dfrac{n}{n+1}\theta$ - the maximum systematically under-estimates $\theta$, so the correction factor exceeds 1.",
       r"$E(\bar X)=\theta/2$ for $U(0,\theta)$, so double it.",
       r"Options 1 and 4 push the estimators in the wrong direction and are both biased downwards."],
 sol=[r"$E(X_i)=\dfrac\theta2$, so $E(2\bar X)=\theta$: estimator 2 is unbiased.",
      r"$F_{X_{(n)}}(x)=\left(\dfrac x\theta\right)^n$ gives $E\left[X_{(n)}\right]=\displaystyle\int_0^\theta x\frac{nx^{n-1}}{\theta^n}dx=\frac{n\theta}{n+1}$.",
      r"Hence $E\left[\dfrac{n+1}{n}X_{(n)}\right]=\theta$: estimator 3 is unbiased.",
      r"Estimators 1 and 4 have expectations $\theta/4$ and $\dfrac{n^2\theta}{(n+1)^2}$, both $\ne\theta$.",
      r"Hence 2 and 3 - option (b)."]),

"2020-Q38": dict(ans=3, qtype="Numerical", diff="Hard",
 sc=r"$V(2\bar X)=\frac{\theta^2}{3n}$ and $V\left(\frac{n+1}nX_{(n)}\right)=\frac{\theta^2}{n(n+2)}$, so the efficiency is $\frac{3}{n+2}$.",
 tips=[r"$V(X)=\theta^2/12$ for $U(0,\theta)$, so $V(2\bar X)=4\cdot\frac{\theta^2}{12n}=\frac{\theta^2}{3n}$.",
       r"$V\left[X_{(n)}\right]=\dfrac{n\theta^2}{(n+1)^2(n+2)}$; multiply by $\left(\frac{n+1}{n}\right)^2$ to get $\dfrac{\theta^2}{n(n+2)}$.",
       r"Efficiency $<1$ for $n>1$, so the order statistic is far better - a good order-of-magnitude check."],
 sol=[r"$V(X)=\dfrac{\theta^2}{12}$, so $V(2\bar X)=4\cdot\dfrac{\theta^2}{12n}=\dfrac{\theta^2}{3n}$.",
      r"$E\left[X_{(n)}^2\right]=\dfrac{n\theta^2}{n+2}$, so $V\left[X_{(n)}\right]=\dfrac{n\theta^2}{n+2}-\dfrac{n^2\theta^2}{(n+1)^2}=\dfrac{n\theta^2}{(n+1)^2(n+2)}$.",
      r"$V\left[\dfrac{n+1}{n}X_{(n)}\right]=\dfrac{(n+1)^2}{n^2}\cdot\dfrac{n\theta^2}{(n+1)^2(n+2)}=\dfrac{\theta^2}{n(n+2)}$.",
      r"Efficiency of $2\bar X$ relative to it $=\dfrac{\theta^2/[n(n+2)]}{\theta^2/(3n)}=\dfrac{3}{n+2}$ - option (d)."]),
}
