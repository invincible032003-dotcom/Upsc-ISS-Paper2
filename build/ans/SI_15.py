# -*- coding: utf-8 -*-
# Statistical Inference answer pack 15 (2025 Q48-Q50, 2026 Q1-Q25)
A = {
"2025-Q48": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"The MLE of $\sigma^2$ uses divisor $n$: $\hat\sigma^2=\frac1n\sum(x_i-\bar x)^2$ (biased).",
 tips=[r"MLE divisor is $n$; the UNBIASED divisor is $n-1$. The question asks for the MLE.",
       r"$E\left(\hat\sigma^2\right)=\frac{n-1}{n}\sigma^2$ - biased downwards.",
       r"Both estimators are consistent; only one is unbiased and only one is the MLE."],
 sol=[r"$\log L=-\dfrac n2\log(2\pi\sigma^2)-\dfrac{\sum(x_i-\mu)^2}{2\sigma^2}$; substituting $\hat\mu=\bar x$ and differentiating in $\sigma^2$:",
      r"$-\dfrac{n}{2\sigma^2}+\dfrac{\sum(x_i-\bar x)^2}{2\sigma^4}=0$.",
      r"$\hat\sigma^2=\dfrac1n\sum_{i=1}^n(x_i-\bar x)^2$ - option (c)."]),

"2025-Q49": dict(ans=1, qtype="Conceptual", diff="Medium",
 sc=r"Wald's exact inequality is $A\le\frac{1-\beta}{\alpha}$ (the approximation replaces $\le$ by $=$).",
 tips=[r"The exact relations are $A\le\frac{1-\beta}{\alpha}$ and $B\ge\frac{\beta}{1-\alpha}$ - note the opposite directions.",
       r"Derivation: integrate the likelihood ratio over the rejection region to get $1-\beta\ge A\alpha$.",
       r"Using the equalities as approximations makes the achieved error probabilities slightly smaller than nominal."],
 sol=[r"Let $R$ be the event of rejecting $H_0$. On $R$ the terminal likelihood ratio is at least $A$, so $P_1(R)\ge A\,P_0(R)$.",
      r"$P_1(R)=1-\beta$ and $P_0(R)=\alpha$, giving $1-\beta\ge A\alpha$.",
      r"Hence $A\le\dfrac{1-\beta}{\alpha}$ - option (b)."]),

"2025-Q50": dict(ans=1, qtype="Conceptual", diff="Medium",
 sc=r"The companion inequality is $B\ge\frac{\beta}{1-\alpha}$.",
 tips=[r"Apply the same argument on the ACCEPTANCE event, where the ratio is at most $B$.",
       r"$\beta\le B(1-\alpha)$ rearranges to $B\ge\frac{\beta}{1-\alpha}$.",
       r"$A\le\cdot$ but $B\ge\cdot$ - the directions are opposite and are frequently swapped in distractors."],
 sol=[r"Let $Ac$ be the event of accepting $H_0$. On $Ac$ the terminal likelihood ratio is at most $B$, so $P_1(Ac)\le B\,P_0(Ac)$.",
      r"$P_1(Ac)=\beta$ and $P_0(Ac)=1-\alpha$, giving $\beta\le B(1-\alpha)$.",
      r"Hence $B\ge\dfrac{\beta}{1-\alpha}$ - option (b)."]),

"2026-Q1": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"$\bar X$ is unbiased with $V(\bar X)=\sigma^2/n\to0$, hence consistent for $\mu$ by Chebyshev.",
 tips=[r"Extremes and the midrange converge to the support endpoints, not to the mean, for general distributions.",
       r"$\bar X$ is consistent for the mean of ANY distribution with finite variance - the WLLN.",
       r"The midrange is consistent for $\mu$ only for symmetric bounded distributions - not in general."],
 sol=[r"$E(\bar X)=\mu$ and $V(\bar X)=\dfrac{\sigma^2}{n}\to0$.",
      r"By Chebyshev's inequality, $P\left(|\bar X-\mu|>\varepsilon\right)\le\dfrac{\sigma^2}{n\varepsilon^2}\to0$, so $\bar X\xrightarrow{P}\mu$.",
      r"The minimum, maximum and midrange do not in general converge to $\mu$.",
      r"Option (c)."]),

"2026-Q2": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"$L=\theta^n\left(\prod x_i^{-2}\right)I\left(X_{(1)}\ge\theta\right)$, so the MINIMUM is sufficient.",
 tips=[r"The support constraint $x\ge\theta$ puts $X_{(1)}$ into the indicator - that is where sufficiency lives.",
       r"$\prod x_i^{-2}$ is free of $\theta$ and goes into $h(x)$.",
       r"Option (d), $\min(x_i^2)$, is a one-to-one function of $\min(x_i)$ for positive data, but the standard answer is the minimum itself."],
 sol=[r"$f(x)=\dfrac{\theta}{x^2}$ for $x\ge\theta$, so $L(\theta)=\theta^n\left(\prod_{i=1}^n\dfrac1{x_i^2}\right)I\left(\min_i x_i\ge\theta\right)$.",
      r"Factorising with $g_\theta(t)=\theta^nI(t\ge\theta)$, $t=X_{(1)}$, and $h(x)=\prod x_i^{-2}$ gives sufficiency.",
      r"Hence $\min(x_1,\dots,x_n)$ is sufficient - option (c)."]),

"2026-Q3": dict(ans=3, qtype="Numerical", diff="Hard",
 sc=r"$\log\frac{L_1}{L_0}=n\log2+2n-2\sum x_i$, so the region $\frac{L_1}{L_0}\ge k$ becomes $\bar x\le1+\frac{\log2}{2}-\frac{\log k}{2n}$.",
 tips=[r"Both densities are defined only where every $x_i\ge3$ (the larger of the two thresholds), so the indicator is common and cancels.",
       r"$-4\sum(x_i-2)+2\sum(x_i-3)=-2\sum x_i+8n-6n=-2\sum x_i+2n$.",
       r"Dividing by $-2n$ flips the inequality, producing an upper-bound (left-tail) region in $\bar x$."],
 sol=[r"$L_0=2^n\exp\left[-2\sum(x_i-3)\right]$ and $L_1=4^n\exp\left[-4\sum(x_i-2)\right]$, both on the common region $x_i\ge3$.",
      r"$\log\dfrac{L_1}{L_0}=n\log2-4\sum x_i+8n+2\sum x_i-6n=n\log2+2n-2\sum x_i$.",
      r"$\dfrac{L_1}{L_0}\ge k\iff n\log2+2n-2\sum x_i\ge\log k\iff\sum x_i\le\dfrac{n\log2+2n-\log k}{2}$.",
      r"Dividing by $n$: $\bar x\le1+\dfrac{\log2}{2}-\dfrac{\log k}{2n}$ - option (d)."]),

"2026-Q4": dict(ans=3, qtype="Conceptual", diff="Hard",
 sc=r"$\frac{2\theta^2}{n}$ is the bound for $\sigma^2$, not for $\sigma$ (the bound for $\sigma$ is $\frac{\sigma^2}{2n}$); and $E|X-\mu|=\sigma\sqrt{2/\pi}\ne\sigma$. Both statements fail.",
 tips=[r"Bound for $\psi(\theta)=\sqrt\theta$: $\frac{[1/(2\sqrt\theta)]^2}{n/(2\theta^2)}=\frac{\theta}{2n}=\frac{\sigma^2}{2n}$.",
       r"$E|Z|=\sqrt{2/\pi}$ for standard normal, so the mean absolute deviation estimates $\sigma\sqrt{2/\pi}\approx0.798\sigma$.",
       r"The unbiased version is $\sqrt{\frac{\pi}{2}}\cdot\frac{\sum|x_i-\mu|}{n}$ - the missing constant is the whole point."],
 sol=[r"Statement I: with $\theta=\sigma^2$, $I_n(\theta)=\dfrac{n}{2\theta^2}$, so the bound for $\theta$ itself is $\dfrac{2\theta^2}{n}$. For $\psi(\theta)=\sigma=\sqrt\theta$, $\psi'=\dfrac1{2\sqrt\theta}$ and the bound is $\dfrac{1/(4\theta)}{n/(2\theta^2)}=\dfrac{\theta}{2n}$. So the quoted value is the bound for $\sigma^2$, not $\sigma$. False.",
      r"Statement II: $E|X-\mu|=\sigma E|Z|=\sigma\sqrt{\dfrac2\pi}$, so $E\left[\dfrac{\sum|x_i-\mu|}{n}\right]=\sigma\sqrt{\dfrac2\pi}\ne\sigma$. False.",
      r"Neither statement holds - option (d)."]),

"2026-Q5": dict(ans=3, qtype="Numerical", diff="Medium",
 sc=r"$W=n\left(X_{(1)}-\theta\right)\sim\text{Exp}(1)$; the interval corresponds to $0\le W<\log40$, with probability $1-\frac1{40}=0.975$.",
 tips=[r"$\theta<x_{(1)}$ is automatic, so only the lower endpoint constrains the coverage.",
       r"$P(W<\log c)=1-\frac1c$ - so $c=40$ gives $0.975$ directly.",
       r"Compare the companion 2023 item where $\log4$ gave $0.75$ - same mechanism, different constant."],
 sol=[r"$P\left(X_{(1)}>t\right)=e^{-n(t-\theta)}$, so $W=n\left(X_{(1)}-\theta\right)\sim\text{Exp}(1)$.",
      r"$\theta>x_{(1)}-\dfrac{\log40}{n}\iff W<\log40$; and $\theta<x_{(1)}\iff W>0$, which always holds.",
      r"Coverage $=P\left(0<W<\log40\right)=1-e^{-\log40}=1-\dfrac1{40}=0.975$.",
      r"Option (d)."]),

"2026-Q6": dict(ans=2, qtype="Numerical", diff="Hard",
 sc=r"By invariance the MLE of $e^{2\theta}$ is $e^{2\bar x}$, whose expectation is $e^{2\theta}e^{2\sigma^2/n}$ - a multiplicative bias factor of $e^{2\sigma^2/n}-1$; dividing by $e^{2\sigma^2/n}$ gives the unbiased $e^{2\bar x-2\sigma^2/n}$.",
 tips=[r"$\bar X\sim N\left(\theta,\frac{\sigma^2}{n}\right)$, so $E\left(e^{t\bar X}\right)=e^{t\theta+t^2\sigma^2/(2n)}$; put $t=2$.",
       r"$E\left(e^{2\bar X}\right)=e^{2\theta+2\sigma^2/n}$, so the ABSOLUTE bias is $e^{2\theta}\left(e^{2\sigma^2/n}-1\right)$ and the RELATIVE bias is $e^{2\sigma^2/n}-1$.",
       r"Invariance preserves the MLE property but destroys unbiasedness - the correction factor restores it."],
 sol=[r"The MLE of $\theta$ is $\bar x$, so by invariance the MLE of $e^{2\theta}$ is $e^{2\bar x}$. Statement I's first half is correct.",
      r"$\bar X\sim N\left(\theta,\dfrac{\sigma^2}{n}\right)$, so $E\left(e^{2\bar X}\right)=\exp\left(2\theta+\dfrac{2\sigma^2}{n}\right)=e^{2\theta}\,e^{2\sigma^2/n}$.",
      r"Relative to the target $e^{2\theta}$, the estimator is inflated by the factor $e^{2\sigma^2/n}$, i.e. a proportional bias of $e^{2\sigma^2/n}-1$ - the quantity quoted in Statement I.",
      r"Statement II: $E\left(e^{2\bar X-2\sigma^2/n}\right)=e^{-2\sigma^2/n}e^{2\theta+2\sigma^2/n}=e^{2\theta}$ - exactly unbiased. True.",
      r"Option (c)."]),

"2026-Q7": dict(ans=2, qtype="Numerical", diff="Medium",
 sc=r"$E(X)=\frac{2(1-\theta)+2\theta}{2-\theta}=\frac{2}{2-\theta}$; setting this to $\bar x$ gives $\hat\theta=2\left(1-\frac1{\bar x}\right)$.",
 tips=[r"Compute $E(X)=1\cdot P(X=1)+2\cdot P(X=2)$ and watch the numerator collapse to $2$.",
       r"$\frac{2}{2-\theta}=\bar x\Rightarrow2-\theta=\frac{2}{\bar x}\Rightarrow\theta=2-\frac2{\bar x}$.",
       r"Since $X\in\{1,2\}$, $\bar x\in[1,2]$, and the formula returns $\theta\in[0,1]$ as required."],
 sol=[r"$E(X)=1\cdot\dfrac{2(1-\theta)}{2-\theta}+2\cdot\dfrac{\theta}{2-\theta}=\dfrac{2-2\theta+2\theta}{2-\theta}=\dfrac{2}{2-\theta}$.",
      r"Method of moments: $\dfrac{2}{2-\hat\theta}=\bar x$.",
      r"$2-\hat\theta=\dfrac{2}{\bar x}\Rightarrow\hat\theta=2-\dfrac2{\bar x}=2\left(1-\dfrac1{\bar x}\right)$.",
      r"Option (c)."]),

"2026-Q8": dict(ans=0, qtype="Numerical", diff="Hard",
 sc=r"$f_1/f_0=\frac{1}{2x^4}$ is DECREASING, so the MP region is $x\le c$ with $c^8=0.10$; the power is $c^4=\sqrt{0.10}$.",
 tips=[r"Decreasing likelihood ratio $\Rightarrow$ LEFT-tail critical region - the direction is the whole trick.",
       r"$P_{H_0}(X\le c)=\int_0^c8x^7dx=c^8$ and $P_{H_1}(X\le c)=\int_0^c4x^3dx=c^4$.",
       r"Power $=c^4=\left(c^8\right)^{1/2}=\sqrt{\alpha}$ - a neat square-root relation."],
 sol=[r"$\dfrac{f_1(x)}{f_0(x)}=\dfrac{4x^3}{8x^7}=\dfrac{1}{2x^4}$, which decreases in $x$, so the MP test rejects for SMALL $x$: $C=\{x\le c\}$.",
      r"Size: $\displaystyle\int_0^c8x^7dx=c^8=0.10$, so $c=(0.10)^{1/8}$.",
      r"Power: $\displaystyle\int_0^c4x^3dx=c^4=(0.10)^{4/8}=(0.10)^{1/2}$.",
      r"$=\sqrt{0.10}$ - option (a)."]),

"2026-Q9": dict(ans=3, qtype="Conceptual", diff="Easy",
 sc=r"$L=\theta^{-n}\exp\left(-\frac{\sum x_i}{\theta}\right)$, so $\sum X_i$ is sufficient.",
 tips=[r"The exponential family's natural statistic is the SUM, not the sum of squares.",
       r"Adding a constant (options (b), (c)) still gives a sufficient statistic mathematically, but the canonical answer is $\sum x_i$.",
       r"$\bar X$ is equally sufficient, being a one-to-one function."],
 sol=[r"$L(\theta)=\prod\dfrac1\theta e^{-x_i/\theta}=\dfrac{1}{\theta^n}\exp\left(-\dfrac{\sum x_i}{\theta}\right)$.",
      r"Factorising with $g_\theta(t)=\theta^{-n}e^{-t/\theta}$, $t=\sum x_i$, and $h(x)=1$ gives sufficiency.",
      r"Option (d)."]),

"2026-Q10": dict(ans=0, qtype="Conceptual", diff="Easy",
 sc=r"The score is $\frac{n}{2\sigma^4}\left[\frac{\sum x_i^2}{n}-\sigma^2\right]$, exactly the MVB form, so $\frac{\sum x_i^2}{n}$ is the MVB estimator.",
 tips=[r"Mean known ($=0$) $\Rightarrow$ deviations are the raw observations; divisor is $n$.",
       r"Options (b), (c), (d) are linear in $x_i$ and cannot estimate a variance (wrong units).",
       r"$V\left(\frac{\sum X_i^2}{n}\right)=\frac{2\sigma^4}{n}$, equal to the CR bound."],
 sol=[r"$\log L=-\dfrac n2\log(2\pi\sigma^2)-\dfrac{\sum x_i^2}{2\sigma^2}$.",
      r"$\dfrac{\partial\log L}{\partial\sigma^2}=\dfrac{n}{2\sigma^4}\left[\dfrac{\sum x_i^2}{n}-\sigma^2\right]$, which is $A(\sigma^2)\left[T-\sigma^2\right]$ with $T=\dfrac{\sum x_i^2}{n}$.",
      r"So $T$ attains the Cramer-Rao bound and is the MVB estimator of $\sigma^2$.",
      r"Option (a)."]),

"2026-Q11": dict(ans=0, qtype="Numerical", diff="Easy",
 sc=r"Half-width $=\frac{407.84-400}{2}=3.92$, and $\text{SE}=\frac{3.92}{1.96}=2$.",
 tips=[r"SE $=$ half-width divided by $z_{\alpha/2}$.",
       r"$3.92=2\times1.96$ exactly - the numbers are chosen to be clean.",
       r"Do not divide the full width by $z$; halve it first."],
 sol=[r"The interval $(400,\ 407.84)$ has width $7.84$, so the half-width is $3.92$.",
      r"Half-width $=Z_{\alpha/2}\times\text{SE}(\bar X)$, i.e. $3.92=1.96\times\text{SE}$.",
      r"$\text{SE}=\dfrac{3.92}{1.96}=2$ - option (a)."]),

"2026-Q12": dict(ans=1, qtype="Definition", diff="Easy",
 sc=r"$T$ is sufficient iff the conditional distribution of the sample given $T=t$ is INDEPENDENT of $\theta$.",
 tips=[r"This is the definition; the factorisation theorem is the equivalent working criterion.",
       r"Intuition: once $T$ is known, the remaining randomness carries no further information about $\theta$.",
       r"Any option asserting dependence on $\theta$ (or on $\theta^2$) reverses the definition."],
 sol=[r"By definition, $T$ is sufficient for $\theta$ if $P\left(X=x\mid T=t\right)$ does not involve $\theta$ for any $t$ with positive probability.",
      r"Equivalently (Fisher-Neyman), $f(x;\theta)=g_\theta(t)h(x)$.",
      r"Option (b)."]),

"2026-Q13": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"$E\left[\sum(x_i-\bar x)^2\right]=(n-1)\sigma^2$, so the unbiased estimator divides by $n-1$.",
 tips=[r"One degree of freedom is lost estimating $\mu$ by $\bar x$ - hence $n-1$.",
       r"Divisor $n$ gives the MLE (biased); divisor $n-1$ gives the unbiased estimator.",
       r"This holds for ANY distribution with finite variance, not just the normal."],
 sol=[r"$E\left[\sum_{i=1}^n(x_i-\bar x)^2\right]=\sum E(x_i^2)-nE(\bar x^2)=n(\sigma^2+\mu^2)-n\left(\dfrac{\sigma^2}{n}+\mu^2\right)=(n-1)\sigma^2$.",
      r"Dividing by $n-1$ gives an unbiased estimator of $\sigma^2$.",
      r"Option (c)."]),

"2026-Q14": dict(ans=2, qtype="Definition", diff="Easy",
 sc=r"Neyman-Pearson: simple null against simple alternative.",
 tips=[r"Both hypotheses must be simple for the likelihood ratio $L_1/L_0$ to be a well-defined number.",
       r"Karlin-Rubin extends this to composite one-sided alternatives via MLR.",
       r"Composite nulls are the domain of the likelihood ratio test."],
 sol=[r"The Neyman-Pearson lemma treats $H_0:\theta=\theta_0$ versus $H_1:\theta=\theta_1$ - both simple.",
      r"It gives the most powerful test of its size via the likelihood ratio criterion.",
      r"Option (c)."]),

"2026-Q15": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"$T=\sum(x_i-\mu)^2$ is sufficient, and $S_0^2=T/n$ is a one-to-one function of it - so both are sufficient.",
 tips=[r"Any one-to-one function of a sufficient statistic is sufficient; dividing by a known constant qualifies.",
       r"$T/\theta\sim\chi^2_n$, so $S_0^2$ is also unbiased and (by completeness) the UMVUE.",
       r"With $\mu$ known there is no need to centre at $\bar x$."],
 sol=[r"$L(\theta)=(2\pi\theta)^{-n/2}\exp\left[-\dfrac{\sum(x_i-\mu)^2}{2\theta}\right]$, so $T=\sum(x_i-\mu)^2$ is sufficient by factorisation. Statement I true.",
      r"$S_0^2=T/n$ is obtained from $T$ by multiplication by the known constant $1/n$ - a one-to-one map - so $S_0^2$ is sufficient too. Statement II true.",
      r"Option (c)."]),

"2026-Q16": dict(ans=3, qtype="Conceptual", diff="Medium",
 sc=r"Any unbiased combination that puts non-zero weight on $T_1$ has variance strictly above $V(T_0)$, so neither $U$ nor $V$ can be MVU.",
 tips=[r"$\operatorname{Cov}(T_0,T_1)=V(T_0)$, so $V\left(a_0T_0+a_1T_1\right)=V_0+a_1^2\left(V_1-V_0\right)>V_0$ whenever $a_1\ne0$.",
       r"Uniqueness of the UMVUE already rules out a second minimum-variance unbiased estimator.",
       r"The sign of $a_1$ is irrelevant because it enters squared - both $U$ and $V$ fail."],
 sol=[r"Using $\operatorname{Cov}(T_0,T_1)=V(T_0)=V_0$ and writing $V_1=V(T_1)>V_0$:",
      r"$V\left(a_0T_0+a_1T_1\right)=a_0^2V_0+a_1^2V_1+2a_0a_1V_0$. With $a_0+a_1=1$ this simplifies to $V_0+a_1^2\left(V_1-V_0\right)$.",
      r"Since $V_1>V_0$ and $a_1\ne0$, this is strictly larger than $V_0$ - so $U$ is not MVU. Statement I false.",
      r"The same computation with $a_0-a_1=1$ again gives a variance exceeding $V_0$, so $V$ is not MVU either. Statement II false.",
      r"Option (d)."]),

"2026-Q17": dict(ans=2, qtype="Numerical", diff="Medium",
 sc=r"$I_1(\theta)=\frac1{2\theta^2}$ per observation, so over the sample $E\left(\frac{\partial^2\log f_\theta}{\partial\theta^2}\right)=-\frac{n}{2\theta^2}$.",
 tips=[r"Information is additive: $I_n=nI_1$, and $E\left(\frac{\partial^2\log L}{\partial\theta^2}\right)=-I_n$.",
       r"$I_1(\sigma^2)=\frac{1}{2\sigma^4}$ - memorise this normal-variance information.",
       r"The sign must be negative (it is minus the information), eliminating options (b) and (d)."],
 sol=[r"With $\theta=\sigma^2$ and $\mu$ known, $\log f_\theta(x)=-\tfrac12\log(2\pi\theta)-\dfrac{(x-\mu)^2}{2\theta}$.",
      r"$\dfrac{\partial^2\log f_\theta}{\partial\theta^2}=\dfrac{1}{2\theta^2}-\dfrac{(x-\mu)^2}{\theta^3}$, and $E(x-\mu)^2=\theta$, so per observation the expectation is $\dfrac1{2\theta^2}-\dfrac1{\theta^2}=-\dfrac1{2\theta^2}$.",
      r"Over the whole sample of $n$ independent observations this multiplies by $n$:",
      r"$E\left(\dfrac{\partial^2\log f_\theta}{\partial\theta^2}\right)=-\dfrac{n}{2\theta^2}$ - option (c)."]),

"2026-Q18": dict(ans=2, qtype="Conceptual", diff="Medium",
 sc=r"$V(S_0^2)=\frac{2\theta^2}{n}$ exactly equals the CR bound, so II and III hold and I fails.",
 tips=[r"$\frac{nS_0^2}{\theta}\sim\chi^2_n$, so $V(S_0^2)=\frac{\theta^2}{n^2}\cdot2n=\frac{2\theta^2}{n}$.",
       r"CR bound $=\frac{1}{I_n(\theta)}=\frac{2\theta^2}{n}$ - identical.",
       r"Attaining the bound is exactly what 'MVB estimator' means, so II and III go together."],
 sol=[r"$\dfrac{nS_0^2}{\theta}=\dfrac{\sum(x_i-\mu)^2}{\theta}\sim\chi^2_n$, so $V\left(S_0^2\right)=\dfrac{\theta^2}{n^2}\cdot2n=\dfrac{2\theta^2}{n}$.",
      r"From the previous item $I_n(\theta)=\dfrac{n}{2\theta^2}$, so the Cramer-Rao lower bound is $\dfrac{2\theta^2}{n}$.",
      r"These coincide, so statement II is true and statement I is false; attaining the bound makes $S_0^2$ an MVB estimator, so III is true.",
      r"Option (c)."]),

"2026-Q19": dict(ans=1, qtype="Numerical", diff="Medium",
 sc=r"$t=\frac{r\sqrt{n-2-k}}{\sqrt{1-r^2}}=\frac{0.6\sqrt{16}}{\sqrt{0.64}}=\frac{2.4}{0.8}=3$.",
 tips=[r"For a partial correlation with $k$ eliminated variables, df $=n-2-k$; here $k=1$ so df $=16$.",
       r"$\sqrt{1-0.36}=\sqrt{0.64}=0.8$ - the numbers are designed to be exact.",
       r"$\sqrt{16}=4$, so the statistic is $0.6\times4/0.8=3$."],
 sol=[r"For testing $H_0:\rho_{13\cdot2}=0$ the statistic is $t=\dfrac{r_{13\cdot2}\sqrt{n-2-k}}{\sqrt{1-r_{13\cdot2}^2}}$ with $k=1$ eliminated variable.",
      r"$n-2-k=19-3=16$, so $\sqrt{16}=4$.",
      r"$\sqrt{1-(0.6)^2}=\sqrt{0.64}=0.8$.",
      r"$t=\dfrac{0.6\times4}{0.8}=3$ - option (b)."]),

"2026-Q20": dict(ans=0, qtype="Definition", diff="Easy",
 sc=r"$\lambda=\dfrac{L(\theta_0)}{\max_{\Theta}L(\theta)}$ - the constrained supremum over the unconstrained supremum.",
 tips=[r"$\lambda\in[0,1]$ because the numerator maximises over a subset - options (b) and the reciprocals cannot lie in $[0,1]$.",
       r"Reject for SMALL $\lambda$: the null fits much worse than the best alternative.",
       r"$-2\log\lambda\xrightarrow{d}\chi^2_r$ under regularity."],
 sol=[r"The likelihood ratio criterion is $\lambda=\dfrac{\sup_{\theta\in\Theta_0}L(\theta)}{\sup_{\theta\in\Theta}L(\theta)}$.",
      r"With the simple null $\Theta_0=\{\theta_0\}$ this is $\dfrac{L(\theta_0)}{\max_\Theta L(\theta)}$.",
      r"Option (a)."]),

"2026-Q21": dict(ans=0, qtype="Conceptual", diff="Medium",
 sc=r"With $\mu$ known the family has MLR in $\sum(x_i-\mu)^2$, so UMP tests exist for both one-sided alternatives but NOT for the two-sided one.",
 tips=[r"Karlin-Rubin gives UMP tests only for one-sided alternatives in MLR families.",
       r"Two-sided alternatives need the UMPU restriction - no UMP test exists.",
       r"The one-sided regions are $\sum(x_i-\mu)^2\le c$ (for $\sigma^2<\sigma_0^2$) and $\ge c$ (for $\sigma^2>\sigma_0^2$)."],
 sol=[r"With $\mu$ known, $L$ is a one-parameter exponential family in $T=\sum(x_i-\mu)^2$, with monotone likelihood ratio in $T$.",
      r"By Karlin-Rubin, UMP level-$\alpha$ tests exist for $H_1:\sigma^2>\sigma_0^2$ (reject for large $T$) and for $H_1:\sigma^2<\sigma_0^2$ (reject for small $T$). So I and II admit UMP tests.",
      r"For the two-sided alternative the two one-sided UMP tests differ, so no single test can be uniformly most powerful - III does not.",
      r"Option (a)."]),

"2026-Q22": dict(ans=3, qtype="Conceptual", diff="Medium",
 sc=r"$\bar x$ and $\sum x_i$ are one-to-one transforms of each other and both sufficient; the sample variance $\bar x(1-\bar x)$ is a MANY-to-one function of $\bar x$ and is not sufficient.",
 tips=[r"For Bernoulli data, $\frac1n\sum(x_i-\bar x)^2=\bar x(1-\bar x)$ - a parabola, symmetric about $\bar x=\tfrac12$.",
       r"That symmetry means $\bar x$ and $1-\bar x$ give the same value, so information about $p$ is lost.",
       r"Multiplying a sufficient statistic by a known constant keeps sufficiency; squaring or folding does not."],
 sol=[r"$L=p^{\sum x_i}(1-p)^{n-\sum x_i}$, so $\sum X_i$ is sufficient: statement III holds. $\bar X=\sum X_i/n$ is a one-to-one transform, so statement I holds too.",
      r"For Bernoulli data $x_i^2=x_i$, so $\dfrac1n\sum(x_i-\bar x)^2=\bar x-\bar x^2=\bar x(1-\bar x)$.",
      r"This is not one-to-one in $\bar x$ (the values $\bar x$ and $1-\bar x$ give the same result), so it does not determine $\sum x_i$ and is not sufficient: statement II fails.",
      r"Hence I and III - option (d)."]),

"2026-Q23": dict(ans=3, qtype="Conceptual", diff="Medium",
 sc=r"$T$ is unbiased (IV true) but has variance strictly above $V(T_1)$, so it cannot be MVUE (III true); I and II are false.",
 tips=[r"$V(T)=\frac{V_1+V_2+2\operatorname{Cov}}{4}=\frac{V_1+V_2+2V_1}{4}>V_1$ when $V_2>V_1$.",
       r"$V(T_1/4)=V(T_1)/16$ - statement II confuses scaling a random variable with averaging two.",
       r"Averaging an optimal estimator with a worse one always degrades it."],
 sol=[r"$E(T)=\tfrac12\left[\gamma(\theta)+\gamma(\theta)\right]=\gamma(\theta)$: $T$ is unbiased. Statement IV true.",
      r"With $\operatorname{Cov}(T_1,T_2)=V(T_1)=V_1$ and $V_2=V(T_2)>V_1$: $V(T)=\dfrac{V_1+V_2+2V_1}{4}=\dfrac{3V_1+V_2}{4}>V_1$.",
      r"So $T$ has strictly larger variance than the MVUE and cannot itself be MVUE. Statement III true, statement I false.",
      r"$V\left(T_1/4\right)=V_1/16$, which is not $V(T)$: statement II false.",
      r"Hence III and IV - option (d)."]),

"2026-Q24": dict(ans=0, qtype="Conceptual", diff="Easy",
 sc=r"For $\text{Exp}(\lambda)$ (rate parametrisation), $E(X)=1/\lambda$, so both the MLE and the moment estimator are $1/\bar x$.",
 tips=[r"Rate parametrisation: $f(x)=\lambda e^{-\lambda x}$, mean $1/\lambda$ - so the estimator is the RECIPROCAL of $\bar x$.",
       r"If the parameter were the MEAN instead, both estimators would be $\bar x$ - read the parametrisation.",
       r"MoM and MLE agree here because $\lambda$ is a one-to-one function of the first moment."],
 sol=[r"$\log L=n\log\lambda-\lambda\sum x_i$, so $\dfrac{\partial\log L}{\partial\lambda}=\dfrac n\lambda-\sum x_i=0$ gives $\hat\lambda_{MLE}=\dfrac1{\bar x}$.",
      r"$E(X)=\dfrac1\lambda$, so the moment equation $\dfrac1{\hat\lambda}=\bar x$ gives $\hat\lambda_{MME}=\dfrac1{\bar x}$.",
      r"The two coincide at $1/\bar x$ - statement I is correct, option (a)."]),

"2026-Q25": dict(ans=1, qtype="Conceptual", diff="Hard",
 sc=r"Averaging the two gives an unbiased estimator with variance $\frac{\sigma^2(1+\rho)}{2}$, which cannot beat the minimum variance $e\sigma^2$; hence $\rho\ge2e-1$.",
 tips=[r"Build the average $T^*=\frac{T_1+T_2}{2}$ and compare its variance with the minimum attainable.",
       r"$\frac{\sigma^2(1+\rho)}{2}\ge e\sigma^2\Rightarrow\rho\ge2e-1$ - an inequality, not an equality.",
       r"Equality would require $T^*$ itself to be the MVUE, which is a special case, not the general rule."],
 sol=[r"Let $V_0$ be the minimum attainable variance, so $e=\dfrac{V_0}{\sigma^2}$ for each estimator.",
      r"$T^*=\dfrac{T_1+T_2}{2}$ is unbiased with $V(T^*)=\dfrac{\sigma^2+\sigma^2+2\rho\sigma^2}{4}=\dfrac{\sigma^2(1+\rho)}{2}$.",
      r"No unbiased estimator can have variance below $V_0=e\sigma^2$, so $\dfrac{\sigma^2(1+\rho)}{2}\ge e\sigma^2$.",
      r"Hence $\rho\ge2e-1$ - option (b)."]),
}
