# -*- coding: utf-8 -*-
# Statistical Inference answer pack 16 (2026 Q26-Q50) - completes SI
A = {
"2026-Q26": dict(ans=2, qtype="Definition", diff="Easy",
 sc=r"Consistency IS convergence in probability to the target: $T_n\xrightarrow{P}\gamma(\theta)$.",
 tips=[r"Unbiasedness plus vanishing variance is sufficient but not necessary - it is not the definition.",
       r"$V(T_n)\le1$ is a bound that does not shrink with $n$ and implies nothing.",
       r"Statement IV converges the wrong quantity ($T_n/n$), so it describes a different estimator entirely."],
 sol=[r"By definition, $T_n$ is (weakly) consistent for $\gamma(\theta)$ iff $T_n\xrightarrow{P}\gamma(\theta)$.",
      r"I alone is not enough ($X_1$ is unbiased but inconsistent); II gives a bound that never shrinks; IV is a statement about $T_n/n$.",
      r"Hence III only - option (c)."]),

"2026-Q27": dict(ans=1, qtype="Numerical", diff="Easy",
 sc=r"$I_1(P)=\frac{1}{P(1-P)}=\frac{1}{P-P^2}$.",
 tips=[r"Information is the RECIPROCAL of the variance here: $V(X)=P(1-P)$ and $I_1=1/[P(1-P)]$.",
       r"$P(1-P)=P-P^2$, which is how option (b) is written.",
       r"Option (d) is $1/(1-P)^2$ - a different quantity."],
 sol=[r"$\log f=x\log P+(1-x)\log(1-P)$, so $\dfrac{\partial\log f}{\partial P}=\dfrac{x}{P}-\dfrac{1-x}{1-P}=\dfrac{x-P}{P(1-P)}$.",
      r"$I_1(P)=E\left[\left(\dfrac{X-P}{P(1-P)}\right)^2\right]=\dfrac{V(X)}{P^2(1-P)^2}=\dfrac{P(1-P)}{P^2(1-P)^2}$.",
      r"$=\dfrac1{P(1-P)}=\dfrac1{P-P^2}$ - option (b)."]),

"2026-Q28": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"$L=\theta^n\left(\prod x_i\right)^{\theta-1}$, so $\prod X_i$ is sufficient.",
 tips=[r"This family has now appeared in six different papers - recognise it instantly.",
       r"Equivalently $\sum\log x_i$ works, being a one-to-one transform.",
       r"Options involving $\sum x_i$ never appear in this likelihood."],
 sol=[r"$L(\theta)=\theta^n\left(\prod_{i=1}^nx_i\right)^{\theta-1}$.",
      r"Factorising with $t=\prod x_i$ and $h(x)=1$ gives sufficiency by Fisher-Neyman.",
      r"Option (c)."]),

"2026-Q29": dict(ans=1, qtype="Definition", diff="Easy",
 sc=r"The most powerful test maximises POWER subject to a fixed size.",
 tips=[r"Size is CONSTRAINED at $\alpha$; power is the objective being maximised.",
       r"Equivalently it minimises the Type-II error probability $\beta$.",
       r"Neyman-Pearson solves exactly this constrained optimisation."],
 sol=[r"Among all tests of size at most $\alpha$, the most powerful test is the one with the largest probability of rejecting $H_0$ when $H_1$ is true.",
      r"That probability is the power, $1-\beta$.",
      r"Option (b)."]),

"2026-Q30": dict(ans=0, qtype="Conceptual", diff="Easy",
 sc=r"$\frac{L_1}{L_0}=\left(\frac{0.6}{0.4}\right)^{\sum x_i}\left(\frac{0.4}{0.6}\right)^{n-\sum x_i}$, a monotone function of $\sum x_i$.",
 tips=[r"The Bernoulli likelihood ratio always reduces to the total number of successes.",
       r"$p_1>p_0$ makes the ratio increasing, so the MP test rejects for LARGE $\sum x_i$.",
       r"$\sum x_i^2=\sum x_i$ for 0-1 data, so option (d) is the same statistic written obscurely - but $\sum x_i$ is the canonical answer."],
 sol=[r"$\dfrac{L_1}{L_0}=\dfrac{0.6^{\sum x_i}0.4^{n-\sum x_i}}{0.4^{\sum x_i}0.6^{n-\sum x_i}}=\left(\dfrac{0.6}{0.4}\right)^{2\sum x_i-n}$, increasing in $\sum x_i$.",
      r"By Neyman-Pearson the most powerful test rejects when this ratio exceeds $k$, i.e. when $\sum x_i$ is large.",
      r"So the test depends on the data only through $\sum_{i=1}^nx_i$ - option (a)."]),

"2026-Q31": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"Wald's exact inequalities: $A\le\frac{1-\beta}{\alpha}$ and $B\ge\frac{\beta}{1-\alpha}$. Both hold.",
 tips=[r"Opposite directions: upper boundary bounded ABOVE, lower boundary bounded BELOW.",
       r"Derived by integrating the likelihood ratio over the rejection and acceptance events respectively.",
       r"Using equalities as approximations makes the realised $\alpha,\beta$ no larger than nominal."],
 sol=[r"On the rejection event the terminal ratio is at least $A$, giving $1-\beta\ge A\alpha$, i.e. $A\le\dfrac{1-\beta}{\alpha}$. Statement I true.",
      r"On the acceptance event the ratio is at most $B$, giving $\beta\le B(1-\alpha)$, i.e. $B\ge\dfrac{\beta}{1-\alpha}$. Statement II true.",
      r"Option (c)."]),

"2026-Q32": dict(ans=3, qtype="Conceptual", diff="Easy",
 sc=r"Under regularity the MLE is asymptotically unbiased, asymptotically normal and asymptotically efficient - all three.",
 tips=[r"$\sqrt n\left(\hat\theta-\theta\right)\xrightarrow{d}N\left(0,\frac1{I_1(\theta)}\right)$ packages all three properties.",
       r"Asymptotic efficiency means the limiting variance attains the Cramer-Rao bound.",
       r"None of these properties is guaranteed in finite samples."],
 sol=[r"Under standard regularity conditions the MLE satisfies $\hat\theta\xrightarrow{P}\theta$ (hence asymptotically unbiased),",
      r"$\sqrt n\left(\hat\theta-\theta\right)\xrightarrow{d}N\left(0,\dfrac1{I_1(\theta)}\right)$ (asymptotically normal),",
      r"and the limiting variance $\dfrac1{nI_1(\theta)}$ equals the Cramer-Rao bound (asymptotically efficient).",
      r"All three hold - option (d)."]),

"2026-Q33": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"Statement I is the MSE decomposition $\text{MSE}=\text{variance}+\text{bias}^2$; statement II is the definition of bias. Both correct.",
 tips=[r"Add and subtract $E_\theta(T)$ inside the square; the cross term vanishes.",
       r"This identity is the single most used tool for comparing estimators.",
       r"Bias is $E(T)-\gamma(\theta)$ - estimator minus target, in that order."],
 sol=[r"Statement I: $E\left[T-\gamma\right]^2=E\left[\left(T-E(T)\right)+\left(E(T)-\gamma\right)\right]^2$. Expanding, the cross term is $2\left(E(T)-\gamma\right)E\left[T-E(T)\right]=0$.",
      r"So $\text{MSE}=V(T)+\left[E(T)-\gamma(\theta)\right]^2$. True.",
      r"Statement II: bias is defined as $b(\theta)=E_\theta(T)-\gamma(\theta)$. True.",
      r"Option (c)."]),

"2026-Q34": dict(ans=1, qtype="Conceptual", diff="Easy",
 sc=r"$\prod x_i$ is sufficient (I true), $\sum x_i$ is not (II false), and the MLE is $-n/\sum\log x_i$ (III true).",
 tips=[r"The likelihood involves $\left(\prod x_i\right)^{\theta-1}$ - the product, never the sum.",
       r"$\log L=n\log\theta+(\theta-1)\sum\log x_i$, so $\hat\theta=-n/\sum\log x_i$.",
       r"$\sum\log x_i<0$, so the MLE is positive as required."],
 sol=[r"$L(\theta)=\theta^n\left(\prod x_i\right)^{\theta-1}$, so $\prod X_i$ is sufficient: I true, and $\sum X_i$ plays no role: II false.",
      r"$\log L=n\log\theta+(\theta-1)\sum\log x_i$, and $\dfrac{\partial\log L}{\partial\theta}=\dfrac n\theta+\sum\log x_i=0$ gives $\hat\theta=-\dfrac{n}{\sum\log x_i}$: III true.",
      r"Hence I and III only - option (b)."]),

"2026-Q35": dict(ans=2, qtype="Conceptual", diff="Medium",
 sc=r"Rao-Blackwell gives unbiasedness (I) and $V(\Phi)\le V(V)$ (II); but $\Phi(t)$ is FREE of $\theta$, so III is false.",
 tips=[r"Sufficiency is exactly what makes $E[V\mid T=t]$ free of $\theta$ - that is the whole point.",
       r"Statement II is correctly phrased with '$\le$' (equality when $V$ is already a function of $T$).",
       r"A statistic that depended on $\theta$ would not be a statistic at all."],
 sol=[r"I: $E\left[\Phi(T)\right]=E\left[E(V\mid T)\right]=E(V)=\gamma(\theta)$ - unbiased. True.",
      r"II: $V(V)=E\left[V(V\mid T)\right]+V\left[\Phi(T)\right]$, so $V\left[\Phi(T)\right]\le V(V)$. True (with equality iff $V$ is already a function of $T$).",
      r"III: because $T$ is sufficient, the conditional distribution of the sample given $T$ is free of $\theta$, so $\Phi(t)$ does NOT depend on $\theta$. False.",
      r"Hence I and II only - option (c)."]),

"2026-Q36": dict(ans=3, qtype="Conceptual", diff="Easy",
 sc=r"The likelihood is maximised by $\left(\bar x,\ \frac1n\sum(x_i-\bar x)^2\right)$ (I true, II false); and unbiasedness belongs to the $n-1$ divisor (IV true, III false).",
 tips=[r"The MLE and the unbiased estimator of $\sigma^2$ are different estimators - the question tests exactly that split.",
       r"Maximising $\Rightarrow$ divisor $n$; unbiased $\Rightarrow$ divisor $n-1$.",
       r"$E\left[\frac1n\sum(x_i-\bar x)^2\right]=\frac{n-1}{n}\sigma^2<\sigma^2$."],
 sol=[r"Maximising $\log L$ gives $\hat\mu=\bar x$ and $\hat\sigma^2=\dfrac1n\sum(x_i-\bar x)^2$: statement I true, statement II false.",
      r"$E\left[\sum(x_i-\bar x)^2\right]=(n-1)\sigma^2$, so $\hat\sigma^2$ is biased (III false) while $S^2$ with divisor $n-1$ is unbiased (IV true).",
      r"Hence I and IV - option (d)."]),

"2026-Q37": dict(ans=0, qtype="Conceptual", diff="Easy",
 sc=r"$e_\theta(T_1/T_2)=\frac{V(T_2)}{V(T_1)}>1$ means $V(T_1)<V(T_2)$, i.e. $T_1$ is more efficient.",
 tips=[r"Read the ratio as written: the SECOND estimator's variance sits in the numerator.",
       r"Ratio $>1$ $\Rightarrow$ the numerator's estimator is worse $\Rightarrow$ the denominator's estimator wins.",
       r"Option (d) reverses the conclusion for the reciprocal ratio."],
 sol=[r"$e_\theta\left(\dfrac{T_1}{T_2}\right)=\dfrac{\text{Var}(T_2)}{\text{Var}(T_1)}$.",
      r"If this exceeds 1 then $\text{Var}(T_2)>\text{Var}(T_1)$.",
      r"A smaller variance means greater efficiency, so $T_1$ is more efficient than $T_2$.",
      r"Option (a)."]),

"2026-Q38": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"$L=(\beta-\alpha)^{-n}$ is maximised by making the interval as short as the data allow: $\hat\alpha=x_{(1)}$, $\hat\beta=x_{(n)}$.",
 tips=[r"Support-dependent parameters: never differentiate, push to the boundary.",
       r"Note the ORDER statistics, not the first and last observations - option (d) is the typographic trap.",
       r"Both MLEs are biased inward; the UMVUEs extrapolate outward."],
 sol=[r"$L(\alpha,\beta)=\dfrac1{(\beta-\alpha)^n}$ provided $\alpha\le X_{(1)}$ and $\beta\ge X_{(n)}$.",
      r"The likelihood increases as $\beta-\alpha$ decreases, so the maximum occurs at the tightest admissible interval.",
      r"$\hat\alpha=x_{(1)}$ and $\hat\beta=x_{(n)}$ - option (c)."]),

"2026-Q39": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"Bootstrap, jackknife and cross-validation are resampling methods; stratified sampling is a SAMPLING DESIGN, not a resampling method. So three.",
 tips=[r"Resampling reuses an existing sample; stratified sampling collects a new one from the population.",
       r"Bootstrap: with replacement. Jackknife: delete-one. Cross-validation: repeated train/test splits.",
       r"The distinguishing question is: does the method draw from data already in hand, or from the population?"],
 sol=[r"Bootstrap draws repeated samples WITH replacement from the observed data - resampling.",
      r"Jackknife systematically deletes one (or $d$) observations at a time - resampling.",
      r"Cross-validation repeatedly partitions the observed data into training and validation parts - resampling.",
      r"Stratified sampling is a design for drawing the ORIGINAL sample from the population - not resampling.",
      r"Three of the four - option (c)."]),

"2026-Q40": dict(ans=0, qtype="Conceptual", diff="Easy",
 sc=r"Unbiasedness means the rejection probability under $H_1$ is at least that under $H_0$: $P(x\in W\mid H_1)\ge P(x\in W\mid H_0)$.",
 tips=[r"Left side is the power; right side is the size.",
       r"Equivalently $1-\beta\ge\alpha$, i.e. $\alpha+\beta\le1$.",
       r"Option (b) reverses the inequality, describing a WORSE-than-useless test."],
 sol=[r"Power $=P(x\in W\mid H_1)$ and size $=P(x\in W\mid H_0)$.",
      r"A test is unbiased if its power is at least its size throughout the alternative.",
      r"$P(x\in W\mid H_1)\ge P(x\in W\mid H_0)$ - option (a)."]),

"2026-Q41": dict(ans=1, qtype="Conceptual", diff="Easy",
 sc=r"$L=N^{-n}I\left(X_{(n)}\le N\right)$ - the maximum is sufficient for $N$.",
 tips=[r"The only information $N$ carries about the data is the upper limit, so the maximum is what matters.",
       r"The minimum is irrelevant because the lower limit $1$ is known.",
       r"$X_{(n)}$ is also complete here, so UMVUEs are functions of it."],
 sol=[r"$P(x_1,\dots,x_n)=\dfrac1{N^n}$ provided every $x_i\in\{1,\dots,N\}$, i.e. $X_{(n)}\le N$.",
      r"So $L=N^{-n}I\left(X_{(n)}\le N\right)$, a function of the data only through $X_{(n)}$.",
      r"Hence $x_{(n)}$ is sufficient - option (b)."]),

"2026-Q42": dict(ans=1, qtype="Conceptual", diff="Easy",
 sc=r"With the RATE parametrisation $E(X)=1/\theta$, so the moment estimator is $1/\bar x$, not $\bar x$ - statement I fails; both methods do give the same $1/\bar x$, so statement II holds.",
 tips=[r"$f(x)=\theta e^{-\theta x}$ means $\theta$ is the RATE, and the mean is its reciprocal.",
       r"Compare the 2025 version where the density was $\frac1\theta e^{-x/\theta}$ (mean parametrisation) and the answer was $\bar x$.",
       r"MoM and MLE agree for both parametrisations - only the formula changes."],
 sol=[r"$E(X)=\dfrac1\theta$, so the moment equation $\dfrac1{\hat\theta}=\bar x$ gives $\hat\theta=\dfrac1{\bar x}$, not $\bar x$: statement I false.",
      r"$\log L=n\log\theta-\theta\sum x_i$ gives $\hat\theta_{MLE}=\dfrac{n}{\sum x_i}=\dfrac1{\bar x}$, the same value: statement II true.",
      r"Hence II only - option (b)."]),

"2026-Q43": dict(ans=1, qtype="Numerical", diff="Medium",
 sc=r"$E\left[T(n-T)\right]=n(n-1)p(1-p)$, so $\frac{T(n-T)}{n(n-1)}$ is the UMVUE of $p(1-p)$.",
 tips=[r"$E(T)=np$ and $E[T(T-1)]=n(n-1)p^2$; combine to get $E[T(n-T)]=n(n-1)p(1-p)$.",
       r"$T(n-T)$ counts ordered success-failure pairs - a combinatorial way to see the answer.",
       r"$\frac Tn\left(1-\frac Tn\right)$ is the MLE and is biased downward."],
 sol=[r"$T\sim B(n,p)$ with $E(T)=np$ and $E\left[T(T-1)\right]=n(n-1)p^2$, so $E(T^2)=n(n-1)p^2+np$.",
      r"$E\left[T(n-T)\right]=nE(T)-E(T^2)=n^2p-n(n-1)p^2-np=n(n-1)p-n(n-1)p^2=n(n-1)p(1-p)$.",
      r"Hence $E\left[\dfrac{T(n-T)}{n(n-1)}\right]=p(1-p)$, and since $T$ is complete sufficient this is the UMVUE.",
      r"Option (b)."]),

"2026-Q44": dict(ans=0, qtype="Conceptual", diff="Medium",
 sc=r"The MLE is always a function of the sufficient statistic (I true); but by invariance the MLE of $\frac{\theta}{1-\theta}$ is $\frac{\hat\theta}{1-\hat\theta}$, not $\frac{\hat\theta}{1+\hat\theta}$ (II false).",
 tips=[r"Invariance: apply the SAME function $g$ to $\hat\theta$ - do not alter its algebraic form.",
       r"$g(\theta)=\frac{\theta}{1-\theta}\Rightarrow g\left(\hat\theta\right)=\frac{\hat\theta}{1-\hat\theta}$; the printed statement flips the sign in the denominator.",
       r"By factorisation the likelihood depends on the data only through $t$, so its maximiser must be a function of $t$."],
 sol=[r"Statement I: if $f(x;\theta)=g_\theta(t)h(x)$ then maximising $L$ over $\theta$ involves only $g_\theta(t)$, so $\hat\theta$ is a function of $t$. True.",
      r"Statement II: the invariance property gives $\widehat{g(\theta)}=g\left(\hat\theta\right)$. With $g(\theta)=\dfrac{\theta}{1-\theta}$ this is $\dfrac{\hat\theta}{1-\hat\theta}$.",
      r"The stated $\dfrac{\hat\theta}{1+\hat\theta}$ has the wrong sign in the denominator. False.",
      r"Hence I only - option (a)."]),

"2026-Q45": dict(ans=3, qtype="Conceptual", diff="Medium",
 sc=r"For the Cauchy location family the score cannot be written as $A(\theta)[T-\theta]$, so no MVB estimator exists.",
 tips=[r"$\bar X$ for a Cauchy sample is again Cauchy - its variance does not even exist.",
       r"There is no non-trivial sufficient statistic, so no candidate for an MVB estimator.",
       r"The Cauchy IS regular ($I_1=\tfrac12$), so the bound exists; what fails is attainment."],
 sol=[r"$\dfrac{\partial\log L}{\partial\theta}=\sum_{i=1}^n\dfrac{2(x_i-\theta)}{1+(x_i-\theta)^2}$, a non-linear function of the observations.",
      r"This cannot be expressed in the form $A(\theta)\left[T(x)-\theta\right]$ required for the Cramer-Rao bound to be attained.",
      r"Moreover $\bar X$ has the same Cauchy distribution as $X_1$, with no finite variance.",
      r"Hence the minimum variance bound estimator does not exist - option (d)."]),

"2026-Q46": dict(ans=1, qtype="Numerical", diff="Easy",
 sc=r"With unit scale, Gamma$(\alpha)$ has $E(X)=\alpha$, so the moment estimator is $\hat\alpha=\bar x$.",
 tips=[r"The density $\frac{1}{\Gamma(\alpha)}x^{\alpha-1}e^{-x}$ has scale fixed at 1, so mean $=$ shape.",
       r"Equating $E(X)=\alpha$ to $\bar x$ gives the estimator immediately - no second moment needed.",
       r"With a free scale parameter you would need both moments (as in the 2024 items)."],
 sol=[r"For $f(x;\alpha)=\dfrac1{\Gamma(\alpha)}x^{\alpha-1}e^{-x}$, the scale is 1 and $E(X)=\alpha$.",
      r"Method of moments: $\hat\alpha=\bar x$.",
      r"Option (b)."]),

"2026-Q47": dict(ans=1, qtype="Conceptual", diff="Easy",
 sc=r"In an SPRT the sample size $n$ is a RANDOM VARIABLE while $\alpha$ and $\beta$ are fixed in advance.",
 tips=[r"This is the defining inversion versus fixed-sample testing, where $n$ is fixed and the error rates follow.",
       r"$E(N)$ is the average sample number (ASN), typically smaller than the fixed-sample size for the same $(\alpha,\beta)$.",
       r"$P[N<\infty]=1$, so the random $n$ is finite with probability one."],
 sol=[r"The SPRT continues sampling until the likelihood ratio leaves the band $(B,A)$, so the stopping time $N$ is determined by the data - a random variable.",
      r"The strength $(\alpha,\beta)$ is specified in advance and fixes the boundaries $A$ and $B$.",
      r"Hence $n$ is random while $\alpha$ and $\beta$ are fixed constants - option (b)."]),

"2026-Q48": dict(ans=1, qtype="Numerical", diff="Hard",
 sc=r"$\sum Z_i=(\theta_1-\theta_0)\sum x_i-\frac{m(\theta_1^2-\theta_0^2)}{2}\ge\log A$ rearranges to $\sum x_i\ge\frac{\log\frac{1-\beta}{\alpha}}{\theta_1-\theta_0}+\frac{m(\theta_0+\theta_1)}{2}$.",
 tips=[r"$\frac{\theta_1^2-\theta_0^2}{2(\theta_1-\theta_0)}=\frac{\theta_0+\theta_1}{2}$ - the SUM appears, with a PLUS sign.",
       r"Rejection uses the upper boundary $A=\frac{1-\beta}{\alpha}$; acceptance uses $B=\frac{\beta}{1-\alpha}$.",
       r"Since $\theta_1>\theta_0$, dividing preserves the inequality, so rejection is an upper tail in $\sum x_i$."],
 sol=[r"$Z_i=\log\dfrac{f(x_i;\theta_1)}{f(x_i;\theta_0)}=(\theta_1-\theta_0)x_i-\dfrac{\theta_1^2-\theta_0^2}{2}$.",
      r"Reject $H_0$ when $\sum_{i=1}^mZ_i\ge\log A$ with $A=\dfrac{1-\beta}{\alpha}$:",
      r"$(\theta_1-\theta_0)\sum x_i-\dfrac{m(\theta_1^2-\theta_0^2)}{2}\ge\log\dfrac{1-\beta}{\alpha}$.",
      r"Dividing by $\theta_1-\theta_0>0$: $\sum x_i\ge\dfrac{1}{\theta_1-\theta_0}\log\left(\dfrac{1-\beta}{\alpha}\right)+\dfrac{m(\theta_0+\theta_1)}{2}$.",
      r"Option (b)."]),

"2026-Q49": dict(ans=1, qtype="Conceptual", diff="Easy",
 sc=r"Neyman-Pearson covers SIMPLE versus SIMPLE, not simple versus composite (I false); the LRT is the general-purpose method for any combination (II true).",
 tips=[r"NP needs both likelihoods to be fully specified numbers - a composite alternative breaks that.",
       r"Extension to composite one-sided alternatives requires MLR (Karlin-Rubin), not NP itself.",
       r"The LRT handles composite nulls and alternatives by taking suprema over each region."],
 sol=[r"Statement I: the Neyman-Pearson lemma applies to $H_0:\theta=\theta_0$ against $H_1:\theta=\theta_1$, both simple. As stated (composite alternative) it is false.",
      r"Statement II: $\lambda=\dfrac{\sup_{\Theta_0}L}{\sup_{\Theta}L}$ is defined whether $\Theta_0$ and $\Theta_1$ are single points or whole sets, so the LRT is a general method. True.",
      r"Hence II only - option (b)."]),

"2026-Q50": dict(ans=0, qtype="Numerical", diff="Easy",
 sc=r"The $t$-interval is symmetric about $\bar x$, so $\bar x=\frac{360+400}{2}=380$.",
 tips=[r"Symmetry holds whether the multiplier is $z$ or $t$ - the midpoint is always the sample mean.",
       r"Half-width $=20$, so $t_{n-1,0.05}\,s/\sqrt n=20$ if needed.",
       r"The confidence level (90% here) does not affect the midpoint."],
 sol=[r"With $\sigma^2$ unknown the interval is $\bar x\pm t_{n-1,\alpha/2}\dfrac{s}{\sqrt n}$, symmetric about $\bar x$.",
      r"$\bar x=\dfrac{360+400}{2}=380$.",
      r"Option (a)."]),
}
