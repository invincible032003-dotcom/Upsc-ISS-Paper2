# -*- coding: utf-8 -*-
# Statistical Inference answer pack 1 (2018 Q16-Q41)
A = {
"2018-Q16": dict(ans=0, qtype="Numerical", diff="Medium",
 sc=r"$\log L=\log(\alpha-x)-2\log\alpha$ gives $\frac1{\alpha-x}=\frac2\alpha\Rightarrow\hat\alpha=2x$. But $E(X)=\alpha/3$, so $E(2X)=2\alpha/3\ne\alpha$: MLE yes, unbiased no.",
 tips=[r"For a triangular density on $(0,\alpha)$, $E(X)=\alpha/3$ - integrate $\frac{2}{\alpha^2}\int_0^\alpha x(\alpha-x)dx$ once and remember it.",
       r"Being the MLE never implies unbiasedness; invariance transformations routinely destroy it.",
       r"Here $3X$ is the unbiased estimator while $2X$ is the MLE - a neat illustration that the two criteria disagree."],
 sol=[r"With $n=1$, $L(\alpha)=\dfrac2{\alpha^2}(\alpha-x)$ for $\alpha>x$, so $\log L=\log2-2\log\alpha+\log(\alpha-x)$.",
      r"$\dfrac{d\log L}{d\alpha}=-\dfrac2\alpha+\dfrac1{\alpha-x}=0\Rightarrow\alpha=2(\alpha-x)\Rightarrow\hat\alpha=2x$. Statement 1 is true.",
      r"$E(X)=\displaystyle\int_0^\alpha x\cdot\frac{2}{\alpha^2}(\alpha-x)\,dx=\frac2{\alpha^2}\left[\frac{\alpha^3}{2}-\frac{\alpha^3}{3}\right]=\frac{\alpha}{3}$.",
      r"Hence $E(2X)=\dfrac{2\alpha}3\ne\alpha$: $2X$ is biased. Statement 2 is false.",
      r"Only statement 1 holds - option (a)."]),

"2018-Q17": dict(ans=1, qtype="Conceptual", diff="Medium",
 sc=r"$E\left[S_n(S_n-1)(S_n-2)\right]=n(n-1)(n-2)p^3$ for $S_n\sim B(n,p)$ - the third factorial moment. Divide by $n(n-1)(n-2)$.",
 tips=[r"Factorial moments of the binomial: $E[S_n(S_n-1)\cdots(S_n-k+1)]=n(n-1)\cdots(n-k+1)p^k$.",
       r"The UMVUE of $p^k$ is therefore $\dfrac{S_n(S_n-1)\cdots(S_n-k+1)}{n(n-1)\cdots(n-k+1)}$ - a formula worth memorising for $k=2$ and $k=3$.",
       r"$(S_n/n)^3$ is the MLE but is biased upwards - option (a) is the standard trap."],
 sol=[r"$S_n\sim B(n,p)$, and the $k$-th factorial moment is $E[S_n(S_n-1)\cdots(S_n-k+1)]=n(n-1)\cdots(n-k+1)p^k$.",
      r"For $k=3$: $E[S_n(S_n-1)(S_n-2)]=n(n-1)(n-2)p^3$.",
      r"Therefore $T=\dfrac{S_n(S_n-1)(S_n-2)}{n(n-1)(n-2)}$ satisfies $E(T)=p^3$.",
      r"Since $S_n$ is complete sufficient, $T$ is in fact the UMVUE. Option (b)."]),

"2018-Q18": dict(ans=1, qtype="Numerical", diff="Easy",
 sc=r"$E(X_{i+1}-X_i)^2=V=2\sigma^2$, so $E(T)=\frac{k}{n-1}(n-1)2\sigma^2=2k\sigma^2$; unbiasedness forces $k=\tfrac12$.",
 tips=[r"Successive differences of iid variables have variance $2\sigma^2$ and mean $0$, so each squared difference has expectation $2\sigma^2$.",
       r"This estimator (von Neumann's mean square successive difference) is the basis of the Durbin-Watson idea.",
       r"Count the terms: the sum has exactly $n-1$ differences, which cancels the $n-1$ divisor."],
 sol=[r"$X_{i+1}-X_i$ has mean $0$ and variance $\sigma^2+\sigma^2=2\sigma^2$ by independence.",
      r"Hence $E\left[(X_{i+1}-X_i)^2\right]=2\sigma^2$ for each of the $n-1$ terms.",
      r"$E(T)=\dfrac{k}{n-1}\cdot(n-1)\cdot2\sigma^2=2k\sigma^2$.",
      r"Setting $2k=1$ gives $k=\tfrac12$ - option (b)."]),

"2018-Q19": dict(ans=0, qtype="Numerical", diff="Medium",
 sc=r"$X_{(1)}\sim$ exponential with mean $\frac1{n\theta}$, so $Z=nX_{(1)}$ has mean $\frac1\theta$ (unbiased) but variance $\frac1{\theta^2}$, which does not vanish - not consistent.",
 tips=[r"The minimum of $n$ iid exponentials with rate $\theta$ is exponential with rate $n\theta$ - the single most useful order-statistic fact.",
       r"Unbiasedness alone never gives consistency: you also need $V(T_n)\to0$.",
       r"$Z$ has the same distribution for every $n$, so it cannot converge to a constant."],
 sol=[r"For exponential with mean $1/\theta$, $P(X_{(1)}>t)=e^{-n\theta t}$, so $X_{(1)}\sim\text{Exp}(n\theta)$ with mean $\frac1{n\theta}$ and variance $\frac1{n^2\theta^2}$.",
      r"$Z=nX_{(1)}$ therefore has $E(Z)=\frac1\theta$: statement 1 is true.",
      r"$V(Z)=n^2\cdot\frac1{n^2\theta^2}=\frac1{\theta^2}$, which is free of $n$ and does not tend to $0$.",
      r"In fact $Z\sim\text{Exp}(\theta)$ for every $n$, so $Z$ does not converge in probability to $1/\theta$: statement 2 is false.",
      r"Only 1 holds - option (a)."]),

"2018-Q20": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"The efficiency-correlation identity: if $t_1$ is most efficient and $t_2$ has efficiency $e$, then $\rho=\sqrt e$.",
 tips=[r"Proof sketch: $V(t_2-t_1)\ge0$ and $\operatorname{Cov}(t_1,t_2)=V(t_1)$ give $\rho=\sqrt{V(t_1)/V(t_2)}=\sqrt e$.",
       r"Consequence: $\operatorname{Cov}(t_1,t_2-t_1)=0$ - the most efficient estimator is uncorrelated with the 'error' of any other unbiased estimator.",
       r"This single identity answers the same question in 2019, 2020, 2022, 2023 and 2024 papers."],
 sol=[r"Efficiency of $t_2$ relative to the most efficient $t_1$ is $e=\dfrac{V(t_1)}{V(t_2)}$.",
      r"Since $t_1$ is minimum variance, $\operatorname{Cov}(t_1,t_2)=V(t_1)$ (otherwise a combination would beat $t_1$).",
      r"Hence $\rho=\dfrac{\operatorname{Cov}(t_1,t_2)}{\sqrt{V(t_1)V(t_2)}}=\dfrac{V(t_1)}{\sqrt{V(t_1)V(t_2)}}=\sqrt{\dfrac{V(t_1)}{V(t_2)}}=\sqrt e$.",
      r"Option (c)."]),

"2018-Q21": dict(ans=2, qtype="Numerical", diff="Medium",
 sc=r"For Poisson, $E[X(X-1)]=\lambda^2$, so $\frac1n\sum x_i(x_i-1)$ is unbiased for $\lambda^2$.",
 tips=[r"Poisson factorial moments: $E[X(X-1)\cdots(X-k+1)]=\lambda^k$ - the cleanest family in all of estimation theory.",
       r"$E(X^2)=\lambda+\lambda^2$, so $\frac1n\sum x_i^2$ is unbiased for $\lambda+\lambda^2$, not $\lambda^2$ - option (b) is the trap.",
       r"$E[X(X+1)]=\lambda^2+2\lambda$, killing option (d)."],
 sol=[r"For $X\sim\text{Poisson}(\lambda)$: $E(X)=\lambda$ and $E(X^2)=\lambda+\lambda^2$.",
      r"Hence $E[X(X-1)]=E(X^2)-E(X)=(\lambda+\lambda^2)-\lambda=\lambda^2$.",
      r"Averaging over the sample, $E\left[\frac1n\sum x_i(x_i-1)\right]=\lambda^2$.",
      r"Option (c)."]),

"2018-Q22": dict(ans=3, qtype="Conceptual", diff="Medium",
 sc=r"Sufficiency needs the likelihood to depend on the data only through the statistic. $X_1-X_2+X_3$ loses the total when signs cancel, so it is not sufficient.",
 tips=[r"For Bernoulli the likelihood is $p^{\sum x_i}(1-p)^{n-\sum x_i}$: anything from which $\sum x_i$ can be recovered is sufficient.",
       r"Options (b) and (c) are vectors from which $\sum x_i$ is recoverable by addition - hence sufficient (though not minimal).",
       r"$X_1-X_2+X_3=1$ arises both from $(1,0,0)$ and from $(1,1,1)$, which have different totals - so it cannot be sufficient."],
 sol=[r"The likelihood is $L=p^{t}(1-p)^{3-t}$ with $t=x_1+x_2+x_3$, so any statistic determining $t$ is sufficient.",
      r"(a) $X_1+X_2+X_3=t$ directly. (b) and (c) are two-component vectors whose components sum to $t$. All three are sufficient.",
      r"(d) $X_1-X_2+X_3=1$ occurs for $(1,0,0)$ (with $t=1$) and for $(1,1,1)$ (with $t=3$); the conditional distribution of the sample given this value still depends on $p$.",
      r"Hence $X_1-X_2+X_3$ is not sufficient - option (d)."]),

"2018-Q23": dict(ans=3, qtype="Numerical", diff="Hard",
 sc=r"$\frac{d}{d\theta}\log L=-\frac4\theta+\frac1{\theta-x_1}+\frac1{\theta-x_2}=0$ reduces to $2\theta^2-3(x_1+x_2)\theta+4x_1x_2=0$; the admissible root is $\frac{3(x_1+x_2)+\sqrt{9(x_1-x_2)^2+4x_1x_2}}{4}$.",
 tips=[r"Take logs before differentiating: $\log L=\text{const}-4\log\theta+\log(\theta-x_1)+\log(\theta-x_2)$.",
       r"Rewrite the discriminant using $9(x_1+x_2)^2-32x_1x_2=9(x_1-x_2)^2+4x_1x_2$ - that identity is what matches the printed options.",
       r"Only the $+$ root can exceed $\max(x_1,x_2)$, which the support $0<x<\theta$ demands."],
 sol=[r"$L(\theta)=\dfrac4{\theta^4}(\theta-x_1)(\theta-x_2)$ for $\theta>\max(x_1,x_2)$.",
      r"$\dfrac{d\log L}{d\theta}=-\dfrac4\theta+\dfrac1{\theta-x_1}+\dfrac1{\theta-x_2}=0$. Multiplying through by $\theta(\theta-x_1)(\theta-x_2)$ gives $-4(\theta-x_1)(\theta-x_2)+\theta(\theta-x_2)+\theta(\theta-x_1)=0$.",
      r"Expanding: $-2\theta^2+3(x_1+x_2)\theta-4x_1x_2=0$, i.e. $2\theta^2-3(x_1+x_2)\theta+4x_1x_2=0$.",
      r"$\theta=\dfrac{3(x_1+x_2)\pm\sqrt{9(x_1+x_2)^2-32x_1x_2}}{4}$, and $9(x_1+x_2)^2-32x_1x_2=9(x_1-x_2)^2+4x_1x_2$.",
      r"Taking the $+$ root (the only one exceeding $\max(x_1,x_2)$) gives option (d)."]),

"2018-Q24": dict(ans=3, qtype="Conceptual", diff="Easy",
 sc=r"$P(|T-\theta|>\varepsilon)\le V(T)/\varepsilon^2\to0$ - this is Chebyshev's (Tchebychev's) inequality.",
 tips=[r"Unbiasedness plus vanishing variance is a *sufficient* (not necessary) condition for consistency - the route is always Chebyshev.",
       r"Rao-Blackwell is about variance reduction; Cramer-Rao gives a lower bound; the CLT gives a limiting distribution, not convergence to a constant.",
       r"If $T$ is only asymptotically unbiased, the same argument works with the bias added inside the bound."],
 sol=[r"Chebyshev's inequality states $P(|T-E(T)|\ge\varepsilon)\le\dfrac{V(T)}{\varepsilon^2}$.",
      r"Since $T$ is unbiased, $E(T)=\theta$, so $P(|T-\theta|\ge\varepsilon)\le\dfrac{V(T)}{\varepsilon^2}$.",
      r"As $n\to\infty$, $V(T)\to0$, so the right-hand side tends to $0$ and $T\xrightarrow{P}\theta$, i.e. $T$ is consistent.",
      r"Hence option (d)."]),

"2018-Q25": dict(ans=3, qtype="Numerical", diff="Medium",
 sc=r"The unrestricted MLE is $x\in\{0,1\}$; projecting onto $[\tfrac13,\tfrac23]$ gives $\tfrac13$ when $x=0$ and $\tfrac23$ when $x=1$, i.e. $\frac{x+1}{3}$.",
 tips=[r"With a restricted parameter space, the MLE is the admissible point closest to the unrestricted maximiser (likelihood is monotone on either side).",
       r"Test the candidate formulae at $x=0$ and $x=1$ - only $(x+1)/3$ returns $1/3$ and $2/3$.",
       r"$L(p)=p^x(1-p)^{1-x}$ is increasing in $p$ when $x=1$ and decreasing when $x=0$, which is why the answer sits at an endpoint."],
 sol=[r"For one Bernoulli observation, $L(p)=p^x(1-p)^{1-x}$.",
      r"If $x=1$, $L(p)=p$ is increasing, so the maximum over $[\tfrac13,\tfrac23]$ is at $p=\tfrac23$.",
      r"If $x=0$, $L(p)=1-p$ is decreasing, so the maximum is at $p=\tfrac13$.",
      r"Both values are produced by $\hat p=\dfrac{x+1}{3}$ - option (d)."]),

"2018-Q26": dict(ans=0, qtype="Conceptual", diff="Easy",
 sc=r"With $\mu$ known, $\hat\sigma^2=\frac1n\sum(X_i-\mu)^2$ - and this one is unbiased as well as the MLE.",
 tips=[r"$\mu$ known $\Rightarrow$ no degree of freedom is spent estimating it, so the divisor is $n$ and the estimator is unbiased.",
       r"Contrast $\mu$ unknown: $\hat\sigma^2=\frac1n\sum(X_i-\bar X)^2$ is the MLE but is biased; $\frac1{n-1}\sum(X_i-\bar X)^2$ is unbiased.",
       r"$\sum(X_i-\mu)^2/\sigma^2\sim\chi^2_n$ here, versus $\chi^2_{n-1}$ when $\bar X$ replaces $\mu$."],
 sol=[r"$\log L=-\dfrac n2\log(2\pi\sigma^2)-\dfrac{1}{2\sigma^2}\sum(X_i-\mu)^2$.",
      r"$\dfrac{\partial\log L}{\partial\sigma^2}=-\dfrac n{2\sigma^2}+\dfrac{1}{2\sigma^4}\sum(X_i-\mu)^2=0$.",
      r"Solving: $\hat\sigma^2=\dfrac{\sum_{i=1}^n(X_i-\mu)^2}{n}$.",
      r"Option (a)."]),

"2018-Q27": dict(ans=1, qtype="Numerical", diff="Medium",
 sc=r"For $U(\theta_1,\theta_2)$: mean $=\frac{\theta_1+\theta_2}2$, variance $=\frac{(\theta_2-\theta_1)^2}{12}$. Equating gives $\hat\theta_{1,2}=\bar X\mp\sqrt{3m_2}$.",
 tips=[r"The half-width is $\sqrt3$ times the sample standard deviation (with divisor $n$) - the factor $\sqrt3$ is the whole question.",
       r"Order matters: $\theta_1<\theta_2$, so the minus sign belongs to $T_1$. Options (c) and (d) give equal estimates, which is impossible.",
       r"Derivation: $\frac{(\theta_2-\theta_1)^2}{12}=m_2\Rightarrow\theta_2-\theta_1=2\sqrt{3m_2}$, so each endpoint is $\sqrt{3m_2}$ from the mean."],
 sol=[r"For $U(\theta_1,\theta_2)$: $\mu_1'=\dfrac{\theta_1+\theta_2}2$ and $\mu_2=\dfrac{(\theta_2-\theta_1)^2}{12}$.",
      r"Method of moments: $\dfrac{\hat\theta_1+\hat\theta_2}2=\bar X$ and $\dfrac{(\hat\theta_2-\hat\theta_1)^2}{12}=m_2=\dfrac1n\sum(X_i-\bar X)^2$.",
      r"The second gives $\hat\theta_2-\hat\theta_1=2\sqrt{3m_2}$, so each endpoint is $\sqrt{3m_2}$ away from $\bar X$.",
      r"Hence $T_1=\bar X-\sqrt{\frac3n\sum(X_i-\bar X)^2}$ and $T_2=\bar X+\sqrt{\frac3n\sum(X_i-\bar X)^2}$ - option (b)."]),

"2018-Q28": dict(ans=0, qtype="Conceptual", diff="Easy",
 sc=r"Support is $x\ge\mu$, so the likelihood $e^{-\sum(x_i-\mu)}=e^{n\mu-\sum x_i}$ increases in $\mu$ up to the constraint $\mu\le X_{(1)}$: $\hat\mu=X_{(1)}$.",
 tips=[r"When the support depends on the parameter, never differentiate - push the parameter to the boundary allowed by the data.",
       r"$X_{(1)}$ is also the complete sufficient statistic here, and $X_{(1)}-\frac1n$ is the UMVUE of $\mu$.",
       r"$n(X_{(1)}-\mu)\sim\text{Exp}(1)$ - the fact used to build exact confidence intervals in later papers."],
 sol=[r"$L(\mu)=\prod e^{-(x_i-\mu)}=e^{n\mu-\sum x_i}$ for $\mu\le\min_i x_i$, and $L=0$ otherwise.",
      r"$e^{n\mu}$ is strictly increasing in $\mu$, so the likelihood is maximised by taking $\mu$ as large as the constraint permits.",
      r"The constraint is $\mu\le X_{(1)}$, so $\hat\mu=X_{(1)}$.",
      r"Option (a)."]),

"2018-Q29": dict(ans=0, qtype="Definition", diff="Easy",
 sc=r"This is the invariance property of maximum likelihood estimation.",
 tips=[r"Invariance holds for arbitrary $g$, not merely one-to-one $g$ (extended invariance, via the induced likelihood).",
       r"It is the reason $\widehat{\sigma}=\sqrt{\widehat{\sigma^2}}$ and $\widehat{e^{\theta}}=e^{\hat\theta}$.",
       r"Invariance does not preserve unbiasedness - $e^{\bar X}$ is the MLE of $e^\mu$ but is biased upwards."],
 sol=[r"If $\hat\theta$ maximises $L(\theta)$ and $\phi$ is one-to-one, then re-parametrising by $\eta=\phi(\theta)$ gives a likelihood maximised at $\eta=\phi(\hat\theta)$.",
      r"So the MLE of $\phi(\theta)$ is $\phi(\hat\theta)$.",
      r"This is called the invariance property of the MLE - option (a)."]),

"2018-Q30": dict(ans=1, qtype="Conceptual", diff="Easy",
 sc=r"MLEs are consistent and asymptotically normal and may be non-unique, but they are NOT always unbiased ($\hat\sigma^2$ with divisor $n$). So 1, 3, 4.",
 tips=[r"Standard counterexample to unbiasedness: $\hat\sigma^2=\frac1n\sum(X_i-\bar X)^2$ in the normal model.",
       r"Standard counterexample to uniqueness: $U(\theta-\tfrac12,\theta+\tfrac12)$, where any $\theta$ in an interval maximises $L$.",
       r"The correct slogan is 'asymptotically unbiased, consistent, asymptotically normal, asymptotically efficient' - all the good properties are asymptotic."],
 sol=[r"Statement 1: under regularity conditions MLEs are consistent. True.",
      r"Statement 2: MLEs are often biased in finite samples (e.g. $\hat\sigma^2$ with divisor $n$). False.",
      r"Statement 3: under regularity, $\sqrt n(\hat\theta-\theta)\xrightarrow{d}N(0,1/I_1(\theta))$. True.",
      r"Statement 4: the MLE need not be unique - e.g. $U(\theta-\tfrac12,\theta+\tfrac12)$. True.",
      r"Hence 1, 3 and 4 - option (b)."]),

"2018-Q31": dict(ans=0, qtype="Conceptual", diff="Hard",
 sc=r"The score is $\frac{\partial\log L}{\partial\theta}=n(\bar X-\theta)$. Equality in the Cramer-Rao inequality requires the score to be proportional to $T-\psi(\theta)$, which forces $T$ to be of the form $A\bar X+B$.",
 tips=[r"MVB attainment condition: $\dfrac{\partial\log L}{\partial\theta}=A(\theta)\left[T-\psi(\theta)\right]$.",
       r"Because the score here is linear in $\bar X$, only linear functions of $\bar X$ can attain the bound; any non-linear $f(\bar X)$ cannot.",
       r"Hence in $N(\theta,1)$ the functions of $\theta$ admitting MVB estimators are exactly the affine ones $A\theta+B$."],
 sol=[r"For $N(\theta,1)$, $\log L=\text{const}-\tfrac12\sum(x_i-\theta)^2$, so $\dfrac{\partial\log L}{\partial\theta}=\sum(x_i-\theta)=n(\bar X-\theta)$.",
      r"The Cramer-Rao bound is attained by $T$ (unbiased for $\psi(\theta)$) if and only if $\dfrac{\partial\log L}{\partial\theta}=A(\theta)\left[T-\psi(\theta)\right]$.",
      r"Writing $n(\bar X-\theta)=\dfrac nA\left[(A\bar X+B)-(A\theta+B)\right]$ shows every $T=A\bar X+B$ attains the bound for $\psi(\theta)=A\theta+B$.",
      r"No non-linear function of $\bar X$ can make the score proportional to $T-\psi(\theta)$.",
      r"So the MVB class is exactly $\{A\bar X+B\}$ - option (a)."]),

"2018-Q32": dict(ans=3, qtype="Numerical", diff="Medium",
 sc=r"Mean is known ($=0$), so the MVB estimator of $\sigma^2$ is $\frac1n\sum x_i^2=121/10=12.1$.",
 tips=[r"$N(0,\sigma^2)$: the score is $\frac{\partial\log L}{\partial\sigma^2}=\frac{n}{2\sigma^4}\left[\frac1n\sum x_i^2-\sigma^2\right]$ - exactly the MVB attainment form.",
       r"Compute $\sum x_i^2$ carefully: $25+4+9+16+9+4+16+25+9+4=121$.",
       r"Do not subtract $\bar x$ - the mean is given as $0$, so deviations are taken from $0$."],
 sol=[r"For $N(0,\sigma^2)$ the MVB (and UMVU) estimator of $\sigma^2$ is $T=\dfrac1n\sum_{i=1}^n X_i^2$.",
      r"Squares: $5^2,2^2,3^2,4^2,3^2,2^2,4^2,5^2,3^2,2^2=25,4,9,16,9,4,16,25,9,4$.",
      r"$\sum x_i^2=121$ and $n=10$.",
      r"$T=121/10=12.1$ - option (d)."]),

"2018-Q33": dict(ans=0, qtype="Conceptual", diff="Medium",
 sc=r"The mean is known ($=1$), so the score gives the MVB estimator $\frac1n\sum(X_i-1)^2$, which is unbiased with variance $2\sigma^4/n$ equal to the CR bound.",
 tips=[r"With $\mu$ known, the sufficient statistic is $\sum(X_i-\mu)^2$ and the CR bound $2\sigma^4/n$ is attained exactly.",
       r"$\frac1{n-1}\sum(X_i-\bar X)^2$ is unbiased but has variance $2\sigma^4/(n-1)>2\sigma^4/n$ - it does NOT attain the bound.",
       r"Read the mean: '$N(1,\sigma^2)$' means $\mu=1$ is known, which changes the answer completely."],
 sol=[r"Here $\mu=1$ is known, so $\log L=\text{const}-\frac n2\log\sigma^2-\frac{1}{2\sigma^2}\sum(x_i-1)^2$.",
      r"$\dfrac{\partial\log L}{\partial\sigma^2}=\dfrac{n}{2\sigma^4}\left[\dfrac1n\sum(x_i-1)^2-\sigma^2\right]$, which is of the MVB form $A(\sigma^2)[T-\sigma^2]$ with $T=\frac1n\sum(x_i-1)^2$.",
      r"$\sum(X_i-1)^2/\sigma^2\sim\chi^2_n$, so $E(T)=\sigma^2$ and $V(T)=\dfrac{2\sigma^4}{n}$, which equals the CR bound.",
      r"Hence option (a)."]),

"2018-Q34": dict(ans=2, qtype="Numerical", diff="Medium",
 sc=r"With $\theta=\sigma^2$ and $\mu$ known, $I_1(\theta)=\frac1{2\theta^2}$, so the bound is $\frac1{nI_1}=\frac{2\theta^2}{n}$.",
 tips=[r"Memorise $I(\sigma^2)=\dfrac{1}{2\sigma^4}$ per observation in the normal model - it recurs every other year.",
       r"The bound has $\theta^2$ in the numerator, so options linear in $\theta$ can be discarded on dimensional grounds.",
       r"Attained exactly by $\frac1n\sum(X_i-\mu)^2$, whose variance is $2\sigma^4/n$."],
 sol=[r"With $\theta=\sigma^2$ and $\mu$ known, $\log f=-\tfrac12\log(2\pi\theta)-\dfrac{(x-\mu)^2}{2\theta}$.",
      r"$\dfrac{\partial\log f}{\partial\theta}=-\dfrac1{2\theta}+\dfrac{(x-\mu)^2}{2\theta^2}$ and $\dfrac{\partial^2\log f}{\partial\theta^2}=\dfrac1{2\theta^2}-\dfrac{(x-\mu)^2}{\theta^3}$.",
      r"$I_1(\theta)=-E\left[\dfrac{\partial^2\log f}{\partial\theta^2}\right]=-\dfrac1{2\theta^2}+\dfrac{\theta}{\theta^3}=\dfrac1{2\theta^2}$.",
      r"CR bound $=\dfrac1{nI_1(\theta)}=\dfrac{2\theta^2}{n}$ - option (c)."]),

"2018-Q35": dict(ans=3, qtype="Conceptual", diff="Easy",
 sc=r"$L=\theta^{-n}\prod I(x_i\le\theta)=\theta^{-n}I(X_{(n)}\le\theta)$: the data enter only through $X_{(n)}$.",
 tips=[r"Fisher-Neyman factorisation with an indicator: $g_\theta(t)=\theta^{-n}I(t\le\theta)$ and $h(x)=I(x_{(1)}\ge0)$.",
       r"$X_{(n)}$ is also complete for $U(0,\theta)$, so $\frac{n+1}nX_{(n)}$ is the UMVUE of $\theta$.",
       r"For a two-sided uniform $U(\alpha,\beta)$ the sufficient statistic is the pair $(X_{(1)},X_{(n)})$."],
 sol=[r"$f(x,\theta)=\dfrac1\theta$ on $[0,\theta]$, so $L(\theta)=\dfrac1{\theta^n}$ provided every $x_i\le\theta$, i.e. $X_{(n)}\le\theta$.",
      r"Writing $L=\underbrace{\theta^{-n}I\left(X_{(n)}\le\theta\right)}_{g_\theta(X_{(n)})}\cdot\underbrace{I\left(X_{(1)}\ge0\right)}_{h(x)}$ satisfies the factorisation criterion.",
      r"Hence $X_{(n)}=\max(X_1,\dots,X_n)$ is sufficient - option (d)."]),

"2018-Q36": dict(ans=1, qtype="Conceptual", diff="Easy",
 sc=r"$L=\theta^n\left(\prod x_i\right)^{\theta-1}$, so the data enter only through $\prod x_i$.",
 tips=[r"Equivalently $\sum\log x_i$ is sufficient - any one-to-one function of a sufficient statistic is sufficient.",
       r"MLE here is $\hat\theta=-n/\sum\log x_i$, a function of that sufficient statistic as theory guarantees.",
       r"This family ($\theta x^{\theta-1}$ on $(0,1)$) has been examined six times across 2018-2026 - own it completely."],
 sol=[r"$L(\theta)=\prod_{i=1}^n\theta x_i^{\theta-1}=\theta^n\left(\prod_{i=1}^n x_i\right)^{\theta-1}$.",
      r"Take $g_\theta(t)=\theta^n t^{\theta-1}$ with $t=\prod x_i$, and $h(x)=1$.",
      r"By the Fisher-Neyman factorisation theorem $T=\prod_{i=1}^n X_i$ is sufficient for $\theta$.",
      r"Option (b)."]),

"2018-Q37": dict(ans=2, qtype="Numerical", diff="Easy",
 sc=r"$\sum x_i=40$ over $n=10$, and for Poisson the UMVUE of $\lambda$ is $\bar X=4$.",
 tips=[r"$\sum X_i$ is complete sufficient for Poisson, so by Lehmann-Scheffe the unbiased function of it, namely $\bar X$, is the UMVUE.",
       r"Add carefully: $5+3+3+6+2+3+5+4+3+6=40$.",
       r"Option (a) is the total and option (b) halves it - both test whether you divided by $n$."],
 sol=[r"For a Poisson sample, $T=\sum X_i$ is complete and sufficient for $\lambda$.",
      r"$E(\bar X)=\lambda$, and $\bar X$ is a function of $T$; by the Lehmann-Scheffe theorem $\bar X$ is the UMVUE.",
      r"$\sum x_i=5+3+3+6+2+3+5+4+3+6=40$, $n=10$.",
      r"$\bar X=40/10=4$ - option (c)."]),

"2018-Q38": dict(ans=3, qtype="Numerical", diff="Medium",
 sc=r"$\hat p=40/60=0.667$, SE $=\sqrt{0.667(0.333)/60}=0.0609$, and $0.667\pm1.96(0.0609)=(0.547,0.786)$, i.e. about 55% to 79%.",
 tips=[r"Large-sample CI for a proportion: $\hat p\pm z_{\alpha/2}\sqrt{\hat p(1-\hat p)/n}$.",
       r"$\hat p(1-\hat p)=2/9$ exactly here, so SE $=\sqrt{2/540}=0.0609$ - keep fractions to avoid rounding drift.",
       r"The interval must straddle $\hat p=66.7\%$; option (a) and (b) both start at 66%, which cannot be a two-sided interval around 66.7%."],
 sol=[r"$\hat p=\dfrac{40}{60}=0.6667$ and $n=60$.",
      r"$\text{SE}(\hat p)=\sqrt{\dfrac{\hat p(1-\hat p)}{n}}=\sqrt{\dfrac{0.6667\times0.3333}{60}}=\sqrt{0.003704}=0.0609$.",
      r"$95\%$ limits: $0.6667\pm1.96\times0.0609=0.6667\pm0.1193$.",
      r"$=(0.5474,\ 0.7860)$, i.e. approximately $55\%$ and $79\%$ - option (d)."]),

"2018-Q39": dict(ans=2, qtype="Numerical", diff="Medium",
 sc=r"$\alpha=P(X>3\mid\lambda=1)=1-e^{-1}\left(1+1+\tfrac12+\tfrac16\right)=1-\dfrac{8}{3e}=\dfrac{3e-8}{3e}$.",
 tips=[r"Type-I error is always computed under $H_0$ - here $\lambda=1$, so every term carries $e^{-1}$.",
       r"$1+1+\tfrac12+\tfrac16=\tfrac{8}{3}$; multiply by $e^{-1}$ and subtract from 1.",
       r"Options with $e^2$ come from mistakenly using $\lambda=2$ (the alternative) - a deliberate trap."],
 sol=[r"Under $H_0:\lambda=1$, $P(X=k)=\dfrac{e^{-1}}{k!}$.",
      r"$P(X\le3)=e^{-1}\left(\dfrac1{0!}+\dfrac1{1!}+\dfrac1{2!}+\dfrac1{3!}\right)=e^{-1}\left(1+1+\dfrac12+\dfrac16\right)=e^{-1}\cdot\dfrac83$.",
      r"$\alpha=P(X>3)=1-\dfrac{8}{3e}=\dfrac{3e-8}{3e}$.",
      r"Option (c)."]),

"2018-Q40": dict(ans=1, qtype="Conceptual", diff="Easy",
 sc=r"With $\sigma^2$ unknown the pivot is $\frac{\sqrt n(\bar X-\mu)}{S}\sim t_{n-1}$, giving the shortest expected-length interval.",
 tips=[r"$\sigma$ known $\Rightarrow$ normal pivot; $\sigma$ unknown $\Rightarrow$ $t$ pivot. That is the entire decision.",
       r"Shortest expected length among $t$-based intervals comes from the symmetric equal-tail choice, because the $t$ density is symmetric and unimodal.",
       r"Chi-square is for $\sigma^2$ and $F$ is for a ratio of variances - neither gives an interval for $\mu$."],
 sol=[r"With both $\mu$ and $\sigma^2$ unknown, $\sigma$ must be replaced by the sample standard deviation $S$.",
      r"The pivot $\dfrac{\sqrt n(\bar X-\mu)}{S}$ follows Student's $t$ distribution with $n-1$ degrees of freedom, free of all unknown parameters.",
      r"Inverting it gives $\bar X\pm t_{n-1,\alpha/2}\dfrac{S}{\sqrt n}$, which has the shortest expected length among such intervals by symmetry and unimodality of the $t$ density.",
      r"Option (b)."]),

"2018-Q41": dict(ans=1, qtype="Numerical", diff="Easy",
 sc=r"Density $\theta e^{-\theta x}$; power $=P(X>2\mid\theta=2)=e^{-2\times2}=e^{-4}$.",
 tips=[r"Exponential tail: $P(X>c)=e^{-\theta c}$ - no integration needed.",
       r"Power is evaluated under $H_1$ ($\theta=2$), size under $H_0$ ($\theta=1$, giving $e^{-2}$).",
       r"Mean $1/\theta$ means rate $\theta$; mixing the two up produces option (d)."],
 sol=[r"Mean $\tfrac1\theta$ means the density is $f(x)=\theta e^{-\theta x}$, $x>0$.",
      r"$P(X>2)=\displaystyle\int_2^\infty\theta e^{-\theta x}dx=e^{-2\theta}$.",
      r"Power at $\theta=2$: $e^{-2(2)}=e^{-4}=\dfrac1{e^4}$.",
      r"Option (b)."]),
}
