# -*- coding: utf-8 -*-
# Linear Models answer pack 3 (2021 Q7-Q10, 2022 Q1-Q10, Q21-Q30, 2023 Q51)
A = {

"2021-Q7": dict(ans=2, qtype="Conceptual", diff="Medium",
 sc=r"A g-inverse of a symmetric matrix need not be symmetric (statement 1 true), but $\tfrac12\left(X^-+(X^-)'\right)$ is always a symmetric g-inverse (statement 2 true).",
 tips=[r"Symmetry is not part of the definition $XX^-X=X$, so nothing forces $X^-$ to inherit it.",
       r"Construction to remember: if $G$ is a g-inverse of a symmetric $X$, then so is $G'$, and so is $\tfrac12(G+G')$, which is symmetric.",
       r"This exact item was repeated verbatim in 2023 (Q63) and 2024 (Q51) - three appearances in four years."],
 sol=[r"Statement 1: the only requirement on $X^-$ is $XX^-X=X$. Example: many non-symmetric matrices satisfy this even for symmetric $X$, so a g-inverse is not necessarily symmetric. True.",
      r"Statement 2: if $G$ is any g-inverse of the symmetric $X$, then $XG'X=(X'GX')'=(XGX)'=X'=X$, so $G'$ is also a g-inverse.",
      r"Hence $G^*=\tfrac12(G+G')$ satisfies $XG^*X=\tfrac12(XGX+XG'X)=X$ and is symmetric. A symmetric g-inverse can therefore always be constructed. True.",
      r"Both statements are correct - option (c)."]),

"2021-Q8": dict(ans=1, qtype="Numerical", diff="Easy",
 sc=r"One-way with $k=5$ methods and $n=6$ each: $N=30$, error df $=N-k=25$ (equivalently $k(n-1)=5\times5$).",
 tips=[r"Balanced one-way: error df $=k(n-1)$, treatment df $=k-1$, total df $=kn-1$.",
       r"Check the three add up: $4+25=29=30-1$.",
       r"Distractor 24 comes from $kn-k-1$, distractor 26 from $N-k+1$ - both fail the addition check."],
 sol=[r"There are $k=5$ treatments (packing methods) with $n=6$ observations each, so $N=30$.",
      r"Treatment df $=k-1=4$; total df $=N-1=29$.",
      r"Error df $=N-k=30-5=25$, i.e. $k(n-1)=5\times5=25$.",
      r"Hence option (b)."]),

"2021-Q9": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"In the additive two-way layout only $\mu+\alpha_i+\beta_j$ and contrasts of the $\alpha$'s or $\beta$'s are estimable. That is option (c), $\alpha_i-\alpha_u$.",
 tips=[r"Estimability test: $\lambda_\mu=\sum\lambda_\alpha=\sum\lambda_\beta$. For $\alpha_i-\alpha_u$ all three are zero, so it passes.",
       r"$\alpha_1+\alpha_2$ has $\sum\lambda_\alpha=2\ne0=\lambda_\mu$ - fails. Individual $\alpha_i$ or $\beta_j$ likewise fail.",
       r"General principle in over-parametrised models: differences are estimable, levels are not."],
 sol=[r"Rows of the design matrix are $(1,\mathbf{e}_i,\mathbf{e}_j)$, so any estimable $\lambda$ satisfies $\lambda_\mu=\sum_i\lambda_{\alpha_i}=\sum_j\lambda_{\beta_j}$.",
      r"$\alpha$ alone and $\beta$ alone are not even well-defined single parameters, and individual effects fail the condition.",
      r"$\alpha_1+\alpha_2$: $\lambda_\mu=0$ but $\sum\lambda_\alpha=2$ - not estimable.",
      r"$\alpha_i-\alpha_u$: $\lambda_\mu=0$, $\sum\lambda_\alpha=1-1=0$, $\sum\lambda_\beta=0$ - estimable, with BLUE $\bar y_{i\cdot}-\bar y_{u\cdot}$. Option (c)."]),

"2021-Q10": dict(ans=1, qtype="Conceptual", diff="Easy",
 sc=r"If $A$ is $n\times m$ then $A^-$ is $m\times n$. Here $4\times7$ gives $7\times4$.",
 tips=[r"The order follows from conformability of $AA^-A$: $(4\times7)(?\,)(4\times7)$ forces $?=7\times4$.",
       r"A g-inverse always has the transposed shape of the original, never the same shape.",
       r"Same fact was asked symbolically in 2023 Q62 ($n\times p\Rightarrow p\times n$)."],
 sol=[r"By definition $A^-$ must satisfy $AA^-A=A$.",
      r"For the product to be defined and of order $4\times7$, $A^-$ must have order $7\times4$.",
      r"Hence option (b)."]),

"2022-Q1": dict(ans=1, qtype="Conceptual", diff="Easy",
 sc=r"$\hat Y=HY$ with $H=X(X'X)^{-1}X'$, so $\hat Y\sim N\!\left(X\beta,\ \sigma^2H\right)$ - option (b).",
 tips=[r"$E(\hat Y)=HX\beta=X\beta$ because $HX=X$.",
       r"$V(\hat Y)=H(\sigma^2I)H'=\sigma^2H$ using symmetry and idempotency of $H$.",
       r"$\hat Y$ is $n\times1$, so its dispersion matrix must be $n\times n$: options with $(X'X)^{-1}$ ($k\times k$) are dimensionally impossible."],
 sol=[r"$\hat\beta=(X'X)^{-1}X'Y$ and $\hat Y=X\hat\beta=X(X'X)^{-1}X'Y=HY$.",
      r"$\hat Y$ is a linear function of the normal vector $Y$, hence normal.",
      r"$E(\hat Y)=HE(Y)=HX\beta=X\beta$ since $HX=X$.",
      r"$V(\hat Y)=HV(Y)H'=\sigma^2HH=\sigma^2H=\sigma^2X(X'X)^{-1}X'$.",
      r"Hence $\hat Y\sim N\left(X\beta,\ X(X'X)^{-1}X'\sigma^2\right)$ - option (b)."]),

"2022-Q2": dict(ans=1, qtype="Numerical", diff="Medium",
 sc=r"Least squares minimises $\sum(Y_i-\theta x_i)^2$ giving $\sum x_iY_i/\sum x_i^2$; the Poisson likelihood gives $\sum Y_i/\sum x_i$. In that order, option (b).",
 tips=[r"Least squares ignores the variance structure; the Poisson MLE automatically weights by the mean, so the two differ here.",
       r"Poisson log-likelihood: $\ell=-\theta\sum x_i+\sum Y_i\log\theta+\text{const}$, so $\hat\theta_{MLE}=\sum Y_i/\sum x_i$.",
       r"Read the order in which the question asks - 'least square estimator and the MLE respectively' - options (a) and (b) are the same pair swapped."],
 sol=[r"Least squares: minimise $S(\theta)=\sum_i(Y_i-\theta x_i)^2$. Then $S'(\theta)=-2\sum x_i(Y_i-\theta x_i)=0$ gives $\hat\theta_{LS}=\dfrac{\sum x_iY_i}{\sum x_i^2}$.",
      r"Maximum likelihood: $L(\theta)=\prod_i e^{-\theta x_i}\dfrac{(\theta x_i)^{Y_i}}{Y_i!}$, so $\ell(\theta)=-\theta\sum x_i+\left(\sum Y_i\right)\log\theta+\text{const}$.",
      r"$\dfrac{\partial\ell}{\partial\theta}=-\sum x_i+\dfrac{\sum Y_i}{\theta}=0\Rightarrow\hat\theta_{MLE}=\dfrac{\sum Y_i}{\sum x_i}$.",
      r"So (LS, MLE) $=\left(\dfrac{\sum X_iY_i}{\sum X_i^2},\ \dfrac{\sum Y_i}{\sum X_i}\right)$ - option (b)."]),

"2022-Q3": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"$H^2=AA^-AA^-=(AA^-A)A^-=AA^-=H$ and identically $T^2=T$. Both are idempotent.",
 tips=[r"One line proves both: insert the defining identity $AA^-A=A$ in the middle of the product.",
       r"Idempotent does not mean symmetric - a very common follow-up trap.",
       r"$\operatorname{rank}(H)=\operatorname{rank}(T)=\operatorname{rank}(A)$ and both traces equal that rank."],
 sol=[r"$H=AA^-$, so $H^2=AA^-AA^-=(AA^-A)A^-=AA^-=H$. Hence $H$ is idempotent.",
      r"$T=A^-A$, so $T^2=A^-AA^-A=A^-(AA^-A)=A^-A=T$. Hence $T$ is idempotent.",
      r"Both are idempotent - option (c)."]),

"2022-Q4": dict(ans=3, qtype="Conceptual", diff="Medium",
 sc=r"All three are standard g-inverse identities: transposition, the $(A'A)^-A'$ construction, and the common rank $\operatorname{rank}(A)$.",
 tips=[r"$(A^-)'$ is a g-inverse of $A'$: take the transpose of $AA^-A=A$.",
       r"$(A'A)^-A'$ is always a g-inverse of $A$ - this is the identity behind $P=X(X'X)^-X'$ in linear models.",
       r"$\operatorname{rank}(A^-A)=\operatorname{rank}(AA^-)=\operatorname{rank}(A)$: both are projections of the same rank."],
 sol=[r"Statement 1: transposing $AA^-A=A$ gives $A'(A^-)'A'=A'$, so $(A^-)'$ is a generalized inverse of $A'$, i.e. $(A')^-=(A^-)'$ is a valid choice. True.",
      r"Statement 2: it is a standard result that $A\left[(A'A)^-A'\right]A=A$, so $(A'A)^-A'$ is a generalized inverse of $A$. True.",
      r"Statement 3: $A^-A$ and $AA^-$ are idempotent with $\operatorname{rank}(A^-A)=\operatorname{rank}(AA^-)=\operatorname{rank}(A)$. True.",
      r"All three correct - option (d)."]),

"2022-Q5": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"The Moore-Penrose inverse satisfies all four Penrose conditions, which makes it unique, and the identity $A^+=(A'A)^+A'$ holds.",
 tips=[r"Uniqueness is the defining advantage of $A^+$ over an arbitrary $A^-$ - an almost guaranteed one-mark recall item.",
       r"The four Penrose conditions: $AA^+A=A$, $A^+AA^+=A^+$, $(AA^+)'=AA^+$, $(A^+A)'=A^+A$.",
       r"For full column rank $A$, $A^+=(A'A)^{-1}A'$, the familiar least-squares operator."],
 sol=[r"Statement 1: $(A'A)^+A'$ satisfies all four Penrose conditions for $A$, so it equals $A^+$. True.",
      r"Statement 2: the Moore-Penrose inverse is unique. If $G_1,G_2$ both satisfy the four conditions then a short chain of substitutions gives $G_1=G_2$. True.",
      r"Both correct - option (c)."]),

"2022-Q6": dict(ans=1, qtype="Conceptual", diff="Medium",
 sc=r"$X'X=3\begin{pmatrix}2&-1\\-1&2\end{pmatrix}$ has equal diagonal entries, so $(X'X)^{-1}=\tfrac19\begin{pmatrix}2&1\\1&2\end{pmatrix}$ and $V(\hat\theta_1)=V(\hat\theta_2)=\tfrac{2\sigma^2}{9}$.",
 tips=[r"You never need the estimators themselves: symmetry of the design in $\theta_1$ and $\theta_2$ already forces equal variances.",
       r"$V(\hat\theta)=\sigma^2(X'X)^{-1}$; read the variances off the diagonal.",
       r"Whenever the roles of two parameters can be swapped by relabelling the observations, their BLUEs must have equal variance - a 5-second argument."],
 sol=[r"Design rows: $(1,0)$ three times, $(0,1)$ three times, $(1,-1)$ three times.",
      r"$X'X=\begin{pmatrix}6&-3\\-3&6\end{pmatrix}$, so $(X'X)^{-1}=\dfrac19\begin{pmatrix}2&1\\1&2\end{pmatrix}$.",
      r"$V(\hat\theta_1)=\sigma^2\cdot\dfrac29$ and $V(\hat\theta_2)=\sigma^2\cdot\dfrac29$.",
      r"They are equal - option (b)."]),

"2022-Q7": dict(ans=3, qtype="Numerical", diff="Easy",
 sc=r"Error df $=pq(r-1)=5\times6\times3=90$.",
 tips=[r"$pq(r-1)$ - count cells, then subtract one df per cell for the cell mean.",
       r"Check: $4+5+20+90=119=pqr-1=120-1$.",
       r"Distractor 120 is $pqr$ itself; 96 and 100 come from mis-multiplying $(pq-1)(r-1)$ style formulae."],
 sol=[r"$p=5$, $q=6$, $r=4$ so $N=pqr=120$.",
      r"df(A) $=4$, df(B) $=5$, df(AB) $=4\times5=20$.",
      r"df(error) $=pq(r-1)=30\times3=90$.",
      r"Check: $4+5+20+90=119=N-1$. Option (d)."]),

"2022-Q8": dict(ans=0, qtype="Conceptual", diff="Medium",
 sc=r"From the expected mean squares of a two-way random model, $E(MSA)=\sigma^2+m\sigma^2_{ab}+qm\sigma^2_a$ and $E(MS_{AB})=\sigma^2+m\sigma^2_{ab}$, so $\hat\sigma^2_a=\dfrac{MSA-MS(AB)}{qm}$.",
 tips=[r"Memorise the EMS table: $E(MSE)=\sigma^2$; $E(MS_{AB})=\sigma^2+m\sigma^2_{ab}$; $E(MSA)=\sigma^2+m\sigma^2_{ab}+qm\sigma^2_a$; $E(MSB)=\sigma^2+m\sigma^2_{ab}+pm\sigma^2_b$.",
       r"The divisor is always 'number of observations at each level of that factor' $=qm$ for A and $pm$ for B.",
       r"In a random model the main-effect $F$'s use $MS_{AB}$, not $MSE$, as denominator - the single most examinable asymmetry in this topic."],
 sol=[r"For the two-way random effects model with $m$ observations per cell the expected mean squares are $E(MSA)=\sigma^2+m\sigma_{ab}^2+qm\sigma_a^2$ and $E(MS(AB))=\sigma^2+m\sigma_{ab}^2$.",
      r"Subtracting: $E(MSA)-E(MS(AB))=qm\sigma_a^2$.",
      r"Equating observed to expected mean squares (ANOVA method of moments) gives $\hat\sigma_a^2=\dfrac{MSA-MS(AB)}{qm}$.",
      r"Hence option (a)."]),

"2022-Q9": dict(ans=0, qtype="Numerical", diff="Medium",
 sc=r"$a=2$ methods, $b=3$ types, $r=2$ per cell: df(A) $=1$, df(error) $=ab(r-1)=6$. So MSA $=0.114075/1$ and MSE $=0.661950/6=0.110325$.",
 tips=[r"Read the layout from the words: 'two methods', 'three types', 'two pieces per type and method' gives $2\times3\times2=12$ observations.",
       r"df(A) $=1$ means MSA equals SSA numerically - the 0.057037 distractor wrongly divides by 2.",
       r"The companion item (F-ratios) confirms the choice: only MSE $=0.110325$ reproduces the printed $F$ values."],
 sol=[r"Factor A (method) has $a=2$ levels, factor B (type) has $b=3$ levels, with $r=2$ observations per cell, so $N=12$ and total df $=11$.",
      r"df(A) $=a-1=1$; df(B) $=b-1=2$; df(AB) $=2$; df(error) $=ab(r-1)=6$.",
      r"MS(method) $=\dfrac{0.114075}{1}=0.114075$.",
      r"MS(error) $=\dfrac{0.661950}{6}=0.110325$.",
      r"Hence option (a)."]),

"2022-Q10": dict(ans=2, qtype="Numerical", diff="Medium",
 sc=r"$F_A=0.114075/0.110325=1.034$ and $F_B=(25.900117/2)/0.110325=117.381$.",
 tips=[r"Always compute MSB $=$ SSB/df(B) first: $25.900117/2=12.9500585$.",
       r"$F_A\approx1$ means the method effect is negligible; $F_B$ is enormous, so cheese type dominates - a useful sanity read.",
       r"Distractors halve $F_A$ (wrong df for A) or use the wrong MSE."],
 sol=[r"From the previous item: MSA $=0.114075$, MSE $=0.110325$, and MSB $=\dfrac{25.900117}{2}=12.9500585$.",
      r"$F_A=\dfrac{MSA}{MSE}=\dfrac{0.114075}{0.110325}=1.034$.",
      r"$F_B=\dfrac{MSB}{MSE}=\dfrac{12.9500585}{0.110325}=117.381$.",
      r"Hence $1.034$ and $117.381$ - option (c)."]),

"2022-Q21": dict(ans=3, qtype="Conceptual", diff="Easy",
 sc=r"$P$ is symmetric idempotent, so $p_{ii}=\sum_j p_{ij}^2\ge p_{ii}^2$, forcing $0\le p_{ii}\le1$.",
 tips=[r"The diagonal elements of a projection matrix are the leverages; they lie in $[0,1]$ and sum to $k$.",
       r"Average leverage is $k/n$; a rule of thumb flags $p_{ii}>2k/n$ as a high-leverage point.",
       r"Since $V(e_i)=\sigma^2(1-p_{ii})\ge0$, $p_{ii}\le1$ follows immediately from non-negativity of a variance."],
 sol=[r"$P=X(X'X)^{-1}X'$ is symmetric and idempotent, so $P=P^2$ gives $p_{ii}=\sum_{j}p_{ij}p_{ji}=\sum_j p_{ij}^2\ge p_{ii}^2$.",
      r"$p_{ii}\ge p_{ii}^2$ implies $0\le p_{ii}\le1$.",
      r"Equivalently, $V(e_i)=\sigma^2(1-p_{ii})\ge0$ forces $p_{ii}\le1$, and $V(\hat y_i)=\sigma^2p_{ii}\ge0$ forces $p_{ii}\ge0$.",
      r"Hence $0\le p_{ii}\le1$ - option (d)."]),

"2022-Q22": dict(ans=1, qtype="Conceptual", diff="Medium",
 sc=r"$V(e)=\sigma^2(I-P)$, so $\operatorname{Cov}(e_i,e_j)=-\sigma^2p_{ij}$ and $\operatorname{Corr}(e_i,e_j)=\dfrac{-p_{ij}}{\sqrt{(1-p_{ii})(1-p_{jj})}}$.",
 tips=[r"The minus sign is the entire question: residuals from a fitted model are negatively correlated when $p_{ij}>0$.",
       r"$V(e_i)=\sigma^2(1-p_{ii})$ supplies both square roots in the denominator.",
       r"The $\sigma^2$ cancels - a correlation can never retain a variance parameter, which also kills any option carrying $\sigma^2$."],
 sol=[r"$e=(I-P)Y$, so $V(e)=\sigma^2(I-P)$.",
      r"Hence $V(e_i)=\sigma^2(1-p_{ii})$, $V(e_j)=\sigma^2(1-p_{jj})$, and $\operatorname{Cov}(e_i,e_j)=-\sigma^2p_{ij}$.",
      r"$\operatorname{Corr}(e_i,e_j)=\dfrac{-\sigma^2p_{ij}}{\sqrt{\sigma^2(1-p_{ii})\,\sigma^2(1-p_{jj})}}=\dfrac{-p_{ij}}{\sqrt{(1-p_{ii})(1-p_{jj})}}$.",
      r"Hence option (b)."]),

"2022-Q23": dict(ans=0, qtype="Numerical", diff="Medium",
 sc=r"$k=3$, $N=21$: MST $=147.3456/2=73.6728$; SSE $=202.3126-147.3456=54.967$, MSE $=54.967/18=3.05372$; $F=24.1256$.",
 tips=[r"Fill df first: treatment $k-1=2$, error $N-k=18$, total $20$.",
       r"SSE is always obtained by subtraction when only SST and SS(Total) are given.",
       r"Keep four decimals throughout - the four options differ only in the third decimal place."],
 sol=[r"$k=3$ methods with $7$ observations each, so $N=21$; treatment df $=2$, error df $=18$.",
      r"$\text{MST}=\dfrac{147.3456}{2}=73.6728$.",
      r"$\text{SSE}=202.3126-147.3456=54.9670$, so $\text{MSE}=\dfrac{54.9670}{18}=3.05372$.",
      r"$F=\dfrac{73.6728}{3.05372}=24.1256$.",
      r"Hence option (a)."]),

"2022-Q24": dict(ans=0, qtype="Numerical", diff="Medium",
 sc=r"$X'X=\begin{pmatrix}8&4\\4&8\end{pmatrix}$, $(X'X)^{-1}=\tfrac1{12}\begin{pmatrix}2&-1\\-1&2\end{pmatrix}$, $X'\mathbf{Y}=(X+Z,\ Y+Z)'$, giving $\hat\theta_1=\tfrac1{12}[2X-Y+Z]$.",
 tips=[r"With $E(Z_i)=\theta_1+\theta_2$ the off-diagonal of $X'X$ is $+4$, so the inverse carries $-1$ off-diagonal - the sign flip is where marks are lost.",
       r"Audit by unbiasedness: $E\left(\tfrac1{12}[2X-Y+Z]\right)=\tfrac1{12}[8\theta_1-4\theta_2+4\theta_1+4\theta_2]=\theta_1$.",
       r"Same skeleton as 2021 Q3, 2023 Q56-57 and 2025 Q61-62 - only the signs in $E(Z_i)$ move."],
 sol=[r"Design rows: $(1,0)$ four times, $(0,1)$ four times, $(1,1)$ four times.",
      r"$X'X=4\begin{pmatrix}1&0\\0&0\end{pmatrix}+4\begin{pmatrix}0&0\\0&1\end{pmatrix}+4\begin{pmatrix}1&1\\1&1\end{pmatrix}=\begin{pmatrix}8&4\\4&8\end{pmatrix}$, $|X'X|=48$.",
      r"$(X'X)^{-1}=\dfrac1{48}\begin{pmatrix}8&-4\\-4&8\end{pmatrix}=\dfrac1{12}\begin{pmatrix}2&-1\\-1&2\end{pmatrix}$ and $X'\mathbf{Y}=\begin{pmatrix}X+Z\\Y+Z\end{pmatrix}$.",
      r"$\hat\theta_1=\dfrac1{12}\left[2(X+Z)-(Y+Z)\right]=\dfrac1{12}\left[2X-Y+Z\right]$.",
      r"Hence option (a)."]),

"2022-Q25": dict(ans=2, qtype="Numerical", diff="Medium",
 sc=r"Second row of $(X'X)^{-1}X'\mathbf{Y}$: $\hat\theta_2=\tfrac1{12}\left[-(X+Z)+2(Y+Z)\right]=\tfrac1{12}[-X+2Y+Z]$.",
 tips=[r"By the symmetry of the design in $\theta_1,\theta_2$, the answer to this item is the previous one with $X$ and $Y$ interchanged.",
       r"Unbiasedness audit: $\tfrac1{12}[-4\theta_1+8\theta_2+4\theta_1+4\theta_2]=\theta_2$.",
       r"Both BLUEs have variance $\dfrac{2\sigma^2}{12}=\dfrac{\sigma^2}{6}$ since the diagonal of $(X'X)^{-1}$ is $(2/12,2/12)$."],
 sol=[r"With $(X'X)^{-1}=\dfrac1{12}\begin{pmatrix}2&-1\\-1&2\end{pmatrix}$ and $X'\mathbf{Y}=(X+Z,\ Y+Z)'$:",
      r"$\hat\theta_2=\dfrac1{12}\left[-(X+Z)+2(Y+Z)\right]$.",
      r"$=\dfrac1{12}\left[-X+2Y+Z\right]$.",
      r"Hence option (c)."]),

"2022-Q26": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"One-way classification: treatment df $=k-1$, error df $=N-k$.",
 tips=[r"The three df must satisfy $(k-1)+(N-k)=N-1$; only option (c) does.",
       r"This addition check alone eliminates the other three options without any thought about the model.",
       r"$N=\sum n_i$ allows unequal group sizes - the formula is unchanged."],
 sol=[r"There are $k$ treatments, so the treatment sum of squares carries $u=k-1$ degrees of freedom.",
      r"Total df $=N-1$.",
      r"Error df $=v=(N-1)-(k-1)=N-k$.",
      r"Hence $(u,v)=(k-1,\,N-k)$ - option (c)."]),

"2022-Q27": dict(ans=0, qtype="Conceptual", diff="Easy",
 sc=r"$t=p-1$, $u=q-1$, $v=(p-1)(q-1)$, $w=pq(r-1)$ - the standard two-way-with-replication table.",
 tips=[r"Match each df to its own index range: $\alpha$ runs over $p$ levels, so $p-1$; $\beta$ over $q$, so $q-1$.",
       r"Options (b), (c), (d) scramble $p$, $q$ and $r$; check the first entry and the rest follows.",
       r"Verify by summation: $(p-1)+(q-1)+(p-1)(q-1)+pq(r-1)=pqr-1$."],
 sol=[r"$\alpha_i$ has $p$ levels subject to one constraint, so $t=p-1$.",
      r"$\beta_j$ has $q$ levels subject to one constraint, so $u=q-1$.",
      r"Interaction: $v=(p-1)(q-1)$.",
      r"Error: $w=pq(r-1)$, one df lost per cell out of the $r$ replicates.",
      r"These sum to $pqr-1$, matching the total. Option (a)."]),

"2022-Q28": dict(ans=1, qtype="Numerical", diff="Medium",
 sc=r"RBD error df $=(b-1)(v-1)=3\times4=12$; each missing value costs one more df, so $12-1=11$.",
 tips=[r"Missing-plot technique: estimating a missing value uses up one error degree of freedom.",
       r"Generic rule: error df $=(b-1)(v-1)-(\text{number of missing observations})$.",
       r"The full-data answer 12 is planted as option (a) to catch candidates who forget the adjustment."],
 sol=[r"For a randomized block design with $b=4$ blocks and $v=5$ treatments, error df $=(b-1)(v-1)=3\times4=12$.",
      r"One observation is missing and is estimated by the missing-plot formula, which consumes one degree of freedom from the error line.",
      r"Error df $=12-1=11$.",
      r"Hence option (b)."]),

"2022-Q29": dict(ans=0, qtype="Numerical", diff="Hard",
 flag=r"Source anomaly: the printed stem writes $E(y_1)=\beta_1-\beta_3$. With three free parameters and only three observations the model would be saturated (RSS $=0$), and neither statement nor any option in the companion item could hold. Under the standard two-parameter form $E(y_1)=\beta_1-\beta_2$ the arithmetic matches the printed options exactly, so the key is set on that reading. Source wording and options are preserved unchanged.",
 sc=r"With $X=\begin{pmatrix}1&-1\\2&0\\1&1\end{pmatrix}$, $X'X=\operatorname{diag}(6,2)$, so $\hat\beta_1=\tfrac16(y_1+2y_2+y_3)$ and $\hat\beta_2=\tfrac12(y_3-y_1)$ - statement 1 true, statement 2 has the wrong sign.",
 tips=[r"An orthogonal design ($X'X$ diagonal) lets you read each BLUE off separately: $\hat\beta_j=\dfrac{\text{column}_j'\mathbf{y}}{\|\text{column}_j\|^2}$.",
       r"Always unbiasedness-test a printed candidate: $E\left[\tfrac12(y_1+y_3)\right]=\beta_1$, not $\beta_2$, so statement 2 falls at once.",
       r"$\tfrac12(y_3-y_1)$ versus $\tfrac12(y_1+y_3)$ - sign traps of this kind are the examiner's favourite device in this cluster."],
 sol=[r"Take $\beta'=(\beta_1,\beta_2)$ with $X=\begin{pmatrix}1&-1\\2&0\\1&1\end{pmatrix}$; the two columns are orthogonal since $1(-1)+2(0)+1(1)=0$.",
      r"$X'X=\begin{pmatrix}6&0\\0&2\end{pmatrix}$ and $X'\mathbf{y}=\begin{pmatrix}y_1+2y_2+y_3\\-y_1+y_3\end{pmatrix}$.",
      r"$\hat\beta_1=\dfrac{y_1+2y_2+y_3}{6}$ - statement 1 is correct.",
      r"$\hat\beta_2=\dfrac{y_3-y_1}{2}$, not $\dfrac{y_1+y_3}{2}$ - statement 2 is incorrect.",
      r"Hence 1 only - option (a)."]),

"2022-Q30": dict(ans=3, qtype="Numerical", diff="Hard",
 flag=r"Same source anomaly as the companion item: the printed $E(y_1)=\beta_1-\beta_3$ makes the model saturated with RSS $=0$, which matches no option. The key uses the standard reading $E(y_1)=\beta_1-\beta_2$, under which the residual sum of squares is exactly option (d).",
 sc=r"$\text{RSS}=\mathbf{y}'\mathbf{y}-\dfrac{(y_1+2y_2+y_3)^2}{6}-\dfrac{(y_3-y_1)^2}{2}=\dfrac13(y_1-y_2+y_3)^2$.",
 tips=[r"With an orthogonal design, $\text{RSS}=\sum y_i^2-\sum_j\dfrac{(\text{col}_j'\mathbf{y})^2}{\|\text{col}_j\|^2}$ - no matrix inversion needed.",
       r"Degrees of freedom check: $n-\operatorname{rank}(X)=3-2=1$, so RSS must be a single squared error contrast - all options are indeed one squared term.",
       r"Identify the error contrast directly: $\mathbf{c}=(1,-1,1)/\sqrt{3}$ is orthogonal to both columns of $X$, so $\text{RSS}=\dfrac{(y_1-y_2+y_3)^2}{3}$."],
 sol=[r"With $X=\begin{pmatrix}1&-1\\2&0\\1&1\end{pmatrix}$ the columns are orthogonal with squared norms $6$ and $2$.",
      r"$\text{RSS}=\mathbf{y}'\mathbf{y}-\hat\beta'X'\mathbf{y}=\left(y_1^2+y_2^2+y_3^2\right)-\dfrac{(y_1+2y_2+y_3)^2}{6}-\dfrac{(y_3-y_1)^2}{2}$.",
      r"Expanding and simplifying: $\text{RSS}=\dfrac13\left(y_1^2+y_2^2+y_3^2-2y_1y_2+2y_1y_3-2y_2y_3\right)$.",
      r"This is exactly $\dfrac13(y_1-y_2+y_3)^2$.",
      r"Cross-check: $(1,-1,1)$ is orthogonal to both design columns, so it spans the one-dimensional error space and $\text{RSS}=\dfrac{(\mathbf{c}'\mathbf{y})^2}{\|\mathbf{c}\|^2}$ with $\|\mathbf{c}\|^2=3$. Option (d)."]),

"2023-Q51": dict(ans=0, qtype="Numerical", diff="Hard",
 sc=r"$\mathbf{Y}'\Sigma^{-1}\mathbf{Y}\sim\chi'^2(3,\lambda)$ with $\lambda=\tfrac12\mu'\Sigma^{-1}\mu=\tfrac12\left(\tfrac92+\tfrac44+\tfrac13\right)=2.9167$.",
 tips=[r"Standardise first: $\mathbf{Z}=\Sigma^{-1/2}\mathbf{Y}\sim N_3(\Sigma^{-1/2}\mu,I)$, so $\mathbf{Y}'\Sigma^{-1}\mathbf{Y}=\mathbf{Z}'\mathbf{Z}$ is non-central $\chi^2$ with $3$ df.",
       r"Non-centrality convention used here (and in most linear-model texts): $\lambda=\tfrac12\mu'\Sigma^{-1}\mu$. The options deliberately offer both $\lambda$ and $2\lambda$.",
       r"With $\Sigma$ diagonal the quadratic form is just $\sum\mu_i^2/\sigma_i^2$ - no matrix work at all."],
 sol=[r"$\Sigma=\operatorname{diag}(2,4,3)$ so $\Sigma^{-1}=\operatorname{diag}(1/2,1/4,1/3)$.",
      r"$\mu'\Sigma^{-1}\mu=\dfrac{3^2}{2}+\dfrac{(-2)^2}{4}+\dfrac{1^2}{3}=4.5+1+0.3333=5.8333$.",
      r"Writing $\mathbf{Z}=\Sigma^{-1/2}\mathbf{Y}\sim N_3(\Sigma^{-1/2}\mu,\,I_3)$ gives $\mathbf{Y}'\Sigma^{-1}\mathbf{Y}=\mathbf{Z}'\mathbf{Z}$, a non-central chi-square with $3$ degrees of freedom.",
      r"The non-centrality parameter is $\lambda=\tfrac12\mu'\Sigma^{-1}\mu=\tfrac12(5.8333)=2.9167$.",
      r"Hence $\chi^2(3,\,2.9167)$ - option (a)."]),
}
