# -*- coding: utf-8 -*-
# Statistical Inference answer pack 10 (2023 Q11-Q38)
A = {
"2023-Q11": dict(ans=2, qtype="Numerical", diff="Medium",
 sc=r"$X-7\sim U(0,\theta)$ and $E\left[(X-7)^2\right]=\theta^2/3$, so $a=3$, $b=7$.",
 tips=[r"Shift by the known lower endpoint first: the problem always reduces to $U(0,\theta)$.",
       r"$E(Y^2)=\theta^2/3$ for $Y\sim U(0,\theta)$ - the reciprocal $3$ is the multiplier.",
       r"$b$ must be the known left endpoint $7$; options with $b=0$ ignore the shift entirely."],
 sol=[r"$X\sim U(7,7+\theta)$, so $Y=X-7\sim U(0,\theta)$.",
      r"$E(Y^2)=\displaystyle\int_0^\theta\frac{y^2}{\theta}dy=\frac{\theta^2}{3}$.",
      r"Therefore $E\left[3(X-7)^2\right]=\theta^2$, giving $a=3$ and $b=7$.",
      r"Option (c)."]),

"2023-Q12": dict(ans=1, qtype="Conceptual", diff="Easy",
 sc=r"For an exponential sample with mean $\lambda$, $\frac{2T}{\lambda}\sim\chi^2_{2n}$ - a distribution free of $\lambda$, hence a pivot.",
 tips=[r"$T=\sum X_i\sim\text{Gamma}(n,\lambda)$, and $2T/\lambda\sim\chi^2_{2n}$ - memorise this conversion.",
       r"A pivot must combine data and parameter with a parameter-free distribution; options (a), (c), (d) all retain $\lambda$.",
       r"Inverting gives the exact interval $\left(\frac{2T}{\chi^2_{2n,\alpha/2}},\ \frac{2T}{\chi^2_{2n,1-\alpha/2}}\right)$."],
 sol=[r"$X_i$ exponential with mean $\lambda$ means $X_i/\lambda\sim\text{Exp}(1)$, so $T/\lambda\sim\text{Gamma}(n,1)$.",
      r"A Gamma$(n,1)$ variable doubled is $\chi^2_{2n}$: $\dfrac{2T}{\lambda}\sim\chi^2_{2n}$.",
      r"This distribution does not involve $\lambda$, so $\dfrac{2T}{\lambda}$ is a pivotal quantity.",
      r"Option (b)."]),

"2023-Q13": dict(ans=3, qtype="Numerical", diff="Medium",
 sc=r"Distinct (unordered) bootstrap resamples number $\binom{2n-1}{n}=\binom95=126$.",
 tips=[r"Counting multisets of size $n$ from $n$ items: $\binom{2n-1}{n}$ - the stars-and-bars formula.",
       r"$n^n=5^5=3125$ counts ORDERED resamples; $625$ and $252$ are other planted values.",
       r"$\binom95=126$; note $\binom{9}{5}=\binom{9}{4}$."],
 sol=[r"A bootstrap resample of size $n$ drawn with replacement is a multiset of $n$ items chosen from the $n$ original observations.",
      r"The number of such multisets is $\dbinom{n+n-1}{n}=\dbinom{2n-1}{n}$.",
      r"For $n=5$: $\dbinom95=\dfrac{9!}{5!4!}=126$.",
      r"Option (d)."]),

"2023-Q14": dict(ans=2, qtype="Conceptual", diff="Medium",
 sc=r"Bernoulli, Poisson and the logistic location family have monotone likelihood ratio; the Cauchy does not. So three.",
 tips=[r"Every one-parameter exponential family with a natural parameter monotone in $\theta$ has MLR - Bernoulli and Poisson qualify.",
       r"The logistic location family is not exponential but is still an MLR family (its log-likelihood ratio is monotone).",
       r"The Cauchy's likelihood ratio is non-monotone - the standard counterexample, and the reason no UMP test exists there."],
 sol=[r"Bernoulli $b(1,\theta)$: $\dfrac{L_{\theta_2}}{L_{\theta_1}}$ is monotone in $\sum x_i$ - MLR holds.",
      r"Poisson $P(\theta)$: likewise monotone in $\sum x_i$ - MLR holds.",
      r"Cauchy $C(\theta,1)$: the likelihood ratio is a non-monotone function of the data - MLR fails.",
      r"Logistic with location $\theta$: the log-likelihood ratio is monotone increasing in $x$ - MLR holds.",
      r"Three of the four possess MLR - option (c)."]),

"2023-Q15": dict(ans=3, qtype="Conceptual", diff="Medium",
 sc=r"$\sigma^2$ appears as an unspecified parameter, so neither $\mu=2$ nor $\mu=3$ pins down the distribution - both hypotheses are composite.",
 tips=[r"Simple means EVERY unknown parameter is fixed; leaving $\sigma^2$ free makes a hypothesis composite.",
       r"Had the stem said 'variance known', both would be simple - read for that qualifier.",
       r"A one-point hypothesis about ONE parameter is not automatically simple in a multi-parameter model."],
 sol=[r"The model is $N(\mu,\sigma^2)$ with $\sigma^2$ written as an unspecified parameter, so the parameter is the pair $(\mu,\sigma^2)$.",
      r"$H_0:\mu=2$ fixes only $\mu$; $\sigma^2$ remains free, so $H_0$ corresponds to a whole family of distributions - composite.",
      r"$H_1:\mu=3$ is composite for the same reason.",
      r"Hence both are composite - option (d)."]),

"2023-Q16": dict(ans=1, qtype="Conceptual", diff="Medium",
 sc=r"For $U(\theta-\tfrac12,\theta+\tfrac12)$ the likelihood is flat on $\left[X_{(n)}-\tfrac12,\ X_{(1)}+\tfrac12\right]$, so every point there is an MLE.",
 tips=[r"Non-uniqueness arises when BOTH support endpoints move with $\theta$, trapping $\theta$ in an interval.",
       r"$U(0,\theta)$ and the shifted exponential each have ONE moving endpoint, giving a unique boundary solution.",
       r"$(1+\theta)x^\theta$ is a smooth regular family with a unique interior maximum."],
 sol=[r"(a) $U(0,\theta)$: $L=\theta^{-n}$ for $\theta\ge X_{(n)}$, decreasing, so $\hat\theta=X_{(n)}$ - unique.",
      r"(c) $e^{-(x-\theta)}$, $x\ge\theta$: $L$ increases in $\theta$ up to $X_{(1)}$, so $\hat\theta=X_{(1)}$ - unique.",
      r"(d) $(1+\theta)x^\theta$: a regular family with $\hat\theta=-\dfrac{n}{\sum\ln x_i}-1$ - unique.",
      r"(b) $U(\theta-\tfrac12,\theta+\tfrac12)$: $L=1$ for every $\theta\in\left[X_{(n)}-\tfrac12,\ X_{(1)}+\tfrac12\right]$, an interval of maximisers - NOT unique.",
      r"Option (b)."]),

"2023-Q17": dict(ans=3, qtype="Numerical", diff="Hard",
 sc=r"The likelihood ratio $1+\theta(2x-1)$ is increasing in $x$, so the UMP region is an upper tail; under $\theta=0$, $X\sim U(0,1)$, so $P(X\ge c)=1-c=0.05$ gives $c=\tfrac{19}{20}$.",
 tips=[r"$\dfrac{f_\theta(x)}{f_0(x)}=2\theta x+1-\theta=1+\theta(2x-1)$, increasing in $x$ for $\theta>0$ - MLR in $x$ itself.",
       r"Under $H_0:\theta=0$ the density is $f(x)=1$, the standard uniform, so tail probabilities are just lengths.",
       r"$\alpha=0.05$ needs an interval of length $0.05$ at the top: $\left[\tfrac{19}{20},1\right]$."],
 sol=[r"Under $H_0:\theta=0$, $f(x,0)=1$ on $(0,1)$, i.e. $X\sim U(0,1)$.",
      r"For $\theta>0$, $\dfrac{f(x,\theta)}{f(x,0)}=2\theta x+1-\theta=1+\theta(2x-1)$, which increases in $x$.",
      r"By Karlin-Rubin the UMP test rejects for large $x$: $C=\{x\ge c\}$.",
      r"Size: $P(X\ge c\mid\theta=0)=1-c=0.05\Rightarrow c=0.95=\dfrac{19}{20}$.",
      r"Option (d)."]),

"2023-Q18": dict(ans=1, qtype="Conceptual", diff="Easy",
 sc=r"Statement-I is the invariance property; Statement-II is the asymptotic (normality) property.",
 tips=[r"Invariance: $\widehat{g(\theta)}=g\left(\hat\theta\right)$ for any function $g$.",
       r"Asymptotic normality: $\sqrt n\left(\hat\theta-\theta\right)\xrightarrow{d}N\left(0,1/I_1(\theta)\right)$.",
       r"Consistency is convergence in probability to $\theta$ - a different statement from normality of the limit law."],
 sol=[r"Statement-I says the MLE transforms by applying the function - the invariance property.",
      r"Statement-II says the sampling distribution of the MLE becomes normal for large $n$ - the asymptotic normality property.",
      r"Hence the pair is invariance and asymptotic - option (b)."]),

"2023-Q19": dict(ans=1, qtype="Numerical", diff="Hard",
 sc=r"Median $=6$ gives $n_1=n_2=4$; $E(U)=\frac{2n_1n_2}{n_1+n_2}+1=5$ and $V(U)=\frac{2n_1n_2(2n_1n_2-n_1-n_2)}{(n_1+n_2)^2(n_1+n_2-1)}=\frac{12}{7}$.",
 tips=[r"Classify each observation as above ($+$) or below ($-$) the median, then count $n_1$ and $n_2$.",
       r"$E(U)=\frac{2n_1n_2}{n}+1$ and $V(U)=\frac{2n_1n_2(2n_1n_2-n)}{n^2(n-1)}$ with $n=n_1+n_2$.",
       r"Balanced case $n_1=n_2=4$: $2n_1n_2=32$, $n=8$, so $E(U)=5$ and $V(U)=\frac{32(24)}{64(7)}=\frac{12}{7}$."],
 sol=[r"Sorted data: $1,3,4,5,7,8,10,19$, so the median is $\dfrac{5+7}{2}=6$.",
      r"Signs in the original order $\{7,19,3,1,8,5,10,4\}$: $+,+,-,-,+,-,+,-$, giving $n_1=4$ above and $n_2=4$ below.",
      r"$E(U)=\dfrac{2n_1n_2}{n_1+n_2}+1=\dfrac{2(16)}{8}+1=5$.",
      r"$V(U)=\dfrac{2n_1n_2\left(2n_1n_2-n_1-n_2\right)}{(n_1+n_2)^2(n_1+n_2-1)}=\dfrac{32(32-8)}{64\times7}=\dfrac{768}{448}=\dfrac{12}{7}$.",
      r"Option (b)."]),

"2023-Q20": dict(ans=0, qtype="Conceptual", diff="Medium",
 sc=r"$X_{(1)}$ is sufficient (statement 1 true), but the support depends on $\theta$ so the Cramer-Rao machinery fails and $\bar x$ is not an MVB estimator (statement 2 false).",
 tips=[r"Support-dependent parameter $\Rightarrow$ regularity fails $\Rightarrow$ no CR bound, hence no MVB estimator.",
       r"$\bar x$ is not even a function of the sufficient statistic here, so it cannot be optimal.",
       r"The UMVUE of $\theta$ is $X_{(1)}-\frac1n$, based on the sufficient statistic."],
 sol=[r"$L(\theta)=e^{n\theta-\sum x_i}I\left(X_{(1)}>\theta\right)$, so by factorisation $X_{(1)}=\min x_i$ is sufficient: statement 1 true.",
      r"The support $x>\theta$ depends on $\theta$, so the regularity conditions behind the Cramer-Rao inequality fail and no minimum variance bound exists.",
      r"Moreover $\bar x$ is not a function of $X_{(1)}$, so it cannot be an optimal estimator: statement 2 false.",
      r"Hence 1 only - option (a)."]),

"2023-Q21": dict(ans=3, qtype="Numerical", diff="Hard",
 sc=r"$\sum(x_i-\mu)^2/\sigma^2\sim\chi^2_n$ and $E\left(\sqrt{\chi^2_n}\right)=\sqrt2\frac{\Gamma\left(\frac{n+1}2\right)}{\Gamma\left(\frac n2\right)}$, so $k=\sqrt{\frac n2}\frac{\Gamma(n/2)}{\Gamma((n+1)/2)}$.",
 tips=[r"$E\left[(\chi^2_n)^{1/2}\right]=\sqrt2\,\dfrac{\Gamma\left(\frac{n+1}{2}\right)}{\Gamma\left(\frac n2\right)}$ - the key Gamma-function moment.",
       r"$T=k\sigma\sqrt{\chi^2_n/n}$, so $E(T)=k\sigma\sqrt{2/n}\,\frac{\Gamma((n+1)/2)}{\Gamma(n/2)}$; set equal to $\sigma$ and invert.",
       r"The correction factor is close to 1 for large $n$, so any option without both $\sqrt{n/2}$ and the Gamma ratio inverted is wrong."],
 sol=[r"With $\mu$ known, $W=\dfrac{\sum(x_i-\mu)^2}{\sigma^2}\sim\chi^2_n$, so $T=k\sigma\sqrt{\dfrac Wn}$.",
      r"$E\left(\sqrt W\right)=\sqrt2\,\dfrac{\Gamma\left(\frac{n+1}{2}\right)}{\Gamma\left(\frac n2\right)}$.",
      r"$E(T)=\dfrac{k\sigma}{\sqrt n}\cdot\sqrt2\,\dfrac{\Gamma\left(\frac{n+1}{2}\right)}{\Gamma\left(\frac n2\right)}$, and unbiasedness requires this to equal $\sigma$.",
      r"Hence $k=\sqrt{\dfrac n2}\cdot\dfrac{\Gamma\left(\frac n2\right)}{\Gamma\left(\frac{n+1}{2}\right)}$ - option (d)."]),

"2023-Q22": dict(ans=0, qtype="Conceptual", diff="Medium",
 sc=r"Sets 1 and 2 are one-to-one transforms of each other and both determine the likelihood; set 3 loses the SIGN of $\sum x_i$ and is not sufficient.",
 tips=[r"$\sum(x_i-\bar x)^2=\sum x_i^2-\frac{(\sum x_i)^2}{n}$, so sets 1 and 2 carry identical information.",
       r"From set 3 you can recover $\left(\sum x_i\right)^2$ but not $\sum x_i$ itself - the sign of $\mu$ is lost.",
       r"Sufficiency is preserved under one-to-one transformations only."],
 sol=[r"The likelihood is $L\propto\sigma^{-n}\exp\left[-\dfrac{\sum x_i^2-2\mu\sum x_i+n\mu^2}{2\sigma^2}\right]$, so $\left(\sum x_i,\sum x_i^2\right)$ is jointly sufficient: set 1 works.",
      r"Set 2 is a one-to-one function of set 1 via $\sum(x_i-\bar x)^2=\sum x_i^2-\frac{\left(\sum x_i\right)^2}{n}$, so it is equally sufficient.",
      r"Set 3 gives $\left(\sum x_i\right)^2=n\left[\sum x_i^2-\sum(x_i-\bar x)^2\right]$ but not the sign of $\sum x_i$, so it cannot reconstruct the likelihood: not sufficient.",
      r"Hence 1 and 2 only - option (a)."]),

"2023-Q23": dict(ans=1, qtype="Conceptual", diff="Medium",
 sc=r"The Cauchy has no mean, so $\bar X$ is not unbiased (statement 1 fails); the sample median is consistent for the location parameter (statement 2 holds).",
 tips=[r"'Unbiased' presupposes that the expectation exists - for the Cauchy it does not.",
       r"$\bar X$ has the same Cauchy$(\theta,1)$ distribution as a single observation, so it is also inconsistent.",
       r"The sample median is consistent for the population median $\theta$ and is asymptotically normal with variance $\pi^2/(4n)$."],
 sol=[r"Statement 1: for the Cauchy, $E|X|=\infty$, so $E(\bar X)$ does not exist and $\bar X$ cannot be called unbiased. The statement is therefore incorrect (its inconsistency claim is right, but the unbiasedness claim is not). False.",
      r"Statement 2: the population median is $\theta$ and the density is positive there, so the sample median converges in probability to $\theta$. True.",
      r"Hence 2 only - option (b)."]),

"2023-Q24": dict(ans=2, qtype="Numerical", diff="Hard",
 sc=r"Conditioning the indicator $I(X_1=k)$ on the complete sufficient $T=\sum X_i$ gives the binomial form $\binom Tk\frac{(n-1)^{T-k}}{n^T}$.",
 tips=[r"Given $T=t$, $X_1\mid T=t\sim B\left(t,\frac1n\right)$ - the classic Poisson conditioning result.",
       r"So $P(X_1=k\mid T=t)=\binom tk\left(\frac1n\right)^k\left(1-\frac1n\right)^{t-k}=\binom tk\frac{(n-1)^{t-k}}{n^t}$.",
       r"Option (d) omits the $n^{-k}$ factor and so is not a probability - check that the expression is $\le1$."],
 sol=[r"$T=\sum X_i$ is complete sufficient for $\theta$; start from the unbiased but crude estimator $U=I(X_1=k)$, since $E(U)=P(X=k)$.",
      r"Rao-Blackwell: $\phi(t)=E\left[U\mid T=t\right]=P(X_1=k\mid T=t)$.",
      r"For independent Poissons, $X_1\mid\sum X_i=t\sim B\left(t,\dfrac1n\right)$, so $\phi(t)=\dbinom tk\left(\dfrac1n\right)^k\left(\dfrac{n-1}{n}\right)^{t-k}=\dbinom tk\dfrac{(n-1)^{t-k}}{n^{t}}$.",
      r"By Lehmann-Scheffe this is the MVUE - option (c)."]),

"2023-Q25": dict(ans=0, qtype="Numerical", diff="Hard",
 sc=r"With $\theta_1>\theta_0$ the likelihood ratio decreases in $\sum x_i$, so the BCR is a lower tail; using $2\theta_0\sum X_i\sim\chi^2_{2n}$ gives $\sum x_i\le\frac{\chi^2_{1-\alpha,2n}}{2\theta_0}$.",
 tips=[r"$\theta$ here is the RATE, so a larger $\theta$ means smaller observations - reject for small $\sum x_i$.",
       r"$2\theta\sum X_i\sim\chi^2_{2n}$ is the pivot; solve $P\left(\chi^2_{2n}\le2\theta_0c\right)=\alpha$.",
       r"With the upper-tail convention, the lower $\alpha$-point is written $\chi^2_{1-\alpha,2n}$ - hence option (a)."],
 sol=[r"$\dfrac{L_1}{L_0}=\left(\dfrac{\theta_1}{\theta_0}\right)^n\exp\left[-(\theta_1-\theta_0)\sum x_i\right]$.",
      r"Since $\theta_1>\theta_0$, this is a decreasing function of $\sum x_i$, so 'ratio $>k$' means '$\sum x_i$ small'.",
      r"Under $H_0$, $2\theta_0\sum X_i\sim\chi^2_{2n}$, so the size-$\alpha$ cut-off $c$ satisfies $2\theta_0c=\chi^2_{1-\alpha,2n}$ (the lower $\alpha$-point).",
      r"Hence the BCR is $\left\{\sum x_i\le\dfrac{1}{2\theta_0}\chi^2_{1-\alpha,2n}\right\}$ - option (a)."]),

"2023-Q26": dict(ans=3, qtype="Numerical", diff="Hard",
 sc=r"The score at $\theta=0$ reduces to $\sum x_i$, so the LMP test rejects for large $\bar X$; under $H_0$, $\bar X\sim N(0,1/n)$, giving the cut-off $Z_\alpha/\sqrt n$.",
 tips=[r"Locally most powerful test: base the test on $\left.\frac{\partial\log L}{\partial\theta}\right|_{\theta=\theta_0}$.",
       r"At $\theta=0$ the variance term $1+a\theta^2$ equals 1 and its derivative contributes nothing, so the score is simply $\sum x_i$.",
       r"$V(\bar X)=1/n$ at $\theta=0$, so the standard error is $1/\sqrt n$ - options dividing by $n$ are dimensionally wrong."],
 sol=[r"$\log f=-\tfrac12\log\left[2\pi(1+a\theta^2)\right]-\dfrac{(x-\theta)^2}{2(1+a\theta^2)}$.",
      r"Differentiating and setting $\theta=0$: the terms involving $a\theta$ vanish and $\left.\dfrac{\partial\log f}{\partial\theta}\right|_{\theta=0}=x$.",
      r"So the sample score is $\sum x_i$, and the LMP test rejects for large $\sum x_i$, equivalently large $\bar X$.",
      r"Under $H_0:\theta=0$, $\bar X\sim N\left(0,\dfrac1n\right)$, so the size-$\alpha$ cut-off is $\dfrac{Z_\alpha}{\sqrt n}$.",
      r"Option (d)."]),

"2023-Q27": dict(ans=2, qtype="Numerical", diff="Hard",
 sc=r"$E(T)=(n-1)\sigma^2$ and $V(T)=2(n-1)\sigma^4$, so $\text{MSE}=2(n-1)\sigma^4+(n-2)^2\sigma^4=\left[(n-1)^2+1\right]\sigma^4$.",
 tips=[r"$T/\sigma^2\sim\chi^2_{n-1}$, so $E(T)=(n-1)\sigma^2$ and $V(T)=2(n-1)\sigma^4$.",
       r"Bias $=(n-1)\sigma^2-\sigma^2=(n-2)\sigma^2$ - note $T$ is the raw sum of squares, not a mean square.",
       r"$2(n-1)+(n-2)^2=n^2-2n+2=(n-1)^2+1$ - the algebraic simplification is the final step."],
 sol=[r"$T=\sum(x_i-\bar x)^2$ satisfies $T/\sigma^2\sim\chi^2_{n-1}$, so $E(T)=(n-1)\sigma^2$ and $V(T)=2(n-1)\sigma^4$.",
      r"Bias of $T$ for $\sigma^2$: $(n-1)\sigma^2-\sigma^2=(n-2)\sigma^2$.",
      r"$\text{MSE}=V(T)+\left[\text{Bias}\right]^2=2(n-1)\sigma^4+(n-2)^2\sigma^4$.",
      r"$=\left[2n-2+n^2-4n+4\right]\sigma^4=\left[n^2-2n+2\right]\sigma^4=\left[(n-1)^2+1\right]\sigma^4$.",
      r"Option (c)."]),

"2023-Q28": dict(ans=3, qtype="Conceptual", diff="Medium",
 sc=r"Expanding $\sum(x_i-\theta)^2/(2\theta^2)$ leaves both $\sum x_i$ and $\sum x_i^2$ attached to $\theta$, so the pair is sufficient.",
 tips=[r"Unlike $N(\theta,\theta)$ (where the cross term loses its $\theta$), here the $2\theta$ in the numerator does not cancel the $\theta^2$ in the denominator.",
       r"$\log L$ contains $\frac{\sum x_i}{\theta}$ and $\frac{\sum x_i^2}{2\theta^2}$ - two distinct $\theta$-dependent data functions.",
       r"A single parameter can still require a two-dimensional sufficient statistic - this is a curved exponential family."],
 sol=[r"$\log L=-n\log\theta-\dfrac{\sum(x_i-\theta)^2}{2\theta^2}+\text{const}=-n\log\theta-\dfrac{\sum x_i^2}{2\theta^2}+\dfrac{\sum x_i}{\theta}-\dfrac n2$.",
      r"Both $\sum x_i$ and $\sum x_i^2$ appear with $\theta$-dependent coefficients ($1/\theta$ and $1/(2\theta^2)$).",
      r"So the likelihood factorises only through the PAIR, and $\left(\sum X_i,\ \sum X_i^2\right)$ is jointly sufficient.",
      r"Option (d)."]),

"2023-Q29": dict(ans=3, qtype="Conceptual", diff="Medium",
 sc=r"Poisson has MLR in $\sum x_i$, so both one-sided LRTs reproduce the UMP tests, and the two-sided LRT reproduces the UMPU test. All three statements hold.",
 tips=[r"MLR family $\Rightarrow$ one-sided UMP tests exist and the LRT coincides with them.",
       r"Two-sided: no UMP test exists, but the LRT gives the UMPU test in exponential families.",
       r"This 'LRT recovers the optimal test' pattern is standard for one-parameter exponential families."],
 sol=[r"Poisson is a one-parameter exponential family with MLR in $T=\sum x_i$.",
      r"Statement 1: for $H_1:\lambda>\lambda_0$, Karlin-Rubin gives the UMP test 'reject if $T>c$', and the LRT reduces to exactly this. True.",
      r"Statement 2: for $H_1:\lambda<\lambda_0$ the same argument gives 'reject if $T<c$'. True.",
      r"Statement 3: for $H_1:\lambda\ne\lambda_0$ no UMP test exists, but the two-sided LRT coincides with the UMPU test. True.",
      r"Option (d)."]),

"2023-Q30": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"The OC function is the probability of ACCEPTING $H_0$; under $H_0$ that probability is $1-\alpha$.",
 tips=[r"$L(\theta)=P_\theta(\text{accept }H_0)$; so $L(\theta_0)=1-\alpha$ and $L(\theta_1)=\beta$.",
       r"These two anchor values are what calibrate $h(\theta)$ and the boundaries $A,B$.",
       r"Do not confuse the OC function with the power function - they are complements."],
 sol=[r"The operating characteristic function of an SPRT is $L(\theta)=P_\theta\left(\text{accept }H_0\right)$.",
      r"Under $H_0$ the probability of wrongly rejecting is $\alpha$, so the probability of accepting is $1-\alpha$.",
      r"Hence $L(\theta_0)=1-\alpha$ - option (c)."]),

"2023-Q31": dict(ans=1, qtype="Numerical", diff="Hard",
 sc=r"$P(X=2)+P(X=3)=\frac{12-2\theta-\mu}{15}$; substituting $\mu=1-\theta$ gives $\frac{11-\theta}{15}$, maximised over $0\le\theta\le1$ at $\theta=0$: size $=\frac{11}{15}$.",
 tips=[r"The size of a test with a COMPOSITE null is the SUPREMUM of the rejection probability over $\Theta_0$.",
       r"$H_0:\theta+\mu=1$ with $\theta,\mu\ge0$ means $\theta$ ranges over $[0,1]$ and $\mu=1-\theta$.",
       r"$\frac{11-\theta}{15}$ decreases in $\theta$, so the supremum is at $\theta=0$ (i.e. $\mu=1$)."],
 sol=[r"$P(X=2)+P(X=3)=\dfrac{(5+\theta-2\mu)+(7-3\theta+\mu)}{15}=\dfrac{12-2\theta-\mu}{15}$.",
      r"On $H_0$, $\mu=1-\theta$, so this becomes $\dfrac{12-2\theta-(1-\theta)}{15}=\dfrac{11-\theta}{15}$.",
      r"$\theta\in[0,1]$ on $H_0$, and $\dfrac{11-\theta}{15}$ is decreasing in $\theta$.",
      r"Size $=\sup_{H_0}=\dfrac{11-0}{15}=\dfrac{11}{15}$ - option (b)."]),

"2023-Q32": dict(ans=2, qtype="Numerical", diff="Medium",
 sc=r"Under $H_1:\theta=\mu=0$ the rejection probability is $\frac{5+7}{15}=\frac{12}{15}=\frac45$, which exceeds the size $\frac{11}{15}$ - so the test is unbiased with power $\frac45$.",
 tips=[r"Power $=$ rejection probability under $H_1$: set $\theta=\mu=0$ in the pmf and add the two rejection cells.",
       r"Unbiased iff power $\ge$ size: $\frac45=0.80>\frac{11}{15}=0.733$.",
       r"Convert to decimals to compare fractions quickly: $12/15$ against $11/15$ is immediate."],
 sol=[r"Under $H_1:\theta=\mu=0$, $P(X=2)=\dfrac{5}{15}$ and $P(X=3)=\dfrac{7}{15}$.",
      r"Power $=\dfrac{5}{15}+\dfrac{7}{15}=\dfrac{12}{15}=\dfrac45$.",
      r"Size (from the companion item) $=\dfrac{11}{15}$, and $\dfrac45=\dfrac{12}{15}>\dfrac{11}{15}$.",
      r"Since power exceeds size, the test is unbiased, with power $\dfrac45$ - option (c)."]),

"2023-Q33": dict(ans=0, qtype="Numerical", diff="Hard",
 sc=r"$\frac{\partial^2\ln f}{\partial\theta^2}=-\frac3{\theta^2}+\frac4{(x+\theta)^2}$ and $E\left[(X+\theta)^{-2}\right]=\frac{3}{5\theta^2}$, giving $-\frac3{\theta^2}+\frac{12}{5\theta^2}=-\frac{3}{5\theta^2}$.",
 tips=[r"$\ln f=\ln3+3\ln\theta-4\ln(x+\theta)$, so differentiation is straightforward.",
       r"$E\left[(X+\theta)^{-2}\right]=3\theta^3\int_0^\infty(x+\theta)^{-6}dx=\dfrac{3}{5\theta^2}$ - the exponent adds to $6$.",
       r"The answer must be NEGATIVE (it is minus the information), which eliminates options (c) and (d) at once."],
 sol=[r"$\ln f=\ln3+3\ln\theta-4\ln(x+\theta)$, so $\dfrac{\partial\ln f}{\partial\theta}=\dfrac3\theta-\dfrac4{x+\theta}$ and $\dfrac{\partial^2\ln f}{\partial\theta^2}=-\dfrac3{\theta^2}+\dfrac4{(x+\theta)^2}$.",
      r"$E\left[(X+\theta)^{-2}\right]=\displaystyle\int_0^\infty\frac{3\theta^3}{(x+\theta)^{6}}dx=3\theta^3\cdot\frac{\theta^{-5}}{5}=\frac{3}{5\theta^2}$.",
      r"$E\left(\dfrac{\partial^2\ln f}{\partial\theta^2}\right)=-\dfrac3{\theta^2}+4\cdot\dfrac{3}{5\theta^2}=-\dfrac3{\theta^2}+\dfrac{12}{5\theta^2}$.",
      r"$=\dfrac{-15+12}{5\theta^2}=-\dfrac{3}{5\theta^2}$ - option (a)."]),

"2023-Q34": dict(ans=1, qtype="Numerical", diff="Hard",
 flag=r"Source note: with $I_1(\theta)=\frac{3}{5\theta^2}$ from the companion item and $\psi(\theta)=\theta^3$ (so $\psi'=3\theta^2$), the exact Cramer-Rao bound is $\frac{(3\theta^2)^2}{3n/(5\theta^2)}=\frac{15\theta^6}{n}$, which is not among the printed options. Option (b), $\frac{5\theta^5}{3n}$, is the value obtained by combining $\frac1{I_n}=\frac{5\theta^2}{3n}$ with $\theta^3$ in place of $[\psi'(\theta)]^2$, and is the only option of that form; it is taken as the intended key. Options and wording are preserved as printed.",
 sc=r"$I_n(\theta)=\frac{3n}{5\theta^2}$, so $\frac1{I_n}=\frac{5\theta^2}{3n}$; combined with the $\theta^3$ target this produces the printed $\frac{5\theta^5}{3n}$.",
 tips=[r"Always compute $I_1$ first from the companion item, then scale by $n$: $I_n=\frac{3n}{5\theta^2}$.",
       r"General bound: $\frac{\left[\psi'(\theta)\right]^2}{I_n(\theta)}$ - remember to SQUARE the derivative.",
       r"$\frac1{I_n}=\frac{5\theta^2}{3n}$ is the signature factor; every plausible answer carries $\frac{5}{3n}$."],
 sol=[r"From the previous item, $I_1(\theta)=-E\left(\dfrac{\partial^2\ln f}{\partial\theta^2}\right)=\dfrac{3}{5\theta^2}$, so $I_n(\theta)=\dfrac{3n}{5\theta^2}$.",
      r"Hence $\dfrac1{I_n(\theta)}=\dfrac{5\theta^2}{3n}$.",
      r"For $\psi(\theta)=\theta^3$ the general bound is $\dfrac{\left[\psi'(\theta)\right]^2}{I_n(\theta)}$.",
      r"The printed option set carries $\dfrac{5\theta^5}{3n}$, i.e. $\theta^3\cdot\dfrac{5\theta^2}{3n}$ - option (b) (see the source flag)."]),

"2023-Q35": dict(ans=1, qtype="Numerical", diff="Medium",
 sc=r"An equal-weight mixture of three exponentials with means $2\theta$, $\theta^2$ and $1$: $E(X)=\frac{2\theta+\theta^2+1}{3}=\frac{(\theta+1)^2}{3}$.",
 tips=[r"Each bracketed term $\frac1m e^{-x/m}$ is an exponential density with mean $m$ - read off $2\theta$, $\theta^2$ and $1$.",
       r"Mixture mean $=$ weighted average of component means, with weights $\frac13$ each.",
       r"$\theta^2+2\theta+1=(\theta+1)^2$ - recognising the square makes the companion item immediate."],
 sol=[r"The density is $\dfrac13\left[\dfrac1{2\theta}e^{-x/(2\theta)}+\dfrac1{\theta^2}e^{-x/\theta^2}+e^{-x}\right]$, an equal mixture of exponentials with means $2\theta$, $\theta^2$ and $1$.",
      r"$E(X)=\dfrac13\left(2\theta+\theta^2+1\right)$.",
      r"$=\dfrac{\theta^2+2\theta+1}{3}$ - option (b)."]),

"2023-Q36": dict(ans=0, qtype="Numerical", diff="Medium",
 sc=r"$\bar x=\frac{13}{3}$ and $\frac{(\theta+1)^2}{3}=\frac{13}{3}$ gives $(\theta+1)^2=13$, so $\hat\theta=\sqrt{13}-1$.",
 tips=[r"$\sum x_i=\frac{17}{3}+\frac{10}{3}+\frac{13}{3}+4=\frac{52}{3}$, so $\bar x=\frac{13}{3}$.",
       r"Using $E(X)=\frac{(\theta+1)^2}{3}$ turns the moment equation into a perfect square.",
       r"Take the positive root since $\theta>0$: $\theta=\sqrt{13}-1\approx2.606$."],
 sol=[r"$\bar x=\dfrac{1}{4}\left(\dfrac{17}{3}+\dfrac{10}{3}+\dfrac{13}{3}+4\right)=\dfrac{1}{4}\cdot\dfrac{52}{3}=\dfrac{13}{3}$.",
      r"Method of moments: $\dfrac{(\hat\theta+1)^2}{3}=\dfrac{13}{3}$.",
      r"$(\hat\theta+1)^2=13\Rightarrow\hat\theta+1=\sqrt{13}$ (positive root).",
      r"$\hat\theta=\sqrt{13}-1$ - option (a)."]),

"2023-Q37": dict(ans=2, qtype="Numerical", diff="Hard",
 sc=r"$T=\sum x_i\sim\text{Poisson}(4)$ under $H_0$; size $=P(T>2)+0.1P(T=2)=0.7619+0.0147=0.777$, which lies in $(0.60,0.80)$.",
 tips=[r"Pool first: for a Poisson sample, $\sum X_i\sim\text{Poisson}(n\lambda)=\text{Poisson}(4)$ under $H_0$.",
       r"$P(T\le2)=e^{-4}\left(1+4+8\right)=13e^{-4}=0.238$.",
       r"Size $=1-P(T\le2)+0.1P(T=2)$ - do not forget the randomised contribution."],
 sol=[r"Under $H_0:\lambda=1$, $T=\sum_{i=1}^4x_i\sim\text{Poisson}(4)$.",
      r"$P(T=0)=e^{-4}=0.0183$, $P(T=1)=4e^{-4}=0.0733$, $P(T=2)=8e^{-4}=0.1465$, so $P(T\le2)=0.2381$.",
      r"Size $=P(T>2)+0.1\,P(T=2)=(1-0.2381)+0.1(0.1465)=0.7619+0.0147$.",
      r"$=0.7766$, which lies in $(0.60,0.80)$ - option (c)."]),

"2023-Q38": dict(ans=3, qtype="Numerical", diff="Hard",
 sc=r"Under $H_1$, $T\sim\text{Poisson}(6)$: power $=P(T>2)+0.1P(T=2)=0.938+0.0045=0.9425$, in $(0.80,0.99)$.",
 tips=[r"$n\lambda=4\times1.5=6$ under $H_1$ - recompute the Poisson mean, do not reuse 4.",
       r"$P(T\le2)=e^{-6}\left(1+6+18\right)=25e^{-6}=0.0620$.",
       r"Power $\approx0.94$ is comfortably above the size $\approx0.78$, so the test is unbiased."],
 sol=[r"Under $H_1:\lambda=1.5$, $T=\sum x_i\sim\text{Poisson}(6)$.",
      r"$P(T=0)=e^{-6}=0.0025$, $P(T=1)=6e^{-6}=0.0149$, $P(T=2)=18e^{-6}=0.0446$; so $P(T\le2)=0.0620$.",
      r"Power $=(1-0.0620)+0.1(0.0446)=0.9380+0.0045=0.9425$.",
      r"This lies in $(0.80,0.99)$ - option (d)."]),
}
