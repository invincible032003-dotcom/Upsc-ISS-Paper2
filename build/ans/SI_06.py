# -*- coding: utf-8 -*-
# Statistical Inference answer pack 6 (2020 Q39-Q70, 2021 Q11-Q26)
A = {
"2020-Q39": dict(ans=2, qtype="Numerical", diff="Medium",
 sc=r"$V(\hat\theta_1)=\frac{\theta(1-\theta)}{n}=\frac1{4n}$ and $\text{MSE}(\hat\theta_2)=\left(\frac13-\frac12\right)^2=\frac1{36}$. $\frac1{36}<\frac1{4n}\iff n<9$.",
 tips=[r"A constant estimator has zero variance, so its MSE is pure squared bias.",
       r"$\theta(1-\theta)=\tfrac14$ at $\theta=\tfrac12$ - the maximum possible binomial variance.",
       r"$n<9$ means $n\le8$; the strict inequality at $n=9$ gives equality, so $9$ is excluded."],
 sol=[r"$\hat\theta_1=X/n$ with $X\sim B(n,\theta)$: $E(\hat\theta_1)=\theta$ and $V(\hat\theta_1)=\dfrac{\theta(1-\theta)}{n}=\dfrac{1}{4n}$ at $\theta=\tfrac12$.",
      r"$\hat\theta_2=\tfrac13$ is a constant, so $\text{MSE}(\hat\theta_2)=\left(\tfrac13-\tfrac12\right)^2=\dfrac1{36}$.",
      r"Require $\dfrac1{36}<\dfrac1{4n}$, i.e. $4n<36$, i.e. $n<9$.",
      r"So $1\le n\le8$ - option (c)."]),

"2020-Q40": dict(ans=1, qtype="Numerical", diff="Easy",
 sc=r"$E(X)=\theta/3$ for this decreasing triangular density, so the moment estimator is $\hat\theta=3\bar X$.",
 tips=[r"$\int_0^\theta x\cdot\frac{2(\theta-x)}{\theta^2}dx=\frac{2}{\theta^2}\left[\frac{\theta^3}{2}-\frac{\theta^3}{3}\right]=\frac\theta3$.",
       r"For the mirror-image density $\frac{2x}{\theta^2}$ the mean would be $\frac{2\theta}{3}$ - check which way the triangle leans.",
       r"Method of moments: set $E(X)=\bar X$ and solve; no likelihood needed."],
 sol=[r"$E(X)=\displaystyle\int_0^\theta x\cdot\frac{2(\theta-x)}{\theta^2}dx=\frac{2}{\theta^2}\left[\frac{\theta\cdot\theta^2}{2}-\frac{\theta^3}{3}\right]=\frac{2}{\theta^2}\cdot\frac{\theta^3}{6}=\frac{\theta}{3}$.",
      r"Equating the population and sample first moments: $\dfrac{\hat\theta}{3}=\bar X$.",
      r"Hence $\hat\theta=3\bar X$ - option (b)."]),

"2020-Q61": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"Width $\propto\sigma=\sqrt{\text{variance}}$, so doubling the variance multiplies the width by $\sqrt2$.",
 tips=[r"CI width $=2z_{\alpha/2}\sigma/\sqrt n$ - linear in $\sigma$, hence square-root in the variance.",
       r"Doubling the SAMPLE SIZE, by contrast, divides the width by $\sqrt2$.",
       r"Confusing variance with standard deviation is the single commonest error in this topic."],
 sol=[r"The confidence interval for $\mu$ with known variance is $\bar X\pm z_{\alpha/2}\dfrac{\sigma}{\sqrt n}$, so its width is $2z_{\alpha/2}\dfrac{\sigma}{\sqrt n}$.",
      r"If the variance $\sigma^2$ is doubled, the standard deviation becomes $\sqrt{2}\,\sigma$.",
      r"Hence the width is multiplied by $\sqrt2$ - option (c)."]),

"2020-Q62": dict(ans=1, qtype="Numerical", diff="Easy",
 sc=r"With $\sigma$ known the interval is symmetric about $\bar x$, so $\bar x$ is the midpoint $\frac{18000+22000}{2}=20000$.",
 tips=[r"Symmetric interval $\Rightarrow$ sample mean $=$ midpoint; half-width $=z_{\alpha/2}\sigma/\sqrt n$.",
       r"Here the half-width is $2000$, so $\sigma/\sqrt n=2000/1.96=1020.4$ if that is ever asked.",
       r"The same trick recovers the mean in the 2025 and 2026 versions of this item."],
 sol=[r"For known $\sigma$ the confidence interval is $\left(\bar x-z_{\alpha/2}\dfrac{\sigma}{\sqrt n},\ \bar x+z_{\alpha/2}\dfrac{\sigma}{\sqrt n}\right)$, symmetric about $\bar x$.",
      r"Hence $\bar x$ is the midpoint: $\dfrac{18000+22000}{2}=20000$.",
      r"Option (b)."]),

"2020-Q63": dict(ans=1, qtype="Definition", diff="Easy",
 sc=r"The factorisation criterion for sufficiency is the Fisher-Neyman theorem.",
 tips=[r"Fisher-Neyman: $f(x;\theta)=g_\theta(T(x))h(x)$ characterises sufficiency.",
       r"Rao-Blackwell is about improving an unbiased estimator; Cramer-Rao gives a variance bound.",
       r"'Bernoulli's theorem' is the law of large numbers for binomial proportions - unrelated."],
 sol=[r"The result stating that $T$ is sufficient if and only if the joint density factorises as $g(t,\theta)h(x)$ is due to Fisher and, in its general measure-theoretic form, Neyman.",
      r"It is standardly called the Fisher-Neyman factorisation theorem.",
      r"Option (b)."]),

"2020-Q64": dict(ans=1, qtype="Conceptual", diff="Easy",
 sc=r"$f=g(t,\theta)\cdot h(x_1,\dots,x_n)$, where $h$ must be FREE of $\theta$ and $g$ depends on the data only through $t$.",
 tips=[r"Two requirements: $g$ touches the data only via $t$; $h$ does not involve $\theta$ at all.",
       r"Options (a) and (c) let $h$ depend on $\theta$ - fatal; option (d) has $g$ depending on the raw data.",
       r"Write out the factorisation explicitly for one family (say Poisson) and the form becomes automatic."],
 sol=[r"The Fisher-Neyman criterion: $T=t(x_1,\dots,x_n)$ is sufficient for $\theta$ iff the joint density can be written as",
      r"$f(x_1,\dots,x_n;\theta)=g\left(t,\theta\right)\cdot h(x_1,\dots,x_n)$,",
      r"where $g$ involves the data only through $t$, and $h$ is free of $\theta$.",
      r"Option (b)."]),

"2020-Q65": dict(ans=0, qtype="Definition", diff="Easy",
 sc=r"The expected loss is the risk function $R(\theta,\delta)=E_\theta\left[L(\theta,\delta(X))\right]$.",
 tips=[r"Risk is loss averaged over the sampling distribution, for a FIXED $\theta$.",
       r"Bayes risk averages the risk further over the prior; do not confuse the two.",
       r"The power function belongs to testing, not to decision-theoretic estimation."],
 sol=[r"In decision theory, a rule $\delta$ incurs loss $L(\theta,\delta(x))$ when $x$ is observed.",
      r"Averaging this over the sampling distribution of $X$ for a fixed $\theta$ gives $R(\theta,\delta)=E_\theta\left[L(\theta,\delta(X))\right]$.",
      r"This is called the risk function - option (a)."]),

"2020-Q66": dict(ans=0, qtype="Numerical", diff="Easy",
 sc=r"$\beta=P(\text{accept }H_0\mid H_1)=P(X<1\mid\theta=1)=1-e^{-1}=\frac{e-1}{e}$.",
 tips=[r"Type-II error is computed under $H_1$ ($\theta=1$), over the ACCEPTANCE region $x<1$.",
       r"$P(X<c)=1-e^{-\theta c}$ for an exponential with rate $\theta$.",
       r"The size (Type-I error) would be $P(X\ge1\mid\theta=2)=e^{-2}$, planted as option (b)."],
 sol=[r"The critical region is $x\ge1$, so the acceptance region is $x<1$.",
      r"Type-II error $\beta=P(\text{accept }H_0\mid H_1\text{ true})=P(X<1\mid\theta=1)$.",
      r"$P(X<1\mid\theta=1)=\displaystyle\int_0^1 e^{-x}dx=1-e^{-1}=\dfrac{e-1}{e}$.",
      r"Option (a)."]),

"2020-Q67": dict(ans=1, qtype="Conceptual", diff="Medium",
 sc=r"The shortest interval with $\sigma$ unknown is $\bar X\pm t_{n-1}\dfrac{s}{\sqrt n}$ - divisor $\sqrt n$, not $2\sqrt n$, and $n-1$ degrees of freedom.",
 tips=[r"Standard error of $\bar X$ is $s/\sqrt n$; an extra factor of 2 in the denominator is always wrong.",
       r"Degrees of freedom are $n-1$ because $s$ estimates $\sigma$ - option (d) uses $n$ and fails.",
       r"Here $t_{n-1,\alpha}$ denotes the two-tailed critical value cutting off total probability $\alpha$, so option (b) is the intended standard form."],
 sol=[r"With $\sigma$ unknown, the pivot is $\dfrac{\sqrt n(\bar X-\mu)}{s}\sim t_{n-1}$.",
      r"Inverting gives $\bar X\pm t_{n-1}\dfrac{s}{\sqrt n}$, and by symmetry and unimodality of the $t$ density the equal-tailed version is the shortest.",
      r"Options (a), (c) and (d) all insert a spurious factor of 2 in the denominator or use the wrong degrees of freedom.",
      r"Option (b)."]),

"2020-Q68": dict(ans=0, qtype="Conceptual", diff="Easy",
 sc=r"Unbiased means power $\ge$ size, i.e. $1-\beta\ge\alpha$, i.e. $\alpha+\beta\le1$.",
 tips=[r"Rearrange the definition: power $=1-\beta\ge\alpha\iff\alpha+\beta\le1$.",
       r"A test with $\alpha+\beta>1$ is worse than tossing a coin - it is biased.",
       r"The strict version $\alpha+\beta<1$ is the intended answer here."],
 sol=[r"A critical region $w$ is unbiased if the probability of rejecting $H_0$ when $H_1$ is true is at least the probability of rejecting when $H_0$ is true.",
      r"That is, power $\ge$ size: $1-\beta\ge\alpha$.",
      r"Rearranging, $\alpha+\beta\le1$.",
      r"Option (a)."]),

"2020-Q69": dict(ans=2, qtype="Numerical", diff="Easy",
 sc=r"Paired $t$-test on $n$ pairs uses the $n$ differences, so df $=n-1=9$.",
 tips=[r"Paired design reduces the problem to a ONE-sample test on the differences.",
       r"df $=$ (number of pairs) $-1$, never (number of observations) $-1$.",
       r"$18$ would be the df for two independent samples of size 10 each - the planted distractor."],
 sol=[r"In a paired $t$-test the data are reduced to $n$ differences $d_i=x_i-y_i$.",
      r"A one-sample $t$-test on these $n$ differences uses $s_d$, which costs one degree of freedom.",
      r"df $=n-1=10-1=9$ - option (c)."]),

"2020-Q70": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"$\{\mu=\mu_0,\ \sigma^2=\sigma_0^2\}$ specifies the distribution completely - it is simple, hence NOT composite.",
 tips=[r"Scan each option for an inequality; the one with none is the simple hypothesis.",
       r"Both parameters are unknown, so both must be fixed for simplicity.",
       r"'Which is NOT composite' means 'which is simple' - read the negation carefully."],
 sol=[r"With both $\mu$ and $\sigma^2$ unknown, a hypothesis is simple only if it fixes both.",
      r"(a), (b) and (d) each leave at least one parameter free through an inequality, so all are composite.",
      r"(c) fixes $\mu=\mu_0$ and $\sigma^2=\sigma_0^2$, specifying the distribution completely - it is simple.",
      r"Hence option (c)."]),

"2021-Q11": dict(ans=1, qtype="Conceptual", diff="Hard",
 sc=r"$X$ is the whole sample, so it is minimal sufficient. It is NOT complete: $g(-1)=1$, $g(x)=-x$ for $x\ge0$ gives $E_\theta[g(X)]\equiv0$ with $g\not\equiv0$.",
 tips=[r"To disprove completeness, exhibit one non-trivial unbiased estimator of zero.",
       r"Use $\sum_{x\ge0}x\theta^x=\dfrac{\theta}{(1-\theta)^2}$ - exactly the reciprocal of the $(1-\theta)^2$ factor in the pmf.",
       r"$E(X)=-\theta+\dfrac{\theta}{1-\theta}\cdot(\ldots)\ne\theta$, so option (d) fails as well."],
 sol=[r"With a single observation, $X$ itself is trivially sufficient, and no coarser statistic is - so $X$ is minimal sufficient.",
      r"For completeness, take $g(-1)=1$ and $g(x)=-x$ for $x=0,1,2,\dots$.",
      r"$E_\theta[g(X)]=1\cdot\theta+(1-\theta)^2\sum_{x\ge0}(-x)\theta^{x}=\theta-(1-\theta)^2\cdot\dfrac{\theta}{(1-\theta)^2}=\theta-\theta=0$ for every $\theta\in(0,1)$.",
      r"Since $g\not\equiv0$, the family is not complete.",
      r"Hence $X$ is minimal sufficient only - option (b)."]),

"2021-Q12": dict(ans=2, qtype="Conceptual", diff="Medium",
 sc=r"$T=X_{(1)}$ is NOT sufficient for $U(0,\theta)$ (the sufficient statistic is $X_{(n)}$), so the conditional expectation still involves $\theta$.",
 tips=[r"Rao-Blackwell requires conditioning on a SUFFICIENT statistic; otherwise the result is not even a statistic.",
       r"For $U(0,\theta)$ the sufficient statistic is the maximum $X_{(n)}$, not the minimum.",
       r"Conditioning on $X_{(n)}$ would give the UMVUE $\frac{n+1}{n}X_{(n)}$ - the contrast is the point of the question."],
 sol=[r"For $U(0,\theta)$ the likelihood is $\theta^{-n}I\left(X_{(n)}\le\theta\right)$, so $X_{(n)}$ is sufficient while $X_{(1)}$ is not.",
      r"Because $T=X_{(1)}$ is not sufficient, the conditional distribution of the sample given $T=t$ depends on $\theta$.",
      r"Hence $E[U\mid T=t]$ is a function of $\theta$ and is not a statistic at all.",
      r"Option (c)."]),

"2021-Q13": dict(ans=2, qtype="Conceptual", diff="Hard",
 sc=r"The joint density is $\frac1{2\theta^2}\exp\left[-\frac{2x+y}{2\theta}\right]$, so $Y+2X$ is sufficient; with a scale parameter, small values of $Y+2X$ favour small $\theta$, giving a left-tail region.",
 tips=[r"Put both exponents over the common denominator $2\theta$: $\frac x\theta+\frac y{2\theta}=\frac{2x+y}{2\theta}$.",
       r"The weight on $X$ is 2 because $X$ has HALF the mean of $Y$ - weight inversely to the scale.",
       r"Option (d) is internally inconsistent (it calls $Y+2X<C$ a right-tail test), so it can be discarded on wording alone."],
 sol=[r"$f(x,y;\theta)=\dfrac1\theta e^{-x/\theta}\cdot\dfrac1{2\theta}e^{-y/(2\theta)}=\dfrac1{2\theta^2}\exp\left[-\dfrac{2x+y}{2\theta}\right]$.",
      r"By the factorisation theorem the data enter only through $T=2X+Y=Y+2X$, so $T$ is sufficient for $\theta$.",
      r"The family has monotone likelihood ratio in $T$, so UMP one-sided tests are based on $T$; a test against small $\theta$ rejects for small $T$, i.e. the left-tail region $Y+2X<C$.",
      r"Option (c) states the correct sufficient statistic with a consistently labelled left-tail region - option (c)."]),

"2021-Q14": dict(ans=1, qtype="Numerical", diff="Medium",
 sc=r"Partial sums are $1,2,2,3,3,3$. At $m=4$, $r_4=2.45$ and $s_4=3>r_4$, so the upper boundary is crossed: reject $H_0$ at the 4th stage.",
 tips=[r"Tabulate $s_m$, $a_m$, $r_m$ and stop at the first $m$ where $s_m$ leaves the band.",
       r"$s_m\ge r_m$ (upper boundary) $\Rightarrow$ reject $H_0$; $s_m\le a_m$ (lower boundary) $\Rightarrow$ accept $H_0$.",
       r"Once a boundary is crossed the procedure STOPS - later observations are irrelevant, so options mentioning stage 6 are wrong."],
 sol=[r"Cumulative sums of $\{1,1,0,1,0,0\}$: $s_1=1$, $s_2=2$, $s_3=2$, $s_4=3$, $s_5=3$, $s_6=3$.",
      r"Boundaries: $a_m=0.90+0.05m$ gives $0.95,1.00,1.05,1.10,\dots$; $r_m=2.25+0.05m$ gives $2.30,2.35,2.40,2.45,\dots$",
      r"$m=1,2,3$: $s_m$ lies strictly between $a_m$ and $r_m$ ($1\in(0.95,2.30)$, $2\in(1.00,2.35)$, $2\in(1.05,2.40)$) - continue.",
      r"$m=4$: $s_4=3>2.45=r_4$, so the upper boundary is crossed and $H_0$ is rejected at the 4th stage.",
      r"Option (b)."]),

"2021-Q15": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"$\sum X_i$ is complete sufficient for the exponential scale, and $\bar X$ is its unbiased function - hence the UMVUE.",
 tips=[r"Exponential with mean $\alpha$: $E(X)=\alpha$, so $\bar X$ is immediately unbiased.",
       r"Lehmann-Scheffe then upgrades it to UMVUE because $\sum X_i$ is complete sufficient.",
       r"$V(\bar X)=\alpha^2/n$, exactly the Cramer-Rao bound, so $\bar X$ is also MVB."],
 sol=[r"$L(\alpha)=\alpha^{-n}\exp\left(-\dfrac{\sum x_i}{\alpha}\right)$, so $T=\sum X_i$ is sufficient, and being the natural statistic of a full-rank exponential family, complete.",
      r"$E(\bar X)=\alpha$, and $\bar X=T/n$ is a function of $T$.",
      r"By the Lehmann-Scheffe theorem, $\bar X$ is the unique UMVUE of $\alpha$.",
      r"Option (c)."]),

"2021-Q16": dict(ans=0, qtype="Numerical", diff="Hard",
 sc=r"$T=\frac1n\sum X_i^2$ has $E(T)=\sigma^2$, $V(T)=\frac{2\sigma^4}n$; inverting $\left|\frac{T-\sigma^2}{\sigma^2\sqrt{2/n}}\right|<1.96$ gives $\left(\frac{T}{1+1.96\sqrt{2/n}},\ \frac{T}{1-1.96\sqrt{2/n}}\right)$.",
 tips=[r"$\sum X_i^2/\sigma^2\sim\chi^2_n$ with mean $n$ and variance $2n$, hence $V(T)=2\sigma^4/n$.",
       r"The estimator must be the AVERAGE $\frac1n\sum X_i^2$, not the raw sum - options (c) and (d) fail on that alone.",
       r"$1.96$ is the $95\%$ two-sided multiplier; $1.645$ in option (d) corresponds to $90\%$."],
 sol=[r"With $\mu=0$ known, $T=\dfrac1n\sum X_i^2$ satisfies $E(T)=\sigma^2$ and $V(T)=\dfrac{2\sigma^4}{n}$ (since $\sum X_i^2/\sigma^2\sim\chi^2_n$).",
      r"For large $n$, $\dfrac{T-\sigma^2}{\sigma^2\sqrt{2/n}}\approx N(0,1)$.",
      r"$P\left(-1.96<\dfrac{T-\sigma^2}{\sigma^2\sqrt{2/n}}<1.96\right)=0.95$ rearranges to $\dfrac{T}{1+1.96\sqrt{2/n}}<\sigma^2<\dfrac{T}{1-1.96\sqrt{2/n}}$.",
      r"Option (a)."]),

"2021-Q17": dict(ans=0, qtype="Conceptual", diff="Medium",
 sc=r"Only consistency survives squaring (continuous mapping). Unbiasedness does not ($E(T^2)=V(T)+\theta^2>\theta^2$), and $T^2$ is a many-to-one function of $T$, so sufficiency is not guaranteed.",
 tips=[r"Consistency is preserved under any continuous transformation - Slutsky/continuous mapping theorem.",
       r"Unbiasedness is destroyed by any strictly convex transformation (Jensen): $E(T^2)>\left[E(T)\right]^2$ unless $V(T)=0$.",
       r"Sufficiency is preserved only under ONE-TO-ONE transformations; squaring merges $t$ and $-t$."],
 sol=[r"Statement 1: $E(T^2)=V(T)+\left[E(T)\right]^2=V(T)+\theta^2>\theta^2$ whenever $V(T)>0$. False.",
      r"Statement 2: if $T\xrightarrow{P}\theta$ then $T^2\xrightarrow{P}\theta^2$ by the continuous mapping theorem. True.",
      r"Statement 3: sufficiency is preserved under one-to-one transformations; $t\mapsto t^2$ is not one-to-one in general, so information can be lost. False.",
      r"Hence 2 only - option (a)."]),

"2021-Q18": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"Convergence in probability is preserved by sums and products (continuous mapping), so both statements hold.",
 tips=[r"$T_1\xrightarrow{P}\theta_1$, $T_2\xrightarrow{P}\theta_2$ $\Rightarrow$ $g(T_1,T_2)\xrightarrow{P}g(\theta_1,\theta_2)$ for continuous $g$.",
       r"Addition and multiplication are continuous, so both sums and products inherit consistency.",
       r"Contrast unbiasedness, which fails for products unless the estimators are independent."],
 sol=[r"Consistency means $T_1\xrightarrow{P}\theta_1$ and $T_2\xrightarrow{P}\theta_2$.",
      r"The map $(u,v)\mapsto u+v$ is continuous, so $T_1+T_2\xrightarrow{P}\theta_1+\theta_2$. Statement 1 true.",
      r"The map $(u,v)\mapsto uv$ is continuous, so $T_1T_2\xrightarrow{P}\theta_1\theta_2$. Statement 2 true.",
      r"Option (c)."]),

"2021-Q19": dict(ans=2, qtype="Numerical", diff="Medium",
 sc=r"$E(a^X)=e^{\theta(a-1)}$ for Poisson; with $a=5$ this is $e^{4\theta}$.",
 tips=[r"Poisson pgf: $E(a^X)=e^{\theta(a-1)}$ - one formula answers the whole family of such questions.",
       r"$a=5\Rightarrow a-1=4$, giving $e^{4\theta}$, not $5^\theta$.",
       r"For an unbiased estimator of $e^{-k\theta}$ you would need $a=1-k$, hence the alternating signs seen elsewhere."],
 sol=[r"$E\left(5^X\right)=\sum_{x\ge0}5^x\dfrac{e^{-\theta}\theta^x}{x!}=e^{-\theta}\sum_{x\ge0}\dfrac{(5\theta)^x}{x!}=e^{-\theta}e^{5\theta}$.",
      r"$=e^{4\theta}$.",
      r"So $5^X$ is unbiased for $e^{4\theta}$ - option (c)."]),

"2021-Q20": dict(ans=0, qtype="Numerical", diff="Easy",
 sc=r"Geometric on $\{1,2,\dots\}$ has $E(X)=1/\theta$; $\bar x=48/12=4$, so $\hat\theta=1/4=0.25$.",
 tips=[r"For $f(x)=\theta(1-\theta)^{x-1}$ (support starting at 1), $E(X)=1/\theta$.",
       r"If the support started at $0$ the mean would be $(1-\theta)/\theta$ - check the support before writing the mean.",
       r"Sum the data once: $4+3+6+3+6+2+3+4+4+3+5+5=48$."],
 sol=[r"For the geometric distribution $f(x)=\theta(1-\theta)^{x-1}$, $x=1,2,\dots$, the mean is $E(X)=\dfrac1\theta$.",
      r"$\sum x_i=4+3+6+3+6+2+3+4+4+3+5+5=48$, so $\bar x=\dfrac{48}{12}=4$.",
      r"Method of moments: $\dfrac1{\hat\theta}=\bar x=4$, so $\hat\theta=0.25$.",
      r"Option (a)."]),

"2021-Q21": dict(ans=0, qtype="Conceptual", diff="Easy",
 sc=r"$\hat\theta=\bar x$ is correct; by invariance the MLE of $\theta^2$ is $\bar x^2$, not $\frac1n\sum x_i^2$.",
 tips=[r"Invariance: MLE of $g(\theta)$ is $g\left(\hat\theta\right)$ - apply $g$ to the estimate, never to the data separately.",
       r"$\frac1n\sum x_i^2$ estimates $E(X^2)=\theta^2+4$, a different quantity.",
       r"The variance $4$ is known here, so it plays no role in estimating $\theta$."],
 sol=[r"For $N(\theta,4)$, $\log L=\text{const}-\dfrac{1}{8}\sum(x_i-\theta)^2$, so $\hat\theta=\bar x$. Statement 1 is true.",
      r"By the invariance property, the MLE of $\theta^2$ is $\left(\hat\theta\right)^2=\bar x^2$.",
      r"$\dfrac1n\sum x_i^2$ has expectation $\theta^2+4\ne\theta^2$ and is not the MLE. Statement 2 is false.",
      r"Hence 1 only - option (a)."]),

"2021-Q22": dict(ans=1, qtype="Numerical", diff="Medium",
 sc=r"$E\left(Y^3\right)=\frac{n\theta^3}{n+3}$, so $\frac{n+3}{n}Y^3$ is unbiased for $\theta^3$.",
 tips=[r"$f_Y(y)=\frac{n y^{n-1}}{\theta^n}$, so $E(Y^r)=\frac{n\theta^r}{n+r}$ - one formula for every power.",
       r"The correction factor always EXCEEDS 1 because the maximum under-estimates $\theta$.",
       r"Option (d) inverts the factor and is the standard trap."],
 sol=[r"$F_Y(y)=\left(\dfrac y\theta\right)^n$, so $f_Y(y)=\dfrac{n y^{n-1}}{\theta^n}$ on $(0,\theta)$.",
      r"$E\left(Y^3\right)=\displaystyle\int_0^\theta y^3\frac{ny^{n-1}}{\theta^n}dy=\frac{n}{\theta^n}\cdot\frac{\theta^{n+3}}{n+3}=\frac{n\theta^3}{n+3}$.",
      r"Hence $E\left[\dfrac{n+3}{n}Y^3\right]=\theta^3$.",
      r"Option (b)."]),

"2021-Q23": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"$x_{(n)}$ is the MLE (the likelihood $\theta^{-n}$ decreases in $\theta$) and it is consistent, since $P\left(\theta-X_{(n)}>\varepsilon\right)=\left(1-\varepsilon/\theta\right)^n\to0$.",
 tips=[r"$X_{(n)}$ is biased ($E=\frac{n\theta}{n+1}$) but consistent - bias and consistency are independent properties.",
       r"Convergence is at rate $1/n$, faster than the usual $1/\sqrt n$ - a super-efficient estimator.",
       r"$P\left(X_{(n)}\le\theta-\varepsilon\right)=\left(\frac{\theta-\varepsilon}{\theta}\right)^n\to0$ for every fixed $\varepsilon>0$."],
 sol=[r"$L(\theta)=\theta^{-n}$ for $\theta\ge X_{(n)}$, which is decreasing, so $\hat\theta=X_{(n)}$. Statement 1 true.",
      r"$P\left(\left|X_{(n)}-\theta\right|>\varepsilon\right)=P\left(X_{(n)}<\theta-\varepsilon\right)=\left(\dfrac{\theta-\varepsilon}{\theta}\right)^n\to0$ as $n\to\infty$.",
      r"So $X_{(n)}\xrightarrow{P}\theta$: statement 2 is true.",
      r"Option (c)."]),

"2021-Q24": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"$\bar x$ is both the MLE and (being a one-to-one function of $\sum x_i$) sufficient for $\theta$.",
 tips=[r"$\sum X_i$ is sufficient; $\bar X=\sum X_i/n$ is a one-to-one function of it, hence equally sufficient.",
       r"MLEs are always functions of the sufficient statistic when one exists - the two statements reinforce each other.",
       r"For Poisson, $\bar X$ is simultaneously MLE, MoM estimator, UMVUE and MVB estimator."],
 sol=[r"$\log L=-n\theta+\left(\sum x_i\right)\log\theta-\log\prod x_i!$, so $\hat\theta=\bar x$. Statement 1 true.",
      r"By factorisation, $\sum X_i$ is sufficient; $\bar X$ is a one-to-one function of it, so $\bar X$ is sufficient too. Statement 2 true.",
      r"Option (c)."]),

"2021-Q25": dict(ans=3, qtype="Conceptual", diff="Hard",
 sc=r"The Cramer-Rao bound applies to UNBIASED estimators, not to 'sufficient' ones; and an MVB estimator requires far more than the mere existence of a sufficient statistic. Both statements are wrong.",
 tips=[r"MVB $\Rightarrow$ sufficient, but sufficient $\not\Rightarrow$ MVB - the implication runs one way only.",
       r"Counterexample to statement 2: $U(0,\theta)$ has the sufficient statistic $X_{(n)}$ yet no MVB estimator exists (regularity fails).",
       r"Statement 1 substitutes 'sufficient' for 'unbiased' in the Cramer-Rao hypothesis - a planted swap."],
 sol=[r"Statement 1: the Cramer-Rao inequality bounds the variance of an UNBIASED estimator of $\gamma(\theta)$. Sufficiency is neither assumed nor sufficient for the bound to apply. False as stated.",
      r"Statement 2: an MVB estimator, when it exists, must be a function of a sufficient statistic - so MVB $\Rightarrow$ sufficient.",
      r"The converse fails: $U(0,\theta)$ has the sufficient statistic $X_{(n)}$, but the regularity conditions break down and no MVB estimator exists. So the 'if and only if' is false.",
      r"Neither statement is correct - option (d)."]),

"2021-Q26": dict(ans=1, qtype="Numerical", diff="Easy",
 sc=r"$I_1(\theta)=1/\theta^2$ for the exponential with mean $\theta$, so the bound is $\theta^2/n$.",
 tips=[r"Score: $\frac{\partial\log f}{\partial\theta}=\frac{x-\theta}{\theta^2}$, giving $I_1=V(X)/\theta^4=1/\theta^2$.",
       r"$\bar X$ attains the bound exactly: $V(\bar X)=\theta^2/n$.",
       r"Information adds over observations, so the bound always carries a $1/n$."],
 sol=[r"$\log f=-\log\theta-\dfrac x\theta$, so $\dfrac{\partial\log f}{\partial\theta}=\dfrac{x-\theta}{\theta^2}$.",
      r"$I_1(\theta)=E\left[\left(\dfrac{X-\theta}{\theta^2}\right)^2\right]=\dfrac{\theta^2}{\theta^4}=\dfrac1{\theta^2}$, so $I_n(\theta)=\dfrac{n}{\theta^2}$.",
      r"Cramer-Rao lower bound $=\dfrac{1}{I_n(\theta)}=\dfrac{\theta^2}{n}$.",
      r"Option (b)."]),
}
