# -*- coding: utf-8 -*-
# Statistical Inference answer pack 12 (2024 Q14-Q41)
A = {
"2024-Q14": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"In both models $\bar X$ attains the bound: $V(\bar X)=\sigma^2/n$ against bound $\sigma^2/n$; and $V(\bar X)=\theta(1-\theta)/n$ against bound $\theta(1-\theta)/n$.",
 tips=[r"Both are one-parameter exponential families whose score is linear in $\bar X$ - the MVB attainment condition.",
       r"Normal with known variance: $I_1=1/\sigma^2$. Bernoulli: $I_1=\frac1{\theta(1-\theta)}$.",
       r"The mean of a one-parameter exponential family in canonical mean-parametrisation always attains the bound."],
 sol=[r"Case I: $\dfrac{\partial\log L}{\partial\theta}=\dfrac{n(\bar X-\theta)}{\sigma^2}$, of the MVB form, and $V(\bar X)=\dfrac{\sigma^2}{n}=\dfrac1{I_n(\theta)}$ - attained.",
      r"Case II: $\dfrac{\partial\log L}{\partial\theta}=\dfrac{n(\bar X-\theta)}{\theta(1-\theta)}$, again of the MVB form, and $V(\bar X)=\dfrac{\theta(1-\theta)}{n}=\dfrac1{I_n(\theta)}$ - attained.",
      r"Hence the bound coincides with $V(\bar X)$ in both cases - option (c)."]),

"2024-Q15": dict(ans=2, qtype="Conceptual", diff="Medium",
 sc=r"Uniform most accuracy is defined by minimising false-coverage probability (I true), and the confidence level is the INFIMUM of the true-coverage probability (II true).",
 tips=[r"False coverage: the interval covers a wrong value $\theta'\ne\theta$; UMA bounds minimise this uniformly.",
       r"The confidence coefficient is $\inf_\theta P_\theta\left(\theta\in S(X)\right)$ - literally the smallest true-coverage probability.",
       r"Accuracy (false coverage) and level (true coverage) are complementary design criteria."],
 sol=[r"Statement I: a lower confidence bound $\underline\theta$ is uniformly most accurate at level $1-\alpha$ if it minimises $P_\theta\left(\underline\theta\le\theta'\right)$ for every $\theta'<\theta$ - i.e. the probability of covering a false value. True.",
      r"Statement II: the confidence level (confidence coefficient) of a family of sets $S(X)$ is defined as $\inf_\theta P_\theta\left(\theta\in S(X)\right)$, the smallest probability of true coverage. True.",
      r"Option (c)."]),

"2024-Q16": dict(ans=3, qtype="Numerical", diff="Hard",
 sc=r"Matching power series in $\theta$ gives $g(t)=\frac{t(t-1)(t-2)(n-2)^{t-3}}{n^t}$.",
 tips=[r"Set $E[g(T)]=\sum_t g(t)\frac{e^{-n\theta}(n\theta)^t}{t!}$ equal to $e^{-2\theta}\theta^3$ and cancel $e^{-n\theta}$.",
       r"$e^{(n-2)\theta}\theta^3=\sum_{k\ge0}\frac{(n-2)^k\theta^{k+3}}{k!}$ - reindex with $t=k+3$.",
       r"Comparing coefficients of $\theta^t$ gives $g(t)\frac{n^t}{t!}=\frac{(n-2)^{t-3}}{(t-3)!}$, and $\frac{t!}{(t-3)!}=t(t-1)(t-2)$."],
 sol=[r"$T=\sum x_i\sim\text{Poisson}(n\theta)$ is complete sufficient, so the UMVUE is the unique $g(T)$ with $E[g(T)]=e^{-2\theta}\theta^3$.",
      r"$\sum_t g(t)\dfrac{(n\theta)^t}{t!}=e^{n\theta}e^{-2\theta}\theta^3=\theta^3\sum_{k\ge0}\dfrac{\left[(n-2)\theta\right]^k}{k!}=\sum_{t\ge3}\dfrac{(n-2)^{t-3}\theta^{t}}{(t-3)!}$.",
      r"Comparing coefficients of $\theta^t$: $g(t)\dfrac{n^t}{t!}=\dfrac{(n-2)^{t-3}}{(t-3)!}$.",
      r"$g(t)=\dfrac{t!}{(t-3)!}\cdot\dfrac{(n-2)^{t-3}}{n^t}=\dfrac{(n-2)^{T-3}\,T(T-1)(T-2)}{n^T}$ - option (d)."]),

"2024-Q17": dict(ans=0, qtype="Numerical", diff="Medium",
 sc=r"Under $H_0:\sigma^2=\sigma_0^2$, $\frac{\sum x_i^2}{\sigma_0^2}\sim\chi^2_n$, so the size-$\alpha$ upper-tail region is $\sum x_i^2\ge\sigma_0^2\chi^2_{n,\alpha}$. Matching the printed $2\chi^2_{n,\alpha}$ gives $\sigma_0^2=2$, and an upper tail means $H_1:\sigma^2>2$.",
 tips=[r"Read the multiplier of $\chi^2_{n,\alpha}$: it IS $\sigma_0^2$.",
       r"An UPPER-tail region in $\sum x_i^2$ corresponds to a LARGER variance under $H_1$.",
       r"Options (c) and (d) pair an upper tail with a smaller-variance alternative - internally inconsistent."],
 sol=[r"With mean zero, $\dfrac{\sum X_i^2}{\sigma^2}\sim\chi^2_n$.",
      r"A size-$\alpha$ upper-tail critical region under $H_0:\sigma^2=\sigma_0^2$ is $\sum x_i^2\ge\sigma_0^2\,\chi^2_{n,\alpha}$.",
      r"The printed region is $\sum x_i^2\ge2\chi^2_{n,\alpha}$, so $\sigma_0^2=2$.",
      r"The likelihood ratio increases with $\sum x_i^2$ when $\sigma^2$ increases, so the upper tail is UMP for $H_1:\sigma^2>2$ - option (a)."]),

"2024-Q18": dict(ans=2, qtype="Numerical", diff="Medium",
 sc=r"$X_{(1)}\sim\mu+\text{Exp}(3n)$, so $E\left[X_{(1)}\right]=\mu+\frac1{3n}$ (biased) and $V=\frac1{9n^2}\to0$ (consistent).",
 tips=[r"Minimum of $n$ exponentials with rate $3$ is exponential with rate $3n$ shifted to $\mu$.",
       r"Bias $\frac1{3n}\to0$ and variance $\frac1{9n^2}\to0$, so consistency holds despite the bias.",
       r"The MVUE is the bias-corrected $X_{(1)}-\frac1{3n}$, which is why $X_{(1)}$ itself cannot be MVUE."],
 sol=[r"$P\left(X_{(1)}>t\right)=e^{-3n(t-\mu)}$ for $t>\mu$, so $X_{(1)}-\mu\sim\text{Exp}(3n)$.",
      r"$E\left[X_{(1)}\right]=\mu+\dfrac1{3n}\ne\mu$: biased.",
      r"$V\left[X_{(1)}\right]=\dfrac1{9n^2}\to0$ and the bias $\to0$, so $X_{(1)}\xrightarrow{P}\mu$: consistent.",
      r"Hence biased and consistent - option (c)."]),

"2024-Q19": dict(ans=3, qtype="Numerical", diff="Hard",
 sc=r"$\lambda_1=2e^{-x}$ and the continuation band is $\frac19<2e^{-x}<9$, i.e. $x<\ln18$; under $H_0$ this has probability $1-e^{-\ln18}=\frac{17}{18}$.",
 tips=[r"$A=\frac{1-\beta}{\alpha}=9$ and $B=\frac{\beta}{1-\alpha}=\frac19$ when $\alpha=\beta=0.1$.",
       r"$\lambda_1=\frac{2e^{-2x}}{e^{-x}}=2e^{-x}<2<9$ always, so only the lower boundary can bind.",
       r"$2e^{-x}>\frac19\iff e^{-x}>\frac1{18}\iff x<\ln18$, and $P(X<\ln18\mid\theta=1)=1-\frac1{18}$."],
 sol=[r"With $\alpha=\beta=0.1$: $A=\dfrac{1-\beta}{\alpha}=9$ and $B=\dfrac{\beta}{1-\alpha}=\dfrac19$.",
      r"$\lambda_1=\dfrac{f_1(x_1)}{f_0(x_1)}=\dfrac{2e^{-2x_1}}{e^{-x_1}}=2e^{-x_1}$.",
      r"No decision means $B<\lambda_1<A$: the upper condition $2e^{-x_1}<9$ always holds, and $2e^{-x_1}>\tfrac19$ gives $x_1<\ln18$.",
      r"Under $H_0$ ($\theta=1$), $P(X_1<\ln18)=1-e^{-\ln18}=1-\dfrac1{18}=\dfrac{17}{18}$.",
      r"Option (d)."]),

"2024-Q20": dict(ans=1, qtype="Conceptual", diff="Medium",
 sc=r"$x_1$ is not complete (symmetry gives $E(x_1)=0$ for all $\theta$), but $x_1^2\sim\theta\chi^2_1$ is a complete exponential family.",
 tips=[r"Symmetric families are never complete in the raw variable - odd functions have zero mean.",
       r"Squaring restores completeness by removing the sign information.",
       r"This pair of statements is the standard illustration of why completeness is a delicate property."],
 sol=[r"Statement I: with $g(x)=x$, $E_\theta\left[g(x_1)\right]=0$ for every $\theta>0$ although $g\not\equiv0$ - so $x_1$ is not complete. False.",
      r"Statement II: $x_1^2/\theta\sim\chi^2_1$, a one-parameter exponential family with natural statistic $x_1^2$ and open natural parameter space - hence complete. True.",
      r"Hence II only - option (b)."]),

"2024-Q21": dict(ans=2, qtype="Numerical", diff="Hard",
 sc=r"Both tests have size $P(Z\ge2)$: $\varphi_1$ via $x_1\sim N(3,1)$, $\varphi_2$ via $\bar x\sim N(3,\tfrac1{16})$. But $\beta_1=P(Z<1)=0.84$ while $\beta_2=P(Z<-2)=0.023$.",
 tips=[r"$\bar x$ has standard deviation $1/\sqrt{16}=0.25$, so $3.5$ is TWO standard errors above $3$ - matching the single-observation cut-off at $5$.",
       r"Equal sizes but very different powers: using one observation throws away 15/16 of the information.",
       r"Under $H_1:\mu=4$: $x_1\sim N(4,1)$ gives $z=1$; $\bar x\sim N(4,0.0625)$ gives $z=-2$."],
 sol=[r"Size of $\varphi_1$: $x_1\sim N(3,1)$ under $H_0$, so $\alpha_1=P(x_1\ge5)=P(Z\ge2)=0.0228$.",
      r"Size of $\varphi_2$: $\bar x\sim N\left(3,\tfrac1{16}\right)$, so $\alpha_2=P\left(Z\ge\tfrac{3.5-3}{0.25}\right)=P(Z\ge2)=0.0228$. Hence $\alpha_1=\alpha_2$.",
      r"Type II error of $\varphi_1$: under $\mu=4$, $\beta_1=P(x_1<5)=P(Z<1)=0.8413$.",
      r"Type II error of $\varphi_2$: $\beta_2=P\left(Z<\tfrac{3.5-4}{0.25}\right)=P(Z<-2)=0.0228$. So $\beta_1>\beta_2$.",
      r"Option (c)."]),

"2024-Q22": dict(ans=2, qtype="Numerical", diff="Hard",
 sc=r"$\log L=-n\log\theta-\frac{\sum x_i}{\theta}+n$ increases in $\theta$ up to $\bar x$, but the support forces $\theta\le X_{(1)}<\bar x$; hence $\hat\theta=X_{(1)}$ and $\widehat{\ln\theta}=\ln X_{(1)}$.",
 tips=[r"Two constraints compete: the smooth maximum at $\bar x$ and the support constraint $\theta\le X_{(1)}$.",
       r"Since $X_{(1)}\le\bar x$ always, the support constraint binds and the MLE sits on the boundary.",
       r"Invariance then gives the MLE of $\ln\theta$ directly as $\ln\left(X_{(1)}\right)$."],
 sol=[r"$L(\theta)=\theta^{-n}\exp\left[-\dfrac{\sum(x_i-\theta)}{\theta}\right]=\theta^{-n}e^{n}\exp\left(-\dfrac{\sum x_i}{\theta}\right)$, valid for $\theta\le X_{(1)}$.",
      r"$\dfrac{\partial\log L}{\partial\theta}=-\dfrac n\theta+\dfrac{\sum x_i}{\theta^2}=\dfrac{n(\bar x-\theta)}{\theta^2}>0$ for $\theta<\bar x$.",
      r"Since $X_{(1)}\le\bar x$, the likelihood is increasing over the whole admissible range, so $\hat\theta=X_{(1)}$.",
      r"By invariance, the MLE of $\ln\theta$ is $\ln\left[x_{(1)}\right]$ - option (c)."]),

"2024-Q23": dict(ans=3, qtype="Conceptual", diff="Easy",
 sc=r"With $\mu$ known, the only $\theta$-dependent data term is $\left(\prod x_i\right)^{\theta-1}$, so $\prod x_i$ is sufficient.",
 tips=[r"$\theta$ is the SHAPE parameter here; shape couples with $\log x$, scale with $x$.",
       r"$\sum x_i$ would be sufficient for $\mu$ if the roles were reversed - read which parameter is known.",
       r"Equivalently $\sum\log x_i$ is sufficient (a one-to-one transformation)."],
 sol=[r"$L=\dfrac{1}{\Gamma(\theta)^n\mu^{n\theta}}\left(\prod x_i\right)^{\theta-1}\exp\left(-\dfrac{\sum x_i}{\mu}\right)$.",
      r"With $\mu$ known, $\exp\left(-\sum x_i/\mu\right)$ and $\left(\prod x_i\right)^{-1}$ are free of $\theta$ and go into $h(x)$.",
      r"The $\theta$-dependent factor is $\dfrac{1}{\Gamma(\theta)^n\mu^{n\theta}}\left(\prod x_i\right)^{\theta}$, a function of $\prod x_i$ and $\theta$ alone.",
      r"Hence $\prod_{i=1}^n X_i$ is sufficient - option (d)."]),

"2024-Q24": dict(ans=3, qtype="Numerical", diff="Easy",
 sc=r"Power $=0.05+0.04+0.03=0.12$, so $P(\text{Type II})=1-0.12=0.88$.",
 tips=[r"Type-II error $=1-$ power; compute the power over the rejection region first.",
       r"Use the $f_1$ row for the power: $x=1,2,3$ contribute $0.05,0.04,0.03$.",
       r"$0.09$ (option a) is $0.05+0.04$ - a planted partial sum."],
 sol=[r"The test rejects when $x\le3$, i.e. $x\in\{1,2,3\}$.",
      r"Power $=f_1(1)+f_1(2)+f_1(3)=0.05+0.04+0.03=0.12$.",
      r"$P(\text{Type II error})=1-\text{power}=1-0.12=0.88$.",
      r"Option (d)."]),

"2024-Q25": dict(ans=2, qtype="Conceptual", diff="Medium",
 sc=r"Statement-I is the standard definition of a test function. Statement-II mislabels: when $\varphi=I_A$, $A$ is the CRITICAL (rejection) region, not the acceptance region.",
 tips=[r"$\varphi(x)$ is the probability of REJECTING $H_0$ when $x$ is observed - so $\{\varphi=1\}$ is the rejection region.",
       r"The acceptance region is the complement $A^c$.",
       r"Any statement swapping 'acceptance' and 'critical' region is a planted error."],
 sol=[r"Statement-I: a (possibly randomised) test is any Borel measurable $\varphi:\mathbb{R}^n\to[0,1]$, where $\varphi(x)$ is the rejection probability. Correct.",
      r"Statement-II: if $\varphi=I_A$ then the test rejects exactly on $A$, so $A$ is the critical (rejection) region; the acceptance region is $A^c$. Incorrect.",
      r"Option (c)."]),

"2024-Q26": dict(ans=3, qtype="Conceptual", diff="Easy",
 sc=r"A small $P$-value is evidence AGAINST $H_0$ (I false), and the NP most powerful test is always a function of the sufficient statistic (II false).",
 tips=[r"$P$-value $=$ probability, under $H_0$, of data at least as extreme as observed - small means $H_0$ fits badly.",
       r"The likelihood ratio $L_1/L_0$ factorises through any sufficient statistic, so the NP test does too.",
       r"Both statements are sign-flips of true facts - a common UPSC construction."],
 sol=[r"Statement I: a smaller $P$-value means the observed outcome is more extreme under $H_0$, i.e. stronger evidence AGAINST $H_0$. False as written.",
      r"Statement II: by the factorisation theorem, $\dfrac{L_1}{L_0}=\dfrac{g_{\theta_1}(T)}{g_{\theta_0}(T)}$ depends on the data only through $T$, so the NP test is always a function of the sufficient statistic. False as written.",
      r"Neither is correct - option (d)."]),

"2024-Q27": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"$\left(\sum X_i,\sum X_i^2\right)$ is complete sufficient; the unbiased functions of it are $\bar X$ for $\mu$ and $S^2$ (divisor $n-1$) for $\sigma^2$.",
 tips=[r"Unbiasedness picks the divisor: $n-1$, not $n$ - option (d) uses the biased MLE.",
       r"Lehmann-Scheffe then upgrades both to UMVUE status.",
       r"Options (a) and (b) give raw sums, whose expectations are $n\mu$ and $n(\mu^2+\sigma^2)$ - not unbiased."],
 sol=[r"For $N(\mu,\sigma^2)$ with both parameters unknown, $\left(\sum X_i,\sum X_i^2\right)$ is complete and sufficient.",
      r"$E(\bar X)=\mu$ and $\bar X$ is a function of the statistic, so $\bar X$ is the UMVUE of $\mu$.",
      r"$E\left[\dfrac{1}{n-1}\sum(X_i-\bar X)^2\right]=\sigma^2$, and it too is a function of the statistic, so it is the UMVUE of $\sigma^2$.",
      r"Option (c)."]),

"2024-Q28": dict(ans=2, qtype="Definition", diff="Easy",
 sc=r"The Neyman-Pearson lemma applies to a SIMPLE null against a SIMPLE alternative.",
 tips=[r"Both hypotheses must specify the distribution completely for the likelihood ratio to be well defined.",
       r"Extension to a composite alternative requires MLR (Karlin-Rubin) and yields UMP tests.",
       r"Composite nulls are handled by the likelihood ratio test, not by NP."],
 sol=[r"The lemma considers $H_0:\theta=\theta_0$ against $H_1:\theta=\theta_1$, both simple.",
      r"It then shows the test rejecting when $L(\theta_1)/L(\theta_0)>k$ is most powerful of its size.",
      r"Option (c)."]),

"2024-Q29": dict(ans=0, qtype="Conceptual", diff="Easy",
 sc=r"I, II and III are correct; IV is wrong because $-2\ln\lambda$ is asymptotically CHI-SQUARE, not $N(0,1)$.",
 tips=[r"Wilks: $-2\ln\lambda\xrightarrow{d}\chi^2_r$ - never normal.",
       r"$\lambda\in[0,1]$ since the numerator maximises over a subset.",
       r"The LR statistic factorises through any sufficient statistic, since the whole likelihood does."],
 sol=[r"I: for simple versus simple the two non-randomised tests coincide (the LR criterion is a monotone function of $L_1/L_0$). True.",
      r"II: $\lambda=\dfrac{\sup_{\Theta_0}L}{\sup_\Theta L}\in[0,1]$. True.",
      r"III: by factorisation both suprema depend on the data only through the sufficient statistic. True.",
      r"IV: Wilks' theorem gives a chi-square limit, not $N(0,1)$. False.",
      r"Hence I, II and III - option (a)."]),

"2024-Q30": dict(ans=0, qtype="Conceptual", diff="Hard",
 sc=r"The double exponential admits no non-trivial sufficient statistic - the full order statistic is minimal sufficient.",
 tips=[r"$\sum|x_i-\theta|$ cannot be reduced to any fixed finite-dimensional statistic free of $\theta$.",
       r"The MLE here is the sample MEDIAN (it minimises $\sum|x_i-\theta|$), so options (c) and (d) are wrong.",
       r"Option (b) is in fact also true (the median's distribution is symmetric about $\theta$); option (a) is the sufficiency-theoretic statement this item is testing."],
 sol=[r"$L(\theta)=2^{-n}\exp\left[-\sum_i|x_i-\theta|\right]$. The exponent depends on $\theta$ through all the order statistics simultaneously and cannot be factorised through any statistic of dimension less than $n$.",
      r"Hence the minimal sufficient statistic is the complete order statistic - no scalar sufficient statistic exists. Statement (a) is correct.",
      r"(c) is wrong: the MLE minimises $\sum|x_i-\theta|$ and is the sample median, not $X_{(n)}$; and $X_{(n)}$ is not sufficient.",
      r"(d) is wrong: the sample mean is not the MLE for this family.",
      r"Option (a)."]),

"2024-Q31": dict(ans=3, qtype="Numerical", diff="Hard",
 sc=r"For two equally efficient unbiased estimators with common efficiency $e$, $\rho\ge2e-1=0.6$; so $\rho\in(0.6,1)$.",
 tips=[r"Take $T^*=\frac{T_1+T_2}{2}$: unbiased with $V(T^*)=\frac{V(1+\rho)}{2}$, which cannot beat the minimum variance $eV$.",
       r"That gives $\frac{(1+\rho)}{2}\ge e$, i.e. $\rho\ge2e-1$.",
       r"$2(0.8)-1=0.6$, and $\rho\le1$ always - hence the interval $(0.6,1)$."],
 sol=[r"Let $V=V(T_1)=V(T_2)$ and let $V_0$ be the minimum attainable variance, so $e=\dfrac{V_0}{V}=0.8$.",
      r"$T^*=\dfrac{T_1+T_2}{2}$ is unbiased with $V(T^*)=\dfrac{V(1+\rho)}{2}$.",
      r"No unbiased estimator can have variance below $V_0=eV$, so $\dfrac{V(1+\rho)}{2}\ge eV$, giving $\rho\ge2e-1=0.6$.",
      r"Combined with $\rho\le1$, the correlation lies in $(0.6,1)$ - option (d)."]),

"2024-Q32": dict(ans=3, qtype="Numerical", diff="Easy",
 sc=r"$U(-\theta,\theta)$ requires $\theta\ge\max_i|x_i|=\max(7,12)=12$; the likelihood $(2\theta)^{-n}$ decreases, so $\hat\theta=12$.",
 tips=[r"The binding constraint is on the ABSOLUTE values - the sufficient statistic is $\max|X_i|$.",
       r"$|-12|=12>|7|$, so the smallest observation drives the estimate here.",
       r"$\theta>0$ by assumption, so negative options are impossible on sight."],
 sol=[r"$L(\theta)=\dfrac1{(2\theta)^n}$ provided $-\theta\le x_i\le\theta$ for all $i$, i.e. $\theta\ge\max_i|x_i|$.",
      r"$\max_i|x_i|=\max(|7|,|-12|)=12$.",
      r"$L$ is decreasing in $\theta$, so the maximum is at the smallest admissible value $\hat\theta=12$.",
      r"Option (d)."]),

"2024-Q33": dict(ans=0, qtype="Numerical", diff="Easy",
 sc=r"$\bar x=\frac{0+4+2+3}{4}=2.25$, $\text{SE}=\sqrt{2.25/4}=0.75$, so the interval is $2.25\pm1.96(0.75)=(0.78,3.72)$.",
 tips=[r"Poisson SE is $\sqrt{\bar x/n}$; here $\sqrt{0.5625}=0.75$ exactly.",
       r"Margin $=1.96\times0.75=1.47$.",
       r"The interval must be centred at $2.25$ - options (b), (c), (d) are not."],
 sol=[r"$\bar x=\dfrac{0+4+2+3}{4}=2.25$, the MLE of $\theta$.",
      r"$\text{SE}=\sqrt{\dfrac{\bar x}{n}}=\sqrt{\dfrac{2.25}{4}}=0.75$.",
      r"$95\%$ interval: $2.25\pm1.96(0.75)=2.25\pm1.47$.",
      r"$=(0.78,\ 3.72)$ - option (a)."]),

"2024-Q34": dict(ans=0, qtype="Numerical", diff="Medium",
 sc=r"Under $H_1$, $T=\sum x_i\sim\text{Poisson}(3)$, so power $=1-P(T\le1)=1-4e^{-3}=1-0.1991=0.80$.",
 tips=[r"Pool first: $\sum_{i=1}^3X_i\sim\text{Poisson}(3\lambda)$, which is Poisson(3) under $H_1:\lambda=1$.",
       r"$P(T\le1)=e^{-3}(1+3)=4e^{-3}$ - the given constant is designed for this.",
       r"$4\times0.04978=0.19912$, so the power is $0.80088$."],
 sol=[r"Under $H_1:\lambda=1$, $T=\sum_{i=1}^3x_i\sim\text{Poisson}(3)$.",
      r"$P(T\le1)=P(T=0)+P(T=1)=e^{-3}+3e^{-3}=4e^{-3}=4(0.04978)=0.19912$.",
      r"Power $=P(T>1)=1-0.19912=0.80088$.",
      r"$\approx0.80$ - option (a)."]),

"2024-Q35": dict(ans=2, qtype="Conceptual", diff="Medium",
 sc=r"A Bayes rule with constant risk is minimax (I true), and a unique minimax estimator is admissible (II true).",
 tips=[r"Constant risk $\Rightarrow$ the Bayes risk equals the maximum risk, which no rule can beat - hence minimax.",
       r"If a unique minimax rule were inadmissible, the dominating rule would also be minimax, contradicting uniqueness.",
       r"These two results are the standard bridge between Bayes, minimax and admissibility."],
 sol=[r"Statement I: let $\delta$ be Bayes with constant risk $c$. For any rule $\delta'$, $\sup_\theta R(\theta,\delta')\ge$ Bayes risk of $\delta'$ $\ge$ Bayes risk of $\delta=c=\sup_\theta R(\theta,\delta)$. So $\delta$ is minimax. True.",
      r"Statement II: if a unique minimax $\delta$ were inadmissible, some $\delta'$ would dominate it, making $\sup_\theta R(\theta,\delta')\le\sup_\theta R(\theta,\delta)$ - so $\delta'$ would also be minimax, contradicting uniqueness. True.",
      r"Option (c)."]),

"2024-Q36": dict(ans=2, qtype="Numerical", diff="Hard",
 sc=r"Evaluate $\binom m3p^3(1-p)^{m-3}$ at all four $(m,p)$ pairs; the largest is $\left(\tfrac34\right)^3=0.4219$ at $(3,\tfrac34)$.",
 tips=[r"With a finite parameter set, just tabulate the likelihood at every admissible point.",
       r"$(3,\tfrac34)$: $\binom33\left(\tfrac34\right)^3=\tfrac{27}{64}=0.4219$ - the clear maximum.",
       r"$(5,\tfrac34)$: $\binom53\left(\tfrac34\right)^3\left(\tfrac14\right)^2=10\cdot\tfrac{27}{64}\cdot\tfrac1{16}=0.2637$."],
 sol=[r"$L(m,p)=\dbinom m3p^3(1-p)^{m-3}$.",
      r"$(3,\tfrac14)$: $1\cdot\left(\tfrac14\right)^3=0.0156$. $(3,\tfrac34)$: $1\cdot\left(\tfrac34\right)^3=0.4219$.",
      r"$(5,\tfrac14)$: $10\left(\tfrac14\right)^3\left(\tfrac34\right)^2=0.0879$. $(5,\tfrac34)$: $10\left(\tfrac34\right)^3\left(\tfrac14\right)^2=0.2637$.",
      r"The maximum is at $(m,p)=(3,\tfrac34)$ - option (c)."]),

"2024-Q37": dict(ans=1, qtype="Numerical", diff="Medium",
 sc=r"$t=\frac{\sqrt4\,\bar x}{S}=\frac{2\bar x}{S}$, and the two-sided size-0.05 test rejects when $|t|>t_{3,0.025}$, i.e. $\frac{|\bar x|}{S}>\frac12t_{3,0.025}$.",
 tips=[r"$\sqrt n=2$ for $n=4$, so the $t$ statistic is $2\bar x/S$ - divide the critical value by 2.",
       r"Degrees of freedom are $n-1=3$, not $4$ - options (c) and (d) use the wrong df.",
       r"Two-sided at $\alpha=0.05$ needs the $0.025$ upper point, not $0.05$."],
 sol=[r"With $n=4$, the $t$ statistic is $t=\dfrac{\sqrt n\,\bar x}{S}=\dfrac{2\bar x}{S}$, with $n-1=3$ degrees of freedom.",
      r"The two-sided size-$0.05$ LRT rejects when $|t|>t_{3,0.025}$.",
      r"$\dfrac{2|\bar x|}{S}>t_{3,0.025}\iff\dfrac{|\bar x|}{S}>\dfrac12t_{3,0.025}$.",
      r"So $k=\dfrac12t_{(3,0.025)}$ - option (b)."]),

"2024-Q38": dict(ans=1, qtype="Conceptual", diff="Easy",
 sc=r"$L=\theta^n\left(\prod x_i\right)^{\theta-1}$, so $\prod x_i$ is sufficient.",
 tips=[r"Pattern-match the family $\theta x^{\theta-1}$ - the product (or $\sum\log x_i$) is always the sufficient statistic.",
       r"This family has now appeared six times in the 2018-2026 papers.",
       r"The MLE $-n/\sum\log x_i$ is a function of it, as theory requires."],
 sol=[r"$L(\theta)=\prod_{i=1}^n\theta x_i^{\theta-1}=\theta^n\left(\prod_{i=1}^n x_i\right)^{\theta-1}$.",
      r"Factorising with $g_\theta(t)=\theta^nt^{\theta-1}$, $t=\prod x_i$, and $h(x)=1$ gives sufficiency.",
      r"Option (b)."]),

"2024-Q39": dict(ans=3, qtype="Numerical", diff="Medium",
 sc=r"$\hat\theta=\frac1n\sum x_i^2$ (I), $X^2\sim\text{Exp}(\text{mean }\theta)$ so $E\left(\hat\theta\right)=\theta$ (II), and $\sum X_i^2$ is the sufficient statistic (III). All three hold.",
 tips=[r"The transformation $Y=X^2$ turns this Rayleigh-type density into an exponential with mean $\theta$ - everything follows.",
       r"$\log L=-n\log\theta-\frac{\sum x_i^2}{\theta}+\text{const}$, so the MLE is the mean of the squares.",
       r"Unbiasedness here is unusual for an MLE - it happens because the model is exponential in $\sum X_i^2$."],
 sol=[r"$\log L=n\log2+\sum\log x_i-n\log\theta-\dfrac{\sum x_i^2}{\theta}$, so $\dfrac{\partial\log L}{\partial\theta}=-\dfrac n\theta+\dfrac{\sum x_i^2}{\theta^2}=0$ gives $\hat\theta=\dfrac1n\sum x_i^2$. Statement I true.",
      r"Setting $Y=X^2$, $f_Y(y)=\frac1\theta e^{-y/\theta}$, so $E(X^2)=\theta$ and $E\left(\hat\theta\right)=\theta$. Statement II true.",
      r"By factorisation the likelihood depends on the data only through $\sum x_i^2$, so $\hat\theta$ is sufficient. Statement III true.",
      r"Option (d)."]),

"2024-Q40": dict(ans=1, qtype="Conceptual", diff="Hard",
 sc=r"$E(u)=\frac1{2n}P(X_1+X_2=1)=p(1-p)^{2n-1}$, which is neither $\frac{p(1-p)}{n}$ nor $p$ - so I and II fail; but $\sum X_i$ is complete sufficient, so III holds.",
 tips=[r"$X_1+X_2\sim B(2n,p)$, so $P(X_1+X_2=1)=2np(1-p)^{2n-1}$.",
       r"$u$ is unbiased for $p(1-p)^{2n-1}$ - check what an estimator actually estimates before accepting a claim.",
       r"Binomial with known $n$ is a one-parameter exponential family, so $\sum X_i$ is complete sufficient for $p$."],
 sol=[r"$X_1+X_2\sim B(2n,p)$, so $P(X_1+X_2=1)=\dbinom{2n}{1}p(1-p)^{2n-1}=2np(1-p)^{2n-1}$.",
      r"$E(u)=\dfrac1{2n}\cdot2np(1-p)^{2n-1}=p(1-p)^{2n-1}$, which is not $\dfrac{p(1-p)}{n}$: statement I false.",
      r"Since $E(u)\ne p$, $u$ is not even unbiased for $p$, let alone UMVUE: statement II false.",
      r"$L=\prod\binom{n}{x_i}p^{\sum x_i}(1-p)^{mn-\sum x_i}$, so $\sum X_i$ is sufficient, and being the natural statistic of a full-rank exponential family it is complete: statement III true.",
      r"Hence III only - option (b)."]),

"2024-Q41": dict(ans=2, qtype="Numerical", diff="Easy",
 sc=r"$\bar X\sim N(0,\tfrac1n)$ under $H_0$, so $P(\bar X>\theta)=\alpha$ gives $\theta=\dfrac{Z_\alpha}{\sqrt n}$.",
 tips=[r"Standard error of $\bar X$ is $1/\sqrt n$ when the population variance is 1.",
       r"$P\left(\sqrt n\bar X>Z_\alpha\right)=\alpha$, so the cut-off on the $\bar X$ scale is $Z_\alpha/\sqrt n$.",
       r"Dividing by $n$ instead of $\sqrt n$ is the classic slip (option (b))."],
 sol=[r"Under $H_0:\mu=0$, $\bar X\sim N\left(0,\dfrac1n\right)$, so $\sqrt n\,\bar X\sim N(0,1)$.",
      r"Size $\alpha$ requires $P\left(\bar X>\theta\right)=P\left(\sqrt n\bar X>\sqrt n\theta\right)=\alpha$, so $\sqrt n\theta=Z_\alpha$.",
      r"$\theta=\dfrac{Z_\alpha}{\sqrt n}$ - option (c)."]),
}
