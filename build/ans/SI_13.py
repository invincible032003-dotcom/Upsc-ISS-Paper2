# -*- coding: utf-8 -*-
# Statistical Inference answer pack 13 (2024 Q42-Q50, 2025 Q1-Q19)
A = {
"2024-Q42": dict(ans=1, qtype="Numerical", diff="Medium",
 sc=r"Power $=P\left(\bar X>\frac{Z_\alpha}{\sqrt n}\mid\mu=4\right)=P\left(Z>Z_\alpha-4\sqrt n\right)=\Phi\left(4\sqrt n-Z_\alpha\right)$.",
 tips=[r"Standardise under $H_1$: $\sqrt n(\bar X-4)\sim N(0,1)$.",
       r"$\sqrt n\left(\frac{Z_\alpha}{\sqrt n}-4\right)=Z_\alpha-4\sqrt n$, and $P(Z>u)=\Phi(-u)$.",
       r"Power must increase with $n$, so the answer must contain $+4\sqrt n$ - option (a) has $4/\sqrt n$ and decreases."],
 sol=[r"From the companion item, the cut-off is $\theta=\dfrac{Z_\alpha}{\sqrt n}$.",
      r"Under $H_1:\mu=4$, $\bar X\sim N\left(4,\dfrac1n\right)$, so $\sqrt n(\bar X-4)\sim N(0,1)$.",
      r"Power $=P\left(\bar X>\dfrac{Z_\alpha}{\sqrt n}\right)=P\left(\sqrt n(\bar X-4)>Z_\alpha-4\sqrt n\right)=1-\Phi\left(Z_\alpha-4\sqrt n\right)$.",
      r"$=\Phi\left(4\sqrt n-Z_\alpha\right)$ - option (b)."]),

"2024-Q43": dict(ans=2, qtype="Numerical", diff="Medium",
 sc=r"$\alpha=P(X>1\mid\lambda=1)+\tfrac12P(X=1\mid\lambda=1)=\left(1-2e^{-1}\right)+\tfrac12e^{-1}=1-1.5e^{-1}$.",
 tips=[r"$P(X\le1)=e^{-1}+e^{-1}=2e^{-1}$ when $\lambda=1$ - both terms equal because $\lambda^1/1!=1$.",
       r"The coin adds $\tfrac12\times P(X=1)=\tfrac12e^{-1}$ to the rejection probability.",
       r"$1-2e^{-1}+0.5e^{-1}=1-1.5e^{-1}$ - combine the $e^{-1}$ terms carefully."],
 sol=[r"Under $H_0:\lambda=1$, $P(X=0)=e^{-1}$ and $P(X=1)=e^{-1}$, so $P(X>1)=1-2e^{-1}$.",
      r"The test rejects with probability 1 when $X>1$ and with probability $\tfrac12$ when $X=1$.",
      r"$\alpha=\left(1-2e^{-1}\right)+\tfrac12e^{-1}=1-\tfrac32e^{-1}=1-1.5e^{-1}$.",
      r"Option (c)."]),

"2024-Q44": dict(ans=3, qtype="Numerical", diff="Medium",
 sc=r"Power $=\left(1-3e^{-2}\right)+\tfrac12\left(2e^{-2}\right)=1-2e^{-2}$, so $\beta=1-\text{power}=2e^{-2}$.",
 tips=[r"Under $\lambda=2$: $P(X=0)=e^{-2}$, $P(X=1)=2e^{-2}$, so $P(X>1)=1-3e^{-2}$.",
       r"Randomised contribution is $\tfrac12\times2e^{-2}=e^{-2}$.",
       r"Power $=1-2e^{-2}\approx0.729$, hence $\beta\approx0.271$."],
 sol=[r"Under $H_1:\lambda=2$: $P(X=0)=e^{-2}$, $P(X=1)=2e^{-2}$, so $P(X>1)=1-3e^{-2}$.",
      r"Power $=\left(1-3e^{-2}\right)+\tfrac12\left(2e^{-2}\right)=1-3e^{-2}+e^{-2}=1-2e^{-2}$.",
      r"$\beta=1-\text{power}=2e^{-2}$.",
      r"Option (d)."]),

"2024-Q45": dict(ans=0, qtype="Conceptual", diff="Easy",
 sc=r"$E(X_n)=\mu$ (I true), but $E\left(\frac{\sum X_i}{n+1}\right)=\frac{n\mu}{n+1}\ne\mu$ (II false).",
 tips=[r"A single observation is always unbiased for the mean - it just has large variance.",
       r"Dividing a sum of $n$ terms by $n+1$ shrinks the estimator toward zero, creating bias $-\frac{\mu}{n+1}$.",
       r"The bias vanishes asymptotically, which is why $\hat\mu_{2,n}$ is still consistent (next item)."],
 sol=[r"Statement I: $E\left(\hat\mu_{1,n}\right)=E(X_n)=\mu$ - unbiased. True.",
      r"Statement II: $E\left(\hat\mu_{2,n}\right)=\dfrac{1}{n+1}\sum_{i=1}^nE(X_i)=\dfrac{n\mu}{n+1}\ne\mu$ - biased. False.",
      r"Hence I only - option (a)."]),

"2024-Q46": dict(ans=1, qtype="Conceptual", diff="Easy",
 sc=r"$X_n$ has variance $\sigma^2$ for every $n$ so it is not consistent (I false); $\hat\mu_{2,n}=\frac{n}{n+1}\bar X\xrightarrow{P}\mu$ (II true).",
 tips=[r"Consistency needs the sampling distribution to concentrate - a single observation never does.",
       r"$\frac{n}{n+1}\to1$ and $\bar X\xrightarrow{P}\mu$, so Slutsky gives $\hat\mu_{2,n}\xrightarrow{P}\mu$.",
       r"Together with the previous item: unbiased $\ne$ consistent, and consistent $\ne$ unbiased."],
 sol=[r"Statement I: $V\left(\hat\mu_{1,n}\right)=\sigma^2$ for every $n$, so the distribution never concentrates and $X_n\not\xrightarrow{P}\mu$. False.",
      r"Statement II: $\hat\mu_{2,n}=\dfrac{n}{n+1}\bar X$. Since $\bar X\xrightarrow{P}\mu$ and $\dfrac{n}{n+1}\to1$, Slutsky's theorem gives $\hat\mu_{2,n}\xrightarrow{P}\mu$. True.",
      r"Hence II only - option (b)."]),

"2024-Q47": dict(ans=0, qtype="Numerical", diff="Medium",
 sc=r"Gamma has mean $\alpha\beta$ and variance $\alpha\beta^2$, so $\hat\alpha=\frac{\bar X^2}{m_2}=\frac{n\bar X^2}{\sum(X_i-\bar X)^2}$.",
 tips=[r"$\dfrac{(\text{mean})^2}{\text{variance}}=\dfrac{\alpha^2\beta^2}{\alpha\beta^2}=\alpha$ - the shape is the squared coefficient of variation inverted.",
       r"$m_2=\frac1n\sum(X_i-\bar X)^2$, so writing it out introduces the factor $n$ in the numerator.",
       r"Check dimensions: $\alpha$ is dimensionless, and $\bar X^2/m_2$ indeed is."],
 sol=[r"For Gamma$(\alpha,\beta)$ in this parametrisation: $E(X)=\alpha\beta$ and $V(X)=\alpha\beta^2$.",
      r"Method of moments: $\hat\alpha\hat\beta=\bar X$ and $\hat\alpha\hat\beta^2=m_2=\dfrac1n\sum(X_i-\bar X)^2$.",
      r"Dividing: $\hat\beta=\dfrac{m_2}{\bar X}$, hence $\hat\alpha=\dfrac{\bar X}{\hat\beta}=\dfrac{\bar X^2}{m_2}$.",
      r"$=\dfrac{n\bar X^2}{\sum(X_i-\bar X)^2}$ - option (a)."]),

"2024-Q48": dict(ans=0, qtype="Numerical", diff="Easy",
 sc=r"$\hat\beta=\frac{m_2}{\bar X}=\frac{\sum(X_i-\bar X)^2}{n\bar X}$.",
 tips=[r"$\dfrac{\text{variance}}{\text{mean}}=\dfrac{\alpha\beta^2}{\alpha\beta}=\beta$ - the scale is the variance-to-mean ratio.",
       r"Remember $m_2$ carries the divisor $n$.",
       r"Cross-check: $\hat\alpha\hat\beta=\dfrac{n\bar X^2}{\sum(X_i-\bar X)^2}\cdot\dfrac{\sum(X_i-\bar X)^2}{n\bar X}=\bar X$ - consistent."],
 sol=[r"From $\hat\alpha\hat\beta=\bar X$ and $\hat\alpha\hat\beta^2=m_2$, dividing gives $\hat\beta=\dfrac{m_2}{\bar X}$.",
      r"With $m_2=\dfrac1n\sum(X_i-\bar X)^2$:",
      r"$\hat\beta=\dfrac{\sum_{i=1}^n(X_i-\bar X)^2}{n\bar X}$ - option (a)."]),

"2024-Q49": dict(ans=0, qtype="Numerical", diff="Easy",
 sc=r"$E(X)=\int_0^1x\cdot\theta x^{\theta-1}dx=\frac{\theta}{\theta+1}$.",
 tips=[r"$\int_0^1\theta x^\theta dx=\frac{\theta}{\theta+1}$ - one line.",
       r"Sanity check: as $\theta\to\infty$ the density piles up near 1 and $E(X)\to1$; as $\theta\to0$, $E(X)\to0$.",
       r"$E(X)<1$ always, which rules out options with $\theta-1$ in the denominator."],
 sol=[r"$E(X)=\displaystyle\int_0^1x\cdot\theta x^{\theta-1}dx=\theta\int_0^1x^\theta dx$.",
      r"$=\theta\left[\dfrac{x^{\theta+1}}{\theta+1}\right]_0^1=\dfrac{\theta}{\theta+1}$.",
      r"Option (a)."]),

"2024-Q50": dict(ans=1, qtype="Numerical", diff="Medium",
 sc=r"$\frac{\hat\theta}{\hat\theta+1}=\bar X$ gives $\hat\theta=\frac{\bar X}{1-\bar X}=\frac{\sum X_i}{n-\sum X_i}$.",
 tips=[r"Solve $\frac{\theta}{\theta+1}=m$ for $\theta$: $\theta=\frac{m}{1-m}$.",
       r"Substituting $m=\frac{\sum X_i}{n}$ and clearing the fraction gives the printed form.",
       r"Sanity: as $\bar X\to1$ the estimate $\to\infty$, matching the shape of the density."],
 sol=[r"Method of moments equates $E(X)=\dfrac{\theta}{\theta+1}$ to $\bar X$.",
      r"$\dfrac{\hat\theta}{\hat\theta+1}=\bar X\Rightarrow\hat\theta=\bar X\hat\theta+\bar X\Rightarrow\hat\theta(1-\bar X)=\bar X$.",
      r"$\hat\theta=\dfrac{\bar X}{1-\bar X}=\dfrac{\sum X_i/n}{1-\sum X_i/n}=\dfrac{\sum_{i=1}^nX_i}{n-\sum_{i=1}^nX_i}$.",
      r"Option (b)."]),

"2025-Q1": dict(ans=3, qtype="Conceptual", diff="Easy",
 sc=r"MLEs need not be unbiased (I false); unbiased estimators are not unique (II false); products of consistent estimators are consistent (III true).",
 tips=[r"$\hat\sigma^2$ with divisor $n$ kills statement I immediately.",
       r"Convex combinations of unbiased estimators are unbiased, killing statement II.",
       r"Continuous mapping theorem gives statement III for sums, products and quotients (where defined)."],
 sol=[r"Statement I: MLEs are frequently biased (e.g. $\hat\sigma^2$ with divisor $n$), so 'MLEs are unbiased' is false.",
      r"Statement II: $aT_1+(1-a)T_2$ is unbiased for every $a$ whenever $T_1,T_2$ are - uniqueness fails. False.",
      r"Statement III: $U\xrightarrow{P}\theta_1$ and $W\xrightarrow{P}\theta_2$ give $UW\xrightarrow{P}\theta_1\theta_2$ by the continuous mapping theorem. True.",
      r"Hence III only - option (d)."]),

"2025-Q2": dict(ans=1, qtype="Definition", diff="Easy",
 sc=r"Convergence of the estimator to the parameter as $n$ grows is consistency.",
 tips=[r"Consistency is the only one of the four properties that is asymptotic in nature.",
       r"Formally $T_n\xrightarrow{P}\theta$: $P\left(|T_n-\theta|<\varepsilon\right)\to1$.",
       r"Unbiasedness, sufficiency and completeness are all finite-sample properties."],
 sol=[r"An estimator $T_n$ is consistent for $\theta$ if $T_n\xrightarrow{P}\theta$ as $n\to\infty$.",
      r"That is precisely the statement that the estimator gets closer and closer to the parameter as the sample grows.",
      r"Option (b)."]),

"2025-Q3": dict(ans=1, qtype="Numerical", diff="Hard",
 sc=r"$V(S^2)=\frac{2(n-1)\sigma^4}{n^2}<\frac{2\sigma^4}{n-1}=V(s^2)$, and $\text{MSE}(S^2)=\frac{(2n-1)\sigma^4}{n^2}<\text{MSE}(s^2)$ - so $s^2$ is the LESS efficient of the two.",
 tips=[r"$S^2=\frac{n-1}{n}s^2$, so $V(S^2)=\left(\frac{n-1}{n}\right)^2V(s^2)$, strictly smaller.",
       r"MSE comparison: $\frac{2n-1}{n^2}$ against $\frac{2}{n-1}$; cross-multiplying gives $2n^2-3n+1<2n^2$.",
       r"The biased MLE beats the unbiased estimator on MSE - a classic bias-variance illustration."],
 sol=[r"$\dfrac{(n-1)s^2}{\sigma^2}\sim\chi^2_{n-1}$, so $V(s^2)=\dfrac{2\sigma^4}{n-1}$.",
      r"$S^2=\dfrac{n-1}{n}s^2$ gives $V(S^2)=\dfrac{(n-1)^2}{n^2}\cdot\dfrac{2\sigma^4}{n-1}=\dfrac{2(n-1)\sigma^4}{n^2}<V(s^2)$.",
      r"Including bias: $\text{MSE}(S^2)=\dfrac{2(n-1)\sigma^4}{n^2}+\dfrac{\sigma^4}{n^2}=\dfrac{(2n-1)\sigma^4}{n^2}$, and $(2n-1)(n-1)=2n^2-3n+1<2n^2$, so $\text{MSE}(S^2)<\text{MSE}(s^2)$.",
      r"On either measure $S^2$ is better, i.e. $s^2$ is less efficient than $S^2$ - option (b)."]),

"2025-Q4": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"$\sum X_i$ is complete sufficient and $\bar X=\frac{\sum X_i}{n}$ is its unbiased function - the UMVUE.",
 tips=[r"Lehmann-Scheffe in one line: unbiased function of a complete sufficient statistic.",
       r"$\frac{\sum X_i^2}{n}$ estimates $\lambda+\lambda^2$, not $\lambda$.",
       r"Dividing by $n+1$ (option b) destroys unbiasedness."],
 sol=[r"$T=\sum X_i$ is complete and sufficient for the Poisson parameter.",
      r"$E\left(\dfrac{T}{n}\right)=\lambda$, so $\bar X$ is an unbiased function of $T$.",
      r"By Lehmann-Scheffe it is the unique UMVUE.",
      r"Option (c)."]),

"2025-Q5": dict(ans=2, qtype="Definition", diff="Easy",
 sc=r"$T_0$ is UMVUE if $E_\theta(T_0-\theta)^2\le E_\theta(T-\theta)^2$ for every unbiased $T$ and every $\theta$.",
 tips=[r"For unbiased estimators, $E(T-\theta)^2$ IS the variance - the definition compares variances.",
       r"Note the direction: the UMVUE's variance is the SMALLEST, hence $\le$.",
       r"Options (a) and (d) compare first moments, which are all zero for unbiased estimators."],
 sol=[r"Within the class $S$ of unbiased estimators with finite second moment, $E_\theta(T-\theta)^2=V_\theta(T)$.",
      r"$T_0$ is uniformly minimum variance unbiased if its variance is no larger than that of any competitor, for every $\theta$.",
      r"That is $E_\theta(T_0-\theta)^2\le E_\theta(T-\theta)^2$ for all $T\in S$ and all $\theta\in\Theta$.",
      r"Option (c)."]),

"2025-Q6": dict(ans=0, qtype="Numerical", diff="Easy",
 sc=r"$\frac{(n-1)s^2}{\sigma^2}\sim\chi^2_{n-1}$ with variance $2(n-1)$, so $V(s^2)=\frac{2\sigma^4}{n-1}$.",
 tips=[r"$V\left(\chi^2_k\right)=2k$ - the only fact needed.",
       r"$V(s^2)=\left(\frac{\sigma^2}{n-1}\right)^2\cdot2(n-1)=\frac{2\sigma^4}{n-1}$.",
       r"The answer must carry $\sigma^4$ (a variance of a variance), which rules out options (b), (c), (d)."],
 sol=[r"For a normal sample, $\dfrac{(n-1)s^2}{\sigma^2}\sim\chi^2_{n-1}$.",
      r"$V\left(\chi^2_{n-1}\right)=2(n-1)$, so $V\left(\dfrac{(n-1)s^2}{\sigma^2}\right)=2(n-1)$.",
      r"$\dfrac{(n-1)^2}{\sigma^4}V(s^2)=2(n-1)\Rightarrow V(s^2)=\dfrac{2\sigma^4}{n-1}$.",
      r"Option (a)."]),

"2025-Q7": dict(ans=2, qtype="Conceptual", diff="Medium",
 sc=r"With $\sigma$ known the length $2z_{\alpha/2}\sigma/\sqrt n$ is deterministic, so $n$ can be chosen; with $\sigma$ unknown the length involves the random $S$, so no fixed $n$ guarantees a fixed width.",
 tips=[r"$\sigma$ known $\Rightarrow$ length $=2z_{\alpha/2}\sigma/\sqrt n$: solve for $n$ directly.",
       r"$\sigma$ unknown $\Rightarrow$ length $=2t_{n-1,\alpha/2}S/\sqrt n$, and $S$ is random - the width is random too.",
       r"Fixed-width intervals with unknown $\sigma$ require SEQUENTIAL sampling (Stein's two-stage procedure)."],
 sol=[r"Statement I: with $\sigma$ known the interval is $\bar X\pm z_{\alpha/2}\dfrac{\sigma}{\sqrt n}$, of fixed length $2z_{\alpha/2}\sigma/\sqrt n$; setting this equal to a target length determines $n$. True.",
      r"Statement II: with $\sigma$ unknown the interval is $\bar X\pm t_{n-1,\alpha/2}\dfrac{S}{\sqrt n}$, whose length depends on the random $S$, so no fixed $n$ can guarantee a prescribed length. True.",
      r"Option (c)."]),

"2025-Q8": dict(ans=3, qtype="Conceptual", diff="Hard",
 sc=r"The range $T=T_2-T_1$ is ANCILLARY - its distribution is free of $\theta$ - so it is neither sufficient nor does it form a complete family.",
 tips=[r"$T_2-T_1=\left(X_{(n)}-\theta\right)-\left(X_{(1)}-\theta\right)$ depends only on the centred observations.",
       r"An ancillary statistic carries no information about $\theta$, so it cannot be sufficient.",
       r"Its family consists of a SINGLE distribution, so $E[g(T)]=0$ imposes only one equation and cannot force $g\equiv0$ - completeness fails."],
 sol=[r"Writing $Y_i=X_i-\theta\sim U(-\tfrac12,\tfrac12)$: $T=Y_{(n)}-Y_{(1)}$, whose distribution is free of $\theta$.",
      r"Statement I: an ancillary statistic carries no information about $\theta$; the likelihood cannot be factorised through $T$ alone, so $T$ is not sufficient. False.",
      r"Statement II: the family $\{P_\theta^T\}$ is a single fixed distribution. Take any non-zero $g$ with $E[g(T)]=0$ (e.g. $g(t)=t-E(T)$): completeness fails. False.",
      r"Neither statement is correct - option (d)."]),

"2025-Q9": dict(ans=0, qtype="Numerical", diff="Medium",
 sc=r"$E(T)=\mu$ (unbiased) but $V(T)=\tfrac14\left(\tfrac1n+1\right)\to\tfrac14\ne0$, so $T$ is inconsistent.",
 tips=[r"$X_{n+1}$ gets weight $\tfrac12$ regardless of $n$, so its variance contribution never vanishes.",
       r"$V(T)=\tfrac14V(\bar X_n)+\tfrac14V(X_{n+1})=\tfrac14\left(\tfrac1n+1\right)$.",
       r"Any estimator giving a single observation fixed positive weight cannot be consistent."],
 sol=[r"$E(T)=\tfrac12\left[E(\bar X_n)+E(X_{n+1})\right]=\tfrac12(\mu+\mu)=\mu$: unbiased.",
      r"$\bar X_n$ and $X_{n+1}$ are independent, so $V(T)=\tfrac14\left[\dfrac1n+1\right]$.",
      r"As $n\to\infty$, $V(T)\to\tfrac14\ne0$, so $T$ does not converge in probability to $\mu$.",
      r"Hence unbiased but inconsistent - option (a)."]),

"2025-Q10": dict(ans=3, qtype="Conceptual", diff="Hard",
 sc=r"Only $\frac{X_{(2)}+X_{(1)}-2\theta}{s}$ has both numerator and denominator scaling with $\sigma$, so its distribution is free of $(\theta,\sigma)$.",
 tips=[r"Write $X_i=\theta+\sigma Z_i$ with $Z_i$ from the standard member of the family.",
       r"Numerator: $X_{(2)}+X_{(1)}-2\theta=\sigma\left(Z_{(2)}+Z_{(1)}\right)$; denominator: $s=\sigma s_Z$. The $\sigma$ cancels.",
       r"Options (a) and (b) leave a free $\sigma$; option (c) uses $2\theta$ where $\theta$ belongs, so it is not even centred."],
 sol=[r"Write $X_i=\theta+\sigma Z_i$ where the $Z_i$ come from the standard member $f(z)$, whose law involves no parameters.",
      r"(a) $\bar X-\theta=\sigma\bar Z$: distribution depends on $\sigma$ - not a pivot.",
      r"(b) $X_{(2)}+X_{(1)}-2\theta=\sigma\left(Z_{(2)}+Z_{(1)}\right)$: still depends on $\sigma$ - not a pivot.",
      r"(d) $\dfrac{X_{(2)}+X_{(1)}-2\theta}{s}=\dfrac{\sigma\left(Z_{(2)}+Z_{(1)}\right)}{\sigma s_Z}=\dfrac{Z_{(2)}+Z_{(1)}}{s_Z}$, whose distribution involves no unknown parameter - a genuine pivot.",
      r"Option (d)."]),

"2025-Q11": dict(ans=0, qtype="Definition", diff="Easy",
 sc=r"For a composite null, $\alpha=\sup_{h\in H_0}P(x\in C\mid h)$ - the supremum of the rejection probability.",
 tips=[r"The level must bound the Type-I error for EVERY point of $\Theta_0$, hence the supremum.",
       r"$C^c$ is the acceptance region, so options (b) and (d) describe the wrong event.",
       r"Using the infimum (option c) would under-report the worst-case error rate."],
 sol=[r"A test of level $\alpha$ must satisfy $P_\theta(X\in C)\le\alpha$ for every $\theta\in\Theta_0$.",
      r"The size is the smallest such $\alpha$, namely $\sup_{h\in H_0}P(X\in C\mid h)$.",
      r"Option (a)."]),

"2025-Q12": dict(ans=2, qtype="Numerical", diff="Hard",
 sc=r"$f_1/f_0=\frac{3x}{4}$ is increasing, so the MP region is $x\ge c$; $1-\frac{c^2}{4}=0.36$ gives $c=1.6$, and the power is $1-\frac{1.6^3}{8}=0.488>0.36$, so the test is unbiased.",
 tips=[r"$\int_c^2\frac x2dx=1-\frac{c^2}{4}$ and $\int_c^2\frac{3x^2}{8}dx=1-\frac{c^3}{8}$ - two clean antiderivatives.",
       r"$c^2=4(1-0.36)=2.56$ gives $c=1.6$ exactly.",
       r"$1.6^3=4.096$, so the power is $1-0.512=0.488$; power $>$ size means unbiased."],
 sol=[r"$\dfrac{f_1(x)}{f_0(x)}=\dfrac{3x^2/8}{x/2}=\dfrac{3x}{4}$, increasing in $x$, so the MP test rejects for large $x$: $C=\{x\ge c\}$.",
      r"Size: $\displaystyle\int_c^2\frac x2dx=1-\frac{c^2}{4}=0.36\Rightarrow c^2=2.56\Rightarrow c=1.6$.",
      r"Power: $\displaystyle\int_{1.6}^2\frac{3x^2}{8}dx=1-\frac{(1.6)^3}{8}=1-\frac{4.096}{8}=1-0.512=0.488$. Statement I true.",
      r"Power $0.488>$ size $0.36$, so the test is unbiased. Statement II true.",
      r"Option (c)."]),

"2025-Q13": dict(ans=0, qtype="Numerical", diff="Medium",
 sc=r"$f_1/f_0=\frac{3x}{2}$ increasing, so reject for $x\ge c$; $1-c^2=0.19$ gives $c=0.9$, and the power is $1-0.9^3=0.271$.",
 tips=[r"$\int_c^1 2x\,dx=1-c^2$ and $\int_c^13x^2dx=1-c^3$ - the cleanest pair in this family.",
       r"$c^2=0.81\Rightarrow c=0.9$; then $c^3=0.729$.",
       r"$0.729$ is planted as option (c) - that is $c^3$, not the power."],
 sol=[r"$\dfrac{f_1(x)}{f_0(x)}=\dfrac{3x^2}{2x}=\dfrac{3x}{2}$, increasing, so the MP critical region is $x\ge c$.",
      r"Size: $P_{H_0}(X\ge c)=\displaystyle\int_c^12x\,dx=1-c^2=0.19\Rightarrow c^2=0.81\Rightarrow c=0.9$.",
      r"Power: $P_{H_1}(X\ge0.9)=\displaystyle\int_{0.9}^13x^2dx=1-(0.9)^3=1-0.729=0.271$.",
      r"Option (a)."]),

"2025-Q14": dict(ans=3, qtype="Conceptual", diff="Medium",
 sc=r"The normal family (one parameter unknown) is the standard MLR family on which UMP one-sided tests are built; the Cauchy is the classic family WITHOUT MLR.",
 tips=[r"One-parameter exponential families have MLR in the natural statistic - the normal with known variance is the archetype.",
       r"The Cauchy fails MLR, which is exactly why no UMP test exists for its location parameter.",
       r"Poisson and hypergeometric also possess MLR, so read the question as asking for the standard textbook family used to construct UMP tests."],
 sol=[r"A family has monotone likelihood ratio in $T$ if $\dfrac{L_{\theta_2}}{L_{\theta_1}}$ is a monotone function of $T$ whenever $\theta_2>\theta_1$.",
      r"For $N(\theta,\sigma^2)$ with $\sigma^2$ known, $\dfrac{L_{\theta_2}}{L_{\theta_1}}=\exp\left[\dfrac{(\theta_2-\theta_1)\sum x_i}{\sigma^2}-\text{const}\right]$, strictly increasing in $\sum x_i$ - MLR holds.",
      r"Karlin-Rubin then delivers the UMP one-sided test 'reject if $\bar X>c$'.",
      r"The Cauchy has no such monotone ratio, so it is the non-example. Option (d)."]),

"2025-Q15": dict(ans=0, qtype="Numerical", diff="Easy",
 sc=r"$E(X)=\frac{\theta+2\theta}{2}=\frac{3\theta}{2}$; with $\bar x=1.15$, $\hat\theta=\frac{2\bar x}{3}=0.767\approx0.77$.",
 tips=[r"$U(\theta,2\theta)$ has midpoint $\frac{3\theta}{2}$ - the mean of the endpoints.",
       r"$\bar x=\frac{1.25+0.85+1.35}{3}=\frac{3.45}{3}=1.15$.",
       r"$\hat\theta=\frac23(1.15)=0.7667$; option (c) is the raw total $3.45$ and (b) is one observation."],
 sol=[r"For $U(\theta,2\theta)$, $E(X)=\dfrac{\theta+2\theta}{2}=\dfrac{3\theta}{2}$.",
      r"$\bar x=\dfrac{1.25+0.85+1.35}{3}=\dfrac{3.45}{3}=1.15$.",
      r"Method of moments: $\dfrac{3\hat\theta}{2}=1.15\Rightarrow\hat\theta=\dfrac{2(1.15)}{3}=0.7667$.",
      r"$\approx0.77$ - option (a)."]),

"2025-Q16": dict(ans=1, qtype="Conceptual", diff="Hard",
 sc=r"The correct OC exponent is $h(\mu)=\frac{\mu_0+\mu_1-2\mu}{\mu_1-\mu_0}$, so statement I is mis-stated. Statement II is true for the normal because $|E_{\mu_0}(Z)|=E_{\mu_1}(Z)$.",
 tips=[r"Check $h$ at the endpoints: the correct formula gives $h(\mu_0)=1$ and $h(\mu_1)=-1$; the printed version does not.",
       r"For $N(\mu,1)$, $E_\mu(Z)=(\mu_1-\mu_0)\left[\mu-\frac{\mu_0+\mu_1}{2}\right]$, so the drifts under $H_0$ and $H_1$ are equal and opposite.",
       r"With $\alpha=\beta$ we get $B=1/A$, so $\log B=-\log A$ and the two ASN expressions coincide."],
 sol=[r"Statement I: solving $E_\mu\left[e^{hZ}\right]=1$ for $N(\mu,1)$ gives $h(\mu)=\dfrac{\mu_0+\mu_1-2\mu}{\mu_1-\mu_0}$, with $h(\mu_0)=1$ and $h(\mu_1)=-1$. The printed expression has the sum and difference interchanged, so it is incorrect. False.",
      r"Statement II: $E_\mu(Z)=(\mu_1-\mu_0)\left[\mu-\dfrac{\mu_0+\mu_1}{2}\right]$, so $E_{\mu_0}(Z)=-\dfrac{(\mu_1-\mu_0)^2}{2}$ and $E_{\mu_1}(Z)=+\dfrac{(\mu_1-\mu_0)^2}{2}$.",
      r"With $\alpha=\beta$, $A=\dfrac{1-\alpha}{\alpha}$ and $B=\dfrac{\alpha}{1-\alpha}=\dfrac1A$, so $\log B=-\log A$. Substituting into $E(N)=\dfrac{L\log B+(1-L)\log A}{E(Z)}$ with $L(\mu_0)=1-\alpha$, $L(\mu_1)=\beta=\alpha$ gives the same value under both hypotheses. True.",
      r"Hence II only - option (b)."]),

"2025-Q17": dict(ans=2, qtype="Conceptual", diff="Medium",
 sc=r"$X_{(n)}$ is complete sufficient for both $U(0,\theta)$ and the discrete uniform on $\{1,\dots,N\}$.",
 tips=[r"Both likelihoods are of the form $c(\theta)^{-n}I\left(X_{(n)}\le\theta\right)$ - sufficiency is immediate.",
       r"Completeness in the continuous case: $\int_0^\theta g(t)nt^{n-1}dt=0$ for all $\theta$ forces $g\equiv0$ by differentiation.",
       r"Completeness in the discrete case follows by induction on $N$ from $E_N[g]=0$."],
 sol=[r"Case I: $L=\theta^{-n}I\left(X_{(n)}\le\theta\right)$, so $X_{(n)}$ is sufficient. If $E_\theta\left[g\left(X_{(n)}\right)\right]=\int_0^\theta g(t)\frac{nt^{n-1}}{\theta^n}dt=0$ for all $\theta$, differentiating in $\theta$ gives $g(\theta)\theta^{n-1}=0$, so $g\equiv0$: complete.",
      r"Case II: $L=N^{-n}I\left(X_{(n)}\le N\right)$, so $X_{(n)}$ is sufficient. $E_N\left[g\left(X_{(n)}\right)\right]=0$ for $N=1,2,\dots$ forces $g(1)=0$, then $g(2)=0$, and so on by induction: complete.",
      r"Both hold - option (c)."]),

"2025-Q18": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"$\left(\sum X_i,\sum X_i^2\right)$ is the natural statistic of the two-parameter normal exponential family with an open natural parameter space - sufficient AND complete.",
 tips=[r"Full-rank exponential family $\Rightarrow$ the natural sufficient statistic is complete.",
       r"This is why $\bar X$ and $S^2$ are the UMVUEs of $\mu$ and $\sigma^2$.",
       r"Contrast $N(\theta,\theta^2)$, a CURVED exponential family, where the same statistic is sufficient but not complete."],
 sol=[r"$L\propto\sigma^{-n}\exp\left[-\dfrac{\sum x_i^2}{2\sigma^2}+\dfrac{\mu\sum x_i}{\sigma^2}-\dfrac{n\mu^2}{2\sigma^2}\right]$, so $T$ is sufficient by factorisation.",
      r"The natural parameters $\left(\dfrac{\mu}{\sigma^2},-\dfrac{1}{2\sigma^2}\right)$ range over an open subset of $\mathbb{R}^2$, making this a full-rank two-parameter exponential family.",
      r"Hence the natural sufficient statistic $T$ is also complete.",
      r"Option (c)."]),

"2025-Q19": dict(ans=3, qtype="Conceptual", diff="Medium",
 sc=r"$-\log F_\theta(X_i)\sim\text{Exp}(1)$, so $-\sum\log F_\theta(X_i)\sim\text{Gamma}(n,1)$ - a distribution free of $\theta$, hence a pivot.",
 tips=[r"Probability integral transform: $F_\theta(X)\sim U(0,1)$, and $-\log U\sim\text{Exp}(1)$.",
       r"$-2\sum\log F_\theta(X_i)\sim\chi^2_{2n}$ - the form used in Fisher's method of combining $p$-values.",
       r"The SIGN matters: $\sum\log F_\theta(X_i)$ is negative; option (b) omits the minus sign but is still parameter-free - option (d) is the standard positive-valued pivot."],
 sol=[r"By the probability integral transform, $U_i=F_\theta(X_i)\sim U(0,1)$ independently.",
      r"$-\log U_i\sim\text{Exp}(1)$, so $-\sum_{i=1}^n\log F_\theta(X_i)\sim\text{Gamma}(n,1)$.",
      r"This distribution involves no unknown parameter, so the quantity is pivotal (and $-2\sum\log F_\theta(X_i)\sim\chi^2_{2n}$).",
      r"Option (d)."]),
}
