# -*- coding: utf-8 -*-
# Statistical Inference answer pack 3 (2019 Q13-Q47)
A = {
"2019-Q13": dict(ans=1, qtype="Conceptual", diff="Easy",
 sc=r"A simple hypothesis pins down EVERY unknown parameter. With both $\mu$ and $\sigma^2$ unknown, only $\{\mu=\mu_0,\ \sigma^2=\sigma_0^2\}$ does that.",
 tips=[r"Simple $\iff$ the hypothesis specifies the distribution completely (a single point of the parameter space).",
       r"Any inequality or any '$\ne$' makes the hypothesis composite - scan for those symbols first.",
       r"If $\sigma^2$ were known, $\mu=\mu_0$ alone would be simple - always check which parameters are declared unknown."],
 sol=[r"The parameter is the pair $(\mu,\sigma^2)$, both unknown, so the parameter space is two-dimensional.",
      r"A simple hypothesis must reduce that space to a single point.",
      r"(a), (c) and (d) leave one component free (via $\ne$, $>$ or $<$), so they are composite.",
      r"Only (b) fixes both components - option (b)."]),

"2019-Q14": dict(ans=2, qtype="Numerical", diff="Easy",
 sc=r"Under $H_0:\theta=1$, $X\sim U(0,1)$ so $P(X>1)=0$. The size is exactly $0$.",
 tips=[r"Size is computed under $H_0$ only - here the support is $(0,1)$, so the region $\{x>1\}$ is impossible.",
       r"Such a test has size $0$ but power $P(X>1\mid\theta=2)=\tfrac12$ - a legitimate, if unusual, test.",
       r"Whenever a critical region lies outside the null support, the answer is $0$ without any integration."],
 sol=[r"Under $H_0:\theta=1$, $X$ is uniform on $(0,1)$.",
      r"The critical region is $\omega=\{x>1\}$, which lies entirely outside the support of $X$ under $H_0$.",
      r"Hence size $=P(X>1\mid\theta=1)=0$.",
      r"Option (c)."]),

"2019-Q15": dict(ans=2, qtype="Numerical", diff="Hard",
 sc=r"For $U(0,\theta)^2$, $P(X+Y\le s)=\frac{s^2}{2\theta^2}$ while $s\le\theta$. So $\alpha=1-\frac{0.75^2}{2}=0.72$ and power $=1-\frac{0.75^2}{8}=0.93$.",
 tips=[r"The sum of two independent uniforms has a triangular density; for $s$ below the upper limit the cdf is just the area of a right triangle, $\frac{s^2}{2\theta^2}$.",
       r"Under $H_0$, $\theta=1$: area $=0.75^2/2=0.28125$. Under $H_1$, $\theta=2$: area $=0.5625/8=0.0703$.",
       r"Power must exceed size for a sensible test - a quick screen that kills option (b)."],
 sol=[r"For $X,Y$ iid $U(0,\theta)$ and $0\le s\le\theta$, $P(X+Y\le s)=\dfrac{s^2}{2\theta^2}$ (area of the triangle $x+y\le s$ divided by $\theta^2$).",
      r"Type-I error at $\theta=1$: $\alpha=1-\dfrac{(0.75)^2}{2(1)^2}=1-0.28125=0.71875\approx0.72$.",
      r"Power at $\theta=2$: $1-\dfrac{(0.75)^2}{2(2)^2}=1-\dfrac{0.5625}{8}=1-0.0703=0.9297\approx0.93$.",
      r"Hence $(0.72,\ 0.93)$ - option (c)."]),

"2019-Q16": dict(ans=0, qtype="Conceptual", diff="Easy",
 sc=r"The critical region is calibrated so that the rejection probability under $H_0$ equals $\alpha$ - so the null distribution is what is needed.",
 tips=[r"Under $H_0$ you fix the size; under $H_1$ you compute the power. Two different jobs, two different distributions.",
       r"This is why 'null distribution of the test statistic' is the phrase that appears in every testing derivation.",
       r"Without $H_0$ the critical value cannot even be defined."],
 sol=[r"A critical region $C$ of size $\alpha$ satisfies $P(T\in C\mid H_0)=\alpha$.",
      r"To solve for the critical value we therefore need the sampling distribution of $T$ under $H_0$.",
      r"The alternative distribution is used later, to evaluate power.",
      r"Option (a)."]),

"2019-Q17": dict(ans=3, qtype="Conceptual", diff="Hard",
 flag=r"Source anomaly: statements 1 and 4 are true and statements 2 and 3 are false, so the exact truth set $\{1,4\}$ is not among the printed options. Option (d) is the closest offered combination (it contains both true statements) and is taken as the intended key; the source wording and options are preserved unchanged.",
 sc=r"A UMP test is always unbiased (statement 1) and an unbiased test need not be UMP (statement 4). Statements 2 and 3 are false.",
 tips=[r"UMP $\Rightarrow$ unbiased: compare with the trivial test $\phi\equiv\alpha$, whose power is $\alpha$ everywhere.",
       r"Consistency is an asymptotic property (power $\to1$ as $n\to\infty$); it says nothing about optimality at fixed $n$.",
       r"Statements 1 and 3 are direct contradictions, so at most one of them can be in the key - a useful internal check."],
 sol=[r"Statement 1: a UMP test has power at least that of $\phi\equiv\alpha$, hence power $\ge\alpha$ - it is unbiased. True.",
      r"Statement 2: consistency means power $\to1$ as $n\to\infty$; it carries no finite-sample optimality, so a consistent test need not be UMP. False.",
      r"Statement 3: contradicts statement 1. False.",
      r"Statement 4: many unbiased tests are not most powerful (e.g. any inefficient unbiased test). True.",
      r"The genuine answer set is $\{1,4\}$; among the printed options only (d) contains both, and it is taken as the key."]),

"2019-Q18": dict(ans=0, qtype="Numerical", diff="Hard",
 sc=r"UMP region is $\{X_{(2)}>c\}$ with $1-(c/2)^2=\alpha$, so $c=2\sqrt{1-\alpha}$; power at $\theta_1$ is $1-(c/\theta_1)^2=1-\left(\frac2{\theta_1}\right)^2(1-\alpha)$.",
 tips=[r"$U(0,\theta)$ has monotone likelihood ratio in $X_{(n)}$, so Karlin-Rubin gives a UMP one-sided test based on the maximum.",
       r"$P(X_{(n)}\le c)=(c/\theta)^n$ - here $n=2$, which is where the square comes from.",
       r"Unusually, the same test is UMP for the two-sided alternative too, because the power function is monotone over the whole admissible range."],
 sol=[r"For $X_1,X_2$ iid $U(0,\theta)$, $P(X_{(2)}\le c)=\left(\dfrac c\theta\right)^2$.",
      r"The UMP test for $\theta=2$ vs $\theta>2$ rejects when $X_{(2)}>c$, with $\alpha=1-\left(\dfrac c2\right)^2$, i.e. $c^2=4(1-\alpha)$.",
      r"Power at $\theta_1$: $1-\left(\dfrac{c}{\theta_1}\right)^2=1-\dfrac{4(1-\alpha)}{\theta_1^2}=1-\left(\dfrac{2}{\theta_1}\right)^2(1-\alpha)$.",
      r"Option (a)."]),

"2019-Q19": dict(ans=0, qtype="Numerical", diff="Medium",
 sc=r"$\log\frac{L_1}{L_0}=n(\mu_1-\mu_0)\bar x-\frac n2(\mu_1^2-\mu_0^2)>\log k$ rearranges to $\bar x\ge\frac{\mu_0+\mu_1}2+\frac{\log k}{n(\mu_1-\mu_0)}$.",
 tips=[r"Since $\mu_1>\mu_0$, dividing by $n(\mu_1-\mu_0)>0$ preserves the inequality - the region is an upper tail in $\bar x$.",
       r"$\frac{\mu_1^2-\mu_0^2}{2(\mu_1-\mu_0)}=\frac{\mu_0+\mu_1}{2}$: the SUM appears, not the difference. Options (c) and (d) use the difference and are wrong.",
       r"In practice the constant reduces to $\mu_0+z_\alpha/\sqrt n$, but the NP form is what is asked here."],
 sol=[r"$\dfrac{L_1}{L_0}=\exp\left\{-\tfrac12\sum(x_i-\mu_1)^2+\tfrac12\sum(x_i-\mu_0)^2\right\}=\exp\left\{n(\mu_1-\mu_0)\bar x-\tfrac n2(\mu_1^2-\mu_0^2)\right\}$.",
      r"Rejecting when this exceeds $k_\alpha$ means $n(\mu_1-\mu_0)\bar x>\log k_\alpha+\tfrac n2(\mu_1^2-\mu_0^2)$.",
      r"Dividing by $n(\mu_1-\mu_0)>0$: $\bar x>\dfrac{\mu_1+\mu_0}{2}+\dfrac{\log k_\alpha}{n(\mu_1-\mu_0)}$.",
      r"Option (a)."]),

"2019-Q20": dict(ans=1, qtype="Numerical", diff="Hard",
 sc=r"With $\theta_1<\theta_0$ the likelihood ratio is decreasing in $\sum x_i$, so the BCR is a LOWER tail: $\sum x_i<\frac{\theta_0}2\chi^2_{2n,\alpha}$, using $2\sum X_i/\theta\sim\chi^2_{2n}$.",
 tips=[r"$2\sum X_i/\theta\sim\chi^2_{2n}$ for an exponential sample with mean $\theta$ - the pivotal fact for this whole family.",
       r"Direction rule: alternative with SMALLER mean $\Rightarrow$ reject for SMALL $\sum x_i$.",
       r"Here $\chi^2_{2n,\alpha}$ denotes the lower $\alpha$-point, so that $P(\chi^2_{2n}<\chi^2_{2n,\alpha})=\alpha$."],
 sol=[r"$\dfrac{L_1}{L_0}=\left(\dfrac{\theta_0}{\theta_1}\right)^n\exp\left\{-\left(\dfrac1{\theta_1}-\dfrac1{\theta_0}\right)\sum x_i\right\}$.",
      r"Since $\theta_1<\theta_0$, the coefficient $\left(\frac1{\theta_1}-\frac1{\theta_0}\right)>0$, so the ratio decreases in $\sum x_i$; the ratio exceeds $k$ exactly when $\sum x_i$ is small.",
      r"Under $H_0$, $\dfrac{2\sum X_i}{\theta_0}\sim\chi^2_{2n}$, so the size-$\alpha$ cut-off satisfies $\dfrac{2c}{\theta_0}=\chi^2_{2n,\alpha}$, the lower $\alpha$-point.",
      r"Hence the BCR is $\left\{\sum x_i<\dfrac{\theta_0}{2}\chi^2_{2n,\alpha}\right\}$ - option (b)."]),

"2019-Q24": dict(ans=1, qtype="Conceptual", diff="Easy",
 sc=r"Lindeberg-Feller and Lyapunov are central limit theorems; Khinchin's theorem is a WEAK LAW OF LARGE NUMBERS.",
 tips=[r"Khinchin: iid with finite mean $\Rightarrow$ $\bar X\xrightarrow{P}\mu$. That is convergence to a constant, not to a normal law.",
       r"Lyapunov's condition (a moment condition) implies Lindeberg's condition, which is the definitive CLT criterion for non-identically distributed summands.",
       r"CLT $\Rightarrow$ a distributional limit; LLN $\Rightarrow$ a constant limit. Classify by what the limit is."],
 sol=[r"Lindeberg-Feller gives conditions under which a sum of independent (not necessarily identically distributed) variables is asymptotically normal - a CLT.",
      r"Lyapunov's theorem is a CLT under a moment condition that implies Lindeberg's condition.",
      r"Khinchin's theorem states the weak law of large numbers for iid variables with finite mean - not a CLT.",
      r"Hence 1 and 2 - option (b)."]),

"2019-Q31": dict(ans=0, qtype="Conceptual", diff="Easy",
 sc=r"$\hat\mu=\bar X$ is unbiased, but $\hat\sigma^2=\frac1n\sum(X_i-\bar X)^2$ has expectation $\frac{n-1}{n}\sigma^2$ - biased.",
 tips=[r"The MLE of $\sigma^2$ uses divisor $n$ and is the classic example of a biased MLE.",
       r"$E\left[\sum(X_i-\bar X)^2\right]=(n-1)\sigma^2$ always - one df is lost estimating $\mu$.",
       r"The bias disappears asymptotically, consistent with MLEs being asymptotically unbiased."],
 sol=[r"Maximising the normal likelihood gives $\hat\mu=\bar X$ and $\hat\sigma^2=\dfrac1n\sum(X_i-\bar X)^2$.",
      r"$E(\hat\mu)=\mu$: statement 1 is true.",
      r"$E\left[\sum(X_i-\bar X)^2\right]=(n-1)\sigma^2$, so $E(\hat\sigma^2)=\dfrac{n-1}{n}\sigma^2\ne\sigma^2$: statement 2 is false.",
      r"Hence 1 only - option (a)."]),

"2019-Q32": dict(ans=0, qtype="Conceptual", diff="Easy",
 sc=r"A stationary point is a MAXIMUM when the second derivative is negative: $l<0$.",
 tips=[r"$\frac{\partial\log L}{\partial\theta}=0$ only locates a stationary point; the sign of the second derivative classifies it.",
       r"$-\frac{\partial^2\log L}{\partial\theta^2}$ evaluated at $\hat\theta$ is the observed information, which must be positive.",
       r"If $l=0$ the test is inconclusive and higher derivatives must be examined."],
 sol=[r"$\hat\theta$ solves the likelihood equation $\dfrac{\partial\log L}{\partial\theta}=0$.",
      r"By the second-derivative test, this stationary point is a local maximum of $\log L$ (and hence of $L$) if $\left[\dfrac{\partial^2\log L}{\partial\theta^2}\right]_{\hat\theta}<0$.",
      r"So the condition is $l<0$ - option (a)."]),

"2019-Q33": dict(ans=1, qtype="Numerical", diff="Hard",
 sc=r"$L=(\theta^2-\theta)^{-3}$ subject to $\theta\le X_{(1)}$ and $\theta^2\ge X_{(3)}$. For $\theta>1$, $L$ decreases in $\theta$, so take $\theta$ as small as allowed: $\hat\theta=\sqrt{X_{(3)}}$.",
 tips=[r"Support-dependent parameter: read off the two constraints, then see which direction the likelihood pushes.",
       r"$\theta^2-\theta$ is increasing for $\theta>1$, so $1/(\theta^2-\theta)^3$ is decreasing - the maximum is at the SMALLEST admissible $\theta$.",
       r"The binding lower constraint is $\theta\ge\sqrt{X_{(3)}}$, coming from the upper endpoint of the support."],
 sol=[r"The density is $\dfrac1{\theta^2-\theta}$ on $(\theta,\theta^2)$, so $L(\theta)=\dfrac1{(\theta^2-\theta)^3}$ provided $\theta\le X_{(1)}$ and $X_{(3)}\le\theta^2$.",
      r"The constraints give $\sqrt{X_{(3)}}\le\theta\le X_{(1)}$.",
      r"For $\theta>1$, $\theta^2-\theta$ is increasing, so $L$ is strictly decreasing in $\theta$; the maximum is at the smallest admissible value.",
      r"Hence $\hat\theta=\sqrt{X_{(3)}}$ - option (b)."]),

"2019-Q34": dict(ans=3, qtype="Conceptual", diff="Medium",
 sc=r"Equality in the Cramer-Rao inequality holds iff the SCORE is proportional to $t-\theta$: $\frac{\partial\log L}{\partial\theta}=\frac{t-\theta}{\lambda}$.",
 tips=[r"The condition involves the FIRST derivative of $\log L$, not the second - options (a) and (c) can be discarded at once.",
       r"$\lambda=\lambda(\theta)$ may depend on $\theta$ but never on the data - that is exactly what 'free from sample values' means.",
       r"When this holds, $V(t)=\lambda(\theta)$ equals the bound; it is the definition of an MVB estimator."],
 sol=[r"The Cramer-Rao inequality comes from the Cauchy-Schwarz inequality applied to $\operatorname{Cov}\left(t,\dfrac{\partial\log L}{\partial\theta}\right)$.",
      r"Equality in Cauchy-Schwarz holds iff the two variables are linearly related, i.e. $\dfrac{\partial\log L}{\partial\theta}=A(\theta)\left[t-\theta\right]$.",
      r"Writing $A(\theta)=1/\lambda$ gives $\dfrac{\partial\log L}{\partial\theta}=\dfrac{t-\theta}{\lambda}$.",
      r"Option (d)."]),

"2019-Q35": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"Mean zero is known, so the score gives $T=\frac1n\sum X_i^2$, which attains the bound $2\sigma^4/n$.",
 tips=[r"With a known mean, deviations are measured from $0$ and no degree of freedom is lost - divisor $n$, not $n-1$.",
       r"$\sum X_i^2/\sigma^2\sim\chi^2_n$ gives $V(T)=2\sigma^4/n$, exactly the CR bound.",
       r"Estimators built on $\bar X$ waste information here, and their variance $2\sigma^4/(n-1)$ exceeds the bound."],
 sol=[r"For $N(0,\sigma^2)$, $\log L=-\tfrac n2\log(2\pi\sigma^2)-\dfrac{\sum x_i^2}{2\sigma^2}$.",
      r"$\dfrac{\partial\log L}{\partial\sigma^2}=\dfrac{n}{2\sigma^4}\left[\dfrac{\sum x_i^2}{n}-\sigma^2\right]$, which is of the MVB attainment form with $T=\dfrac1n\sum X_i^2$.",
      r"$\sum X_i^2/\sigma^2\sim\chi^2_n$, so $E(T)=\sigma^2$ and $V(T)=\dfrac{2\sigma^4}{n}$, equal to the CR bound.",
      r"Option (c)."]),

"2019-Q36": dict(ans=0, qtype="Conceptual", diff="Medium",
 sc=r"The support $(0,\theta)$ depends on $\theta$, so the regularity conditions fail and the Cramer-Rao bound does not exist.",
 tips=[r"Regularity requires the support to be free of $\theta$ so that differentiation under the integral sign is valid.",
       r"For $U(0,\theta)$ the UMVUE $\frac{n+1}nX_{(n)}$ has variance $\frac{\theta^2}{n(n+2)}$, of order $n^{-2}$ - faster than any CR-type $n^{-1}$ bound could allow.",
       r"Whenever the support endpoints involve the parameter, write 'CR bound not applicable' rather than computing anything."],
 sol=[r"The Cramer-Rao inequality requires regularity: in particular the support of $f(x;\theta)$ must not depend on $\theta$, so that $\dfrac{\partial}{\partial\theta}\int f\,dx=\int\dfrac{\partial f}{\partial\theta}dx$.",
      r"For $U(0,\theta)$ the support is $(0,\theta)$, which depends on $\theta$; the interchange fails.",
      r"Hence the Cramer-Rao lower bound does not exist for this family.",
      r"Option (a)."]),

"2019-Q37": dict(ans=2, qtype="Numerical", diff="Easy",
 sc=r"$I_1(\theta)=1/\theta^2$ for the exponential with mean $\theta$, so the bound is $\theta^2/n$ (attained by $\bar X$).",
 tips=[r"$\log f=-\log\theta-x/\theta$, $\frac{\partial\log f}{\partial\theta}=-\frac1\theta+\frac{x}{\theta^2}$, giving $I_1=\frac1{\theta^2}$.",
       r"$V(\bar X)=\theta^2/n$ exactly, so the bound is attained and $\bar X$ is the MVB estimator.",
       r"The bound must have the same units as $\theta^2$; options linear in $\theta$ are dimensionally impossible."],
 sol=[r"$\log f(x;\theta)=-\log\theta-\dfrac x\theta$, so $\dfrac{\partial\log f}{\partial\theta}=-\dfrac1\theta+\dfrac{x}{\theta^2}=\dfrac{x-\theta}{\theta^2}$.",
      r"$I_1(\theta)=E\left[\left(\dfrac{X-\theta}{\theta^2}\right)^2\right]=\dfrac{V(X)}{\theta^4}=\dfrac{\theta^2}{\theta^4}=\dfrac1{\theta^2}$.",
      r"$I_n(\theta)=\dfrac{n}{\theta^2}$, so the CR lower bound for unbiased estimators of $\theta$ is $\dfrac1{I_n}=\dfrac{\theta^2}{n}$.",
      r"Option (c)."]),

"2019-Q38": dict(ans=3, qtype="Numerical", diff="Easy",
 sc=r"$E(\bar X\mid X_1)=\tfrac12\left[X_1+E(X_2)\right]=\dfrac{X_1+\theta}{2}$ by independence.",
 tips=[r"Conditioning on $X_1$ leaves $X_2$ untouched: $E(X_2\mid X_1)=E(X_2)=\theta$.",
       r"The answer still involves $\theta$ because $X_1$ is NOT sufficient - this is exactly why Rao-Blackwell insists on conditioning on a sufficient statistic.",
       r"Contrast: $E(X_1\mid\bar X)=\bar X$, which is free of $\theta$ and is the genuine Rao-Blackwell improvement."],
 sol=[r"$\bar X=\dfrac{X_1+X_2}{2}$, so $E(\bar X\mid X_1)=\dfrac12\left[X_1+E(X_2\mid X_1)\right]$.",
      r"$X_1$ and $X_2$ are independent, so $E(X_2\mid X_1)=E(X_2)=\theta$.",
      r"Hence $E(\bar X\mid X_1)=\dfrac{X_1+\theta}{2}$ - option (d)."]),

"2019-Q39": dict(ans=1, qtype="Conceptual", diff="Easy",
 sc=r"For $B(2,\theta)$ the likelihood is $\theta^{\sum x_i}(1-\theta)^{4-\sum x_i}$ times a binomial constant, so $X_1+X_2$ is sufficient.",
 tips=[r"Binomial and Bernoulli always reduce to the total $\sum X_i$ - a one-parameter exponential family.",
       r"Differences and ratios throw away the total and cannot be sufficient.",
       r"Here $X_1+X_2\sim B(4,\theta)$, which is also complete, so $\frac{X_1+X_2}{4}$ is the UMVUE of $\theta$."],
 sol=[r"$P(X_i=x_i)=\binom{2}{x_i}\theta^{x_i}(1-\theta)^{2-x_i}$, so $L=\binom{2}{x_1}\binom{2}{x_2}\theta^{x_1+x_2}(1-\theta)^{4-(x_1+x_2)}$.",
      r"Factorising with $g_\theta(t)=\theta^{t}(1-\theta)^{4-t}$, $t=x_1+x_2$, and $h(x)=\binom{2}{x_1}\binom{2}{x_2}$, the total is sufficient.",
      r"Option (b)."]),

"2019-Q40": dict(ans=0, qtype="Numerical", diff="Medium",
 sc=r"$E(\bar X^2)=\theta^2+\frac1n$, so subtracting $\frac1n$ gives an unbiased function of the complete sufficient statistic $\bar X$: the MVUE is $\bar X^2-\frac1n$.",
 tips=[r"Never estimate $\theta^2$ by $\bar X^2$ - the bias is exactly the variance of $\bar X$.",
       r"$\bar X$ is complete sufficient for $N(\theta,1)$, so any unbiased function of it is the UMVUE (Lehmann-Scheffe).",
       r"The same correction appears everywhere: $\widehat{\mu^2}=\bar X^2-\frac{\sigma^2}{n}$."],
 sol=[r"$\bar X\sim N\left(\theta,\dfrac1n\right)$, so $E(\bar X^2)=V(\bar X)+\left[E(\bar X)\right]^2=\dfrac1n+\theta^2$.",
      r"Hence $T=\bar X^2-\dfrac1n$ satisfies $E(T)=\theta^2$.",
      r"$\bar X$ is complete and sufficient for $\theta$, and $T$ is a function of it, so by Lehmann-Scheffe $T$ is the unique UMVUE.",
      r"Option (a)."]),

"2019-Q41": dict(ans=0, qtype="Numerical", diff="Medium",
 sc=r"$E(\bar X^2)=\frac{\theta^2}n+\theta^2=\frac{(n+1)\theta^2}{n}$, so $E\left[\frac{n\bar X^2}{n+1}\right]=\theta^2=E(S^2)$; the difference has expectation $0$.",
 tips=[r"In $N(\theta,\theta^2)$ the variance equals $\theta^2$, so $V(\bar X)=\theta^2/n$ - the model deliberately links mean and variance.",
       r"$\frac{n}{n+1}\bar X^2$ is the bias-corrected version of $\bar X^2$ for estimating $\theta^2$ in this model.",
       r"$S^2$ (divisor $n-1$) is always unbiased for the variance, here $\theta^2$."],
 sol=[r"$\bar X\sim N\left(\theta,\dfrac{\theta^2}{n}\right)$, so $E(\bar X^2)=\dfrac{\theta^2}{n}+\theta^2=\dfrac{(n+1)\theta^2}{n}$.",
      r"Hence $E\left[\dfrac{n\bar X^2}{n+1}\right]=\dfrac{n}{n+1}\cdot\dfrac{(n+1)\theta^2}{n}=\theta^2$.",
      r"$E(S^2)=\sigma^2=\theta^2$.",
      r"Therefore $E\left[\dfrac{n\bar X^2}{n+1}-S^2\right]=\theta^2-\theta^2=0$ - option (a)."]),

"2019-Q42": dict(ans=0, qtype="Conceptual", diff="Medium",
 sc=r"$N(0,\sigma^2)$ is symmetric about $0$, but the family is NOT complete: $E_\sigma(X)=0$ for all $\sigma$ with $g(x)=x\not\equiv0$.",
 tips=[r"Completeness means no non-trivial unbiased estimator of zero exists - here $X$ itself is one.",
       r"Symmetry is exactly what destroys completeness: every odd function has expectation zero for every $\sigma^2$.",
       r"$X^2$ (or $\sum X_i^2$) IS complete and sufficient for this family - the even reduction restores completeness."],
 sol=[r"Statement 1: the $N(0,\sigma^2)$ density is an even function of $x$, so the family is symmetric about $0$. True.",
      r"Statement 2: take $g(x)=x$. Then $E_\sigma[g(X)]=0$ for every $\sigma^2>0$, yet $g$ is not identically zero.",
      r"This violates the definition of completeness, so the family is not complete. False.",
      r"Hence 1 only - option (a)."]),

"2019-Q43": dict(ans=3, qtype="Conceptual", diff="Easy",
 sc=r"Rao-Blackwell: condition the UNBIASED estimator on the SUFFICIENT statistic, i.e. form $E(T_2\mid T_1)$.",
 tips=[r"Order matters: $E(\text{unbiased}\mid\text{sufficient})$. Reversing it gives a quantity that still depends on $\theta$.",
       r"Sufficiency of $T_1$ is what makes $E(T_2\mid T_1)$ a genuine statistic (free of $\theta$).",
       r"The improvement is $V(T_2)-V\left[E(T_2\mid T_1)\right]=E\left[V(T_2\mid T_1)\right]\ge0$."],
 sol=[r"The Rao-Blackwell theorem says: if $T_2$ is unbiased for $\theta$ and $T_1$ is sufficient, then $\phi(T_1)=E(T_2\mid T_1)$ is",
      r"(i) a statistic, because sufficiency makes the conditional distribution free of $\theta$;",
      r"(ii) unbiased, since $E[\phi(T_1)]=E(T_2)=\theta$;",
      r"(iii) at least as good: $V[\phi(T_1)]\le V(T_2)$.",
      r"Hence the improved estimator is $E(T_2\mid T_1)$ - option (d)."]),

"2019-Q44": dict(ans=0, qtype="Conceptual", diff="Medium",
 sc=r"For simple vs simple, the LR criterion $\lambda=L_0/\max(L_0,L_1)$ is small exactly when $L_1/L_0$ is large - the two tests coincide.",
 tips=[r"$\lambda=\dfrac{L_0}{\max(L_0,L_1)}$, so $\lambda<c\iff\dfrac{L_1}{L_0}>\dfrac1c-\dots$ - a monotone relabelling of the same statistic.",
       r"With randomisation allowed on the boundary they agree exactly; the stem restricts attention to the non-randomised case where both exist.",
       r"For composite hypotheses the two procedures genuinely diverge - the LRT is then generally not most powerful."],
 sol=[r"For $H_0:\theta=\theta_0$ vs $H_1:\theta=\theta_1$, $\sup_{\Theta_0}L=L_0$ and $\sup_{\Theta}L=\max(L_0,L_1)$.",
      r"Hence $\lambda=\dfrac{L_0}{\max(L_0,L_1)}$, which is a strictly decreasing function of the likelihood ratio $L_1/L_0$ whenever $L_1>L_0$.",
      r"Rejecting for small $\lambda$ is therefore identical to rejecting for large $L_1/L_0$, which is the Neyman-Pearson rule.",
      r"When both non-randomised tests exist at level $\alpha$, they are one and the same test - option (a)."]),

"2019-Q45": dict(ans=1, qtype="Numerical", diff="Medium",
 sc=r"$P(X_1\ge t)=e^{-(t-\mu)}$ and $\hat\mu=X_{(1)}$; by invariance the MLE is $e^{-(t-X_{(1)})}$.",
 tips=[r"Invariance: MLE of $g(\mu)$ is $g(\hat\mu)$ - no fresh maximisation required.",
       r"$\hat\mu=X_{(1)}$ because the support is $x\ge\mu$ and the likelihood increases in $\mu$.",
       r"Compute the survival function first: $\int_t^\infty e^{-(x-\mu)}dx=e^{-(t-\mu)}$."],
 sol=[r"$P(X_1\ge t)=\displaystyle\int_t^\infty e^{-(x-\mu)}dx=e^{-(t-\mu)}$ for $t>\mu$.",
      r"The MLE of $\mu$ is $\hat\mu=X_{(1)}$ (the likelihood $e^{n\mu-\sum x_i}$ increases in $\mu$ up to $X_{(1)}$).",
      r"By the invariance property, the MLE of $e^{-(t-\mu)}$ is $e^{-(t-\hat\mu)}=e^{-(t-X_{(1)})}$.",
      r"Option (b)."]),

"2019-Q46": dict(ans=0, qtype="Definition", diff="Easy",
 sc=r"$P\{|t_n-\theta|<\varepsilon\}\to1$ for every $\varepsilon>0$ is exactly convergence in probability - the definition of consistency.",
 tips=[r"Consistency is a large-sample (asymptotic) property; unbiasedness and sufficiency are finite-sample properties.",
       r"Convergence in probability says nothing about $E(t_n)$, so consistency neither implies nor is implied by unbiasedness.",
       r"A sufficient condition: unbiased (or asymptotically unbiased) plus $V(t_n)\to0$."],
 sol=[r"$t_n\xrightarrow{P}\theta$ means $P\{|t_n-\theta|\ge\varepsilon\}\to0$, equivalently $P\{|t_n-\theta|<\varepsilon\}\to1$, for every $\varepsilon>0$.",
      r"This is the definition of a (weakly) consistent estimator.",
      r"Option (a)."]),

"2019-Q47": dict(ans=2, qtype="Numerical", diff="Easy",
 sc=r"$\hat p=\bar X$, so by invariance the MLE of $p(1-p)$ is $\bar X(1-\bar X)$.",
 tips=[r"Invariance applies to any function, not just one-to-one ones - $p(1-p)$ is not monotone on $(0,1)$ yet the rule still holds.",
       r"The MLE $\bar X(1-\bar X)$ is biased; the UMVUE is $\frac{Y(n-Y)}{n(n-1)}$ with $Y=\sum X_i$.",
       r"Options dividing $\bar X$ by $n$ confuse the mean with the total - check the scale of $\bar X$ first."],
 sol=[r"For a Bernoulli sample, $\log L=\left(\sum x_i\right)\log p+\left(n-\sum x_i\right)\log(1-p)$, giving $\hat p=\bar X$.",
      r"$h(p)=p(1-p)$.",
      r"By the invariance property of maximum likelihood, $\widehat{h(p)}=h(\hat p)=\bar X(1-\bar X)$.",
      r"Option (c)."]),
}
