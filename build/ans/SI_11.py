# -*- coding: utf-8 -*-
# Statistical Inference answer pack 11 (2023 Q39-Q50, 2024 Q1-Q13)
A = {
"2023-Q39": dict(ans=0, qtype="Numerical", diff="Easy",
 sc=r"$\varphi(0)=1$ and $\varphi(4)=\tfrac12$, so size $=1(0.1)+\tfrac12(0.1)=0.15$.",
 tips=[r"One fair coin gives randomisation probability $\tfrac12$; two coins both heads would give $\tfrac14$.",
       r"Size $=\sum_x\varphi(x)P(x\mid H_0)$ - weight each outcome by its rejection probability.",
       r"Only $x=0$ and $x=4$ contribute; all other cells have $\varphi=0$."],
 sol=[r"The test function is $\varphi(0)=1$, $\varphi(4)=P(\text{head})=\tfrac12$, and $\varphi(x)=0$ otherwise.",
      r"Size $=E_{H_0}\left[\varphi(X)\right]=1\times P(0\mid H_0)+\tfrac12\times P(4\mid H_0)$.",
      r"$=1(0.1)+0.5(0.1)=0.10+0.05=0.15$.",
      r"Option (a)."]),

"2023-Q40": dict(ans=1, qtype="Numerical", diff="Easy",
 sc=r"Power $=1(0.2)+\tfrac12(0.2)=0.30$.",
 tips=[r"Same weights, but now against the $H_1$ row of the table.",
       r"$P(0\mid H_1)=0.2$ and $P(4\mid H_1)=0.2$.",
       r"Power $0.30>$ size $0.15$, so this randomised test is unbiased."],
 sol=[r"Power $=E_{H_1}\left[\varphi(X)\right]=1\times P(0\mid H_1)+\tfrac12\times P(4\mid H_1)$.",
      r"$=1(0.2)+0.5(0.2)=0.20+0.10=0.30$.",
      r"Option (b)."]),

"2023-Q41": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"The Poisson likelihood depends on the data only through $\sum x_i$; $T_2$ determines that total by addition, so both statistics are sufficient.",
 tips=[r"Any statistic from which $\sum x_i$ can be recovered is sufficient - partitioning the sample is harmless.",
       r"$T_2$ is sufficient but not MINIMAL sufficient; $T_1$ is minimal.",
       r"Sufficiency fails only when the components carry UNEQUAL weights (e.g. $x_1+2x_2$)."],
 sol=[r"$L(\lambda)=\dfrac{e^{-n\lambda}\lambda^{\sum x_i}}{\prod x_i!}$, so $T_1=\sum x_i$ is sufficient.",
      r"$T_2=\left(x_1,\ \sum_{i\ge2}x_i\right)$ determines $\sum_{i=1}^n x_i=x_1+\sum_{i\ge2}x_i$, so the likelihood is a function of $T_2$ as well.",
      r"By factorisation both are sufficient - option (c)."]),

"2023-Q42": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"Both $T_3$ and $T_4$ are partitions of the sample into groups whose totals sum to $\sum x_i$, so both determine the likelihood and are sufficient.",
 tips=[r"Group totals with unit weights always preserve sufficiency for additive families like Poisson and Bernoulli.",
       r"Contrast $x_1+2x_2$, where the weight $2$ breaks the correspondence.",
       r"Neither is minimal sufficient, but the question asks only about sufficiency."],
 sol=[r"$T_3=\left(x_1,\ x_2+x_3,\ \sum_{i\ge4}x_i\right)$: adding the three components gives $\sum_{i=1}^nx_i$.",
      r"$T_4=\left(x_1+x_2,\ x_3+x_4,\ \sum_{i\ge5}x_i\right)$: adding the three components again gives $\sum_{i=1}^nx_i$.",
      r"Since the Poisson likelihood is a function of $\sum x_i$ alone, both statistics satisfy the factorisation criterion.",
      r"Option (c)."]),

"2023-Q43": dict(ans=3, qtype="Conceptual", diff="Medium",
 sc=r"$E(x_1)=0\ne\theta$ (not unbiased), the symmetric family makes $x_1$ incomplete, and $x_1$ alone cannot reproduce $\sum x_i^2$ (not sufficient). None of the three holds.",
 tips=[r"$N(0,\theta)$ has mean zero, so any single observation has expectation $0$ - it can never be unbiased for a variance.",
       r"Symmetry kills completeness: $E_\theta(x_1)=0$ for all $\theta$ with $g(x)=x\not\equiv0$.",
       r"Sufficiency concerns the WHOLE sample; one observation discards the other $n-1$."],
 sol=[r"Statement 1: $E(x_1)=0$ for every $\theta$, so $x_1$ is not unbiased for $\theta$. False.",
      r"Statement 2: take $g(x)=x$. Then $E_\theta[g(x_1)]=0$ for all $\theta$ although $g\not\equiv0$, so the family of $x_1$ is not complete. False.",
      r"Statement 3: the likelihood depends on $\sum x_i^2$, which $x_1$ alone does not determine, so $x_1$ is not sufficient. False.",
      r"None of the statements is correct - option (d)."]),

"2023-Q44": dict(ans=0, qtype="Conceptual", diff="Medium",
 sc=r"$x_1^2/\theta\sim\chi^2_1$ makes $x_1^2$ a complete statistic (a one-parameter exponential family), but one observation cannot be sufficient for the whole sample.",
 tips=[r"Squaring removes the symmetry that destroyed completeness for $x_1$.",
       r"$x_1^2$ has density of exponential-family form in $\theta$ with natural statistic $x_1^2$ - hence complete.",
       r"The sample likelihood needs $\sum_{i=1}^n x_i^2$, so a single square is not sufficient."],
 sol=[r"Statement 1: $x_1^2/\theta\sim\chi^2_1$, so the density of $x_1^2$ is a one-parameter exponential family with natural sufficient statistic $x_1^2$ and open natural parameter space - hence complete. True.",
      r"Statement 2: the joint likelihood is $\theta^{-n/2}\exp\left(-\frac{\sum x_i^2}{2\theta}\right)$, requiring $\sum_{i=1}^n x_i^2$; $x_1^2$ alone does not determine it. False.",
      r"Hence 1 only - option (a)."]),

"2023-Q45": dict(ans=0, qtype="Numerical", diff="Medium",
 sc=r"$\sum X_i\sim N(n\theta_0,n)$ under $H_0$, so equal tails of $\alpha/2$ give $c_{1,2}=n\theta_0\mp\sqrt n Z_{\alpha/2}$.",
 tips=[r"$V\left(\sum X_i\right)=n$, so the standard deviation is $\sqrt n$ - not $n$.",
       r"Two-sided $\Rightarrow$ split $\alpha$ into two equal tails, giving $Z_{\alpha/2}$.",
       r"Symmetry of the normal makes the equal-tailed test the UMPU one."],
 sol=[r"Under $H_0:\theta=\theta_0$, $\sum_{i=1}^nX_i\sim N\left(n\theta_0,\ n\right)$.",
      r"An equal-tailed size-$\alpha$ region places $\alpha/2$ in each tail: $\left|\dfrac{\sum X_i-n\theta_0}{\sqrt n}\right|>Z_{\alpha/2}$.",
      r"Hence $c_1=n\theta_0-\sqrt nZ_{\alpha/2}$ and $c_2=n\theta_0+\sqrt nZ_{\alpha/2}$.",
      r"By symmetry this test is UMPU - option (a)."]),

"2023-Q46": dict(ans=1, qtype="Numerical", diff="Easy",
 sc=r"$s^2=1.69\Rightarrow s=1.3$; $t=\frac{\sqrt9(9.8-8.8)}{1.3}=\frac{3}{1.3}=2.31$.",
 tips=[r"The LRT for a normal mean with unknown variance reduces to Student's $t$ statistic.",
       r"$\frac18\sum(x_i-\bar x)^2$ IS $s^2$ with divisor $n-1=8$ - no further adjustment needed.",
       r"$\sqrt{1.69}=1.3$ exactly, so $t=3/1.3=2.3077$."],
 sol=[r"The LRT for $H_0:\mu=\mu_0$ against $\mu>\mu_0$ with $\sigma$ unknown is a monotone function of $t=\dfrac{\sqrt n(\bar x-\mu_0)}{s}$.",
      r"$s^2=\dfrac18\sum(x_i-\bar x)^2=1.69$, so $s=1.3$.",
      r"$t=\dfrac{\sqrt9\,(9.8-8.8)}{1.3}=\dfrac{3\times1}{1.3}=2.3077$.",
      r"$\approx2.31$ - option (b)."]),

"2023-Q47": dict(ans=1, qtype="Numerical", diff="Hard",
 sc=r"$W=n\left(X_{(1)}-\theta\right)\sim\text{Exp}(1)$; the interval is equivalent to $-\ln2<W<\ln4$, and since $W\ge0$ the coverage is $P(W<\ln4)=1-\tfrac14=0.75$.",
 tips=[r"$n\left(X_{(1)}-\theta\right)\sim\text{Exp}(1)$ - the pivotal fact for this family.",
       r"Translate each endpoint into a statement about $W$, then note $W\ge0$ makes the lower one vacuous.",
       r"$P(W<\ln4)=1-e^{-\ln4}=1-\tfrac14$."],
 sol=[r"$P\left(X_{(1)}>t\right)=e^{-n(t-\theta)}$ for $t>\theta$, so $W=n\left(X_{(1)}-\theta\right)\sim\text{Exp}(1)$.",
      r"$\theta>X_{(1)}-\dfrac{\ln4}{n}\iff W<\ln4$; and $\theta<X_{(1)}+\dfrac{\ln2}{n}\iff W>-\ln2$, which always holds since $W\ge0$.",
      r"Coverage $=P(0\le W<\ln4)=1-e^{-\ln4}=1-\dfrac14=0.75$.",
      r"Option (b)."]),

"2023-Q48": dict(ans=2, qtype="Conceptual", diff="Medium",
 sc=r"MVB attainment forces the score to vanish at $T$, so $T$ solves the likelihood equation (1 true); and the Cauchy admits no MVB estimator (2 true).",
 tips=[r"$\frac{\partial\log L}{\partial\theta}=A(\theta)\left[T-\theta\right]$ is zero exactly when $\theta=T$ - so $T$ is the MLE.",
       r"Cauchy: the score is a non-linear function of the data that cannot be written in the MVB form.",
       r"MVB estimators are always MLEs; the converse is false."],
 sol=[r"Statement 1: if an MVB estimator $T$ exists then $\dfrac{\partial\log L}{\partial\theta}=A(\theta)\left[T-\theta\right]$. Setting this to zero gives $\theta=T$, so $T$ solves the likelihood equation. True.",
      r"Statement 2: for the Cauchy location family, $\dfrac{\partial\log L}{\partial\theta}=\sum\dfrac{2(x_i-\theta)}{1+(x_i-\theta)^2}$ cannot be reduced to $A(\theta)[T-\theta]$, so no MVB estimator exists. True.",
      r"Option (c)."]),

"2023-Q49": dict(ans=0, qtype="Conceptual", diff="Medium",
 sc=r"When a sufficient statistic exists it and the MLE are functionally linked (standard property), but a sufficient statistic is never unique - any one-to-one function of it is also sufficient.",
 tips=[r"Uniqueness fails immediately: if $T$ is sufficient so is $2T$, $e^T$, the whole order statistic, etc.",
       r"Minimal sufficiency is what is essentially unique, not sufficiency itself.",
       r"The MLE is always a function of the sufficient statistic - that is the operational content of statement 1."],
 sol=[r"Statement 1: this is the standard property linking sufficiency and maximum likelihood - when a sufficient estimator exists, it and the MLE determine each other. True.",
      r"Statement 2: sufficiency is preserved under one-to-one transformations, and the full order statistic is always sufficient, so a sufficient statistic is never unique. False.",
      r"Hence 1 only - option (a)."]),

"2023-Q50": dict(ans=2, qtype="Conceptual", diff="Medium",
 sc=r"When a UMP test exists (MLR families) the LRT reproduces it, and under regularity the LRT is consistent. Both statements hold.",
 tips=[r"In one-parameter exponential families the LR statistic is a monotone function of the sufficient statistic, so it gives the same critical region as Karlin-Rubin.",
       r"'Generally' is doing real work in statement 1 - it is not a universal theorem but holds in the standard families.",
       r"Consistency of the LRT: power $\to1$ at every fixed alternative as $n\to\infty$."],
 sol=[r"Statement 1: in families with monotone likelihood ratio, the likelihood ratio statistic is a monotone function of the sufficient statistic, so the LRT critical region coincides with the UMP one whenever a UMP test exists. True.",
      r"Statement 2: under standard regularity conditions the LRT is consistent. True.",
      r"Option (c)."]),

"2024-Q1": dict(ans=2, qtype="Conceptual", diff="Medium",
 sc=r"Low MSE does imply high concentration (Chebyshev), but high concentration does not force low MSE; and no estimator minimises MSE uniformly. Both statements hold.",
 tips=[r"Chebyshev: $P\left(|T-\gamma(\theta)|>\varepsilon\right)\le\text{MSE}/\varepsilon^2$ - low MSE forces concentration.",
       r"The converse fails because a heavy tail can inflate MSE while leaving the bulk concentrated.",
       r"Uniform MSE-optimality is impossible: the constant rule $T\equiv\theta_0$ has zero MSE at $\theta_0$."],
 sol=[r"Statement I: by Chebyshev's inequality a small MSE bounds $P\left(|T-\gamma(\theta)|>\varepsilon\right)$, giving high concentration. The converse fails - a distribution can be concentrated yet have a heavy tail inflating the MSE. True.",
      r"Statement II: if such a uniformly best estimator existed it would have to beat the constant estimator $T\equiv\theta_0$ at $\theta=\theta_0$, where that constant has zero MSE - for every $\theta_0$. Impossible. True.",
      r"Option (c)."]),

"2024-Q2": dict(ans=0, qtype="Conceptual", diff="Easy",
 sc=r"$\text{MSE}=V+\text{bias}^2$, so unbiased $\Rightarrow$ MSE $=$ variance (I true). Statement II's 'bias proportional to MSE' is meaningless (II false).",
 tips=[r"The decomposition $\text{MSE}(T)=V(T)+\left[b(\theta)\right]^2$ is the single most used identity in this topic.",
       r"For a highly precise (small-variance) estimator the MSE is dominated by the SQUARED bias, not proportional to the bias.",
       r"Dimensional check: bias has the units of $\theta$; MSE has units of $\theta^2$ - they cannot be proportional."],
 sol=[r"Statement I: $\text{MSE}(T)=V(T)+\left[E(T)-\theta\right]^2$. If $T$ is unbiased the second term vanishes, so MSE $=V(T)$. True.",
      r"Statement II: no such proportionality holds; for small variance the MSE is approximately the SQUARE of the bias, and bias and MSE do not even share units. False.",
      r"Hence I only - option (a)."]),

"2024-Q3": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"$\rho(T_1,T_2)=\sqrt{E_\theta}$ - the efficiency-correlation identity.",
 tips=[r"$\operatorname{Cov}(T_1,T_2)=V(T_1)$ for a minimum-variance $T_1$, which yields $\rho=\sqrt{V(T_1)/V(T_2)}$.",
       r"Option (a) is false: an unbiased combination with weight on $T_2$ has variance strictly above $V(T_1)$.",
       r"Option (b) is false: $T_2$ is an arbitrary unbiased estimator, of which there are infinitely many."],
 sol=[r"Efficiency $E_\theta=\dfrac{V(T_1)}{V(T_2)}$ and, by the minimum-variance property, $\operatorname{Cov}(T_1,T_2)=V(T_1)$.",
      r"$\rho=\dfrac{\operatorname{Cov}(T_1,T_2)}{\sqrt{V(T_1)V(T_2)}}=\sqrt{\dfrac{V(T_1)}{V(T_2)}}=\left(E_\theta\right)^{1/2}$.",
      r"Option (c)."]),

"2024-Q4": dict(ans=1, qtype="Conceptual", diff="Hard",
 sc=r"$\bar X$ attains the bound for $\theta_1$ ($V=\theta_2/n$ exactly), but the UMVUE $S^2$ of $\theta_2$ has variance $\frac{2\theta_2^2}{n-1}>\frac{2\theta_2^2}{n}$.",
 tips=[r"$I(\theta_1)=n/\theta_2$, so the bound for $\theta_1$ is $\theta_2/n$, exactly $V(\bar X)$.",
       r"$I(\theta_2)=\frac{n}{2\theta_2^2}$, giving the bound $\frac{2\theta_2^2}{n}$, but $V(S^2)=\frac{2\theta_2^2}{n-1}$ is strictly larger.",
       r"The loss of one degree of freedom to estimating $\theta_1$ is exactly why the variance bound is missed."],
 sol=[r"For $N(\theta_1,\theta_2)$, the information matrix is diagonal with $I_{11}=\dfrac{n}{\theta_2}$ and $I_{22}=\dfrac{n}{2\theta_2^2}$.",
      r"CR bound for $\theta_1$: $\dfrac{\theta_2}{n}$, and $V(\bar X)=\dfrac{\theta_2}{n}$ - attained.",
      r"CR bound for $\theta_2$: $\dfrac{2\theta_2^2}{n}$, but the UMVUE $S^2=\frac1{n-1}\sum(X_i-\bar X)^2$ has $V(S^2)=\dfrac{2\theta_2^2}{n-1}>\dfrac{2\theta_2^2}{n}$ - not attained.",
      r"Hence the bound is attained for $\theta_1$ but not $\theta_2$ - option (b)."]),

"2024-Q5": dict(ans=2, qtype="Conceptual", diff="Medium",
 sc=r"The range is ancillary (its distribution is free of $\theta$), and any centred random variable has zero expectation - so both statements hold. Together they prove the minimal sufficient statistic is NOT complete.",
 tips=[r"$X_{(n)}-X_{(1)}$ depends only on the differences $X_i-\theta$, whose joint law is free of $\theta$.",
       r"Statement II is trivially true for ANY statistic with finite mean - it is $E\left[R-E(R)\right]=0$.",
       r"Together they exhibit a non-trivial unbiased estimator of zero, which is exactly why $\left(X_{(1)},X_{(n)}\right)$ fails completeness."],
 sol=[r"Statement I: writing $Y_i=X_i-\theta\sim U(-\tfrac12,\tfrac12)$, we get $X_{(n)}-X_{(1)}=Y_{(n)}-Y_{(1)}$, whose distribution does not involve $\theta$. True - the range is ancillary.",
      r"Statement II: for any statistic $R$ with finite mean, $E_\theta\left[R-E(R)\right]=0$; here $E(R)$ is a constant because $R$ is ancillary, so the identity holds for every $\theta$. True.",
      r"Option (c). (These two facts together show $\left(X_{(1)},X_{(n)}\right)$ is not complete.)"]),

"2024-Q6": dict(ans=0, qtype="Conceptual", diff="Medium",
 sc=r"Sufficiency makes $\phi(t)$ free of $\theta$ (I true), but Rao-Blackwell gives $V(\phi)\le V(U)$ with EQUALITY when $U$ is already a function of $T$ - so 'less than' (II) is not always correct.",
 tips=[r"Rao-Blackwell gives $\le$, not $<$: equality holds iff $U$ is already a function of $T$ (almost surely).",
       r"$V(U)-V\left[\phi(T)\right]=E\left[V(U\mid T)\right]$, which is zero exactly when $U$ is $T$-measurable.",
       r"The variance-decomposition identity $V(U)=E\left[V(U\mid T)\right]+V\left[E(U\mid T)\right]$ is the whole proof."],
 sol=[r"Statement I: because $T$ is sufficient, the conditional distribution of the sample given $T=t$ is free of $\theta$, so $\phi(t)=E[U\mid T=t]$ involves no $\theta$ and is a genuine statistic. True.",
      r"Statement II: $V(U)=E\left[V(U\mid T)\right]+V\left[\phi(T)\right]$, so $V\left[\phi(T)\right]\le V(U)$.",
      r"Equality occurs whenever $U$ is already a function of $T$ (then $V(U\mid T)=0$), so the strict inequality claimed is not always true. False.",
      r"Hence I only - option (a)."]),

"2024-Q7": dict(ans=2, qtype="Conceptual", diff="Medium",
 sc=r"The CR bound need not be attainable (I true), and comparing an unbiased estimator's variance with the bound is exactly how one certifies minimum variance (II true).",
 tips=[r"Attainment needs the score to be linear in the estimator; that fails for many families.",
       r"If $V(T)$ equals the bound, $T$ is certified MVUE; if it exceeds the bound, the test is inconclusive (the bound may simply be unattainable).",
       r"So the CR inequality gives a sufficient, not necessary, certificate of optimality."],
 sol=[r"Statement I: the Cramer-Rao bound is a lower bound; the smallest achievable variance may lie strictly above it (e.g. $V(S^2)=\frac{2\sigma^4}{n-1}$ against the bound $\frac{2\sigma^4}{n}$). True.",
      r"Statement II: if an unbiased estimator's variance equals the bound, it is necessarily minimum variance - a practical test of optimality. True.",
      r"Option (c)."]),

"2024-Q8": dict(ans=2, qtype="Conceptual", diff="Medium",
 sc=r"A UMVUE need not attain the CR bound (I true), and completeness is exactly what upgrades Rao-Blackwell to optimality (II true).",
 tips=[r"$S^2$ for the normal variance is UMVUE but not MVB - the standard example for statement I.",
       r"Lehmann-Scheffe: complete $+$ sufficient $\Rightarrow$ the unbiased function of it is the unique UMVUE.",
       r"Without completeness, Rao-Blackwell only improves an estimator; it cannot certify optimality."],
 sol=[r"Statement I: $S^2=\frac1{n-1}\sum(X_i-\bar X)^2$ is the UMVUE of $\sigma^2$ with variance $\frac{2\sigma^4}{n-1}$, strictly above the CR bound $\frac{2\sigma^4}{n}$. True.",
      r"Statement II: Rao-Blackwell improves any unbiased estimator by conditioning on a sufficient statistic; completeness (Lehmann-Scheffe) is what makes the result the unique minimum-variance unbiased estimator. True.",
      r"Option (c)."]),

"2024-Q9": dict(ans=0, qtype="Conceptual", diff="Easy",
 sc=r"$f/n\xrightarrow{P}p$, so by continuous mapping $\frac fn\left(1-\frac fn\right)\xrightarrow{P}p(1-p)$ (I true); but $f/n$ converges to $p$, not $1/p$ (II false).",
 tips=[r"Continuous mapping theorem: $g$ continuous and $T_n\xrightarrow{P}c$ $\Rightarrow$ $g(T_n)\xrightarrow{P}g(c)$.",
       r"$\frac nf$ would be the natural (though not always defined) candidate for $1/p$.",
       r"Consistency targets must match: $f/n$ can only be consistent for $p$."],
 sol=[r"By the weak law of large numbers, $\dfrac fn\xrightarrow{P}p$.",
      r"Statement I: $g(u)=u(1-u)$ is continuous, so $\dfrac fn\left(1-\dfrac fn\right)\xrightarrow{P}p(1-p)$. True.",
      r"Statement II: $\dfrac fn\xrightarrow{P}p\ne\dfrac1p$ in general, so it is not consistent for $\dfrac1p$. False.",
      r"Hence I only - option (a)."]),

"2024-Q10": dict(ans=0, qtype="Conceptual", diff="Easy",
 sc=r"MLEs are functions of sufficient statistics (I), may fail to exist (II) and need not be unbiased (III); but they are NOT always unique (IV false).",
 tips=[r"Non-uniqueness: $U(\theta-\tfrac12,\theta+\tfrac12)$ has an interval of MLEs.",
       r"Non-existence: a likelihood with no maximum (e.g. certain mixture models where $L\to\infty$).",
       r"Identify the one false statement and pick the option that excludes it - here only (a) omits IV."],
 sol=[r"I: by the factorisation theorem the likelihood depends on the data only through a sufficient statistic, so its maximiser must be a function of that statistic. True.",
      r"II: the likelihood may have no maximum (e.g. unbounded likelihoods in some mixture models). True.",
      r"III: $\hat\sigma^2$ with divisor $n$ is biased. True.",
      r"IV: MLEs need not be unique, as $U(\theta-\tfrac12,\theta+\tfrac12)$ shows. False.",
      r"Hence I, II and III - option (a)."]),

"2024-Q11": dict(ans=0, qtype="Conceptual", diff="Medium",
 sc=r"$\hat N=X_{(n)}$ is sufficient, complete and consistent, but BIASED ($E\left[X_{(n)}\right]<N$).",
 tips=[r"$X_{(n)}\le N$ always, so its expectation is strictly below $N$ - unbiasedness is impossible.",
       r"The UMVUE is $\frac{X_{(n)}^{n+1}-\left(X_{(n)}-1\right)^{n+1}}{X_{(n)}^{n}-\left(X_{(n)}-1\right)^{n}}$ - a bias correction of the MLE.",
       r"Three options assert unbiasedness; eliminating that word leaves only one choice."],
 sol=[r"$L(N)=N^{-n}I\left(X_{(n)}\le N\right)$ is decreasing in $N$, so $\hat N=X_{(n)}$.",
      r"$X_{(n)}$ is sufficient (factorisation) and complete for the discrete uniform family.",
      r"$P\left(X_{(n)}\le N-1\right)=\left(\frac{N-1}{N}\right)^n\to0$, so $X_{(n)}\xrightarrow{P}N$: consistent.",
      r"But $X_{(n)}\le N$ with positive probability of being strictly less, so $E\left[X_{(n)}\right]<N$: biased.",
      r"Hence consistent, sufficient and complete - option (a)."]),

"2024-Q12": dict(ans=3, qtype="Conceptual", diff="Easy",
 sc=r"With $\mu$ known, the likelihood depends on the data through $\sum(X_i-\mu)^2$.",
 tips=[r"Note options (a) and (b) square the SUM of deviations, not the sum of SQUARED deviations - a decisive difference.",
       r"Option (c) drops the last observation, discarding information.",
       r"Constant multiples of a sufficient statistic are equally sufficient, but the structure must be right."],
 sol=[r"$L(\theta)=(2\pi\theta)^{-n/2}\exp\left[-\dfrac{\sum_{i=1}^n(X_i-\mu)^2}{2\theta}\right]$.",
      r"By the factorisation theorem the sufficient statistic is $T=\sum_{i=1}^n(X_i-\mu)^2$.",
      r"Options (a) and (b) use $\left[\sum(X_i-\mu)\right]^2$, a different quantity; option (c) omits the $n$-th term.",
      r"Option (d)."]),

"2024-Q13": dict(ans=0, qtype="Definition", diff="Easy",
 sc=r"Estimability means an unbiased estimator exists - nothing more.",
 tips=[r"Estimable $\iff$ some $T$ with $E_\theta(T)=\psi(\theta)$ for all $\theta$ exists.",
       r"$1/p$ in the Bernoulli model is the standard NON-estimable function.",
       r"Requiring an MVUE (option (b)) is a stronger condition and is not the definition."],
 sol=[r"A parametric function $\psi(\theta)$ is called estimable if there exists a statistic $T$ with $E_\theta(T)=\psi(\theta)$ for every $\theta$ - that is, if an unbiased estimator exists.",
      r"Option (b) demands minimum variance in addition, which is not part of the definition; option (c) confuses estimability with an optimality criterion.",
      r"Option (a)."]),
}
