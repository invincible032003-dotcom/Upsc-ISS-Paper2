# -*- coding: utf-8 -*-
# Statistical Inference answer pack 14 (2025 Q20-Q47)
A = {
"2025-Q20": dict(ans=3, qtype="Conceptual", diff="Medium",
 sc=r"For $U(0,\theta)$: $X_{(n)}$ is both the MLE and sufficient, and the family $\{U(0,\theta)\}$ is complete. All three statements hold.",
 tips=[r"$L=\theta^{-n}I\left(X_{(n)}\le\theta\right)$ is decreasing, so the MLE sits at the boundary $X_{(n)}$.",
       r"Completeness: $\int_0^\theta g(x)dx=0$ for all $\theta$ forces $g\equiv0$ by differentiation.",
       r"Complete $+$ sufficient makes $\frac{n+1}{n}X_{(n)}$ the UMVUE of $\theta$."],
 sol=[r"I: $L(\theta)=\theta^{-n}$ for $\theta\ge X_{(n)}$, decreasing, so $\hat\theta=X_{(n)}$. True.",
      r"II: the same factorisation shows $X_{(n)}$ is sufficient. True.",
      r"III: if $E_\theta[g(X)]=\frac1\theta\int_0^\theta g(x)dx=0$ for all $\theta>0$, then $\int_0^\theta g=0$ for all $\theta$; differentiating gives $g(\theta)=0$ a.e. So the family is complete. True.",
      r"Option (d)."]),

"2025-Q21": dict(ans=0, qtype="Conceptual", diff="Medium",
 sc=r"$X_{(1)}\xrightarrow{P}\theta$ since $P\left(X_{(1)}>\theta+\varepsilon\right)=(1-\varepsilon)^n\to0$; the maximum converges to $\theta+1$ and the mean to $\theta+\tfrac12$.",
 tips=[r"For $U(\theta,\theta+1)$ the lower endpoint is $\theta$, so the MINIMUM is the natural consistent estimator.",
       r"$X_{(n)}\xrightarrow{P}\theta+1$ and $\bar X\xrightarrow{P}\theta+\tfrac12$ - both converge to the wrong target.",
       r"Consistency requires convergence to $\theta$ itself, not to a shifted version."],
 sol=[r"$P\left(X_{(1)}>\theta+\varepsilon\right)=\left[P(X_1>\theta+\varepsilon)\right]^n=(1-\varepsilon)^n\to0$ for any $0<\varepsilon<1$.",
      r"Since also $X_{(1)}>\theta$ always, $X_{(1)}\xrightarrow{P}\theta$: the smallest observation is consistent for $\theta$.",
      r"$X_{(n)}\xrightarrow{P}\theta+1\ne\theta$ and $\bar X\xrightarrow{P}E(X)=\theta+\tfrac12\ne\theta$.",
      r"Option (a)."]),

"2025-Q22": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"$E(\bar X)=\lambda$ for Poisson (I true) and $\bar X\xrightarrow{P}\mu$ for the normal (II true).",
 tips=[r"$\bar X$ is unbiased for the mean of ANY distribution with a finite mean.",
       r"Consistency follows from $V(\bar X)=\sigma^2/n\to0$ plus Chebyshev.",
       r"Both statements are among the safest facts in the syllabus."],
 sol=[r"Statement I: for Poisson($\lambda$), $E(X_i)=\lambda$, so $E(\bar X)=\lambda$: unbiased. True.",
      r"Statement II: for $N(\mu,\sigma^2)$, $E(\bar X)=\mu$ and $V(\bar X)=\sigma^2/n\to0$, so $\bar X\xrightarrow{P}\mu$: consistent. True.",
      r"Option (c)."]),

"2025-Q23": dict(ans=1, qtype="Conceptual", diff="Medium",
 sc=r"With a symmetric half-width, the confidence interval is $\bar X\pm c\frac{\sigma}{\sqrt n}$, so its width is $\frac{2c\sigma}{\sqrt n}$.",
 tips=[r"Width $=$ upper limit $-$ lower limit; for a symmetric interval it is twice the half-width.",
       r"The half-width always carries $\sigma/\sqrt n$, so $\sqrt n$ belongs in the DENOMINATOR.",
       r"Option (a) inverts the standard error; options (c) and (d) mis-scale the constants."],
 sol=[r"For $N(\mu,\sigma^2)$ the $(1-\alpha)$ interval for $\mu$ takes the form $\bar X\pm c\dfrac{\sigma}{\sqrt n}$ with $c=z_{\alpha/2}$ (or $t_{n-1,\alpha/2}$).",
      r"Its width is the difference between the endpoints: $2c\dfrac{\sigma}{\sqrt n}$.",
      r"Option (b)."]),

"2025-Q24": dict(ans=2, qtype="Numerical", diff="Hard",
 sc=r"Substituting $Y=\frac{X-\theta}{1-\theta}\sim\text{Beta}(2,1)$ gives $E(X)=\frac{\theta+2}{3}$, so $E(3X-2)=\theta$.",
 tips=[r"Standardise a support-shifted density by mapping the support onto $(0,1)$ - here $Y=\frac{X-\theta}{1-\theta}$.",
       r"$f_Y(y)=2y$ on $(0,1)$, i.e. Beta$(2,1)$ with mean $\tfrac23$.",
       r"$E(X)=\theta+(1-\theta)\tfrac23=\tfrac{\theta+2}{3}$; invert to get $3X-2$."],
 sol=[r"Put $Y=\dfrac{X-\theta}{1-\theta}$, so $X=\theta+(1-\theta)Y$ and $Y\in(0,1)$.",
      r"$f_Y(y)=f_X\left(\theta+(1-\theta)y\right)(1-\theta)=\dfrac{2(1-\theta)y}{(1-\theta)^2}(1-\theta)=2y$, so $Y\sim\text{Beta}(2,1)$ with $E(Y)=\tfrac23$.",
      r"$E(X)=\theta+(1-\theta)\cdot\dfrac23=\dfrac{\theta+2}{3}$.",
      r"Hence $E(3X-2)=3\cdot\dfrac{\theta+2}{3}-2=\theta$, so $3X-2$ is unbiased - option (c)."]),

"2025-Q25": dict(ans=3, qtype="Numerical", diff="Easy",
 sc=r"$E(X)=\theta+1$, so $E(\bar X)=\theta+1$ and $\bar X-1$ is unbiased for $\theta$.",
 tips=[r"The shifted exponential $e^{-(x-\theta)}$ has mean $\theta+1$ - shift the standard exponential mean of 1.",
       r"Correct the whole MEAN, not the sum: $\sum X_i-n$ has expectation $n\theta$, not $\theta$.",
       r"The UMVUE is $X_{(1)}-\frac1n$, which has much smaller variance than $\bar X-1$."],
 sol=[r"For $f(x)=e^{-(x-\theta)}$, $x>\theta$, put $Y=X-\theta\sim\text{Exp}(1)$, so $E(X)=\theta+1$.",
      r"Hence $E(\bar X)=\theta+1$.",
      r"$E(\bar X-1)=\theta$, so $\bar X-1$ is unbiased.",
      r"Option (d)."]),

"2025-Q26": dict(ans=3, qtype="Numerical", diff="Medium",
 sc=r"$P(x_1+x_2+x_3=1)=3p(1-p)^2$, so $E(T)=\frac1{3n}\cdot3p(1-p)^2=\frac{p(1-p)^2}{n}$.",
 tips=[r"$x_1+x_2+x_3\sim B(3,p)$, so $P(\cdot=1)=\binom31p(1-p)^2=3p(1-p)^2$.",
       r"The $\frac1{3n}$ scaling cancels the $3$ and introduces the $\frac1n$.",
       r"Read the exponent: $(1-p)^2$, not $(1-p)$ - options (a) and (b) mis-count the failures."],
 sol=[r"$T$ takes the value $\dfrac1{3n}$ exactly when $x_1+x_2+x_3=1$, and $0$ otherwise.",
      r"$x_1+x_2+x_3\sim B(3,p)$, so $P(x_1+x_2+x_3=1)=\dbinom31p(1-p)^2=3p(1-p)^2$.",
      r"$E(T)=\dfrac1{3n}\cdot3p(1-p)^2=\dfrac{p(1-p)^2}{n}$.",
      r"Option (d)."]),

"2025-Q27": dict(ans=3, qtype="Numerical", diff="Easy",
 sc=r"$E\left[X_{(n)}\right]=\frac{n\theta}{n+1}$, so $\frac{n+1}{n}X_{(n)}$ is unbiased - and being a function of the complete sufficient statistic, it is the MVUE.",
 tips=[r"The maximum under-estimates $\theta$, so the correction factor must EXCEED 1.",
       r"$\frac{n+1}{n}>1$ while $\frac{n+2}{n+1}$ and the other options do not reproduce $E=\theta$.",
       r"Verify: $\frac{n+1}{n}\cdot\frac{n\theta}{n+1}=\theta$ exactly."],
 sol=[r"$F_{X_{(n)}}(t)=\left(\dfrac t\theta\right)^n$ gives $E\left[X_{(n)}\right]=\displaystyle\int_0^\theta t\frac{nt^{n-1}}{\theta^n}dt=\frac{n\theta}{n+1}$.",
      r"Hence $E\left[\dfrac{(n+1)T}{n}\right]=\theta$, so the estimator is unbiased.",
      r"$T=X_{(n)}$ is complete sufficient for $U[0,\theta]$, so by Lehmann-Scheffe this is the MVUE.",
      r"Option (d)."]),

"2025-Q28": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"$U(0,\theta)$ needs only the maximum; $U(\theta-\tfrac12,\theta+\tfrac12)$ needs both extremes. Both statements are correct.",
 tips=[r"Count the moving endpoints: one moving endpoint $\Rightarrow$ one order statistic; two $\Rightarrow$ the pair.",
       r"In case II, $\left(X_{(1)},X_{(n)}\right)$ is minimal sufficient but NOT complete - sufficiency and completeness are different questions.",
       r"The likelihood indicator is what forces the order statistics into the sufficient statistic."],
 sol=[r"Statement I: $L=\theta^{-n}I\left(X_{(n)}\le\theta\right)$, so $\max X_i$ is sufficient. True.",
      r"Statement II: $L=1$ iff $\theta-\tfrac12<X_{(1)}$ and $X_{(n)}<\theta+\tfrac12$, i.e. $L=I\left(X_{(n)}-\tfrac12<\theta<X_{(1)}+\tfrac12\right)$, a function of the pair. True.",
      r"Option (c)."]),

"2025-Q29": dict(ans=3, qtype="Numerical", diff="Easy",
 sc=r"$E(X)=\frac{N+1}{2}$ for the discrete uniform, so $E(2\bar X-1)=N$.",
 tips=[r"Mean of $\{1,2,\dots,N\}$ is $\frac{N+1}{2}$ - the midpoint of the range.",
       r"Invert: $N=2E(X)-1$, so replace $E(X)$ by $\bar X$.",
       r"This is the classic 'German tank problem' moment estimator; the MVUE based on $X_{(n)}$ is much better."],
 sol=[r"$X_i$ is uniform on $\{1,2,\dots,N\}$, so $E(X_i)=\dfrac{1+2+\cdots+N}{N}=\dfrac{N+1}{2}$.",
      r"Hence $E(\bar X)=\dfrac{N+1}{2}$.",
      r"$E\left(2\bar X-1\right)=2\cdot\dfrac{N+1}{2}-1=N$, so $2\bar X-1$ is unbiased for $N$.",
      r"Option (d)."]),

"2025-Q30": dict(ans=1, qtype="Numerical", diff="Medium",
 sc=r"Equal mixture of exponentials with means $\theta$, $\theta^2$, $1$ gives $E(X)=\frac{\theta^2+\theta+1}{3}$; $\bar x=3.25$ leads to $\theta^2+\theta-8.75=0$, whose positive root is $2.5$.",
 tips=[r"Identify the three component means directly from $\frac1m e^{-x/m}$: here $\theta$, $\theta^2$ and $1$.",
       r"$\bar x=\frac{3+4+3.5+2.5}{4}=\frac{13}{4}=3.25$.",
       r"$\theta^2+\theta+1=9.75$ gives $\theta=\frac{-1+\sqrt{36}}{2}=2.5$ - the discriminant is a perfect square by design."],
 sol=[r"The density is an equal-weight mixture of exponentials with means $\theta$, $\theta^2$ and $1$, so $E(X)=\dfrac{\theta+\theta^2+1}{3}$.",
      r"$\bar x=\dfrac{3+4+3.5+2.5}{4}=3.25$.",
      r"$\dfrac{\hat\theta^2+\hat\theta+1}{3}=3.25\Rightarrow\hat\theta^2+\hat\theta-8.75=0$.",
      r"$\hat\theta=\dfrac{-1+\sqrt{1+35}}{2}=\dfrac{-1+6}{2}=2.5$ - option (b)."]),

"2025-Q31": dict(ans=3, qtype="Definition", diff="Easy",
 sc=r"The Cramer-Rao inequality supplies the lower bound on the variance of an unbiased estimator.",
 tips=[r"$V(T)\ge\dfrac{\left[\psi'(\theta)\right]^2}{I_n(\theta)}$.",
       r"Factorisation is about sufficiency, Rao-Blackwell about improvement, NP about testing.",
       r"Bhattacharya's bounds generalise Cramer-Rao using higher derivatives."],
 sol=[r"The Cramer-Rao inequality states that, under regularity, any unbiased estimator $T$ of $\psi(\theta)$ satisfies $V(T)\ge\dfrac{\left[\psi'(\theta)\right]^2}{I_n(\theta)}$.",
      r"This is the only one of the four results that bounds a variance from below.",
      r"Option (d)."]),

"2025-Q32": dict(ans=3, qtype="Conceptual", diff="Easy",
 sc=r"$L=\theta^n\left(\prod x_i\right)^{\theta-1}$, so $\prod X_i$ is sufficient.",
 tips=[r"The recurring family $\theta x^{\theta-1}$ - recognise it instantly and write $\prod x_i$.",
       r"Equivalently $\sum\log x_i$, a one-to-one transform.",
       r"$\sum X_i$ and $\sum X_i^2$ do not appear anywhere in the likelihood."],
 sol=[r"$L(\theta)=\prod_{i=1}^n\theta x_i^{\theta-1}=\theta^n\left(\prod_{i=1}^nx_i\right)^{\theta-1}$.",
      r"Factorising with $t=\prod x_i$ and $h(x)=1$ shows $\prod X_i$ is sufficient.",
      r"Option (d)."]),

"2025-Q33": dict(ans=1, qtype="Numerical", diff="Medium",
 sc=r"Given $X_1+X_2=t$, all $\binom2t$ arrangements are equally likely, so the conditional probability is $\dfrac1{\binom2t}$ - free of $\theta$.",
 tips=[r"$P(X_1=x_1,X_2=x_2)=\theta^t(1-\theta)^{2-t}$ for every arrangement with total $t$ - they all share the same probability.",
       r"$P(X_1+X_2=t)=\binom2t\theta^t(1-\theta)^{2-t}$, so the ratio is $1/\binom2t$.",
       r"Note $\binom2t$ (choose $t$ from 2), not $\binom t2$ - option (a) reverses the arguments."],
 sol=[r"For any $(x_1,x_2)$ with $x_1+x_2=t$: $P(X_1=x_1,X_2=x_2)=\theta^{t}(1-\theta)^{2-t}$.",
      r"$P(X_1+X_2=t)=\dbinom2t\theta^t(1-\theta)^{2-t}$.",
      r"Dividing: $P\left[X_1=x_1,X_2=x_2\mid X_1+X_2=t\right]=\dfrac{1}{\dbinom2t}$, which is free of $\theta$ - confirming sufficiency.",
      r"Option (b)."]),

"2025-Q34": dict(ans=2, qtype="Numerical", diff="Easy",
 sc=r"With $\sigma$ known the interval is symmetric, so $\bar x$ is the midpoint $\frac{200+220}{2}=210$.",
 tips=[r"Symmetric interval $\Rightarrow$ the sample mean is the midpoint.",
       r"Half-width $=10$, so $z_{\alpha/2}\sigma/\sqrt n=10$ if that is ever needed.",
       r"Option (a) is the SUM of the endpoints - a planted slip."],
 sol=[r"For known $\sigma$ the interval is $\left(\bar x-z_{\alpha/2}\dfrac{\sigma}{\sqrt n},\ \bar x+z_{\alpha/2}\dfrac{\sigma}{\sqrt n}\right)$, symmetric about $\bar x$.",
      r"$\bar x=\dfrac{200+220}{2}=210$.",
      r"Option (c)."]),

"2025-Q35": dict(ans=0, qtype="Numerical", diff="Easy",
 sc=r"Paired $t$-test on $n=20$ pairs uses the $20$ differences, so df $=n-1=19$.",
 tips=[r"df $=$ (number of PAIRS) $-1$ - the design reduces to a one-sample problem on the differences.",
       r"$38$ would be the df for two independent samples of 20 each.",
       r"The pairing is what buys the variance reduction; the cost is halving the degrees of freedom."],
 sol=[r"In a paired design the data become $n=20$ differences $d_i$.",
      r"The test statistic is $t=\dfrac{\bar d}{s_d/\sqrt n}$, and estimating $s_d$ costs one degree of freedom.",
      r"df $=20-1=19$ - option (a)."]),

"2025-Q36": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"Unbiased means power $>$ size: $1-\beta>\alpha$, i.e. $1-P(\text{Type-II})>P(\text{Type-I})$.",
 tips=[r"Translate first: power $=1-\beta$, size $=\alpha$. Then the condition reads itself off.",
       r"Equivalently $\alpha+\beta<1$.",
       r"Options comparing $\alpha$ and $\beta$ directly are irrelevant to unbiasedness."],
 sol=[r"A critical region is unbiased if the probability of rejection under $H_1$ is at least that under $H_0$.",
      r"That is, power $\ge$ size: $1-\beta\ge\alpha$, i.e. $1-P(\text{Type-II error})>P(\text{Type-I error})$ in the strict form.",
      r"Option (c)."]),

"2025-Q37": dict(ans=1, qtype="Numerical", diff="Easy",
 sc=r"$\log L=-n\log\theta-\frac{\sum x_i}{\theta}$ gives $\hat\theta=\bar x$.",
 tips=[r"Here $\theta$ is the MEAN of the exponential, so the sample mean estimates it directly.",
       r"If the density were $\theta e^{-\theta x}$ (rate parametrisation) the MLE would be $1/\bar x$ - read the density carefully.",
       r"$\bar x$ is simultaneously MLE, MoM estimator, UMVUE and MVB estimator here."],
 sol=[r"$L(\theta)=\theta^{-n}\exp\left(-\dfrac{\sum x_i}{\theta}\right)$, so $\log L=-n\log\theta-\dfrac{\sum x_i}{\theta}$.",
      r"$\dfrac{\partial\log L}{\partial\theta}=-\dfrac n\theta+\dfrac{\sum x_i}{\theta^2}=0\Rightarrow\hat\theta=\dfrac{\sum x_i}{n}=\bar x$.",
      r"Option (b)."]),

"2025-Q38": dict(ans=1, qtype="Conceptual", diff="Easy",
 sc=r"$E(X)=\theta$, so the moment estimator is $\bar x$ (not $2\bar x$) - and it coincides with the MLE.",
 tips=[r"$2\bar x$ would be the moment estimator for $U(0,\theta)$, not for the exponential - a deliberate cross-family trap.",
       r"For the exponential with mean $\theta$, MoM $=$ MLE $=\bar x$: the two methods agree.",
       r"Agreement of MoM and MLE happens whenever the parameter IS a moment of the distribution."],
 sol=[r"$E(X)=\theta$ for $f_\theta(x)=\dfrac1\theta e^{-x/\theta}$, so the method of moments gives $\hat\theta=\bar x$, not $2\bar x$. Statement I false.",
      r"The MLE is also $\bar x$ (previous item), so the two estimators coincide. Statement II true.",
      r"Hence II only - option (b)."]),

"2025-Q39": dict(ans=1, qtype="Numerical", diff="Easy",
 sc=r"$\hat a=\bar X-\sqrt{3m_2}$ with $m_2=\frac1n\sum(X_i-\bar X)^2$.",
 tips=[r"The $\sqrt3$ comes from $V(X)=\frac{(b-a)^2}{12}$: half-range $=\sqrt{3\times\text{variance}}$.",
       r"Lower endpoint takes the MINUS sign.",
       r"Same item appeared as 2018 Q27 and 2022 Q74 - a three-time repeat."],
 sol=[r"$E(X)=\dfrac{a+b}{2}$ and $V(X)=\dfrac{(b-a)^2}{12}$.",
      r"Method of moments: $\dfrac{\hat a+\hat b}{2}=\bar X$ and $\dfrac{(\hat b-\hat a)^2}{12}=m_2$, so $\hat b-\hat a=2\sqrt{3m_2}$.",
      r"Hence $\hat a=\bar X-\sqrt{3m_2}=\bar X-\sqrt{\dfrac{3\sum(X_i-\bar X)^2}{n}}$.",
      r"Option (b)."]),

"2025-Q40": dict(ans=1, qtype="Numerical", diff="Easy",
 sc=r"$\hat b=\bar X+\sqrt{3m_2}$.",
 tips=[r"Mirror image of the previous item - only the sign changes.",
       r"Check: $\hat b-\hat a=2\sqrt{3m_2}$ reproduces the moment equation for the variance.",
       r"The factor $\sqrt3$, not $\sqrt2$ or $2$, is the discriminating detail."],
 sol=[r"From $\dfrac{\hat a+\hat b}{2}=\bar X$ and $\hat b-\hat a=2\sqrt{3m_2}$:",
      r"$\hat b=\bar X+\sqrt{3m_2}=\bar X+\sqrt{\dfrac{3\sum(X_i-\bar X)^2}{n}}$.",
      r"Option (b)."]),

"2025-Q41": dict(ans=1, qtype="Conceptual", diff="Hard",
 sc=r"Minimal sufficiency does NOT imply completeness (I false); and independence from every ancillary does not guarantee completeness either (II true, as stated).",
 tips=[r"$U(\theta-\tfrac12,\theta+\tfrac12)$: $\left(X_{(1)},X_{(n)}\right)$ is minimal sufficient but not complete.",
       r"Basu's theorem runs one way: complete $+$ sufficient $\Rightarrow$ independent of every ancillary. The converse fails.",
       r"Complete $+$ sufficient $\Rightarrow$ minimal sufficient, but not conversely - remember the direction."],
 sol=[r"Statement I: for $U(\theta-\tfrac12,\theta+\tfrac12)$, $\left(X_{(1)},X_{(n)}\right)$ is minimal sufficient, yet the ancillary range gives a non-trivial unbiased estimator of zero - so it is not complete. False.",
      r"Statement II: Basu's theorem says a complete sufficient statistic is independent of every ancillary. The converse direction is not a theorem - counterexamples exist of statistics independent of every ancillary that are not complete. So 'need not be complete' is correct. True.",
      r"Hence II only - option (b)."]),

"2025-Q42": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"$T=\sum X_i\sim B(n,p)$ is the natural statistic of a full-rank one-parameter exponential family - sufficient AND complete.",
 tips=[r"$L=p^T(1-p)^{n-T}$ gives sufficiency immediately.",
       r"Completeness: $E_p[g(T)]=\sum_t g(t)\binom nt p^t(1-p)^{n-t}=0$ for all $p$ forces every coefficient to vanish.",
       r"This is why $T/n$ is the UMVUE of $p$ and $\frac{T(T-1)}{n(n-1)}$ the UMVUE of $p^2$."],
 sol=[r"$L(p)=p^{\sum x_i}(1-p)^{n-\sum x_i}$, so by factorisation $T=\sum X_i$ is sufficient.",
      r"$E_p[g(T)]=(1-p)^n\sum_{t=0}^{n}g(t)\dbinom nt\left(\dfrac{p}{1-p}\right)^t$; setting this to zero for all $p\in(0,1)$ makes a polynomial in $\frac{p}{1-p}$ vanish identically, so all $g(t)=0$.",
      r"Hence $T$ is both sufficient and complete - option (c)."]),

"2025-Q43": dict(ans=0, qtype="Conceptual", diff="Medium",
 sc=r"Sufficiency for a class carries to any subclass (I true), but sufficiency never implies completeness (II false).",
 tips=[r"If the factorisation holds for every member of a class, it holds for every member of a subclass - trivially.",
       r"The order statistic is always sufficient but almost never complete - the standard counterexample to II.",
       r"Completeness can FAIL when the family is shrunk, even though sufficiency survives."],
 sol=[r"Statement I: if $f_\theta(x)=g_\theta\left(T(x)\right)h(x)$ for every $\theta$ in a class, the same factorisation holds for every $\theta$ in any subclass. True.",
      r"Statement II: the complete order statistic is sufficient for any iid family but is generally not complete (e.g. $U(\theta-\tfrac12,\theta+\tfrac12)$). False.",
      r"Hence I only - option (a)."]),

"2025-Q44": dict(ans=3, qtype="Definition", diff="Easy",
 sc=r"Blackwellisation conditions an unbiased estimator on a sufficient statistic, producing an unbiased estimator with smaller (never larger) variance.",
 tips=[r"The output stays UNBIASED - options describing a biased result are wrong.",
       r"$V\left[E(U\mid T)\right]\le V(U)$ by the variance decomposition.",
       r"Completeness is what turns the improvement into optimality (Lehmann-Scheffe), but it is not part of Blackwellisation itself."],
 sol=[r"Given an unbiased $U$ and a sufficient $T$, Rao-Blackwellisation forms $\phi(T)=E(U\mid T)$.",
      r"$\phi(T)$ is a statistic (sufficiency), is unbiased ($E[\phi(T)]=E(U)$), and has $V[\phi(T)]\le V(U)$.",
      r"So the process produces an unbiased estimator with lesser variance - option (d)."]),

"2025-Q45": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"$S(X)$ is a function of the random sample, hence a random set (I true); once $X=x$ is observed the set is fixed and either contains $\theta$ or not (II true).",
 tips=[r"Confidence statements are about the PROCEDURE, not about the realised interval.",
       r"Before sampling: coverage probability $1-\alpha$. After sampling: coverage is $0$ or $1$, we just do not know which.",
       r"This is the standard frequentist interpretation and is examined almost verbatim."],
 sol=[r"Statement I: $S(X)$ depends on the random vector $X$, so it is a random set; the probability statement $P_\theta[\theta\in S(X)]\ge1-\alpha$ is over the randomness of $X$. True.",
      r"Statement II: for an observed $x$, $S(x)$ is a fixed set, so the event $\theta\in S(x)$ is either true or false - no probability attaches to it in the frequentist framework. True.",
      r"Option (c)."]),

"2025-Q46": dict(ans=2, qtype="Numerical", diff="Medium",
 sc=r"$E\left[(-a)^X\right]=e^{\lambda(-a-1)}=e^{-(a+1)\lambda}$ using the Poisson pgf.",
 tips=[r"Poisson pgf: $E\left(t^X\right)=e^{\lambda(t-1)}$ - substitute $t=-a$.",
       r"$t-1=-a-1=-(a+1)$, giving the exponent directly.",
       r"The alternating sign is what produces a DECAYING exponential; $a^X$ would give $e^{\lambda(a-1)}$."],
 sol=[r"For $X\sim\text{Poisson}(\lambda)$, $E\left(t^X\right)=\sum_{x\ge0}t^x\dfrac{e^{-\lambda}\lambda^x}{x!}=e^{\lambda(t-1)}$.",
      r"Setting $t=-a$: $E\left[(-a)^X\right]=e^{\lambda(-a-1)}=e^{-(a+1)\lambda}$.",
      r"So $(-a)^X$ is an unbiased estimator of $e^{-(a+1)\lambda}$ - option (c)."]),

"2025-Q47": dict(ans=1, qtype="Numerical", diff="Easy",
 sc=r"$\frac{\partial\log L}{\partial\mu}=\frac{\sum(x_i-\mu)}{\sigma^2}=0$ gives $\hat\mu=\bar x$.",
 tips=[r"The MLE of a normal mean is the sample mean, whether or not $\sigma^2$ is known.",
       r"It is simultaneously unbiased, sufficient, consistent, UMVUE and MVB.",
       r"No scaling factor is involved - options (a) and (c) have no derivation behind them."],
 sol=[r"$\log L=-\dfrac n2\log(2\pi\sigma^2)-\dfrac{1}{2\sigma^2}\sum(x_i-\mu)^2$.",
      r"$\dfrac{\partial\log L}{\partial\mu}=\dfrac{1}{\sigma^2}\sum(x_i-\mu)=0\Rightarrow\sum x_i=n\mu$.",
      r"$\hat\mu=\bar x$ - option (b)."]),
}
