# -*- coding: utf-8 -*-
# Linear Models answer pack 1 (2018 Q1-Q15, 2019 Q1-Q5)
# ans = 0-based index of the correct option.  All explanatory material is AI-derived.
A = {

"2018-Q1": dict(ans=2, qtype="Conceptual", diff="Medium",
 sc=r"Read off the rows of $X$: $(1,1,0)$, $(1,0,1)$, $(1,1,0)$. A function $\lambda'\beta$ is estimable iff $\lambda'$ is a combination of these rows, i.e. $\lambda=(a+b,\,a,\,b)$, which forces $\lambda_1=\lambda_2+\lambda_3$.",
 tips=[r"Estimability test: $\lambda'\beta$ is estimable $\iff$ $\lambda'\in\mathcal{R}(X)$, the row space of the design matrix.",
       r"Duplicate rows ($Y_3$ repeats $Y_1$) add no new direction, so the row space here is only 2-dimensional in $\mathbb{R}^3$ - exactly one linear restriction on $\lambda$ survives.",
       r"Option (d) with an arbitrary constant $C$ is a standard throwaway distractor: estimability is a linear-subspace condition and can never involve a free constant."],
 sol=[r"Write the model as $\mathbf{Y}=X\boldsymbol\beta+\mathbf{e}$ with $X=\begin{pmatrix}1&1&0\\1&0&1\\1&1&0\end{pmatrix}$.",
      r"$\lambda'\beta$ is estimable iff there is $\mathbf{c}$ with $\lambda'=\mathbf{c}'X$, i.e. $\lambda'$ lies in the row space of $X$.",
      r"Rows 1 and 3 coincide, so the row space is spanned by $(1,1,0)$ and $(1,0,1)$; hence $\lambda=a(1,1,0)+b(1,0,1)=(a+b,\,a,\,b)$.",
      r"Therefore $\lambda_1=a+b$, $\lambda_2=a$, $\lambda_3=b$, giving the single condition $\lambda_1=\lambda_2+\lambda_3$, i.e. $\lambda_2+\lambda_3=\lambda_1$.",
      r"Hence option (c) is correct."]),

"2018-Q2": dict(ans=0, qtype="Numerical", diff="Easy",
 sc=r"The $x$'s are symmetric about $0$, so $\bar x=0$: the design is orthogonal and $\hat\beta_0=\bar y$, $\hat\beta_1=\dfrac{\sum x_iy_i}{\sum x_i^2}=\dfrac{y_3-y_1}{2}$.",
 tips=[r"When $\sum x_i=0$ the two normal equations decouple - no need to invert a $2\times2$ matrix.",
       r"$\hat\beta_1=\dfrac{S_{xy}}{S_{xx}}$; here $S_{xy}=(-1)y_1+0\cdot y_2+(1)y_3$ and $S_{xx}=2$.",
       r"Spot-check: any option whose intercept is not $\bar y$ can be eliminated instantly, because $\bar x=0$ forces $\hat\beta_0=\bar y-\hat\beta_1\bar x=\bar y$."],
 sol=[r"Here $x_1=-1$, $x_2=0$, $x_3=1$, so $\bar x=0$ and $S_{xx}=\sum(x_i-\bar x)^2=1+0+1=2$.",
      r"$S_{xy}=\sum x_iy_i-n\bar x\bar y=(-1)y_1+0\cdot y_2+(1)y_3=y_3-y_1$.",
      r"Hence $\hat\beta_1=S_{xy}/S_{xx}=\dfrac{y_3-y_1}{2}$.",
      r"And $\hat\beta_0=\bar y-\hat\beta_1\bar x=\bar y$ because $\bar x=0$.",
      r"So the BLUE pair is $\left(\bar y,\ \dfrac{y_3-y_1}{2}\right)$ - option (a). By Gauss-Markov these least-squares estimators are BLUE."]),

"2018-Q3": dict(ans=1, qtype="Conceptual", diff="Medium",
 sc=r"Statement 1 confuses near multicollinearity with exact multicollinearity - only exact linear dependence makes $X'X$ singular. Statement 2 is true (least squares is one of the four estimation methods named in the syllabus). So '2 only'.",
 tips=[r"Perfect multicollinearity $\Rightarrow$ $|X'X|=0$ $\Rightarrow$ inverse does not exist. Near multicollinearity $\Rightarrow$ inverse exists but is ill-conditioned, producing huge standard errors.",
       r"The Statistics-II syllabus itself lists 'least squares' among the estimation methods, which settles statement 2.",
       r"Watch the word 'cannot': absolute words in a statement are usually the ones that are false."],
 sol=[r"Statement 1: multicollinearity in practice means the columns of $X$ are nearly, not exactly, linearly dependent. Then $|X'X|$ is small but non-zero and $(X'X)^{-1}$ still exists.",
      r"Only exact (perfect) multicollinearity makes $X'X$ singular. As stated, statement 1 is therefore false.",
      r"Statement 2: the method of least squares is a recognised statistical method of estimation - it appears in the syllabus alongside maximum likelihood, moments and minimum chi-square. It is true.",
      r"Hence only statement 2 is correct - option (b)."]),

"2018-Q4": dict(ans=1, qtype="Conceptual", diff="Easy",
 sc=r"A g-inverse is unique only for a non-singular square matrix, so statement 1 is false; enlarging the sample is a standard remedy for multicollinearity, so statement 2 is true - answer '2 only'.",
 tips=[r"$A^-$ satisfies only $AA^-A=A$; that single condition leaves many solutions. Uniqueness needs all four Penrose conditions (the Moore-Penrose inverse $A^+$).",
       r"Remedies for multicollinearity: more data, drop/combine variables, ridge regression, principal components.",
       r"Any statement containing 'the g-inverse is unique' is false unless the matrix is explicitly square and non-singular."],
 sol=[r"Statement 1: the defining property of a generalized inverse is $AA^-A=A$. For a singular or rectangular $A$ infinitely many matrices satisfy it, so $A^-$ is not unique. Only the Moore-Penrose inverse (all four conditions) is unique. Statement 1 is false.",
      r"Statement 2: multicollinearity is a data problem - a larger and better-spread sample increases $S_{xx}$-type quantities and reduces the variance inflation, so it is a standard remedy. Statement 2 is true.",
      r"Hence option (b), '2 only'."]),

"2018-Q5": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"Both are standard theorems: full rank $\Rightarrow$ every $\lambda'\beta$ estimable; and estimable $\iff$ $\lambda'=\mathbf{c}'X$, i.e. the function is $\mathbf{c}'X\beta$. Answer: both.",
 tips=[r"Memorise the chain: $\lambda'\beta$ estimable $\iff$ $\lambda'\in\mathcal{R}(X)$ $\iff$ $\lambda'=\mathbf{c}'X$ $\iff$ $\lambda'(X'X)^-X'X=\lambda'$.",
       r"Number of linearly independent estimable functions $=\operatorname{rank}(X)$.",
       r"A common trap variant replaces 'rank$(X)=p$' by 'rank$(X)>p$' - impossible, since rank cannot exceed the number of columns."],
 sol=[r"Statement 1: if $X$ is of full column rank $p$, then $\mathcal{R}(X)=\mathbb{R}^p$, so every $\lambda\in\mathbb{R}^p$ lies in the row space and every linear parametric function is estimable. True.",
      r"Statement 2: by definition $\lambda'\beta$ is estimable iff some linear function $\mathbf{c}'\mathbf{Y}$ is unbiased for it, i.e. $E(\mathbf{c}'\mathbf{Y})=\mathbf{c}'X\beta=\lambda'\beta$ for all $\beta$. Hence every estimable function has the form $\mathbf{c}'X\beta$. True.",
      r"Both statements are correct - option (c)."]),

"2018-Q6": dict(ans=1, qtype="Numerical", diff="Easy",
 sc=r"Interaction df $=(p-1)(q-1)=5\times3=15$; error df $=pq(r-1)=24\times2=48$.",
 tips=[r"Two-way with $r$ observations per cell: $\alpha$: $p-1$, $\beta$: $q-1$, interaction: $(p-1)(q-1)$, error: $pq(r-1)$, total: $pqr-1$.",
       r"Check the arithmetic by adding: $5+3+15+48=71=pqr-1=72-1$. If your four df do not sum to $N-1$, you have made an error.",
       r"The 45-vs-48 distractor comes from wrongly using $(pq-1)(r-1)$ or $pqr-pq-1$; always use $pq(r-1)$."],
 sol=[r"Here $p=6$ rows, $q=4$ columns, $r=3$ observations per cell, so $N=pqr=72$.",
      r"Interaction df $=(p-1)(q-1)=(6-1)(4-1)=15$.",
      r"Error (residual) df $=pq(r-1)=6\times4\times2=48$.",
      r"Verification: $ (p-1)+(q-1)+(p-1)(q-1)+pq(r-1)=5+3+15+48=71=N-1$. Correct.",
      r"Hence 15 and 48 - option (b)."]),

"2018-Q7": dict(ans=0, qtype="Conceptual", diff="Easy",
 sc=r"$F=\text{MST}/\text{MSE}$ and chance variability is exactly MSE, the denominator. Shrink the denominator and $F$ rises.",
 tips=[r"'Variability due to chance' = error/residual variation = MSE.",
       r"In ANOVA the signal-to-noise reading is literal: $F$ = between-group variance / within-group variance.",
       r"Smaller MSE also means a more sensitive experiment - this is why blocking and replication are used."],
 sol=[r"The ANOVA $F$-statistic is $F=\dfrac{\text{MST}}{\text{MSE}}=\dfrac{\text{between-treatment mean square}}{\text{error mean square}}$.",
      r"'Variability due to chance' is the error (residual) component, which is the denominator MSE.",
      r"With the treatment mean square held fixed, decreasing MSE increases the ratio.",
      r"Hence the $F$-statistic will always increase - option (a)."]),

"2018-Q8": dict(ans=1, qtype="Conceptual", diff="Easy",
 sc=r"A significant $F$ means treatment deviations dominate error deviations, so the average error deviation is relatively small.",
 tips=[r"$F$ large $\iff$ MST $\gg$ MSE $\iff$ error deviations small relative to treatment deviations.",
       r"Option (d) is a category error: the error deviation is a single pooled quantity, it does not 'differ between populations'.",
       r"Remember the logic of rejection: reject $H_0$ when between-group spread cannot be explained by within-group noise."],
 sol=[r"When the $k$ population means genuinely differ, the treatment (between-group) sum of squares is inflated by the real differences among means.",
      r"A significant $F=\text{MST}/\text{MSE}$ requires the numerator to be large relative to the denominator.",
      r"Equivalently the average error (within-group) deviation must be relatively small compared with the average treatment deviations.",
      r"Hence option (b)."]),

"2018-Q9": dict(ans=2, qtype="Numerical", diff="Medium",
 sc=r"$\operatorname{Cov}(\hat\beta_0,\hat\beta_1)=-\dfrac{\bar x\sigma^2}{S_{xx}}$. Here $\bar x=5$, $S_{xx}=260-10(25)=10$, $\sigma^2=1$, giving $-0.5$.",
 tips=[r"The covariance of the intercept and slope estimators is always the negative of $\bar x\sigma^2/S_{xx}$ - the minus sign is the whole question.",
       r"$\operatorname{Cov}(\hat\beta_0,\hat\beta_1)=0$ if and only if $\bar x=0$ (centred regressor).",
       r"$S_{xx}=\sum x_i^2-n\bar x^2$; do not forget the correction term."],
 sol=[r"Given $n=10$, $\sum x_i=50$ so $\bar x=5$; $\sum x_i^2=260$; $\sigma^2=1$.",
      r"$S_{xx}=\sum x_i^2-n\bar x^2=260-10(5)^2=260-250=10$.",
      r"For simple linear regression, $\operatorname{Cov}(\hat\beta_0,\hat\beta_1)=-\dfrac{\bar x\,\sigma^2}{S_{xx}}$.",
      r"Substituting: $-\dfrac{5\times1}{10}=-0.5$.",
      r"Hence option (c)."]),

"2018-Q10": dict(ans=3, qtype="Numerical", diff="Hard",
 sc=r"Test $AMA=A$ for each candidate. $P$ passes; $Q$ and $R$ both fail. The true statement is '$Q$ and $R$ only fail', which is not offered - so 'None of the options is correct'.",
 tips=[r"A generalized inverse is defined by one condition only: $AA^-A=A$. Verify it directly, never by inspection.",
       r"Note $R_3=R_1+R_2$ in $A$, so $\operatorname{rank}(A)=2$ and $A$ has no ordinary inverse; g-inverses exist but are not unique.",
       r"When three of four options make specific claims and your computation matches none of them, 'None of the options is correct' is the intended answer - do not force a fit."],
 sol=[r"A matrix $M$ is a generalized inverse of $A$ iff $AMA=A$.",
      r"Computing $APA$ gives back $A$ exactly, so $P$ is a generalized inverse of $A$.",
      r"Computing $AQA$ gives $\begin{pmatrix}-9&-8&-5\\-11/2&-4&-7/2\\-29/2&-12&-17/2\end{pmatrix}\ne A$, so $Q$ is not a generalized inverse.",
      r"Computing $ARA$ gives $\begin{pmatrix}-5/2&-2&-3/2\\-7/2&-2&-5/2\\-6&-4&-4\end{pmatrix}\ne A$, so $R$ is not a generalized inverse either.",
      r"So exactly $Q$ and $R$ fail. Option (a) names $P$, (b) names $Q$ alone, (c) names $P$ and $R$ - all wrong. Hence option (d)."]),

"2018-Q11": dict(ans=2, qtype="Conceptual", diff="Medium",
 sc=r"Scheffe covers all contrasts (1 true); Bartlett is a variance-homogeneity test, not a multiple-comparison procedure (2 false); Tukey controls family-wise Type-I error (3 true). So 1 and 3 only.",
 tips=[r"Bartlett's test = homogeneity of variances. Planting it among multiple-comparison procedures is a recurring UPSC trap.",
       r"Scheffe: all simple and complex contrasts, most conservative. Tukey: all pairwise comparisons via the studentised range. SNK/Duncan: stepwise on ordered means.",
       r"Every genuine multiple-comparison procedure exists to control the inflated family-wise Type-I error."],
 sol=[r"Statement 1: Scheffe's method gives simultaneous confidence bounds for every contrast - simple (pairwise) and complex - so it is true.",
      r"Statement 2: Bartlett's test tests $H_0:\sigma_1^2=\cdots=\sigma_k^2$, i.e. homogeneity of variances. It is not a multiple-comparison test at all, so the statement is false.",
      r"Statement 3: Tukey's HSD uses the studentised range distribution to hold the family-wise error rate at $\alpha$, which reduces the Type-I error inflation caused by repeated $t$-tests. True.",
      r"Hence 1 and 3 only - option (c)."]),

"2018-Q12": dict(ans=2, qtype="Conceptual", diff="Medium",
 sc=r"1 is the standard motivation for multiple comparisons (true); Scheffe does not need equal group sizes (2 false); SNK is stepwise and more powerful, hence lower Type-II risk (3 true). So 1 and 3 only.",
 tips=[r"Family-wise error with $m$ independent tests at level $\alpha$ is $1-(1-\alpha)^m$ - it explodes with $m$; this is why all-pairwise $t$-testing is wrong.",
       r"Scheffe is the most flexible procedure: unequal $n_i$ are allowed. It is Tukey's HSD that is designed for (and works best with) equal $n$.",
       r"Conservativeness ladder: Scheffe > Tukey > SNK > Duncan. Less conservative = more power = smaller Type-II error."],
 sol=[r"Statement 1: performing all $\binom{k}{2}$ pairwise $t$-tests at level $\alpha$ inflates the family-wise Type-I error far above $\alpha$. True.",
      r"Statement 2: Scheffe's procedure is derived from the $F$-distribution and applies to all contrasts with arbitrary (possibly unequal) group sizes. The statement is false.",
      r"Statement 3: the Student-Newman-Keuls procedure applies progressively smaller critical ranges to ordered means, making it more powerful than Tukey and so reducing the Type-II error risk. True.",
      r"Hence 1 and 3 only - option (c)."]),

"2018-Q13": dict(ans=2, qtype="Conceptual", diff="Medium",
 sc=r"This is the cell-means parametrisation $E(Y_{ij})=\mu_i$: the design matrix has $p$ independent indicator columns, so it is full rank - every $\sum l_i\mu_i$ is estimable and $\hat\mu_i$ is the group mean.",
 tips=[r"Cell-means model ($E(Y_{ij})=\mu_i$) is full rank; the over-parametrised model ($\mu+\tau_i$) is not. Read which one is printed.",
       r"In the over-parametrised version only $\mu+\tau_i$ and contrasts $\sum c_i\tau_i$ with $\sum c_i=0$ are estimable.",
       r"BLUE of $\mu_i$ is always the $i$-th group mean $\bar Y_{i\cdot}$, whatever the $n_i$ are."],
 sol=[r"Write $\mathbf{Y}=X\boldsymbol\mu+\mathbf{e}$ where $X$ has one indicator column per group. These $p$ columns are linearly independent, so $\operatorname{rank}(X)=p$, i.e. $X$ is of full column rank.",
      r"Full column rank implies every linear parametric function $\sum l_i\mu_i$ is estimable. Statement 1 is true.",
      r"The normal equations give $n_i\hat\mu_i=\sum_j Y_{ij}$, so $\hat\mu_i=\dfrac{\sum_{j}Y_{ij}}{n_i}=\bar Y_{i\cdot}$. Statement 2 is true.",
      r"Both statements are correct - option (c)."]),

"2018-Q14": dict(ans=1, qtype="Conceptual", diff="Medium",
 sc=r"Symmetry of $A^-A$ and $AA^-$ is an extra Penrose condition, so statement 2 is false. Only option (b) omits it.",
 tips=[r"$A$ is $n\times p$ $\Rightarrow$ $A^-$ is $p\times n$ (the orders must allow $AA^-A$).",
       r"$A^-A$ and $AA^-$ are always idempotent but need not be symmetric - symmetry is Penrose conditions 3 and 4.",
       r"Elimination shortcut: identify the single false statement, then pick the only option that excludes it."],
 sol=[r"Statement 1: for $AA^-A$ to be conformable and equal $A$ ($n\times p$), $A^-$ must be $p\times n$. True.",
      r"Statement 2: $A^-A$ and $AA^-$ are idempotent, but symmetry requires the extra Penrose conditions $(AA^-)'=AA^-$ and $(A^-A)'=A^-A$, which a general g-inverse need not satisfy. False.",
      r"Statement 3: when $A$ has full rank, $(A'A)^{-1}A'$ satisfies $A(A'A)^{-1}A'A=A$, so it is a g-inverse. True.",
      r"Statement 4: if $A$ is non-singular, $AA^{-1}A=A$, so $A^{-1}$ is a g-inverse - and in this case the unique one. True.",
      r"Statements 1, 3 and 4 are correct - option (b)."]),

"2018-Q15": dict(ans=0, qtype="Conceptual", diff="Easy",
 sc=r"Count the symbols: one $\mu$, $p$ $\alpha$'s, $q$ $\beta$'s and $pq$ $\gamma$'s, i.e. $1+p+q+pq$ (before any side conditions).",
 tips=[r"The question asks for the number of parameters, not the rank: side conditions such as $\sum\alpha_i=0$ are not imposed here.",
       r"The model is over-parametrised: $1+p+q+pq=(1+p)(1+q)$ parameters but only $pq$ independent cell means, hence rank $pq$ and $1+p+q$ redundancies.",
       r"$e_{ijk}$ is a random error, never counted as a parameter."],
 sol=[r"The model is $y_{ijk}=\mu+\alpha_i+\beta_j+\gamma_{ij}+e_{ijk}$.",
      r"Parameters: the general mean $\mu$ (1); the row effects $\alpha_1,\dots,\alpha_p$ ($p$); the column effects $\beta_1,\dots,\beta_q$ ($q$); the interactions $\gamma_{ij}$ ($pq$).",
      r"Total $=1+p+q+pq$.",
      r"Hence option (a). (Note this equals $(1+p)(1+q)$, while the rank of the design is only $pq$ - the model is over-parametrised.)"]),

"2019-Q1": dict(ans=0, qtype="Conceptual", diff="Medium",
 sc=r"Rows of $X$ are $(1,1,0)$ and $(1,0,1)$ (each twice). $l=a(1,1,0)+b(1,0,1)$ gives $l_1=l_2+l_3$: statement 1 only.",
 tips=[r"Whenever $E(y)$ repeats, the distinct rows are all that matter - duplicates add nothing to the row space.",
       r"Estimability is a statement about $l$ lying in $\mathcal{R}(X)$; translate the expectations into rows and read off the constraint.",
       r"Here $\operatorname{rank}(X)=2<3$, so exactly one linear restriction on $l$ appears."],
 sol=[r"The design matrix rows are $(1,1,0)$ for $y_1,y_2$ and $(1,0,1)$ for $y_3,y_4$, so $\mathcal{R}(X)=\operatorname{span}\{(1,1,0),(1,0,1)\}$.",
      r"$l'\theta$ is estimable iff $l=a(1,1,0)+b(1,0,1)=(a+b,a,b)$ for some $a,b$.",
      r"Then $l_1=a+b$, $l_2=a$, $l_3=b$, so the necessary and sufficient condition is $l_1=l_2+l_3$.",
      r"Only statement 1 gives this condition - option (a)."]),

"2019-Q2": dict(ans=3, qtype="Conceptual", diff="Medium",
 sc=r"$e=(I-H)y$, so $\operatorname{Cov}(e)=\sigma^2(I-H)$ and $\operatorname{Cov}(e_i,e_j)=-\sigma^2h_{ij}$. The printed statement omits the minus sign, so (d) is NOT correct.",
 tips=[r"Memorise: $\hat y=Hy\sim N(X\beta,\sigma^2H)$, $e=(I-H)y\sim N(0,\sigma^2(I-H))$, $\operatorname{Cov}(\hat y,e)=0$.",
       r"$H$ is symmetric idempotent with $\operatorname{rank}(H)=\operatorname{tr}(H)=p$ and $\sum_i h_{ii}=p$.",
       r"'Which is NOT correct' questions: verify the three easy ones first and let the odd one out fall through."],
 sol=[r"With $H=X(X'X)^{-1}X'$: $H$ is symmetric idempotent, so $\operatorname{rank}(H)=\operatorname{tr}(H)=p$. Statement (a) is correct.",
      r"The leverages satisfy $0\le h_{ii}\le1$; for a model with an intercept and no exact duplicate design point they lie strictly inside, so (b) is taken as correct.",
      r"$\hat y=Hy$ gives $V(\hat y)=HV(y)H'=\sigma^2HH'=\sigma^2H$. Statement (c) is correct.",
      r"$e=(I-H)y$ gives $V(e)=\sigma^2(I-H)$, whose $(i,j)$ off-diagonal element is $-\sigma^2h_{ij}$, not $+\sigma^2h_{ij}$.",
      r"Hence the incorrect statement is (d)."]),

"2019-Q3": dict(ans=0, qtype="Conceptual", diff="Easy",
 sc=r"Model 2 nests model 1 ($\gamma=0$ recovers it), so its minimised residual sum of squares cannot be larger: $E\ge E^*$.",
 tips=[r"Adding a regressor can never increase the residual sum of squares - the larger model minimises over a superset of fits.",
       r"This is exactly why $R^2$ never decreases when a variable is added, and why adjusted $R^2$ was invented.",
       r"Option (d) reverses the nesting: $E=0$ with $E^*>0$ is impossible since $E^*\le E$."],
 sol=[r"Model 2 contains model 1 as the special case $\gamma=0$.",
      r"Least squares minimises $\sum(y_i-\text{fit})^2$ over the parameter space; model 2's parameter space contains model 1's.",
      r"A minimum over a larger set cannot exceed the minimum over the smaller set, so $E^*\le E$, i.e. $E\ge E^*$.",
      r"Hence option (a)."]),

"2019-Q4": dict(ans=1, qtype="Numerical", diff="Medium",
 sc=r"$Y_1-Y_2$ and $Y_3-Y_4$ are error contrasts with mean $0$ and variance $2\sigma^2$; dividing by $\sqrt2$ makes $E(e_k^2)=\sigma^2$, so the average $\tfrac12(e_1^2+e_2^2)$ is unbiased.",
 tips=[r"An error contrast is any $\mathbf{c}'\mathbf{Y}$ with $E(\mathbf{c}'\mathbf{Y})=0$; its square has expectation $\sigma^2\|\mathbf{c}\|^2$.",
       r"Normalising by $\|\mathbf{c}\|$ converts each contrast into a one-df estimate of $\sigma^2$; averaging $k$ of them gives $k$ df.",
       r"A difference of squares (option a) can never be an unbiased variance estimator - its expectation is $0$ here. Eliminate on sight."],
 sol=[r"$E(Y_1-Y_2)=0$ and $V(Y_1-Y_2)=\sigma^2+\sigma^2=2\sigma^2$ since $Y_1,Y_2$ are uncorrelated with the same mean.",
      r"So $e_1=\tfrac1{\sqrt2}(Y_1-Y_2)$ has $E(e_1)=0$ and $V(e_1)=\sigma^2$, giving $E(e_1^2)=\sigma^2$.",
      r"Identically $E(e_2^2)=\sigma^2$ from the pair $(Y_3,Y_4)$.",
      r"Hence $E\!\left[\tfrac12(e_1^2+e_2^2)\right]=\tfrac12(\sigma^2+\sigma^2)=\sigma^2$.",
      r"So $\tfrac12(e_1^2+e_2^2)$ is unbiased for $\sigma^2$ - option (b), an estimator with 2 degrees of freedom."]),

"2019-Q5": dict(ans=0, qtype="Numerical", diff="Easy",
 sc=r"$z=12\times25=300$; $x=561-300=261$; $w=14-12=2$; $y=261/2=130.5$. Check $130.5/25=5.22$.",
 tips=[r"Order of filling an ANOVA table: df by subtraction, then SS by subtraction, then MS $=$ SS/df, then $F=$ MS/MSE.",
       r"Always verify with the printed $F$: it is a free consistency check on all four unknowns.",
       r"Total df $=N-1$, so $N=15$ observations and $k-1=2$ means $k=3$ treatments."],
 sol=[r"Error: df $=12$ and MSS $=25$, so $z=\text{SSE}=12\times25=300$.",
      r"Total SS $=561$, hence treatment SS $x=561-300=261$.",
      r"Treatment df $w=$ total df $-$ error df $=14-12=2$.",
      r"Treatment mean SS $y=x/w=261/2=130.5$.",
      r"Check: $F=y/25=130.5/25=5.22$, matching the table. Hence $(w,x,y,z)=(2,261,130.5,300)$ - option (a)."]),
}
