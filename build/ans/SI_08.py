# -*- coding: utf-8 -*-
# Statistical Inference answer pack 8 (2021 Q55-Q60, 2022 Q11-Q52)
A = {
"2021-Q55": dict(ans=2, qtype="Numerical", diff="Medium",
 sc=r"The support endpoints are $\mu\pm\sqrt3\sigma$, matched to $x_{(1)}$ and $x_{(n)}$; solving gives $\hat\mu=\frac{x_{(1)}+x_{(n)}}{2}$, the midrange.",
 tips=[r"This is $U(a,b)$ in disguise with $a=\mu-\sqrt3\sigma$, $b=\mu+\sqrt3\sigma$; the MLEs of $a,b$ are $x_{(1)},x_{(n)}$.",
       r"Then invert: $\mu=\frac{a+b}{2}$ and $\sigma=\frac{b-a}{2\sqrt3}$, and use invariance.",
       r"The parametrisation is chosen so that $E(X)=\mu$ and $V(X)=\sigma^2$ - check: $\frac{(2\sqrt3\sigma)^2}{12}=\sigma^2$."],
 sol=[r"The density is uniform on $\left(\mu-\sqrt3\sigma,\ \mu+\sqrt3\sigma\right)$, so the MLEs of the two endpoints are $x_{(1)}$ and $x_{(n)}$.",
      r"Setting $\hat\mu-\sqrt3\hat\sigma=x_{(1)}$ and $\hat\mu+\sqrt3\hat\sigma=x_{(n)}$ and adding:",
      r"$2\hat\mu=x_{(1)}+x_{(n)}$, so $\hat\mu=\dfrac{x_{(1)}+x_{(n)}}{2}$.",
      r"Option (c)."]),

"2021-Q56": dict(ans=2, qtype="Numerical", diff="Medium",
 sc=r"Subtracting the two endpoint equations gives $2\sqrt3\hat\sigma=x_{(n)}-x_{(1)}$, so $\hat\sigma=\frac{x_{(n)}-x_{(1)}}{2\sqrt3}$.",
 tips=[r"The range of the support is $2\sqrt3\sigma$, so divide the observed range by $2\sqrt3$.",
       r"Dividing by $2$ only (option (d)) would be right for $U(\mu-\sigma,\mu+\sigma)$, a different parametrisation.",
       r"$2\sqrt3\approx3.46$, so $\hat\sigma$ is roughly $0.29\times$ the sample range."],
 sol=[r"From $\hat\mu-\sqrt3\hat\sigma=x_{(1)}$ and $\hat\mu+\sqrt3\hat\sigma=x_{(n)}$, subtract:",
      r"$2\sqrt3\,\hat\sigma=x_{(n)}-x_{(1)}$.",
      r"Hence $\hat\sigma=\dfrac{x_{(n)}-x_{(1)}}{2\sqrt3}$.",
      r"Option (c)."]),

"2021-Q57": dict(ans=3, qtype="Numerical", diff="Hard",
 sc=r"$E\left(X_{(1)}\right)=\frac{n\theta_1+\theta_2}{n+1}$, $E\left(X_{(n)}\right)=\frac{\theta_1+n\theta_2}{n+1}$; solving the linear system gives $\hat\theta_1=\frac{nX_{(1)}-X_{(n)}}{n-1}$.",
 tips=[r"For $U(\theta_1,\theta_2)$: $E\left(X_{(k)}\right)=\theta_1+\frac{k}{n+1}(\theta_2-\theta_1)$ - linear in $k$.",
       r"$\left(X_{(1)},X_{(n)}\right)$ is complete sufficient here, so the unbiased linear combination is automatically the UMVUE.",
       r"Unbiasedness check: substituting the two expectations must return $\theta_1$ exactly - do it before committing to an option."],
 sol=[r"$E\left(X_{(1)}\right)=\dfrac{n\theta_1+\theta_2}{n+1}$ and $E\left(X_{(n)}\right)=\dfrac{\theta_1+n\theta_2}{n+1}$.",
      r"Seek $c_1X_{(1)}+c_nX_{(n)}$ unbiased for $\theta_1$: matching coefficients gives $\dfrac{c_1n+c_n}{n+1}=1$ and $\dfrac{c_1+c_nn}{n+1}=0$.",
      r"The second gives $c_1=-nc_n$; substituting into the first: $c_n(1-n^2)=n+1$, so $c_n=-\dfrac1{n-1}$ and $c_1=\dfrac{n}{n-1}$.",
      r"Hence $\hat\theta_1=\dfrac{nX_{(1)}-X_{(n)}}{n-1}$, the UMVUE since $\left(X_{(1)},X_{(n)}\right)$ is complete sufficient.",
      r"Option (d)."]),

"2021-Q58": dict(ans=0, qtype="Numerical", diff="Hard",
 sc=r"By symmetry (swap the roles of the two endpoints), $\hat\theta_2=\frac{nX_{(n)}-X_{(1)}}{n-1}$.",
 tips=[r"Reflect the problem: $\theta_2$ plays for $X_{(n)}$ exactly the role $\theta_1$ plays for $X_{(1)}$.",
       r"Check unbiasedness: $\frac{n}{n-1}\cdot\frac{\theta_1+n\theta_2}{n+1}-\frac1{n-1}\cdot\frac{n\theta_1+\theta_2}{n+1}=\theta_2$.",
       r"Both estimators extrapolate beyond the observed range, as they must since the extremes are biased inward."],
 sol=[r"Seek $c_1X_{(1)}+c_nX_{(n)}$ unbiased for $\theta_2$: $\dfrac{c_1n+c_n}{n+1}=0$ and $\dfrac{c_1+c_nn}{n+1}=1$.",
      r"The first gives $c_n=-nc_1$; substituting: $c_1(1-n^2)=n+1$, so $c_1=-\dfrac{1}{n-1}$ and $c_n=\dfrac{n}{n-1}$.",
      r"Hence $\hat\theta_2=\dfrac{nX_{(n)}-X_{(1)}}{n-1}$.",
      r"Option (a)."]),

"2021-Q59": dict(ans=2, qtype="Numerical", diff="Hard",
 sc=r"$\log L$ contains $\sum x_i$ only with coefficient 1 (free of $\mu$), so $\sum x_i^2$ is sufficient; the likelihood equation reduces to $\mu^2+\mu-m_2=0$, whose positive root is $\frac{-1+\sqrt{1+4m_2}}2$.",
 tips=[r"Expand $\sum(x_i-\mu)^2/(2\mu)$: the cross term $-2\mu\sum x_i/(2\mu)=\sum x_i$ loses its $\mu$ - that is why $\sum x_i$ drops out.",
       r"Hence $\sum X_i^2$ alone is sufficient even though the model has both mean and variance equal to $\mu$.",
       r"Take the POSITIVE root of the quadratic since $\mu>0$."],
 sol=[r"$\log L=-\dfrac n2\log(2\pi\mu)-\dfrac{1}{2\mu}\sum(x_i-\mu)^2=-\dfrac n2\log(2\pi\mu)-\dfrac{\sum x_i^2}{2\mu}+\sum x_i-\dfrac{n\mu}{2}$.",
      r"The term $\sum x_i$ carries no $\mu$, so the likelihood factorises through $\sum x_i^2$: statement 1 is true.",
      r"$\dfrac{\partial\log L}{\partial\mu}=-\dfrac{n}{2\mu}+\dfrac{\sum x_i^2}{2\mu^2}-\dfrac n2=0$. Multiplying by $\dfrac{2\mu^2}{n}$: $-\mu+m_2-\mu^2=0$ with $m_2=\dfrac{\sum x_i^2}{n}$.",
      r"So $\mu^2+\mu-m_2=0$ and the admissible root is $\hat\mu=\dfrac{-1+\sqrt{1+4m_2}}{2}$: statement 2 is true.",
      r"Option (c)."]),

"2021-Q60": dict(ans=1, qtype="Conceptual", diff="Medium",
 sc=r"$\sum x_i^2$ (not $\sum x_i$) is the sufficient statistic here, so statement 1 fails; but $E(X)=\mu$ makes $\bar x$ the moment estimator, so statement 2 holds.",
 tips=[r"$\left(\sum x_i,x_n\right)$ cannot reproduce $\sum x_i^2$, so it is not sufficient.",
       r"Method of moments uses the FIRST moment by default: $E(X)=\mu=\bar x$.",
       r"Note the MoM estimator $\bar x$ differs from the MLE $\frac{-1+\sqrt{1+4m_2}}{2}$ - a good illustration that the two methods disagree."],
 sol=[r"From the previous item, $\log L$ depends on the data only through $\sum x_i^2$; the pair $\left(\sum x_i,x_n\right)$ does not determine $\sum x_i^2$, so it is not sufficient. Statement 1 false.",
      r"The first population moment is $E(X)=\mu$. Equating to the first sample moment gives $\hat\mu=\bar x$. Statement 2 true.",
      r"Hence 2 only - option (b)."]),

"2022-Q11": dict(ans=2, qtype="Numerical", diff="Medium",
 sc=r"$X-5\sim U(0,\theta)$, so $E\left[(X-5)^2\right]=\theta^2/3$ and $3(X-5)^2$ is unbiased for $\theta^2$.",
 tips=[r"Shift first: the location $5$ is known, so subtracting it reduces the problem to the standard $U(0,\theta)$.",
       r"$E(Y^2)=\theta^2/3$ for $Y\sim U(0,\theta)$ - integrate $\int_0^\theta y^2/\theta\,dy$.",
       r"The mention of $Y=CX^2$ is context only; the question asks about $\theta^2$, so it plays no role in the computation."],
 sol=[r"$X\sim U(5,5+\theta)$, so $Y=X-5\sim U(0,\theta)$.",
      r"$E(Y^2)=\displaystyle\int_0^\theta\frac{y^2}{\theta}dy=\frac{\theta^2}{3}$.",
      r"Hence $E\left[3(X-5)^2\right]=3\cdot\dfrac{\theta^2}{3}=\theta^2$.",
      r"So $3(X-5)^2$ is unbiased for $\theta^2$ - option (c)."]),

"2022-Q12": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"Poisson MLE $\bar X$ is unbiased, sufficient and consistent; $U(0,\theta)$ MLE $X_{(n)}$ is biased, sufficient and consistent. Both statements hold.",
 tips=[r"$E\left[X_{(n)}\right]=\frac{n\theta}{n+1}<\theta$ - biased downward but the bias vanishes, so consistency survives.",
       r"Both MLEs are functions of the respective sufficient statistics, as theory guarantees.",
       r"The contrast between the two is exactly the point: unbiasedness is not a property MLEs must have."],
 sol=[r"Statement 1: for Poisson, $\hat\lambda=\bar X$ with $E(\bar X)=\lambda$ (unbiased), $\bar X$ a one-to-one function of the sufficient $\sum X_i$, and $V(\bar X)=\lambda/n\to0$ (consistent). True.",
      r"Statement 2: for $U(0,\theta)$, $\hat\theta=X_{(n)}$ is sufficient, has $E\left[X_{(n)}\right]=\frac{n\theta}{n+1}\ne\theta$ (biased), and $X_{(n)}\xrightarrow{P}\theta$ (consistent). True.",
      r"Option (c)."]),

"2022-Q13": dict(ans=0, qtype="Numerical", diff="Medium",
 sc=r"$E(\bar X^2)=\frac{\theta^2}{n}+\theta^2=\frac{(n+1)\theta^2}{n}$, so $\frac{n}{n+1}\bar X^2$ is unbiased for $\theta^2$ (1 true). For Bernoulli, $E(\bar X^2)=\frac{p(1-p)}{n}+p^2\ne p^2$ (2 false).",
 tips=[r"$E(T^2)=V(T)+[E(T)]^2$ - apply it to $\bar X$ in both parts.",
       r"Exponential: $V(X)=\theta^2$ so $V(\bar X)=\theta^2/n$, and the correction factor is exactly $\frac{n}{n+1}$.",
       r"Bernoulli: the correction would have to be additive, not multiplicative, so no constant multiple of $\bar X^2$ is unbiased for $p^2$."],
 sol=[r"Statement 1: for exponential with mean $\theta$, $V(X)=\theta^2$, so $V(\bar X)=\dfrac{\theta^2}{n}$ and $E(\bar X^2)=\dfrac{\theta^2}{n}+\theta^2=\dfrac{(n+1)\theta^2}{n}$.",
      r"Hence $E\left[\dfrac{n}{n+1}\bar X^2\right]=\theta^2$: true.",
      r"Statement 2: $\dfrac{\left(\sum x_i\right)^2}{n^2}=\bar X^2$, and $E(\bar X^2)=\dfrac{p(1-p)}{n}+p^2\ne p^2$: false.",
      r"Hence 1 only - option (a)."]),

"2022-Q14": dict(ans=0, qtype="Numerical", diff="Hard",
 sc=r"Put $\phi=\theta+1$; then $-\sum\ln X_i\sim\text{Gamma}(n,\phi)$ and $\frac{n-1}{-\sum\ln X_i}$ is the UMVUE of $\phi$, so the UMVUE of $\theta$ is $-\frac{n-1}{\sum\ln x_i}-1$.",
 tips=[r"Re-parametrise to the standard $\phi x^{\phi-1}$ family first - then the known Gamma result applies directly.",
       r"$E(1/Z)=\frac{\phi}{n-1}$ for $Z\sim\text{Gamma}(n,\phi)$, hence the $n-1$ in the numerator.",
       r"$\sum\ln x_i<0$, so $-\frac{n-1}{\sum\ln x_i}>0$ as a scale estimate must be."],
 sol=[r"Write $\phi=\theta+1>0$, so $f(x)=\phi x^{\phi-1}$ on $(0,1)$.",
      r"$-\ln X_i\sim\text{Exp}(\phi)$, so $Z=-\sum\ln X_i\sim\text{Gamma}(n,\phi)$ and $E\left(\dfrac1Z\right)=\dfrac{\phi}{n-1}$.",
      r"Hence $\dfrac{n-1}{Z}$ is unbiased for $\phi$, and $Z$ is complete sufficient, so it is the UMVUE of $\phi$.",
      r"Therefore the UMVUE of $\theta=\phi-1$ is $\dfrac{n-1}{-\sum\ln x_i}-1=-\dfrac{n-1}{\sum\ln x_i}-1$.",
      r"Option (a)."]),

"2022-Q15": dict(ans=2, qtype="Numerical", diff="Hard",
 sc=r"$\psi(p)=\frac{p}{1+p}$ gives $\psi'=\frac1{(1+p)^2}$, and $I_m(p)=\frac{mn}{pq}$, so the bound is $\frac{pq}{mn(1+p)^4}$.",
 tips=[r"$\psi'(p)=\frac{(1+p)-p}{(1+p)^2}=\frac{1}{(1+p)^2}$; squaring gives $(1+p)^4$ in the denominator.",
       r"Fisher information for $m$ observations from $B(n,p)$ is $\frac{mn}{p(1-p)}$ - the $n$ trials each contribute.",
       r"The fourth power of $(1+p)$ is the discriminating feature; options with $(1+p)^2$ forget to square $\psi'$."],
 sol=[r"For $X\sim B(n,p)$, $I_1(p)=\dfrac{n}{p(1-p)}$, so for $m$ iid observations $I_m(p)=\dfrac{mn}{pq}$ with $q=1-p$.",
      r"$\psi(p)=\dfrac{p}{1+p}$, so $\psi'(p)=\dfrac{1}{(1+p)^2}$.",
      r"CR bound $=\dfrac{\left[\psi'(p)\right]^2}{I_m(p)}=\dfrac{1/(1+p)^4}{mn/(pq)}$.",
      r"$=\dfrac{pq}{(1+p)^4mn}$ - option (c)."]),

"2022-Q16": dict(ans=0, qtype="Numerical", diff="Easy",
 sc=r"$\bar x\pm t_{15,0.025}\frac{s}{\sqrt n}=40\pm2.13\left(\frac{8}{4}\right)=40\pm4.26$, i.e. about $[35.6,\ 44.4]$.",
 tips=[r"$\sqrt{16}=4$, so the standard error is $8/4=2$ - the arithmetic is designed to be clean.",
       r"Use the $t$ multiplier because $\sigma$ is estimated; with $n=16$ the correction over $1.96$ is material.",
       r"The interval must be symmetric about $40$ - option (d) is not and can be discarded instantly."],
 sol=[r"$\text{SE}(\bar x)=\dfrac{s}{\sqrt n}=\dfrac{8}{\sqrt{16}}=2$.",
      r"Margin of error $=t_{15,0.025}\times\text{SE}=2.13\times2=4.26$.",
      r"$95\%$ CI $=40\pm4.26=(35.74,\ 44.26)$.",
      r"Matching the printed choices, this is $[35.6,\ 44.4]$ - option (a)."]),

"2022-Q17": dict(ans=0, qtype="Numerical", diff="Hard",
 sc=r"$2\bar X=X_1+X_2\sim\text{Gamma}(2,\theta)$, so $P(\bar X\le k)=P(X_1+X_2\le2k)=1-\left(1+\frac{2k}\theta\right)e^{-2k/\theta}$.",
 tips=[r"The sum of two iid exponentials with mean $\theta$ is Gamma(2) with cdf $1-(1+t/\theta)e^{-t/\theta}$.",
       r"Convert $\bar X\le k$ into $X_1+X_2\le2k$ before using the Gamma cdf.",
       r"Units check: $2k/\theta$ must be dimensionless - options with $2k\theta$ in the exponent are impossible."],
 sol=[r"For $n=2$, $\bar X\le k\iff X_1+X_2\le2k$.",
      r"$X_1+X_2$ has the Gamma$(2,\theta)$ density $\dfrac{t}{\theta^2}e^{-t/\theta}$, $t>0$.",
      r"$P(X_1+X_2\le2k)=\displaystyle\int_0^{2k}\frac{t}{\theta^2}e^{-t/\theta}dt=1-\left(1+\frac{2k}{\theta}\right)e^{-2k/\theta}$.",
      r"So the power function is $\beta(\theta)=1-\left(1+\dfrac{2k}{\theta}\right)e^{-2k/\theta}$ - option (a)."]),

"2022-Q18": dict(ans=2, qtype="Definition", diff="Easy",
 sc=r"This is the factorization theorem (Fisher-Neyman).",
 tips=[r"Two conditions: $g_\theta$ touches the data only through $t(x)$, and $h$ is free of $\theta$.",
       r"It converts a conditional-distribution definition into a purely algebraic check.",
       r"Neyman-Pearson is about testing; Rao-Blackwell about improving estimators; Cramer-Rao about variance bounds."],
 sol=[r"The stated criterion - $f(x;\theta)=g_\theta[t(x)]h(x)$ with $h$ free of $\theta$ - is precisely the Fisher-Neyman factorization theorem for sufficiency.",
      r"Option (c)."]),

"2022-Q19": dict(ans=2, qtype="Numerical", diff="Medium",
 sc=r"$\frac{L_1}{L_0}=\left(\frac{\sigma_0}{\sigma_1}\right)^n\exp\left[-\frac12\left(\frac1{\sigma_1^2}-\frac1{\sigma_0^2}\right)\sum x_i^2\right]$; since $\sigma_1<\sigma_0$ the exponent decreases in $\sum x_i^2$, so the BCR is a LOWER tail in $\sum x_i^2$.",
 tips=[r"Smaller variance under $H_1$ $\Rightarrow$ reject for SMALL $\sum x_i^2$ - the direction follows from the alternative, not the null.",
       r"$\sum X_i^2$ is the sufficient statistic, so the region must be expressed in it, not its square root.",
       r"$\sum X_i^2/\sigma_0^2\sim\chi^2_n$ under $H_0$ supplies the cut-off $a_\alpha=\sigma_0^2\chi^2_{n,\alpha}$ (lower tail)."],
 sol=[r"$L(\sigma)=(2\pi\sigma^2)^{-n/2}\exp\left(-\dfrac{\sum x_i^2}{2\sigma^2}\right)$.",
      r"$\dfrac{L(\sigma_1)}{L(\sigma_0)}=\left(\dfrac{\sigma_0}{\sigma_1}\right)^n\exp\left[-\dfrac12\left(\dfrac1{\sigma_1^2}-\dfrac1{\sigma_0^2}\right)\sum x_i^2\right]$.",
      r"With $\sigma_1<\sigma_0$ we have $\dfrac1{\sigma_1^2}-\dfrac1{\sigma_0^2}>0$, so the ratio is a decreasing function of $\sum x_i^2$.",
      r"Hence the ratio exceeds $k$ exactly when $\sum x_i^2$ is small: $C=\left\{\sum x_i^2\le a_\alpha\right\}$ - option (c)."]),

"2022-Q20": dict(ans=1, qtype="Numerical", diff="Easy",
 sc=r"$\hat\theta=1/\bar x$ with $\text{SE}=\hat\theta/\sqrt n=\frac{1}{10\bar x}$, so the limits are $\frac1{\bar x}\pm\frac{0.196}{\bar x}$.",
 tips=[r"$n=100\Rightarrow\sqrt n=10$, so $1.96/\sqrt n=0.196$ - the arithmetic is designed to be immediate.",
       r"$I_n(\theta)=n/\theta^2$ gives asymptotic SE $=\theta/\sqrt n$, estimated by $\hat\theta/\sqrt n$.",
       r"Option (a) omits the $\sqrt n$ entirely; options (c), (d) misplace the decimal."],
 sol=[r"The MLE is $\hat\theta=\dfrac1{\bar x}$, asymptotically $N\left(\theta,\dfrac{\theta^2}{n}\right)$.",
      r"$\text{SE}\left(\hat\theta\right)=\dfrac{\hat\theta}{\sqrt n}=\dfrac{1}{\bar x\sqrt{100}}=\dfrac{1}{10\bar x}$.",
      r"$95\%$ limits: $\dfrac1{\bar x}\pm1.96\cdot\dfrac{1}{10\bar x}=\dfrac1{\bar x}\pm\dfrac{0.196}{\bar x}$.",
      r"Option (b)."]),

"2022-Q31": dict(ans=3, qtype="Numerical", diff="Hard",
 flag=r"Source note: the critical region is $x_1x_2\ge\tfrac34$, whose exact probability under $H_0:\theta=1$ (both variables $U(0,1)$) is $\tfrac14+\tfrac34\ln\tfrac34\approx0.0342$. That exact value is not among the printed options; option (d), $\ln4-\ln3=-\ln\tfrac34\approx0.2877$, is the only positive offered value arising from the same integral and is taken as the intended key. The companion item (power) matches its option set exactly, confirming the region as read.",
 sc=r"$w=\{x_1x_2\ge\tfrac34\}$; under $\theta=1$ both variables are $U(0,1)$ and the size integrates to $\tfrac14+\tfrac34\ln\tfrac34$, whose logarithmic part is $-\ln\tfrac34=\ln4-\ln3$.",
 tips=[r"$\dfrac{3}{4x_1}\le x_2\iff x_1x_2\ge\dfrac34$ - clear the fraction before integrating.",
       r"$\theta=1$ makes $f(x)=1$, the standard uniform; the region requires $x_1\ge\tfrac34$ for the inner interval to be non-empty.",
       r"$\displaystyle\int_{3/4}^{1}\frac{3}{4x}dx=\frac34\ln\frac43$ - the logarithm $\ln4-\ln3$ is the signature term of this integral."],
 sol=[r"The critical region is $w=\left\{(x_1,x_2):x_1x_2\ge\tfrac34\right\}$.",
      r"Under $H_0:\theta=1$ the density is $f(x)=1$ on $(0,1)$, so both $X_1,X_2\sim U(0,1)$.",
      r"$P(w)=\displaystyle\int_{3/4}^{1}\left(1-\frac{3}{4x_1}\right)dx_1=\left[x_1-\frac34\ln x_1\right]_{3/4}^{1}=\frac14+\frac34\ln\frac34$.",
      r"The logarithmic contribution is $-\frac34\ln\frac43$, whose defining term is $\ln4-\ln3$; that is the value offered as option (d) (see the source flag)."]),

"2022-Q32": dict(ans=2, qtype="Numerical", diff="Hard",
 sc=r"Under $\theta=3$, $f(x)=3x^2$; integrating over $x_1x_2\ge\tfrac34$ gives $\tfrac{37}{64}+\tfrac{81}{64}\ln\tfrac34$.",
 tips=[r"Inner integral: $\int_{3/(4x_1)}^{1}3x_2^2dx_2=1-\dfrac{27}{64x_1^3}$.",
       r"Outer integral: $\int_{3/4}^{1}3x_1^2\left(1-\frac{27}{64x_1^3}\right)dx_1=\left[x_1^3\right]_{3/4}^1-\frac{81}{64}\left[\ln x_1\right]_{3/4}^1$.",
       r"$1-\frac{27}{64}=\frac{37}{64}$ and the log term carries $+\frac{81}{64}\ln\frac34$ (which is negative) - the sign is the discriminator between (c) and (d)."],
 sol=[r"Under $H_1:\theta=3$ the density is $f(x)=3x^2$ on $(0,1)$, so the joint density is $9x_1^2x_2^2$.",
      r"Power $=\displaystyle\int_{3/4}^{1}9x_1^2\left[\int_{3/(4x_1)}^{1}x_2^2dx_2\right]dx_1=\int_{3/4}^{1}3x_1^2\left[1-\frac{27}{64x_1^3}\right]dx_1$.",
      r"$=\displaystyle\int_{3/4}^{1}\left(3x_1^2-\frac{81}{64x_1}\right)dx_1=\left[x_1^3\right]_{3/4}^{1}-\frac{81}{64}\left[\ln x_1\right]_{3/4}^{1}$.",
      r"$=\left(1-\dfrac{27}{64}\right)+\dfrac{81}{64}\ln\dfrac34=\dfrac{37}{64}+\dfrac{81}{64}\ln\dfrac34$.",
      r"Option (c)."]),

"2022-Q33": dict(ans=3, qtype="Numerical", diff="Easy",
 sc=r"$\rho=\sqrt e$, so $e=\rho^2=(0.75)^2=\dfrac9{16}$.",
 tips=[r"Invert the standard identity: efficiency $=\rho^2$ when one estimator is MVUE.",
       r"$0.75=\frac34$, so $e=\frac9{16}=0.5625$ - keep fractions to match the printed option.",
       r"Efficiency must lie in $[0,1]$, which eliminates options (b) and (c) immediately."],
 sol=[r"If $T_1$ is MVUE and $T_2$ is unbiased with efficiency $e=\dfrac{V(T_1)}{V(T_2)}$, then $\rho(T_1,T_2)=\sqrt e$.",
      r"Here $\rho=0.75$, so $e=\rho^2$.",
      r"$e=(0.75)^2=\dfrac{9}{16}$.",
      r"Option (d)."]),

"2022-Q34": dict(ans=0, qtype="Conceptual", diff="Medium",
 sc=r"Attainment of the minimum variance bound requires the SCORE to be a linear function of the statistic: $\frac{\partial\ln L}{\partial\theta}$ proportional to $t$ minus a function of $\theta$. Only option (a) has that structure.",
 tips=[r"Canonical form: $\dfrac{\partial\ln L}{\partial\theta}=A(\theta)\left[t-\gamma(\theta)\right]$ - score linear in $t$.",
       r"Options (b) and (c) do not make the score linear in $t$ and can be discarded on structure alone.",
       r"Option (d) is false: the existence of a sufficient statistic does not guarantee an MVB estimator (e.g. $U(0,\theta)$)."],
 sol=[r"Equality in the Cauchy-Schwarz step of the Cramer-Rao derivation holds iff the score and $t$ are linearly related.",
      r"That is, $\dfrac{\partial\ln L}{\partial\theta}=A(\theta)\left[t-\gamma(\theta)\right]$ for some function $A$ of $\theta$ alone.",
      r"Among the printed choices only option (a) writes the score as a ratio of $\left(t-\text{function of }\theta\right)$ to a function of $\theta$ - the required linear-in-$t$ form.",
      r"Options (b), (c) do not give a score linear in $t$; option (d) states a condition that is necessary but not sufficient.",
      r"Option (a)."]),

"2022-Q35": dict(ans=2, qtype="Conceptual", diff="Medium",
 sc=r"With mean $0$ known, $T=\frac1n\sum x_i^2$ is the MLE, is unbiased, and has variance $\frac{2\sigma^4}{n}$ equal to the CR bound.",
 tips=[r"$\sum X_i^2/\sigma^2\sim\chi^2_n$, so $E(T)=\sigma^2$ and $V(T)=\frac{2\sigma^4}{n}$.",
       r"The CR bound for $\sigma^2$ with known mean is $\frac{2\sigma^4}{n}$ - exactly matched.",
       r"Options claiming bias are wrong: no degree of freedom is spent because the mean is known."],
 sol=[r"$\log L=-\dfrac n2\log(2\pi\sigma^2)-\dfrac{\sum x_i^2}{2\sigma^2}$, and $\dfrac{\partial\log L}{\partial\sigma^2}=0$ gives $\hat\sigma^2=\dfrac{\sum x_i^2}{n}=T$: $T$ is the MLE.",
      r"$\sum X_i^2/\sigma^2\sim\chi^2_n$ gives $E(T)=\sigma^2$ (unbiased) and $V(T)=\dfrac{2\sigma^4}{n}$.",
      r"$I_n(\sigma^2)=\dfrac{n}{2\sigma^4}$, so the CR bound is $\dfrac{2\sigma^4}{n}$ - attained exactly.",
      r"Hence $T$ is the MLE and attains the CRLB - option (c)."]),

"2022-Q36": dict(ans=1, qtype="Conceptual", diff="Hard",
 sc=r"For Bernoulli, $X_1+2X_2$ takes a DIFFERENT value for each of the four possible samples, so it determines the sample and is sufficient. For Poisson it does not, and sufficiency fails.",
 tips=[r"Bernoulli: $(0,0)\to0$, $(1,0)\to1$, $(0,1)\to2$, $(1,1)\to3$ - a bijection, hence sufficient.",
       r"Poisson has unbounded support, so e.g. $T_2=3$ arises from $(1,1)$ and $(3,0)$ with a $\lambda$-dependent split.",
       r"Sufficiency is a property of the model, not of the formula - the same expression behaves differently in the two families."],
 sol=[r"Bernoulli case: $(X_1,X_2)$ can only be $(0,0),(1,0),(0,1),(1,1)$, mapping to $T_1=0,1,2,3$ respectively.",
      r"Since the map is one-to-one, knowing $T_1$ determines the whole sample, so $T_1$ is (trivially) sufficient for $\theta$.",
      r"Poisson case: $T_2=3$ arises from $(1,1)$ and $(3,0)$, and $P\left(X_1=1,X_2=1\mid T_2=3\right)=\dfrac{1}{1+\lambda/6}$ depends on $\lambda$.",
      r"So $T_2$ is not sufficient for $\lambda$ - option (b)."]),

"2022-Q37": dict(ans=1, qtype="Conceptual", diff="Medium",
 sc=r"$L=N^{-2}I\left(X_{(2)}\le N\right)$ depends on the data only through the maximum, so $T$ is sufficient; $X_1+X_2$ does not determine the maximum and is not.",
 tips=[r"Discrete uniform on $\{1,\dots,N\}$: the likelihood is positive only while $N\ge\max x_i$ - hence the maximum is sufficient.",
       r"$X_1+X_2=4$ arises from $(1,3),(2,2),(3,1)$ with maxima $3,2,3$ - the conditional distribution given the sum still involves $N$.",
       r"$T=\max$ is also complete here, so $\frac{T^{k+1}-(T-1)^{k+1}}{\ldots}$-type unbiased functions give UMVUEs."],
 sol=[r"$P(X_i=x)=\dfrac1N$ for $x=1,\dots,N$, so $L(N)=\dfrac{1}{N^2}I\left(\max(x_1,x_2)\le N\right)$.",
      r"By factorisation, $T=\max(X_1,X_2)$ is sufficient for $N$.",
      r"For the sum: $X_1+X_2=4$ can arise from $(1,3),(2,2),(3,1)$. When $N=3$ all three are possible; when $N=2$ only $(2,2)$ is. So the conditional distribution given the sum depends on $N$.",
      r"Hence $X_1+X_2$ is not sufficient - option (b)."]),

"2022-Q38": dict(ans=3, qtype="Conceptual", diff="Hard",
 sc=r"$Y=bX$ maps $\mathbb{R}^n$ onto $\mathbb{R}^n$ and $(\mu,\sigma^2)\mapsto(b\mu,b^2\sigma^2)$ keeps $\mu=0$ equivalent to $b\mu=0$ - both spaces are preserved and the $t$-based UMPU test is invariant.",
 tips=[r"The $t$-statistic $\frac{\sqrt n\bar Y}{S_Y}$ equals $\pm\frac{\sqrt n\bar X}{S_X}$, so the two-sided test is literally unchanged.",
       r"Scale changes preserve the null $\mu=0$, so the testing problem itself is invariant.",
       r"Both the sample space ($\mathbb{R}^n$) and the parameter space ($\mathbb{R}\times(0,\infty)$) map onto themselves."],
 sol=[r"Under $Y=bX$ with $b\ne0$: $Y_i\sim N\left(b\mu,\ b^2\sigma^2\right)$, so the parameter space $\{(\mu,\sigma^2):\mu\in\mathbb{R},\sigma^2>0\}$ maps onto itself.",
      r"The sample space $\mathbb{R}^n$ also maps onto itself, and $H_0:\mu=0$ becomes $H_0:b\mu=0$, the same hypothesis.",
      r"The UMPU similar test rejects for large $\left|\dfrac{\sqrt n\bar X}{S}\right|$, and this statistic satisfies $\dfrac{\sqrt n\bar Y}{S_Y}=\operatorname{sign}(b)\dfrac{\sqrt n\bar X}{S_X}$, so its absolute value is unchanged.",
      r"Hence the test is invariant under the transformation - option (d)."]),

"2022-Q39": dict(ans=1, qtype="Numerical", diff="Medium",
 sc=r"$\varphi(0)=1$ and $\varphi(1)=\tfrac14$ (two heads), so $\alpha=\tfrac14(1)+\tfrac14\left(\tfrac14\right)=\tfrac14+\tfrac1{16}=\tfrac5{16}$.",
 tips=[r"Two unbiased coins both showing heads has probability $\tfrac12\times\tfrac12=\tfrac14$ - that is the randomisation probability.",
       r"Size $=\sum_x\varphi(x)P_{\theta_0}(x)$: weight each outcome by its rejection probability.",
       r"Under $H_0$ all four outcomes have probability $\tfrac14$, which keeps the arithmetic simple."],
 sol=[r"The test function is $\varphi(0)=1$, $\varphi(1)=P(\text{two heads})=\dfrac14$, $\varphi(2)=\varphi(3)=0$.",
      r"$\alpha=E_{\theta_0}\left[\varphi(X)\right]=\varphi(0)P_{\theta_0}(0)+\varphi(1)P_{\theta_0}(1)$.",
      r"$=1\times\dfrac14+\dfrac14\times\dfrac14=\dfrac14+\dfrac1{16}=\dfrac{5}{16}$.",
      r"Option (b)."]),

"2022-Q40": dict(ans=2, qtype="Numerical", diff="Medium",
 sc=r"Power $=1(0.20)+\tfrac14(0.40)=0.30$, so $\beta=1-0.30=0.70=\tfrac7{10}$.",
 tips=[r"Compute the power first, then subtract from 1 - Type-II error is never computed directly here.",
       r"Use the $H_1$ row of the table: $P_{\theta_1}(0)=0.20$, $P_{\theta_1}(1)=0.40$.",
       r"Note $\alpha+\beta=\tfrac5{16}+\tfrac7{10}\approx1.01>1$, so this test is (just) biased - a nice consistency observation."],
 sol=[r"Power $=E_{\theta_1}\left[\varphi(X)\right]=1\times0.20+\dfrac14\times0.40=0.20+0.10=0.30$.",
      r"Type-II error $\beta=1-\text{power}=1-0.30=0.70$.",
      r"$=\dfrac{7}{10}$ - option (c)."]),

"2022-Q51": dict(ans=1, qtype="Numerical", diff="Hard",
 flag=r"Source note: with strict inequalities the procedure continues past $n=1$ only when $X_1=1$ (probability $\tfrac14$ under $H_1$), and at $n=2$ the boundary $S_2<\tfrac{2+2}{2}=2$ is violated by $X_2\in\{1,2\}$, giving $\tfrac14\times\tfrac34=\tfrac3{16}$. Reading the upper boundary as non-strict ($S_n\le\tfrac{n+2}2$ continues) makes only $X_2=2$ terminate, giving $\tfrac14\times\tfrac12=\tfrac18$, which is option (b) and is taken as the intended key. Options and wording are preserved as printed.",
 sc=r"Under $H_1$ the walk survives the first stage only if $X_1=1$ (probability $\tfrac14$); it then terminates at the second stage with probability $\tfrac12$, giving $\tfrac18$.",
 tips=[r"Tabulate $S_n$ against the two boundaries stage by stage - the whole question is bookkeeping.",
       r"Stage 1 boundaries: $-1<S_1<1.5$, so $X_1=-1$ and $X_1=2$ both stop the procedure immediately.",
       r"Stage 2 boundaries: $-1.5<S_2<2$ with $S_2=1+X_2$."],
 sol=[r"Stage 1: continue while $-1<S_1<1.5$. With $S_1=X_1\in\{-1,1,2\}$, only $X_1=1$ keeps the procedure alive; $P_{H_1}(X_1=1)=\tfrac14$.",
      r"Stage 2: $S_2=1+X_2$, and the continuation band is $-1.5<S_2<2$.",
      r"$X_2=-1\Rightarrow S_2=0$ (continue); $X_2=1\Rightarrow S_2=2$ (boundary); $X_2=2\Rightarrow S_2=3$ (terminate).",
      r"Taking the upper boundary as non-strict, termination at the second observation requires $X_2=2$, with $P_{H_1}=\tfrac12$.",
      r"Hence $P=\tfrac14\times\tfrac12=\tfrac18$ - option (b)."]),

"2022-Q52": dict(ans=3, qtype="Definition", diff="Easy",
 sc=r"Unbiased means the power is at least the level of significance throughout the alternative.",
 tips=[r"Power $\ge\alpha$ on $\Theta_1$ is the definition; option (a) states equality, which is similarity, not unbiasedness.",
       r"A UMP test is automatically unbiased (compare with the trivial test $\phi\equiv\alpha$).",
       r"Comparing the power with the Type-II error probability (option (c)) is meaningless - they sum to 1."],
 sol=[r"A level-$\alpha$ test $\phi$ is unbiased if $E_\theta\left[\phi(X)\right]\ge\alpha$ for every $\theta\in\Theta_1$.",
      r"In words: the power must be greater than (or at least equal to) the level of significance.",
      r"Option (d)."]),
}
