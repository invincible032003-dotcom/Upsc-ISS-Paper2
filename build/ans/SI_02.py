# -*- coding: utf-8 -*-
# Statistical Inference answer pack 2 (2018 Q42-Q60, 2019 Q6-Q12)
A = {
"2018-Q42": dict(ans=1, qtype="Definition", diff="Easy",
 sc=r"The Neyman-Pearson lemma constructs the MOST POWERFUL test for a simple null against a simple alternative.",
 tips=[r"NP lemma: reject when $L_1/L_0>k$, randomising on the boundary to hit the exact size $\alpha$.",
       r"'Uniformly most powerful' applies to a composite alternative and is obtained from NP plus an MLR argument (Karlin-Rubin).",
       r"NP gives most power for a fixed Type-I error - it never controls unbiasedness or randomisation as an objective."],
 sol=[r"The Neyman-Pearson fundamental lemma deals with testing a simple hypothesis $H_0:\theta=\theta_0$ against a simple alternative $H_1:\theta=\theta_1$.",
      r"It asserts that the test rejecting when $\dfrac{L(x;\theta_1)}{L(x;\theta_0)}>k$ is most powerful of its size.",
      r"Hence it is a construction for the most powerful test - option (b)."]),

"2018-Q43": dict(ans=3, qtype="Conceptual", diff="Medium",
 sc=r"The LRT reduces to the two-sided $t$-test, which for a two-sided alternative is the uniformly most powerful UNBIASED test (no UMP test exists two-sided).",
 tips=[r"No UMP test exists for two-sided alternatives in the normal family - restricting to unbiased tests restores optimality.",
       r"The $t$-test is also similar (its size is $\alpha$ for every nuisance $\sigma^2$) and invariant, but the standard optimality statement is UMPU.",
       r"Rule of thumb: one-sided $\Rightarrow$ UMP; two-sided $\Rightarrow$ UMPU."],
 sol=[r"For $H_0:\mu=\mu_0$ vs $H_1:\mu\ne\mu_0$ with $\sigma^2$ unknown, the likelihood ratio reduces to a function of $|t|=\left|\dfrac{\sqrt n(\bar X-\mu_0)}{S}\right|$.",
      r"The test rejects for large $|t|$, i.e. it is the usual two-sided $t$-test.",
      r"For two-sided alternatives no uniformly most powerful test exists; within the class of unbiased tests, the $t$-test is uniformly most powerful.",
      r"Hence the LRT is the UMPU test - option (d)."]),

"2018-Q44": dict(ans=1, qtype="Numerical", diff="Medium",
 sc=r"Unrestricted MLE is $\hat\mu_i=X_i$; under $H_0$ all are $0$. The ratio gives $-2\log\lambda=\sum X_i^2$.",
 tips=[r"Each $\mu_i$ has its own observation, so the unrestricted maximiser is simply $\hat\mu_i=X_i$ and the maximised likelihood is $(2\pi)^{-n/2}$.",
       r"Wilks: $-2\log\lambda=\sum X_i^2\sim\chi^2_n$ under $H_0$, matching the $n$ restrictions imposed.",
       r"$\sum(X_i-\bar X)^2$ would be the statistic for $H_0:\mu_1=\cdots=\mu_n$ (a common unknown value), not for all being zero."],
 sol=[r"Under $H_1$ the parameter space is unrestricted, so $\hat\mu_i=X_i$ and $\sup L=(2\pi)^{-n/2}$.",
      r"Under $H_0$ all $\mu_i=0$, so $\sup_{H_0}L=(2\pi)^{-n/2}\exp\left(-\tfrac12\sum X_i^2\right)$.",
      r"$\lambda=\exp\left(-\tfrac12\sum X_i^2\right)$, so $-2\log\lambda=\sum X_i^2$ and the test rejects for large $\sum X_i^2$.",
      r"Option (b)."]),

"2018-Q45": dict(ans=1, qtype="Conceptual", diff="Easy",
 sc=r"Wilks' theorem: $-2\log\lambda_n\xrightarrow{d}\chi^2_r$, where $r$ is the number of independent restrictions imposed by $H_0$.",
 tips=[r"The degrees of freedom equal (dimension of full parameter space) $-$ (dimension under $H_0$).",
       r"'Asymptotically normal' is the stock distractor - the LRT statistic is asymptotically chi-square, not normal.",
       r"Regularity conditions matter: they fail when the support depends on $\theta$ or $H_0$ sits on the parameter-space boundary."],
 sol=[r"$\lambda_n=\dfrac{\sup_{\Theta_0}L}{\sup_{\Theta}L}\in[0,1]$, so $-2\log\lambda_n\ge0$.",
      r"Wilks' theorem states that under $H_0$ and regularity conditions, $-2\log\lambda_n$ converges in distribution to $\chi^2_r$.",
      r"Hence the asymptotic distribution is chi-square - option (b)."]),

"2018-Q46": dict(ans=0, qtype="Conceptual", diff="Medium",
 sc=r"Reject when $\lambda_m\ge A=\frac{1-\beta}{\alpha}$; ACCEPT when $\lambda_m\le B=\frac{\beta}{1-\alpha}$; CONTINUE in between. Pairs 2 and 3 are swapped.",
 tips=[r"SPRT decision triple: $\lambda_m\ge A$ reject, $\lambda_m\le B$ accept, $B<\lambda_m<A$ continue.",
       r"Mnemonic: a large likelihood ratio favours $H_1$ (reject), a small one favours $H_0$ (accept), and the middle band is the undecided zone.",
       r"The statements here deliberately interchange 'accept' and 'continue sampling' - read both columns before matching."],
 sol=[r"Wald's SPRT uses boundaries $A\approx\dfrac{1-\beta}{\alpha}$ and $B\approx\dfrac{\beta}{1-\alpha}$.",
      r"Statement 1: $\lambda_m\ge A\Rightarrow$ reject $H_0$. Correctly matched.",
      r"Statement 2: $B<\lambda_m<A$ is the CONTINUE-SAMPLING region, not acceptance. Wrongly matched.",
      r"Statement 3: $\lambda_m\le B\Rightarrow$ ACCEPT $H_0$, not continue sampling. Wrongly matched.",
      r"Only statement 1 is correct - option (a)."]),

"2018-Q47": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"Wald proved the SPRT terminates with probability one: $P[N<\infty]=1$.",
 tips=[r"The log-likelihood-ratio random walk has non-zero drift (unless $H_0$ and $H_1$ coincide), so it must eventually cross a boundary.",
       r"The stopping time $N$ is a random variable with finite moments; its expectation is the ASN.",
       r"This is why the SPRT is well defined as a procedure - the sample size is random but finite almost surely."],
 sol=[r"In an SPRT, $\log\lambda_m=\sum_{i=1}^m Z_i$ is a random walk with increments $Z_i=\log\dfrac{f_1(X_i)}{f_0(X_i)}$.",
      r"Since $E(Z_i)\ne0$ under either hypothesis, the walk drifts and crosses one of the two fixed boundaries with probability one.",
      r"Hence the stopping time satisfies $P[N<\infty]=1$ - option (c)."]),

"2018-Q48": dict(ans=1, qtype="Definition", diff="Easy",
 sc=r"Squared error loss $L=(\theta-a)^2$ makes the risk $E[(\theta-\delta)^2]$, which is the mean squared error.",
 tips=[r"Squared error loss $\to$ risk is MSE $\to$ Bayes rule is the posterior mean.",
       r"Absolute error loss $\to$ posterior median; 0-1 loss $\to$ posterior mode.",
       r"A cubic loss is not even non-negative, so it cannot be a loss function at all."],
 sol=[r"Risk is $R(\theta,\delta)=E_\theta\left[L(\theta,\delta(X))\right]$.",
      r"With $L(\theta,a)=(\theta-a)^2$, $R(\theta,\delta)=E_\theta\left[(\theta-\delta(X))^2\right]$, which is precisely the mean squared error of $\delta$.",
      r"Hence option (b)."]),

"2018-Q49": dict(ans=2, qtype="Numerical", diff="Hard",
 sc=r"Normal-normal conjugacy gives posterior $N\!\left(\frac{\sum x_i}{n+1},\frac1{n+1}\right)$; under squared-error loss the Bayes risk is the posterior variance $\frac1{n+1}$.",
 tips=[r"Precisions add: prior precision $1$, data precision $n$, posterior precision $n+1$, hence posterior variance $\frac1{n+1}$.",
       r"A risk is a number, not a statistic - options containing $x_i$ cannot be the Bayes risk. That screen alone leaves (b) and (c).",
       r"Bayes risk under squared error loss equals the expected posterior variance; here that is constant, so it is just $\frac1{n+1}$."],
 sol=[r"Likelihood: $X_i\mid\mu\sim N(\mu,1)$ iid; prior $\mu\sim N(0,1)$.",
      r"Posterior precision $=1+n$, so posterior variance $=\dfrac1{n+1}$, and posterior mean $=\dfrac{n\bar x}{n+1}=\dfrac{\sum x_i}{n+1}$.",
      r"Under squared error loss the Bayes estimator is the posterior mean, and the resulting posterior expected loss is the posterior variance $\dfrac1{n+1}$.",
      r"Since this does not depend on the data, the Bayes risk is also $\dfrac1{n+1}$ - option (c)."]),

"2018-Q50": dict(ans=0, qtype="Conceptual", diff="Easy",
 sc=r"Minimising $E[(\theta-a)^2\mid x]$ over $a$ gives $a=E(\theta\mid x)$, the posterior mean.",
 tips=[r"The three-line table: squared error $\to$ mean, absolute error $\to$ median, 0-1 loss $\to$ mode.",
       r"Proof: $E[(\theta-a)^2\mid x]=V(\theta\mid x)+\left[E(\theta\mid x)-a\right]^2$ is minimised when the bracket vanishes.",
       r"Posterior variance is the resulting minimum risk, never the estimator itself."],
 sol=[r"The Bayes estimator minimises the posterior expected loss $E\left[(\theta-a)^2\mid x\right]$.",
      r"Expanding: $E\left[(\theta-a)^2\mid x\right]=V(\theta\mid x)+\left[E(\theta\mid x)-a\right]^2$.",
      r"This is minimised by taking $a=E(\theta\mid x)$, the posterior mean.",
      r"Option (a)."]),

"2018-Q51": dict(ans=1, qtype="Conceptual", diff="Medium",
 sc=r"A uniformly best decision rule (best for every $\theta$) generally does NOT exist - that is why admissibility, minimaxity and Bayes criteria were invented.",
 tips=[r"If a uniformly best rule existed, the whole apparatus of decision theory would be unnecessary.",
       r"A unique Bayes rule is admissible; a Bayes rule with constant risk is minimax; the posterior median is Bayes under absolute error loss - all true.",
       r"Counterexample to (b): the constant rule $\delta\equiv\theta_0$ has zero risk at $\theta_0$, so no rule can beat every rule everywhere."],
 sol=[r"(a) A unique Bayes solution is admissible - standard theorem. Correct.",
      r"(b) A uniformly best decision rule would have risk no larger than every other rule at every $\theta$. The constant rule $\delta\equiv\theta_0$ has zero risk at $\theta=\theta_0$, so a uniformly best rule would have to have zero risk there too, for every $\theta_0$ - impossible. Hence this statement is NOT correct.",
      r"(c) Under $L=|\theta-a|$ the posterior median minimises posterior expected loss. Correct.",
      r"(d) A Bayes rule with constant risk is minimax - standard theorem. Correct.",
      r"The incorrect statement is option (b)."]),

"2018-Q52": dict(ans=3, qtype="Numerical", diff="Medium",
 sc=r"Put $\phi=\sqrt\theta$; then $\hat\phi=-n/\sum\log x_i$, and by invariance $\hat\theta=\hat\phi^2=n^2/\left(\sum\log x_i\right)^2$.",
 tips=[r"Reparametrise to the familiar $\theta x^{\theta-1}$ form first, then apply invariance - far faster than differentiating in $\theta$.",
       r"$\sum\log x_i<0$ since $0<x_i<1$, so $-n/\sum\log x_i>0$ as required; squaring removes the sign issue.",
       r"Options (a) and (b) are negative or wrongly scaled - a sign check disposes of them instantly."],
 sol=[r"Write $\phi=\sqrt\theta$, so $f(x)=\phi x^{\phi-1}$ on $(0,1)$.",
      r"$\log L=n\log\phi+(\phi-1)\sum\log x_i$, and $\dfrac{\partial\log L}{\partial\phi}=\dfrac n\phi+\sum\log x_i=0$ gives $\hat\phi=\dfrac{-n}{\sum\log x_i}$.",
      r"By the invariance property of the MLE, $\hat\theta=\hat\phi^{\,2}$.",
      r"$\hat\theta=\dfrac{n^2}{\left(\sum_{i=1}^n\log x_i\right)^2}$ - option (d)."]),

"2018-Q53": dict(ans=1, qtype="Numerical", diff="Hard",
 sc=r"$-\log X_i\sim\text{Exp}(\theta)$, so $Z\sim\text{Gamma}(n,\theta)$ and $E(1/Z)=\frac{\theta}{n-1}$; hence $(n-1)/Z$ is unbiased for $\theta$.",
 tips=[r"If $X$ has density $\theta x^{\theta-1}$ on $(0,1)$ then $Y=-\log X$ is exponential with rate $\theta$ - the key transformation.",
       r"For $Z\sim\text{Gamma}(n,\theta)$: $E(Z^{-1})=\dfrac{\theta}{n-1}$ (needs $n>1$), which is why the stem stipulates $n>2$.",
       r"$n/Z$ is the MLE of $\theta$ and is biased upwards by the factor $n/(n-1)$ - option (a) is the trap."],
 sol=[r"$P(-\log X_i>t)=P(X_i<e^{-t})=\left(e^{-t}\right)^\theta=e^{-\theta t}$, so $-\log X_i\sim\text{Exp}(\theta)$.",
      r"Hence $Z=-\sum\log X_i\sim\text{Gamma}(\text{shape }n,\text{ rate }\theta)$ with density $\dfrac{\theta^n}{\Gamma(n)}z^{n-1}e^{-\theta z}$.",
      r"$E\left(\dfrac1Z\right)=\displaystyle\int_0^\infty\frac1z\cdot\frac{\theta^n}{\Gamma(n)}z^{n-1}e^{-\theta z}dz=\frac{\theta^n}{\Gamma(n)}\cdot\frac{\Gamma(n-1)}{\theta^{n-1}}=\frac{\theta}{n-1}$.",
      r"Therefore $E\left(\dfrac{n-1}{Z}\right)=\theta$ - option (b)."]),

"2018-Q54": dict(ans=3, qtype="Conceptual", diff="Easy",
 sc=r"All three are equivalent descriptions of sufficiency: information-preservation, the factorisation form, and the $\theta$-free conditional distribution.",
 tips=[r"Statement 3 is the definition; statement 2 is the Fisher-Neyman criterion; statement 1 is the informal reading.",
       r"The factorisation theorem is the practical tool - use it to identify sufficient statistics without any conditional computation.",
       r"Note $h$ must be free of $\theta$ and $g$ must depend on the data only through $t$."],
 sol=[r"Statement 3 is the definition of sufficiency: $P(X=x\mid T=t)$ does not involve $\theta$. True.",
      r"Statement 2 is the Fisher-Neyman factorisation theorem, which is equivalent to the definition. True.",
      r"Statement 1 is the standard informal restatement: a sufficient statistic captures all sample information about $\theta$. True.",
      r"All three are correct - option (d)."]),

"2018-Q55": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"$L=\theta^{-n}\left(\prod x_i\right)\exp\left(-\frac{\sum x_i^2}{2\theta}\right)$, so $\sum x_i^2$ is complete sufficient.",
 tips=[r"Rayleigh is a one-parameter exponential family in $\sum X_i^2$, so that statistic is complete as well as sufficient.",
       r"$X_i^2/2\sim\text{Exp}(1/\theta)$, giving $E\left(\sum X_i^2\right)=2n\theta$ and hence UMVUE $\frac1{2n}\sum X_i^2$.",
       r"Any density with $e^{-x^2/(2\theta)}$ inside will have $\sum X_i^2$ as the sufficient statistic - pattern-match on the exponent."],
 sol=[r"$L(\theta)=\prod_{i=1}^n\dfrac{x_i}{\theta}e^{-x_i^2/(2\theta)}=\dfrac{1}{\theta^n}\exp\left(-\dfrac{\sum x_i^2}{2\theta}\right)\prod x_i$.",
      r"By factorisation with $g_\theta(t)=\theta^{-n}e^{-t/(2\theta)}$, $t=\sum x_i^2$, and $h(x)=\prod x_i$, the statistic $\sum X_i^2$ is sufficient.",
      r"The family is a one-parameter exponential family with natural sufficient statistic $\sum X_i^2$ and an open natural parameter space, so the statistic is complete.",
      r"Option (c)."]),

"2018-Q56": dict(ans=2, qtype="Numerical", diff="Hard",
 sc=r"Power $=P(X\le3\mid p=\tfrac14)=\frac{3^7}{4^{10}}\left[27+90+135+120\right]=372\cdot\frac{3^7}{4^{10}}$.",
 tips=[r"Factor out $3^7/4^{10}$ at the start: each term $\binom{10}{k}3^{10-k}/4^{10}$ contributes $\binom{10}{k}3^{3-k}$ after the pull-out.",
       r"The bracket is $\binom{10}{0}3^3+\binom{10}{1}3^2+\binom{10}{2}3^1+\binom{10}{3}3^0=27+90+135+120=372$.",
       r"Power is evaluated under $H_1$ ($p=1/4$), never under $H_0$ - that is the first decision to make."],
 sol=[r"Power $=P(X\le3\mid p=\tfrac14)=\sum_{k=0}^{3}\binom{10}{k}\left(\tfrac14\right)^k\left(\tfrac34\right)^{10-k}$.",
      r"$=\dfrac1{4^{10}}\sum_{k=0}^{3}\binom{10}{k}3^{10-k}=\dfrac{3^7}{4^{10}}\sum_{k=0}^{3}\binom{10}{k}3^{3-k}$.",
      r"$=\dfrac{3^7}{4^{10}}\left[1\cdot27+10\cdot9+45\cdot3+120\cdot1\right]=\dfrac{3^7}{4^{10}}\left[27+90+135+120\right]$.",
      r"$=372\left(\dfrac{3^7}{4^{10}}\right)$ - option (c)."]),

"2018-Q57": dict(ans=0, qtype="Conceptual", diff="Easy",
 sc=r"Compare a UMP test with the trivial randomised test $\phi\equiv\alpha$, whose power is $\alpha$ everywhere. Being uniformly most powerful, the UMP test has power $\ge\alpha$, i.e. it is unbiased.",
 tips=[r"Unbiased test: power $\ge$ size at every point of the alternative.",
       r"The trivial test $\phi\equiv\alpha$ is the universal comparison device for proving UMP $\Rightarrow$ unbiased.",
       r"The converse fails - an unbiased test need not be UMP."],
 sol=[r"A test is unbiased at level $\alpha$ if its power satisfies $\beta_\phi(\theta)\ge\alpha$ for all $\theta\in\Theta_1$.",
      r"Consider the randomised test $\phi_0(x)\equiv\alpha$, which has size $\alpha$ and power exactly $\alpha$ everywhere.",
      r"If $\phi$ is UMP of size $\alpha$ then its power is at least that of $\phi_0$ at every $\theta\in\Theta_1$, i.e. $\beta_\phi(\theta)\ge\alpha$.",
      r"Hence a UMP test is always unbiased - option (a)."]),

"2018-Q58": dict(ans=0, qtype="Conceptual", diff="Medium",
 sc=r"Sufficiency fails precisely because the conditional distribution of the sample given $T=X_1+2X_2$ still involves $\lambda$.",
 tips=[r"Unequal weights destroy sufficiency: $\sum X_i$ works for Poisson, $X_1+2X_2$ does not.",
       r"Check by definition: $P(X_1=1,X_2=1\mid T=3)$ competes with $P(X_1=3,X_2=0\mid T=3)$ and the ratio involves $\lambda$.",
       r"Whether $T$ is itself Poisson is irrelevant to sufficiency - options (b) and (c) are category errors."],
 sol=[r"$T=X_1+2X_2=3$ arises from $(X_1,X_2)=(1,1)$ or $(3,0)$.",
      r"$P(X_1=1,X_2=1)=\lambda e^{-\lambda}\cdot\lambda e^{-\lambda}=\lambda^2e^{-2\lambda}$ and $P(X_1=3,X_2=0)=\dfrac{\lambda^3e^{-\lambda}}{6}e^{-\lambda}=\dfrac{\lambda^3e^{-2\lambda}}{6}$.",
      r"Hence $P(X_1=1,X_2=1\mid T=3)=\dfrac{\lambda^2}{\lambda^2+\lambda^3/6}=\dfrac{1}{1+\lambda/6}$, which depends on $\lambda$.",
      r"By the definition of sufficiency, $T$ is therefore not sufficient - option (a)."]),

"2018-Q59": dict(ans=2, qtype="Conceptual", diff="Medium",
 sc=r"The pmf depends on $x$ only through $|x|$, and $|X|\sim\text{Bernoulli}(\theta)$, which is a complete family. So $|X|$ is complete sufficient.",
 tips=[r"Read the exponents: both $|x|$ and $1-|x|$ involve only the absolute value, so $|X|$ carries all the information.",
       r"$P(|X|=1)=\frac\theta2+\frac\theta2=\theta$ and $P(|X|=0)=1-\theta$ - a genuine Bernoulli.",
       r"$X$ itself is sufficient but not complete: $E(X)=0$ for all $\theta$ with $g(x)=x\not\equiv0$."],
 sol=[r"$f(x,\theta)=\left(\tfrac\theta2\right)^{|x|}(1-\theta)^{1-|x|}$ depends on the data only through $|x|$, so $|X|$ is sufficient by factorisation.",
      r"$P(|X|=1)=P(X=1)+P(X=-1)=\tfrac\theta2+\tfrac\theta2=\theta$, and $P(|X|=0)=1-\theta$; hence $|X|\sim\text{Bernoulli}(\theta)$.",
      r"The Bernoulli family is complete: $E_\theta[g(|X|)]=g(1)\theta+g(0)(1-\theta)=0$ for all $\theta\in(0,1)$ forces $g(0)=g(1)=0$.",
      r"So $|X|$ is complete and sufficient - option (c)."]),

"2018-Q60": dict(ans=2, qtype="Numerical", diff="Medium",
 sc=r"$I_n(\theta)=n$ for $N(\theta,1)$ and $\psi(\theta)=\theta^r$ gives $\psi'=r\theta^{r-1}$, so the bound is $\dfrac{r^2\theta^{2r-2}}{n}$.",
 tips=[r"CR bound for $\psi(\theta)$: $\dfrac{[\psi'(\theta)]^2}{I_n(\theta)}$ - square the derivative, never forget the square.",
       r"$I_1(\theta)=1$ for $N(\theta,1)$, so $I_n=n$.",
       r"Check $r=1$: the bound becomes $1/n$, the familiar $V(\bar X)$ - a good sanity test on any option."],
 sol=[r"For $N(\theta,1)$: $\log f=-\tfrac12\log(2\pi)-\tfrac12(x-\theta)^2$, so $\dfrac{\partial\log f}{\partial\theta}=(x-\theta)$ and $I_1(\theta)=E(X-\theta)^2=1$.",
      r"Hence $I_n(\theta)=n$.",
      r"With $\psi(\theta)=\theta^r$, $\psi'(\theta)=r\theta^{r-1}$.",
      r"CR bound $=\dfrac{[\psi'(\theta)]^2}{I_n(\theta)}=\dfrac{r^2\theta^{2r-2}}{n}$ - option (c)."]),

"2019-Q6": dict(ans=0, qtype="Conceptual", diff="Easy",
 sc=r"For a normal distribution mean $=$ median $=$ mode $=\mu$, and the most efficient estimator of $\mu$ is $\bar X$.",
 tips=[r"Symmetric unimodal distribution $\Rightarrow$ the three centres coincide, so estimating the mode reduces to estimating $\mu$.",
       r"$\bar X$ attains the Cramer-Rao bound $\sigma^2/n$; the sample median has variance $\pi\sigma^2/(2n)$, efficiency $2/\pi\approx0.637$.",
       r"The sample mode is not even well defined for continuous data - a distractor by construction."],
 sol=[r"For $N(\mu,\sigma^2)$ the density is symmetric and unimodal about $\mu$, so the mode equals $\mu$.",
      r"Estimating the mode is therefore the same problem as estimating $\mu$.",
      r"The sample mean $\bar X$ is unbiased with variance $\sigma^2/n$, attaining the Cramer-Rao lower bound, hence it is the most efficient estimator.",
      r"Option (a)."]),

"2019-Q7": dict(ans=1, qtype="Numerical", diff="Medium",
 sc=r"Only $\frac2{n(n+1)}\sum i\,x_i$ is a genuine weighted average: its weights $w_i=\frac{2i}{n(n+1)}$ sum to $1$, and $\sum w_i^2=\frac{2(2n+1)}{3n(n+1)}\to0$.",
 tips=[r"Any consistent estimator of a mean must be a weighted average with $\sum w_i=1$ and $\sum w_i^2\to0$.",
       r"$\sum_{i=1}^n i=\frac{n(n+1)}2$, which is exactly what makes these weights add to one.",
       r"Options carrying a free index $i$ outside the summation are not statistics at all - discard them on sight."],
 sol=[r"Consider $T=\dfrac{2}{n(n+1)}\sum_{i=1}^n i\,x_i=\sum_i w_ix_i$ with $w_i=\dfrac{2i}{n(n+1)}$.",
      r"$\sum_i w_i=\dfrac{2}{n(n+1)}\cdot\dfrac{n(n+1)}2=1$, so $E(T)=\mu$: unbiased.",
      r"$V(T)=\sigma^2\sum w_i^2=\sigma^2\dfrac{4}{n^2(n+1)^2}\cdot\dfrac{n(n+1)(2n+1)}6=\dfrac{2(2n+1)\sigma^2}{3n(n+1)}\to0$.",
      r"By Chebyshev, $T$ is consistent for $\mu$. The other options are not properly normalised weighted averages.",
      r"Option (b)."]),

"2019-Q8": dict(ans=0, qtype="Conceptual", diff="Easy",
 sc=r"$\bar X$ and $S^2$ (divisor $n-1$) are both unbiased and consistent; $S_1^2$ (divisor $n$) is consistent but BIASED.",
 tips=[r"$E(S_1^2)=\frac{n-1}{n}\sigma^2$ - biased downwards, though the bias vanishes as $n\to\infty$.",
       r"Consistency is shared by all three; it is unbiasedness that separates them.",
       r"Reading the divisor is the whole question: $n-1$ unbiased, $n$ biased."],
 sol=[r"Statement 1: $E(\bar X)=\mu$ and $V(\bar X)=\sigma^2/n\to0$, so $\bar X$ is unbiased and consistent. True.",
      r"Statement 2: $E(S^2)=\sigma^2$ and $V(S^2)=\dfrac{2\sigma^4}{n-1}\to0$, so $S^2$ is unbiased and consistent. True.",
      r"Statement 3: $E(S_1^2)=\dfrac{n-1}{n}\sigma^2\ne\sigma^2$, so $S_1^2$ is biased (though still consistent). False.",
      r"Hence 1 and 2 only - option (a)."]),

"2019-Q9": dict(ans=2, qtype="Numerical", diff="Hard",
 sc=r"$\partial/\partial\alpha$ gives $\hat\alpha=\bar x$; substituting into $\partial/\partial\lambda=0$ and using $\psi(\lambda)\approx\log\lambda-\frac1{2\lambda}$ gives $\frac1{2\lambda}=\log(\bar x/G)$, i.e. $\hat\lambda=\frac1{2\log(\bar x/G)}$.",
 tips=[r"This is the gamma density re-parametrised so that $E(X)=\alpha$; hence the MLE of $\alpha$ is the arithmetic mean.",
       r"$\sum\log x_i=n\log G$ where $G$ is the geometric mean - this is what makes $\log(\bar x/G)$ appear.",
       r"By the AM-GM inequality $\bar x\ge G$, so $\log(\bar x/G)\ge0$ and $\hat\lambda>0$: options with $\log(G/\bar x)$ give a negative $\lambda$ and are impossible."],
 sol=[r"$\log L=-n\log\Gamma(\lambda)+n\lambda\log\lambda-n\lambda\log\alpha-\dfrac\lambda\alpha\sum x_i+(\lambda-1)\sum\log x_i$.",
      r"$\dfrac{\partial\log L}{\partial\alpha}=-\dfrac{n\lambda}{\alpha}+\dfrac{\lambda}{\alpha^2}\sum x_i=0\Rightarrow\hat\alpha=\bar x$.",
      r"$\dfrac{\partial\log L}{\partial\lambda}=-n\psi(\lambda)+n\log\lambda+n-\dfrac{\sum x_i}{\alpha}+\sum\log x_i$. At $\alpha=\bar x$ the terms $n$ and $\sum x_i/\bar x=n$ cancel.",
      r"Using $\psi(\lambda)=\log\lambda-\dfrac1{2\lambda}$ and $\sum\log x_i=n\log G$: $\dfrac{n}{2\lambda}-n\log\bar x+n\log G=0$, so $\dfrac1{2\lambda}=\log\dfrac{\bar x}{G}$.",
      r"Hence $\hat\lambda=\dfrac1{2\log(\bar x/G)}$ and the pair is $\left(\bar x,\ \dfrac1{2\log(\bar x/G)}\right)$ - option (c)."]),

"2019-Q10": dict(ans=3, qtype="Conceptual", diff="Hard",
 sc=r"For $U(\theta,\theta+1)$ the minimal sufficient statistic is the PAIR $(X_{(1)},X_{(n)})$, not $X_{(1)}$ alone. So (d) is the incorrect statement.",
 tips=[r"Both endpoints constrain $\theta$: $X_{(n)}-1\le\theta\le X_{(1)}$, so both order statistics are needed.",
       r"The MLE is any point of that interval - non-unique, which makes (c) correct.",
       r"$(X_{(1)},X_{(n)})$ is minimal sufficient but NOT complete (the range is ancillary), which is why no UMVUE exists and (b) is correct."],
 sol=[r"The likelihood is $L(\theta)=1$ if $\theta\le X_{(1)}$ and $X_{(n)}\le\theta+1$, i.e. $X_{(n)}-1\le\theta\le X_{(1)}$, and $0$ otherwise.",
      r"(a) $E(X)=\theta+\tfrac12$, so $\bar X-\tfrac12$ is unbiased for $\theta$. Correct.",
      r"(c) The likelihood is flat on $[X_{(n)}-1,X_{(1)}]$, so every point there is an MLE. Correct.",
      r"(b) $(X_{(1)},X_{(n)})$ is minimal sufficient but not complete (the range $X_{(n)}-X_{(1)}$ is ancillary), so Lehmann-Scheffe does not apply and no UMVUE exists. Correct.",
      r"(d) $X_{(1)}$ alone is NOT sufficient - the upper constraint $\theta\ge X_{(n)}-1$ is lost. This is the incorrect statement, option (d)."]),

"2019-Q11": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"With $\mu$ known, $N(\mu,\theta)$ is a one-parameter exponential family with natural sufficient statistic $\sum(x_i-\mu)^2$, which is therefore complete as well as sufficient.",
 tips=[r"One-parameter exponential family with an open natural parameter space $\Rightarrow$ the natural statistic is complete.",
       r"$T/\theta\sim\chi^2_n$, so $T/n$ is the UMVUE of $\theta$ with variance $2\theta^2/n$, attaining the CR bound.",
       r"If $\mu$ were unknown, the sufficient statistic would be the pair $(\sum x_i,\sum x_i^2)$."],
 sol=[r"$L(\theta)=(2\pi\theta)^{-n/2}\exp\left(-\dfrac{\sum(x_i-\mu)^2}{2\theta}\right)$, so by factorisation $T=\sum(x_i-\mu)^2$ is sufficient.",
      r"Writing $L$ in exponential-family form $\exp\left\{-\dfrac{1}{2\theta}T\right\}\cdot c(\theta)$, the natural parameter $-\frac1{2\theta}$ ranges over an open interval.",
      r"Hence $T$ is complete as well as sufficient.",
      r"Option (c)."]),

"2019-Q12": dict(ans=1, qtype="Numerical", diff="Easy",
 sc=r"$s^2=6300/63=100$ so $s=10$; SE $=10/\sqrt{64}=1.25$; CI $=60.4\pm1.96(1.25)=(57.95,62.85)$.",
 tips=[r"Divide by $n-1=63$, not $64$: $6300/63=100$ is a designed-to-be-clean number.",
       r"$n=64$ is large, so the normal multiplier $1.96$ is used rather than $t_{63}$ (they agree to two decimals anyway).",
       r"Half-width $=1.96\times1.25=2.45$ - check that the printed interval is symmetric about $60.4$."],
 sol=[r"$s^2=\dfrac{\sum(x_i-\bar x)^2}{n-1}=\dfrac{6300}{63}=100$, so $s=10$.",
      r"$\text{SE}(\bar x)=\dfrac{s}{\sqrt n}=\dfrac{10}{8}=1.25$.",
      r"$95\%$ CI $=\bar x\pm1.96\,\text{SE}=60.4\pm1.96(1.25)=60.4\pm2.45$.",
      r"$=(57.95,\ 62.85)$ - option (b)."]),
}
