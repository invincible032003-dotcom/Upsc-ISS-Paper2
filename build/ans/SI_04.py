# -*- coding: utf-8 -*-
# Statistical Inference answer pack 4 (2019 Q48-Q75, 2020 Q1-Q10)
A = {
"2019-Q48": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"Basu's theorem: a complete sufficient statistic is independent of every ancillary statistic.",
 tips=[r"Classic application: in $N(\mu,\sigma^2)$ with $\sigma^2$ known, $\bar X$ (complete sufficient) is independent of $S^2$ (ancillary for $\mu$).",
       r"Independence here is distributional, not functional - option (b) is a category error.",
       r"Basu is the fastest route to many independence results that would otherwise need messy Jacobians."],
 sol=[r"$T(X)$ ancillary means its distribution is free of $\theta$.",
      r"$S(X)$ complete sufficient means the conditional distribution given $S$ is free of $\theta$ and $E_\theta[g(S)]=0\ \forall\theta\Rightarrow g\equiv0$.",
      r"Basu's theorem asserts that under these conditions $S(X)$ and $T(X)$ are independent.",
      r"Option (c)."]),

"2019-Q49": dict(ans=2, qtype="Definition", diff="Easy",
 sc=r"Ancillary means the DISTRIBUTION of $T(x)$ does not involve $\theta$ - not that the statistic itself is 'independent of $\theta$'.",
 tips=[r"Every statistic is a function of the data only, so 'independent of $\theta$' is vacuous - the definition must be about the distribution.",
       r"Standard examples: the range in $U(\theta-\tfrac12,\theta+\tfrac12)$; $X_1-X_2$ in $N(\theta,1)$; $S^2$ for a location family.",
       r"An ancillary statistic carries no information about $\theta$ on its own, but can be informative about precision (conditional inference)."],
 sol=[r"By definition, $T(X)$ is ancillary for $\theta$ if the sampling distribution of $T(X)$ is the same for every value of $\theta$.",
      r"Options (a) and (b) confuse the statistic with its distribution; option (d) is the negation of ancillarity.",
      r"Hence option (c)."]),

"2019-Q50": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"$\rho(T^*,T)=\sqrt{e}$, the square root of the efficiency.",
 tips=[r"$\operatorname{Cov}(T^*,T)=V(T^*)$ for the minimum-variance $T^*$, which yields $\rho=\sqrt{V(T^*)/V(T)}=\sqrt e$.",
       r"Since $0\le e\le1$, $\sqrt e\ge e$ - the correlation is always at least the efficiency.",
       r"Repeated almost verbatim in 2018 Q20, 2022 Q33, 2023 Q7 and 2024 Q3."],
 sol=[r"Efficiency of $T$ is $e=\dfrac{V(T^*)}{V(T)}$.",
      r"Minimum-variance property forces $\operatorname{Cov}(T^*,T)=V(T^*)$ (otherwise a linear combination would have smaller variance than $T^*$).",
      r"$\rho=\dfrac{\operatorname{Cov}(T^*,T)}{\sqrt{V(T^*)V(T)}}=\sqrt{\dfrac{V(T^*)}{V(T)}}=\sqrt e=e^{1/2}$.",
      r"Option (c)."]),

"2019-Q61": dict(ans=0, qtype="Definition", diff="Easy",
 sc=r"Power exceeding size at every alternative is exactly the definition of an unbiased test.",
 tips=[r"Read the probabilities: LHS is power, RHS is size. Power $>$ size $\Rightarrow$ unbiased.",
       r"A biased test can have power BELOW its size at some alternatives - a genuinely perverse but possible situation.",
       r"UMP $\Rightarrow$ unbiased, but unbiased $\not\Rightarrow$ UMP."],
 sol=[r"$P(\text{reject }H_0\mid H_0\text{ true})$ is the size $\alpha$; $P(\text{reject }H_0\mid H_0\text{ false})$ is the power.",
      r"The stated inequality says power $>$ size for the alternative considered.",
      r"A test whose power is at least its size throughout the alternative is called unbiased.",
      r"Option (a)."]),

"2019-Q62": dict(ans=2, qtype="Definition", diff="Easy",
 sc=r"A test function taking a value strictly between $0$ and $1$ is by definition randomised.",
 tips=[r"Non-randomised $\iff$ $\phi$ takes only the values $0$ and $1$ (it is an indicator of a set).",
       r"Randomisation exists to achieve an EXACT size $\alpha$ in discrete problems.",
       r"$\gamma$ is chosen so that $E_{H_0}[\phi(X)]=\alpha$ exactly."],
 sol=[r"A test function is $\phi:\mathcal X\to[0,1]$, where $\phi(x)$ is the probability of rejecting $H_0$ when $x$ is observed.",
      r"It is non-randomised precisely when $\phi$ takes only the two values $0$ and $1$.",
      r"Here $\phi(x)=\gamma$ with $\gamma>0$ on the boundary set $\{f_1=kf_0\}$, an intermediate value, so a genuine coin-toss is used.",
      r"Hence $\phi$ is a randomised test function - option (c)."]),

"2019-Q63": dict(ans=0, qtype="Conceptual", diff="Medium",
 sc=r"Reject when $\bar X$ lies outside $\theta_0\pm Z_{\alpha/2}/\sqrt n$ - equal tails of $\alpha/2$ each, and the standard error is $1/\sqrt n$.",
 tips=[r"$\bar X\sim N(\theta,1/n)$, so the standard error is $1/\sqrt n$ - options dividing by $n$ are dimensionally wrong.",
       r"Two-sided alternative $\Rightarrow$ split $\alpha$ equally, so the multiplier is $Z_{\alpha/2}$, not $Z_{1-\alpha}$.",
       r"Symmetry of the normal makes the equal-tailed test UMPU here."],
 sol=[r"Under $H_0$, $\bar X\sim N\left(\theta_0,\dfrac1n\right)$, so $\sqrt n(\bar X-\theta_0)\sim N(0,1)$.",
      r"No UMP test exists two-sided; within unbiased tests the optimum is the symmetric two-tailed test with $\alpha/2$ in each tail.",
      r"Rejection region: $\left|\sqrt n(\bar X-\theta_0)\right|>Z_{\alpha/2}$, i.e. $\bar X<\theta_0-\dfrac{Z_{\alpha/2}}{\sqrt n}$ or $\bar X>\theta_0+\dfrac{Z_{\alpha/2}}{\sqrt n}$.",
      r"Option (a)."]),

"2019-Q64": dict(ans=2, qtype="Numerical", diff="Hard",
 sc=r"$Z=x\log\frac{\theta_1}{\theta_0}+(1-x)\log\frac{1-\theta_1}{1-\theta_0}$; taking $E(X)=\theta$ and collecting terms gives $\theta\log\frac{\theta_1(1-\theta_0)}{\theta_0(1-\theta_1)}+\log\frac{1-\theta_1}{1-\theta_0}$.",
 tips=[r"Write $Z$ as a linear function of $x$ first: $Z=ax+b$ with $a=\log\frac{\theta_1(1-\theta_0)}{\theta_0(1-\theta_1)}$ and $b=\log\frac{1-\theta_1}{1-\theta_0}$.",
       r"Then $E(Z)=a\theta+b$ - the constant term $b$ carries NO factor $\theta$, which is what distinguishes (c) from (d).",
       r"$E(Z)$ is the drift of the SPRT random walk and appears in the denominator of the ASN formula."],
 sol=[r"$\log f(x,\theta)=x\log\theta+(1-x)\log(1-\theta)$, so $Z=x\log\dfrac{\theta_1}{\theta_0}+(1-x)\log\dfrac{1-\theta_1}{1-\theta_0}$.",
      r"Rearranging: $Z=x\left[\log\dfrac{\theta_1}{\theta_0}-\log\dfrac{1-\theta_1}{1-\theta_0}\right]+\log\dfrac{1-\theta_1}{1-\theta_0}=x\log\dfrac{\theta_1(1-\theta_0)}{\theta_0(1-\theta_1)}+\log\dfrac{1-\theta_1}{1-\theta_0}$.",
      r"Taking expectations with $E(X)=\theta$:",
      r"$E(Z)=\theta\log\dfrac{\theta_1(1-\theta_0)}{\theta_0(1-\theta_1)}+\log\dfrac{1-\theta_1}{1-\theta_0}$ - option (c)."]),

"2019-Q65": dict(ans=1, qtype="Numerical", diff="Easy",
 sc=r"Two independent samples with a common variance: df $=n_1+n_2-2=15+15-2=28$.",
 tips=[r"Independent two-sample $t$: df $=n_1+n_2-2$ (one df lost per estimated mean).",
       r"Paired $t$: df $=n-1$ where $n$ is the number of PAIRS - do not confuse the two designs.",
       r"$30$ is the total sample size and $29$ is $N-1$; both are planted distractors."],
 sol=[r"With two independent samples from normal populations with a common unknown variance, the pooled estimator is $S_p^2=\dfrac{(n_1-1)S_1^2+(n_2-1)S_2^2}{n_1+n_2-2}$.",
      r"The test statistic $t=\dfrac{\bar X_1-\bar X_2}{S_p\sqrt{1/n_1+1/n_2}}$ has $n_1+n_2-2$ degrees of freedom.",
      r"Here $15+15-2=28$ - option (b)."]),

"2019-Q66": dict(ans=2, qtype="Numerical", diff="Easy",
 sc=r"$E(\bar X^2)=\mu^2+\frac1n$ since $V(\bar X)=1/n$; hence $\bar X^2-\frac1n$ is unbiased.",
 tips=[r"$E(T^2)=V(T)+[E(T)]^2$ - the identity behind every 'unbiased estimator of a square' question.",
       r"Population variance is $1$, so $V(\bar X)=1/n$ and the correction is $1/n$.",
       r"$\bar X^2$ always over-estimates $\mu^2$; the sign of the correction is therefore negative."],
 sol=[r"$E(\bar X)=\mu$ and $V(\bar X)=\dfrac{\sigma^2}{n}=\dfrac1n$ since the population variance is $1$.",
      r"$E(\bar X^2)=V(\bar X)+\left[E(\bar X)\right]^2=\dfrac1n+\mu^2$.",
      r"Hence $E\left(\bar X^2-\dfrac1n\right)=\mu^2$.",
      r"Option (c)."]),

"2019-Q67": dict(ans=0, qtype="Conceptual", diff="Easy",
 sc=r"$T=\sum x_i$ is complete sufficient (statement 1 true), but the MLE of $\lambda$ is $\bar x=T/n$, not $T$ (statement 2 false).",
 tips=[r"Poisson is a one-parameter exponential family, so the natural statistic $\sum X_i$ is complete and sufficient.",
       r"$E(T)=n\lambda$, so $T$ cannot be the MLE (or any sensible estimator) of $\lambda$ itself.",
       r"The MLE is always a function of the sufficient statistic - here the function is division by $n$."],
 sol=[r"$L(\lambda)=e^{-n\lambda}\lambda^{\sum x_i}/\prod x_i!$, so by factorisation $T=\sum x_i$ is sufficient; being the natural statistic of a full-rank exponential family, it is complete. Statement 1 true.",
      r"$\log L=-n\lambda+T\log\lambda-\log\prod x_i!$, and $\dfrac{\partial\log L}{\partial\lambda}=-n+\dfrac T\lambda=0$ gives $\hat\lambda=\dfrac Tn=\bar x$.",
      r"So the MLE is $\bar x$, not $T$. Statement 2 false.",
      r"Hence 1 only - option (a)."]),

"2019-Q68": dict(ans=3, qtype="Conceptual", diff="Easy",
 sc=r"Every convex combination $aX_1+(1-a)X_2$ is unbiased for $\theta$; since $a$ ranges over the reals, there are infinitely many.",
 tips=[r"Unbiased estimators are almost never unique - any convex combination of two unbiased estimators is unbiased.",
       r"Uniqueness is a property of the UMVUE (when it exists), not of unbiasedness in general.",
       r"Here $\bar X$ ($a=1/2$) is the best of the family, with variance $1/2$ versus $a^2+(1-a)^2$ in general."],
 sol=[r"For any real $a$, $T_a=aX_1+(1-a)X_2$ has $E(T_a)=a\theta+(1-a)\theta=\theta$.",
      r"So $T_a$ is unbiased for every $a\in\mathbb{R}$, giving an uncountable family.",
      r"Hence the number of unbiased estimators is infinite - option (d)."]),

"2019-Q69": dict(ans=3, qtype="Conceptual", diff="Medium",
 sc=r"For the Cauchy location family the score is not proportional to any $T-\theta$, so no unbiased estimator attains the Cramer-Rao bound: the MVB estimator does not exist.",
 tips=[r"Cauchy is regular ($I_1(\theta)=\tfrac12$), so the CR bound EXISTS - what fails is its attainment.",
       r"$\bar X$ is useless here: it has the same Cauchy distribution as a single observation, so it is neither consistent nor sufficient.",
       r"No non-trivial sufficient statistic exists for the Cauchy location family - the order statistic is minimal sufficient."],
 sol=[r"$\log f=-\log\pi-\log\left[1+(x-\theta)^2\right]$, so $\dfrac{\partial\log L}{\partial\theta}=\sum\dfrac{2(x_i-\theta)}{1+(x_i-\theta)^2}$.",
      r"This score cannot be written in the MVB form $A(\theta)\left[T(x)-\theta\right]$ - it is a non-linear function of the observations that does not reduce to any single statistic.",
      r"Hence no unbiased estimator attains the Cramer-Rao lower bound.",
      r"The minimum variance bound estimator does not exist - option (d)."]),

"2019-Q70": dict(ans=3, qtype="Conceptual", diff="Hard",
 sc=r"Any unbiased estimator based on $Y=\sum X_i$ is a polynomial of degree $\le n$ in $p$, but $1/p$ is unbounded as $p\to0$ - no such estimator exists.",
 tips=[r"$E[g(Y)]=\sum_{y=0}^{n}g(y)\binom ny p^y(1-p)^{n-y}$ is a POLYNOMIAL in $p$; it can never equal $1/p$.",
       r"A parametric function is estimable only if some unbiased estimator exists - here none does.",
       r"Compare $p^2$ or $p(1-p)$, which ARE polynomials and hence do have UMVUEs."],
 sol=[r"$Y=\sum X_i\sim B(n,p)$ is complete sufficient, so any unbiased estimator must be a function $g(Y)$.",
      r"$E[g(Y)]=\sum_{y=0}^{n}g(y)\binom ny p^y(1-p)^{n-y}$, which is a polynomial in $p$ of degree at most $n$.",
      r"$1/p$ is not a polynomial - indeed $1/p\to\infty$ as $p\to0^+$ while any polynomial stays bounded on $[0,1]$.",
      r"So no unbiased estimator of $1/p$ exists, hence no UMVUE - option (d)."]),

"2019-Q71": dict(ans=1, qtype="Conceptual", diff="Easy",
 sc=r"$L=(\beta-\alpha)^{-n}I\left(\alpha\le X_{(1)}\right)I\left(X_{(n)}\le\beta\right)$ - both extreme order statistics are needed.",
 tips=[r"Note the ORDER statistics $x_{(1)},x_{(n)}$, not the first and last observations $x_1,x_n$ - option (a) is a deliberate typographic trap.",
       r"Two unknown endpoints $\Rightarrow$ two-dimensional sufficient statistic.",
       r"$(X_{(1)},X_{(n)})$ is also the pair of MLEs of $(\alpha,\beta)$."],
 sol=[r"$L(\alpha,\beta)=\prod\dfrac{1}{\beta-\alpha}$ provided every $x_i\in[\alpha,\beta]$, i.e. $\alpha\le X_{(1)}$ and $X_{(n)}\le\beta$.",
      r"So $L=\dfrac{1}{(\beta-\alpha)^n}\,I\left(\alpha\le X_{(1)}\right)I\left(X_{(n)}\le\beta\right)$, which depends on the data only through $\left(X_{(1)},X_{(n)}\right)$.",
      r"By the factorisation theorem this pair is jointly sufficient.",
      r"Option (b)."]),

"2019-Q72": dict(ans=0, qtype="Conceptual", diff="Medium",
 sc=r"$X_1+X_2$ is sufficient (the Poisson likelihood depends on the data only through the total). The other three statements are false.",
 tips=[r"$E(X_1+X_2)=2\lambda$, so the total is NOT unbiased for $\lambda$ - option (c) fails.",
       r"$(X_1+X_2)/2=\bar X$ IS the UMVUE, so option (d) is false as written.",
       r"Unequal weights ($X_1+2X_2$) destroy sufficiency - see the companion 2018 item."],
 sol=[r"$L=\dfrac{e^{-2\lambda}\lambda^{x_1+x_2}}{x_1!x_2!}$, which depends on the data only through $t=x_1+x_2$; by factorisation $X_1+X_2$ is sufficient. (a) is correct.",
      r"(b) $X_1+2X_2$ is not sufficient - the conditional distribution given it still involves $\lambda$.",
      r"(c) $E(X_1+X_2)=2\lambda\ne\lambda$, so it is not unbiased for $\lambda$.",
      r"(d) $(X_1+X_2)/2=\bar X$ is an unbiased function of the complete sufficient statistic, hence it IS the UMVUE; the statement denies this and is false.",
      r"Option (a)."]),

"2019-Q73": dict(ans=0, qtype="Conceptual", diff="Medium",
 sc=r"When a UMP test of size $\alpha$ exists it is unique (almost everywhere) - any other UMP test agrees with it except on a null set.",
 tips=[r"Uniqueness is 'essentially unique': two UMP tests can differ only where $f_1=kf_0$, a set of probability zero in the continuous case.",
       r"The NP lemma's uniqueness half is the source of this result.",
       r"Option (b) adds a true but irrelevant clause (power $>$ size is just unbiasedness), so it is not the cleanest answer."],
 sol=[r"The Neyman-Pearson lemma has a uniqueness part: any most powerful test of size $\alpha$ must be of the likelihood-ratio form almost everywhere.",
      r"Hence two UMP tests of the same size can differ only on the boundary set $\{f_1=kf_0\}$, which has probability zero in the continuous case.",
      r"So the UMP test, when it exists, is (essentially) unique - option (a)."]),

"2019-Q74": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"Under squared error loss the Bayes estimator is the posterior mean, $E(\theta\mid x)$.",
 tips=[r"Squared error $\to$ posterior mean; absolute error $\to$ posterior median; 0-1 loss $\to$ posterior mode.",
       r"The prior mean is the Bayes estimator only when there are no data - it ignores the likelihood.",
       r"Posterior mean minimises $E\left[(\theta-a)^2\mid x\right]$ by the variance-decomposition identity."],
 sol=[r"The Bayes rule minimises the posterior expected loss $E\left[(\theta-a)^2\mid x\right]$.",
      r"$E\left[(\theta-a)^2\mid x\right]=V(\theta\mid x)+\left[E(\theta\mid x)-a\right]^2$, minimised at $a=E(\theta\mid x)$.",
      r"So the Bayes estimator is the mean of the posterior distribution $\pi(\theta\mid x)$ - option (c)."]),

"2019-Q75": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"$I_1(\lambda)=1/\lambda$, so the bound is $\lambda/n$, and $\bar x$ has exactly that variance - both statements hold.",
 tips=[r"Poisson: $I_1(\lambda)=\dfrac{1}{\lambda}$, so $I_n=\dfrac n\lambda$ and the bound is $\dfrac\lambda n$.",
       r"$V(\bar X)=\lambda/n$, equal to the bound, so $\bar X$ is an MVB estimator - the cleanest example in the syllabus.",
       r"The score $\dfrac{\partial\log L}{\partial\lambda}=\dfrac n\lambda(\bar x-\lambda)$ is visibly of the MVB attainment form."],
 sol=[r"$\log f=-\lambda+x\log\lambda-\log x!$, so $\dfrac{\partial\log f}{\partial\lambda}=\dfrac x\lambda-1$ and $I_1(\lambda)=\dfrac{V(X)}{\lambda^2}=\dfrac1\lambda$.",
      r"$I_n(\lambda)=n/\lambda$, so the Cramer-Rao bound for unbiased estimators of $\lambda$ is $\lambda/n$. Statement 1 true.",
      r"$V(\bar X)=\dfrac{\lambda}{n}$, which equals the bound, so $\bar X$ is a minimum variance bound unbiased estimator. Statement 2 true.",
      r"Option (c)."]),

"2020-Q1": dict(ans=0, qtype="Numerical", diff="Hard",
 sc=r"Size $=0.01+0.04=0.05$; the likelihood ratios are $2,2,0.8,1.11$ at $x=0,1,2,3$, so $\{0,1\}$ collects the two largest ratios - it is the NP most powerful test at level $0.05$.",
 tips=[r"Always rank outcomes by $p_1(x)/p_0(x)$ and fill the critical region from the top until the size budget is used.",
       r"Here the ratios are $0.02/0.01=2$, $0.08/0.04=2$, $0.40/0.50=0.8$, $0.50/0.45=1.11$.",
       r"Power $=0.02+0.08=0.10>0.05=$ size, so the test is unbiased - options (b), (c), (d) all fail on the arithmetic."],
 sol=[r"Size $=p_{\theta_0}(0)+p_{\theta_0}(1)=0.01+0.04=0.05$, so option (d) is wrong.",
      r"Power $=p_{\theta_1}(0)+p_{\theta_1}(1)=0.02+0.08=0.10$, so option (c) is wrong; and power $>$ size means the test is unbiased, killing option (b).",
      r"Likelihood ratios: $x=0:\ 2$; $x=1:\ 2$; $x=2:\ 0.8$; $x=3:\ 1.11$. The two largest ratios occur at $x=0$ and $x=1$.",
      r"The Neyman-Pearson region 'reject when $L_1/L_0>k$' with $1.11<k<2$ is exactly $\{0,1\}$, and it exhausts a size of $0.05$.",
      r"Hence $\varphi$ is a most powerful test at level $0.05$ - option (a)."]),

"2020-Q2": dict(ans=2, qtype="Numerical", diff="Medium",
 sc=r"Size $=\frac{1.5-1}{1.5}=\frac13$; power $=\frac{1.5-1}{2}=0.25<\frac13$, so the test is biased.",
 tips=[r"For $U(0,\theta)$, $P(a\le X\le b)=\frac{b-a}{\theta}$ whenever $[a,b]\subseteq[0,\theta]$ - length over $\theta$.",
       r"Same interval, bigger $\theta$ $\Rightarrow$ SMALLER probability, so power falls below size: a textbook biased test.",
       r"Unbiasedness requires power $\ge$ size; here $0.25<0.333$, so the test is biased."],
 sol=[r"Under $H_0:\theta=1.5$, $X\sim U(0,1.5)$, so size $=P(1\le X\le1.5)=\dfrac{1.5-1}{1.5}=\dfrac{0.5}{1.5}=\dfrac13$.",
      r"Under $H_1:\theta=2$, $X\sim U(0,2)$, so power $=P(1\le X\le1.5)=\dfrac{0.5}{2}=0.25$.",
      r"Power $0.25<\dfrac13=$ size, so the test is biased.",
      r"Hence size $\tfrac13$ and biased - option (c)."]),

"2020-Q3": dict(ans=2, qtype="Numerical", diff="Hard",
 sc=r"Size $=0.01+0.01+0.25(0.01)=0.0225$; power $=0.05+0.04+0.25(0.03)=0.0975$. The MP test of size exactly $0.03$ takes $\{1,2,3\}$ fully and has power $0.12$, so $\varphi$ is not MP at level $0.03$.",
 tips=[r"Size and power of a randomised test: $E_{H_0}[\varphi]$ and $E_{H_1}[\varphi]$ - multiply each probability by $\varphi(x)$ and add.",
       r"Ratios are $5,4,3,2,1,0.894$ at $x=1,\dots,6$; spending the full budget of $0.03$ means taking $x=1,2,3$ outright, giving power $0.12>0.0975$.",
       r"$\varphi$ IS the MP test of its own size $0.0225$, which is why option (d) is false."],
 sol=[r"Size $=1(0.01)+1(0.01)+0.25(0.01)=0.0225$.",
      r"Power $=1(0.05)+1(0.04)+0.25(0.03)=0.09+0.0075=0.0975$.",
      r"Likelihood ratios $p_1/p_0$: $5,4,3,2,1,0.894$ for $x=1,\dots,6$. A size-$0.03$ NP test rejects outright on $x=1,2,3$ (total null probability $0.03$) with power $0.05+0.04+0.03=0.12$.",
      r"Since $0.0975<0.12$, $\varphi$ is not most powerful at level $0.03$ (it does not spend the full size budget).",
      r"Hence option (c)."]),

"2020-Q4": dict(ans=3, qtype="Conceptual", diff="Medium",
 sc=r"Under regularity conditions the likelihood ratio test is consistent - the only defensible statement of the four.",
 tips=[r"Type-I error IS controlled by choosing $\lambda_0$, so (a) is false as written.",
       r"The LRT is not guaranteed unbiased, and its critical region need not be similar when $H_0$ is composite.",
       r"Consistency: power $\to1$ as $n\to\infty$ for any fixed alternative - a standard asymptotic property of the LRT."],
 sol=[r"(a) The cut-off $\lambda_0$ is chosen precisely so that $P(\lambda\le\lambda_0\mid H_0)=\alpha$, so Type-I error IS controlled. False.",
      r"(b) The LRT is not guaranteed to be unbiased in general. False.",
      r"(c) A similar critical region requires constant size over all of $\Theta_0$; the LR region need not have this property. False.",
      r"(d) Under standard regularity conditions the LRT is consistent: its power tends to $1$ at every fixed alternative. True.",
      r"Option (d)."]),

"2020-Q5": dict(ans=3, qtype="Conceptual", diff="Easy",
 sc=r"The four classical criteria for a good estimator are unbiasedness, consistency, sufficiency and efficiency.",
 tips=[r"The standard list (Fisher's criteria): unbiasedness, consistency, efficiency, sufficiency.",
       r"In practice the criteria can conflict, but the textbook 'good estimator' definition demands all four.",
       r"Options (a)-(c) each drop one criterion, so the inclusive option is the intended answer."],
 sol=[r"Classical estimation theory judges an estimator on four criteria: unbiasedness ($E(T)=\theta$), consistency ($T\xrightarrow{P}\theta$), efficiency (minimum variance) and sufficiency (no loss of information).",
      r"A 'good' estimator is one possessing all four properties.",
      r"Option (d)."]),

"2020-Q6": dict(ans=1, qtype="Conceptual", diff="Medium",
 sc=r"For the Cauchy, $\bar X$ has the SAME Cauchy distribution as a single observation, so it is not consistent; the sample median is consistent for the population median.",
 tips=[r"The Cauchy has no finite mean, so the weak law of large numbers does not apply to $\bar X$.",
       r"Remarkable fact: $\bar X$ for Cauchy has exactly the same distribution as $X_1$ - averaging buys nothing.",
       r"The sample median is consistent for the median of any distribution with a positive density there - a completely general result."],
 sol=[r"Statement 1: if $X_i$ are iid Cauchy$(\theta,1)$, then $\bar X$ is again Cauchy$(\theta,1)$ for every $n$. Its distribution does not concentrate, so $\bar X$ is not consistent. False.",
      r"Statement 2: the sample median converges in probability to the population median whenever the density is positive there, which holds for the Cauchy. True.",
      r"Hence 2 only - option (b)."]),

"2020-Q7": dict(ans=2, qtype="Numerical", diff="Hard",
 sc=r"$E\left[(-k)^X\right]=e^{-\lambda}\sum\frac{(-k\lambda)^x}{x!}=e^{-\lambda}e^{-k\lambda}=e^{-(k+1)\lambda}$.",
 tips=[r"For Poisson, $E(a^X)=e^{\lambda(a-1)}$ - the probability generating function; set $a=-k$.",
       r"Then $E\left[(-k)^X\right]=e^{\lambda(-k-1)}=e^{-(k+1)\lambda}$ exactly as required.",
       r"The alternating sign is essential: $k^X$ would give $e^{\lambda(k-1)}$, which grows rather than decays."],
 sol=[r"For $X\sim\text{Poisson}(\lambda)$, the probability generating function is $E(a^X)=\sum_{x\ge0}a^x\dfrac{e^{-\lambda}\lambda^x}{x!}=e^{-\lambda}e^{a\lambda}=e^{\lambda(a-1)}$.",
      r"Putting $a=-k$: $E\left[(-k)^X\right]=e^{\lambda(-k-1)}=e^{-(k+1)\lambda}$.",
      r"Hence $(-k)^X$ is unbiased for $e^{-(k+1)\lambda}$ - option (c)."]),

"2020-Q8": dict(ans=2, qtype="Definition", diff="Easy",
 sc=r"$I_\theta=E\left[\left(\frac{\partial\log f_\theta}{\partial\theta}\right)^2\right]$ - the expected squared score.",
 tips=[r"Equivalent form: $I_\theta=-E\left[\dfrac{\partial^2\log f_\theta}{\partial\theta^2}\right]$ - note the MINUS sign, which option (a) omits.",
       r"Fisher information is additive over independent observations: $I_n(\theta)=nI_1(\theta)$.",
       r"$I_\theta\ge0$ always; the un-negated second derivative in (a) would generally be negative."],
 sol=[r"The score function is $U=\dfrac{\partial\log f_\theta(x)}{\partial\theta}$, and $E_\theta(U)=0$ under regularity.",
      r"Fisher information is the variance of the score: $I_\theta=V(U)=E_\theta\left[\left(\dfrac{\partial\log f_\theta(x)}{\partial\theta}\right)^2\right]$.",
      r"Equivalently $I_\theta=-E_\theta\left[\dfrac{\partial^2\log f_\theta(x)}{\partial\theta^2}\right]$; option (a) drops the minus sign and option (b) squares the wrong quantity.",
      r"Option (c)."]),

"2020-Q9": dict(ans=1, qtype="Conceptual", diff="Easy",
 sc=r"$L=\theta^n\left(\prod x_i\right)^{\theta-1}$, so $\prod X_i$ is sufficient by the factorisation theorem.",
 tips=[r"Any one-to-one function works too, so $\sum\log X_i$ is equally sufficient.",
       r"MLE: $\hat\theta=-n/\sum\log x_i$ - always a function of the sufficient statistic.",
       r"This family appears six times across 2018-2026; recognise $\theta x^{\theta-1}$ instantly."],
 sol=[r"$L(\theta)=\prod_{i=1}^n\theta x_i^{\theta-1}=\theta^n\left(\prod_{i=1}^n x_i\right)^{\theta-1}$.",
      r"Take $g_\theta(t)=\theta^nt^{\theta-1}$ with $t=\prod x_i$ and $h(x)=1$: the factorisation criterion is met.",
      r"Hence $\prod_{i=1}^nX_i$ is sufficient for $\theta$ - option (b)."]),

"2020-Q10": dict(ans=2, qtype="Conceptual", diff="Easy",
 sc=r"Absolute error loss is minimised by the posterior MEDIAN.",
 tips=[r"$E\left[|\theta-d|\mid x\right]$ is minimised at the median of the conditional distribution - a general fact about $L_1$ loss.",
       r"The table to memorise: squared error $\to$ mean; absolute error $\to$ median; 0-1 loss $\to$ mode.",
       r"Median-based rules are more robust to heavy-tailed posteriors than mean-based ones."],
 sol=[r"The Bayes rule minimises the posterior expected loss $E\left[|\theta-d|\ \big|\ X=x\right]$.",
      r"For any random variable $\theta$ with conditional distribution $\pi(\theta\mid x)$, the function $d\mapsto E\left[|\theta-d|\mid x\right]$ is minimised at any median of that conditional distribution.",
      r"Hence the Bayes rule under absolute error loss is the posterior median - option (c)."]),
}
